---
phase: 01-foundation
plan: 08
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - app/api/shopify/sync/route.ts
  - app/api/shopify/sync/__tests__/route.test.ts
autonomous: true
requirements: [FND-05]
must_haves:
  truths:
    - "app/api/shopify/sync/route.ts is reduced to a single-line POST export wrapping `withShopifySession`"
    - "The route still returns HTTP 200 with `{ success: true }` on a valid token + loaded session"
    - "The route still returns HTTP 401 with `{ error: code }` for each of the 5 ShopifyAuthError codes"
    - "Every existing test in app/api/shopify/sync/__tests__/route.test.ts continues to pass GREEN (no error code regressions)"
  artifacts:
    - path: "app/api/shopify/sync/route.ts"
      provides: "Reference implementation of the wrapped admin API route"
      contains: "withShopifySession"
    - path: "app/api/shopify/sync/__tests__/route.test.ts"
      provides: "Sync route tests with mocks aligned to the wrapper"
  key_links:
    - from: "app/api/shopify/sync/route.ts"
      to: "@/lib/shopify/auth"
      via: "withShopifySession import"
      pattern: "from '@/lib/shopify/auth'"
---

<objective>
Rewrite `app/api/shopify/sync/route.ts` to consume `withShopifySession` from Plan 02 — completing FND-05 (D-07 reference implementation). The 35-line inline auth ladder collapses to a single one-line wrapper, and all 6 existing tests in `app/api/shopify/sync/__tests__/route.test.ts` continue to pass after a targeted mock update.

Purpose: Make every Phase 2+ embedded admin route follow the same pattern by example. The sync route is the reference; subsequent routes (`/api/shopify/sync/status`, `/api/settings`, `/api/chat` when wired) will copy this exact shape.
Output: Rewritten 5-10 line `route.ts`; updated test mocks (Option B from §Q7 — keep SDK-level mocks because they still work transitively through `verifyShopSessionToken`). The error code mapping is preserved verbatim per the existing tests — including the test that currently expects `invalid_token` for "non-myshopify domain". See behavior section for the D-06 alignment note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@CLAUDE.md
@app/api/shopify/sync/route.ts
@app/api/shopify/sync/__tests__/route.test.ts
@lib/shopify/auth.ts

<interfaces>
From `@/lib/shopify/auth` (Plan 02 output):
- `withShopifySession(handler: (ctx: { shop: string; session: Session; req: Request }) => Promise<Response>): (req: Request) => Promise<Response>`
- Throws `ShopifyAuthError` with codes: 'missing_token', 'invalid_token', 'invalid_dest', 'invalid_shop_domain', 'no_offline_session' → wrapper converts to `NextResponse.json({ error: code }, { status: 401 })`.

Current sync route (lines 1-45) error code mapping vs the new `verifyShopSessionToken` per Plan 02:
| Failure | Current route emits | New helper emits |
|---------|---------------------|-------------------|
| Missing Authorization | `missing_token` | `missing_token` (same) |
| Token decode throws | `invalid_token` | `invalid_token` (same) |
| `payload.dest` missing | `invalid_token` | `invalid_dest` (D-06 split!) |
| `new URL(payload.dest)` throws | `invalid_token` | `invalid_dest` (D-06 split!) |
| Hostname not `.myshopify.com` | `invalid_token` | `invalid_shop_domain` (D-06 split!) |
| `loadSession` returns null | `no_offline_session` | `no_offline_session` (same) |

This means the existing tests at `app/api/shopify/sync/__tests__/route.test.ts` lines 61-69 (`'returns 401 when payload.dest is not a parseable URL'`) and lines 72-81 (`'returns 401 when payload.dest hostname is not a *.myshopify.com domain'`) will start emitting `invalid_dest` / `invalid_shop_domain` instead of `invalid_token` — the test assertion strings MUST update to match the new (more granular) codes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite the sync route via withShopifySession + update test assertions</name>
  <read_first>
    - app/api/shopify/sync/route.ts (current full 45 lines — being collapsed)
    - app/api/shopify/sync/__tests__/route.test.ts (current full 111 lines — assertions for 5 error codes + happy path; lines 61-69 and 72-81 will switch error codes)
    - lib/shopify/auth.ts (Plan 02 output — the wrapper this route consumes)
    - .planning/phases/01-foundation/01-PATTERNS.md §"app/api/shopify/sync/route.ts" and §"app/api/shopify/sync/__tests__/route.test.ts"
    - .planning/phases/01-foundation/01-RESEARCH.md §Q2 (the 5 codes), §Q7 §"Tests that must stay green" (test impact analysis)
  </read_first>
  <behavior>
    After this task:
    - `app/api/shopify/sync/route.ts` is ~5-10 lines: imports + a single `export const POST = withShopifySession(async ({ shop, session }) => { return NextResponse.json({ success: true }); });` (per the §"After pattern" in PATTERNS.md). Phase 2 will replace the body with real sync logic.
    - The route correctly emits all 6 status/code outcomes through the wrapper:
      1. Missing Authorization → 401 `{ error: 'missing_token' }`
      2. Token decode failure → 401 `{ error: 'invalid_token' }`
      3. Missing or unparseable `payload.dest` → 401 `{ error: 'invalid_dest' }` (NEW — was 'invalid_token' in the legacy route)
      4. Non-`.myshopify.com` hostname → 401 `{ error: 'invalid_shop_domain' }` (NEW — was 'invalid_token' in the legacy route)
      5. No offline session for shop → 401 `{ error: 'no_offline_session' }`
      6. Valid token + session → 200 `{ success: true }`
    - The test file is updated (Option B from §Q7 — keep SDK-level mocks because they still work transitively): the existing `vi.mock('@/lib/shopify/client', ...)` and `vi.mock('@/lib/shopify/session-storage', ...)` blocks stay unchanged. The only test edits are the assertion strings for the two newly-split codes:
      - Test "returns 401 when payload.dest is not a parseable URL" (current line 61-69): change `expect(body.error).toBe('invalid_token')` → `expect(body.error).toBe('invalid_dest')`. Update the test name to mention `invalid_dest`.
      - Test "returns 401 when payload.dest hostname is not a *.myshopify.com domain" (current line 72-81): change `expect(body.error).toBe('invalid_token')` → `expect(body.error).toBe('invalid_shop_domain')`. Update the test name to mention `invalid_shop_domain`.
    - The other 4 tests (missing_token, invalid_token, no_offline_session, 200 success) stay byte-identical.
  </behavior>
  <action>
    Rewrite `app/api/shopify/sync/route.ts`. Replace the entire file (lines 1-45) with:
    - `import { NextResponse } from 'next/server';`
    - `import { withShopifySession } from '@/lib/shopify/auth';`
    - `export const POST = withShopifySession(async ({ shop, session }) => { ... });` — body: a TODO comment referencing Phase 2 sync wiring, then `void shop; void session;` (or use them — both work) and `return NextResponse.json({ success: true });`.

    Edit `app/api/shopify/sync/__tests__/route.test.ts` per Option B in §Q7:
    - Leave the two `vi.mock` blocks at lines 3-25 UNCHANGED (they still work because `verifyShopSessionToken` calls them internally).
    - Update the two test assertions for the D-06 split (test at lines 61-69 → `'invalid_dest'`; test at lines 72-81 → `'invalid_shop_domain'`). Also update the `it(...)` description strings to match.
    - Keep all other tests verbatim.

    Run `bunx vitest run app/api/shopify/sync/__tests__/route.test.ts` — must exit 0 with all 6 tests GREEN.
  </action>
  <verify>
    <automated>bunx vitest run app/api/shopify/sync/__tests__/route.test.ts && wc -l app/api/shopify/sync/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run app/api/shopify/sync/__tests__/route.test.ts` exits 0 with all 6 tests GREEN.
    - `wc -l app/api/shopify/sync/route.ts` returns <= 15 (the collapsed file).
    - `grep -c "withShopifySession" app/api/shopify/sync/route.ts` returns 1 (the route's only call to the auth layer).
    - `grep -c "Authorization" app/api/shopify/sync/route.ts` returns 0 (no inline auth-header parsing remains).
    - `grep -c "decodeSessionToken" app/api/shopify/sync/route.ts` returns 0.
    - `grep -c "console.log" app/api/shopify/sync/route.ts` returns 0.
    - Test file at lines covering the dest-parse failure contains the string `'invalid_dest'` exactly once (not `'invalid_token'`).
    - Test file at lines covering the hostname check contains the string `'invalid_shop_domain'` exactly once.
    - The other 4 test assertions remain byte-identical (no unrelated changes to test code).
  </acceptance_criteria>
  <done>Sync route reduced to a single wrapper invocation; 6 tests GREEN with the two newly-split codes correctly asserted; reference implementation ready for Phase 2 to copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (Shopify Admin iframe) → /api/shopify/sync | Untrusted Bearer JWT in Authorization header. `withShopifySession` → `verifyShopSessionToken` (Plan 02) is the single verification path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Spoofing (token replay) | /api/shopify/sync handler | mitigate | All auth flows through `verifyShopSessionToken` (HS256 + 10s clock skew + offline-session DB check). No inline crypto in the route. |
| T-1-05 | Tampering (route forgets to verify) | /api/shopify/sync POST export | mitigate | The route is now a single wrapper invocation — there is no "before the wrapper" code path. Forgetting to wrap would be a deletion, not an omission. |
| T-1-03 | Information Disclosure | /api/shopify/sync error responses | mitigate | Error response body is exactly `{ error: <code> }` (5 enumerated codes) — no stack trace, no message, no token echo. Verified by 5 of the 6 existing tests. |
</threat_model>

<verification>
- `bunx vitest run app/api/shopify/sync/__tests__/route.test.ts` GREEN.
- `bunx vitest run lib/shopify/__tests__/auth.test.ts` REMAINS GREEN (Plan 02 work not regressed).
- Route file is <= 15 lines.
- Grep gates confirm no inline auth code lingers in the route.
- The two newly-split error codes (`invalid_dest`, `invalid_shop_domain`) are asserted by exactly one test each.
</verification>

<success_criteria>
- Sync route collapsed to wrapper invocation.
- 6 existing tests pass with the two updated assertions.
- D-06 split between `invalid_dest` and `invalid_token` is now visible in HTTP responses (an integration-test improvement).
- Reference implementation in place for Phase 2 to model new routes after.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-08-SUMMARY.md` recording: route line count before/after, the two updated test assertions, confirmation that the 4 unchanged test assertions remained byte-identical, and explicit handoff to Phase 2 (status route, webhook route, etc. follow this pattern).
</output>
