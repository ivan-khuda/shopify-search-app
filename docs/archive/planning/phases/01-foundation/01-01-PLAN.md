---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/shopify/__tests__/auth.test.ts
  - lib/db/repositories/__tests__/ProductRepository.test.ts
  - __tests__/middleware.test.ts
autonomous: true
requirements: [FND-03, FND-04, FND-05]
must_haves:
  truths:
    - "Failing test files exist for lib/shopify/auth.ts (5 error codes + happy path + wrapper)"
    - "Failing test file exists for ProductRepository CRUD with shop-scoping assertions"
    - "middleware.test.ts is updated for `?shop=` only (Bearer fallback tests removed) and still asserts the surviving session-present/session-missing/no-shop behaviors"
  artifacts:
    - path: "lib/shopify/__tests__/auth.test.ts"
      provides: "RED tests for verifyShopSessionToken and withShopifySession"
      contains: "verifyShopSessionToken"
    - path: "lib/db/repositories/__tests__/ProductRepository.test.ts"
      provides: "RED tests for ProductRepository CRUD with shop isolation"
      contains: "productRepository"
    - path: "__tests__/middleware.test.ts"
      provides: "Updated middleware tests (no Bearer fallback)"
      contains: "redirects when no shop"
  key_links:
    - from: "lib/shopify/__tests__/auth.test.ts"
      to: "@/lib/shopify/auth"
      via: "import"
      pattern: "from '../auth'"
    - from: "lib/db/repositories/__tests__/ProductRepository.test.ts"
      to: "@/lib/db/client"
      via: "vi.mock"
      pattern: "vi.mock\\('@/lib/db/client'"
---

<objective>
Wave 0 RED test scaffolds. Create the test files that downstream implementation tasks must turn GREEN. By landing these tests first, the executor for each Wave 1+ plan receives a concrete behavioral spec (test expectations) rather than discovering them through trial and error.

Purpose: Establish the test contract for `lib/shopify/auth.ts` (5 error codes + happy path + wrapper) and `ProductRepository` (4 CRUD methods with shop scoping). Update `__tests__/middleware.test.ts` to drop the Bearer-fallback expectations that D-09 makes invalid.
Output: Three test files committed in a failing-but-well-structured state. Each test names the symbol it expects to exist; missing exports show as import errors, not subtle assertion failures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
@CLAUDE.md
@app/api/shopify/sync/__tests__/route.test.ts
@__tests__/middleware.test.ts
@lib/shopify/client.ts
@lib/shopify/session-storage.ts

<interfaces>
The new files must reference these symbols (defined later by Plan 02 and Plan 06):

From lib/shopify/auth.ts (to be created in Plan 02):
- `export type ShopifyAuthErrorCode = 'missing_token' | 'invalid_token' | 'invalid_dest' | 'invalid_shop_domain' | 'no_offline_session'`
- `export class ShopifyAuthError extends Error { readonly code: ShopifyAuthErrorCode; readonly status: 401 }`
- `export function verifyShopSessionToken(req: Request): Promise<{ shop: string; session: Session }>`
- `export function withShopifySession(handler: (ctx: { shop: string; session: Session; req: Request }) => Promise<Response>): (req: Request) => Promise<Response>`

From lib/db/repositories/ProductRepository.ts (to be rewritten in Plan 06):
- `export const productRepository: ProductRepository`
- `productRepository.upsertProduct(shop: string, input: ProductUpsertInput): Promise<Product>`
- `productRepository.findByShopAndId(shop: string, id: number): Promise<Product | null>`
- `productRepository.listByShop(shop: string, opts?: ListOpts): Promise<Product[]>`
- `productRepository.deleteProduct(shop: string, id: number): Promise<void>`

Existing mocked surfaces:
- `shopifyClient.session.decodeSessionToken(token: string): Promise<{ dest?: string }>`
- `shopifyClient.session.getOfflineId(shop: string): string`
- `sessionStorage.loadSession(id: string): Promise<Session | undefined>`
- `prisma.product / productVariant / productImage / productOption / productEmbedding.{findFirst, findMany, deleteMany, upsert, create, update, createMany}`
- `prisma.$transaction(fn): Promise<T>`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED test file for lib/shopify/auth.ts</name>
  <read_first>
    - lib/shopify/__tests__/auth.test.ts (target — does not yet exist)
    - app/api/shopify/sync/__tests__/route.test.ts (mock + describe/it scaffold to copy)
    - .planning/phases/01-foundation/01-PATTERNS.md §"lib/shopify/__tests__/auth.test.ts" (exact verbatim block to copy)
    - .planning/phases/01-foundation/01-RESEARCH.md §Q2 (error code map) and §Q3 (verifyShopSessionToken control flow)
  </read_first>
  <behavior>
    The test file MUST assert all 6 paths through verifyShopSessionToken and 2 paths through withShopifySession (per D-05/D-06/D-07):
    - verifyShopSessionToken throws ShopifyAuthError with code 'missing_token' when Authorization header is absent or does not start with 'Bearer '.
    - verifyShopSessionToken throws ShopifyAuthError with code 'invalid_token' when shopifyClient.session.decodeSessionToken rejects.
    - verifyShopSessionToken throws ShopifyAuthError with code 'invalid_dest' when payload.dest is missing OR `new URL(payload.dest)` throws. NOTE: This is the new code split out per D-06; the existing sync route currently collapses this into 'invalid_token'. Tests MUST assert 'invalid_dest', not 'invalid_token'.
    - verifyShopSessionToken throws ShopifyAuthError with code 'invalid_shop_domain' when hostname does not end with '.myshopify.com'.
    - verifyShopSessionToken throws ShopifyAuthError with code 'no_offline_session' when sessionStorage.loadSession returns null/undefined.
    - verifyShopSessionToken returns `{ shop, session }` on success, with shop equal to `new URL(payload.dest).hostname` and session equal to the resolved session object.
    - withShopifySession invokes the handler with `{ shop, session, req }` on success and returns its Response unchanged.
    - withShopifySession catches a ShopifyAuthError thrown by verification and returns `NextResponse.json({ error: code }, { status: 401 })` — the response body MUST contain `{ error: '<code>' }` and the status code MUST be 401 for all 5 error codes.
  </behavior>
  <action>
    Create the file `lib/shopify/__tests__/auth.test.ts` following the verbatim mock + import structure from `.planning/phases/01-foundation/01-PATTERNS.md` §"lib/shopify/__tests__/auth.test.ts". Use `vi.mock('@/lib/shopify/client', ...)` and `vi.mock('@/lib/shopify/session-storage', ...)` BEFORE importing from `../auth`. Mock `decodeSessionToken: vi.fn()`, `getOfflineId: vi.fn((shop) => \`offline_${shop}\`)`, `loadSession: vi.fn()`. Implement two describe blocks: `describe('verifyShopSessionToken', ...)` with one `it` per error code plus a happy-path `it`, and `describe('withShopifySession', ...)` with one `it` covering the handler-invoked-on-success path and one parametrised `it.each` covering all 5 error codes → 401 + body shape `{ error: code }`. Each test MUST construct requests with `new Request('http://localhost/', { method: 'POST', headers: ... })`. Use `await expect(...).rejects.toMatchObject({ code: '<code>' })` for thrown assertions. The file MUST fail at test-collection time (the auth module does not yet exist) — this is the intended RED state.
  </action>
  <verify>
    <automated>bunx vitest run lib/shopify/__tests__/auth.test.ts 2>&1 | grep -E "(error|fail|FAIL|cannot find)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at lib/shopify/__tests__/auth.test.ts.
    - File contains the literal strings 'missing_token', 'invalid_token', 'invalid_dest', 'invalid_shop_domain', 'no_offline_session' (one assertion per code).
    - File contains `describe('verifyShopSessionToken'` and `describe('withShopifySession'`.
    - File imports from `'../auth'` (will fail until Plan 02 lands — RED state expected).
    - `grep -c "it(" lib/shopify/__tests__/auth.test.ts` returns >= 8.
  </acceptance_criteria>
  <done>Test file committed; vitest reports it as failing with "Cannot find module '../auth'" or similar import error (this is the intended RED state).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RED test file for ProductRepository</name>
  <read_first>
    - lib/db/repositories/__tests__/ProductRepository.test.ts (target — does not yet exist)
    - lib/db/repositories/ProductRepository.ts (current 9-line stub — being replaced in Plan 06)
    - .planning/phases/01-foundation/01-PATTERNS.md §"lib/db/repositories/__tests__/ProductRepository.test.ts" (verbatim mock and $transaction patterns)
    - .planning/phases/01-foundation/01-RESEARCH.md §Q8 (signatures and recommended interface)
  </read_first>
  <behavior>
    The test file MUST assert (per D-03/D-04 and FND-04):
    - findByShopAndId(shop, id) calls prisma.product.findFirst with `where: { shop, id }` — the shop key MUST appear in the where clause.
    - listByShop(shop) calls prisma.product.findMany with `where: { shop }`; listByShop(shop, { status: 'ACTIVE' }) includes `status: 'ACTIVE'` in the where.
    - listByShop respects `limit` and `offset` via `take` and `skip`.
    - deleteProduct(shop, id) calls prisma.product.deleteMany with `where: { shop, id }` (per Pitfall 2 mitigation — deleteMany avoids dependence on Prisma-generated compound where name).
    - upsertProduct(shop, input) wraps its work in `prisma.$transaction(async (tx) => ...)`. The test wires the $transaction mock to invoke the callback with the prisma mock as `tx` and asserts: (a) a product upsert was attempted, (b) child deleteMany calls used `productShop: shop`, (c) child createMany calls included `shop: shop` and `productShop: shop` on every row.
    - Cross-shop isolation: a single test calls `findByShopAndId('shop-a.myshopify.com', 1)` AND `findByShopAndId('shop-b.myshopify.com', 1)` and asserts the where clauses differ on the `shop` field.
    - TypeScript compile-time check: the test file MUST construct a call that passes `shop` as a non-optional first argument. `productRepository.findByShopAndId(1)` (missing shop) MUST be a TypeScript error (verified separately by Plan 09).
  </behavior>
  <action>
    Create the file `lib/db/repositories/__tests__/ProductRepository.test.ts` following the verbatim structure in `.planning/phases/01-foundation/01-PATTERNS.md` §"lib/db/repositories/__tests__/ProductRepository.test.ts". Hoist `vi.mock('@/lib/db/client', ...)` BEFORE importing from `'../ProductRepository'`. Mock the prisma client with `product: { findFirst, findMany, deleteMany, upsert }`, `productVariant: { deleteMany, createMany }`, `productImage: { deleteMany, createMany }`, `productOption: { deleteMany, createMany }`, and `$transaction: vi.fn()`. In `beforeEach`, call `vi.clearAllMocks()` and rewire `(prisma.$transaction as ReturnType<typeof vi.fn>).mockImplementation(async (fn) => fn(prisma))`. Write tests grouped under `describe('ProductRepository', ...)` with sub-describes for each method. The shop isolation test must use two distinct shop hostnames (`'shop-a.myshopify.com'` and `'shop-b.myshopify.com'`) and assert the where clauses are not equal. For `upsertProduct`, construct a minimal `ProductUpsertInput` (one variant, one image, one option) and assert the `createMany` arguments contain `shop` on every nested data row. The file MUST fail at test-collection (real ProductRepository does not yet expose these methods) — intended RED state.
  </action>
  <verify>
    <automated>bunx vitest run lib/db/repositories/__tests__/ProductRepository.test.ts 2>&1 | grep -E "(error|fail|FAIL|TypeError|is not a function)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at lib/db/repositories/__tests__/ProductRepository.test.ts.
    - `grep -c "shop:" lib/db/repositories/__tests__/ProductRepository.test.ts` returns >= 5 (shop appears in multiple expected where clauses).
    - File contains tests named (case-insensitive) for upsertProduct, findByShopAndId, listByShop, deleteProduct.
    - File contains at least one explicit cross-shop isolation assertion using two distinct `*.myshopify.com` hostnames.
    - `grep -c "it(" lib/db/repositories/__tests__/ProductRepository.test.ts` returns >= 6.
  </acceptance_criteria>
  <done>Test file committed; vitest reports failures (upsertProduct/findByShopAndId/listByShop/deleteProduct are not functions on the stub) — intended RED state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update __tests__/middleware.test.ts for `?shop=` only</name>
  <read_first>
    - __tests__/middleware.test.ts (current — full file, lines 1-82)
    - .planning/phases/01-foundation/01-PATTERNS.md §"__tests__/middleware.test.ts"
    - .planning/phases/01-foundation/01-RESEARCH.md §Q6 §"Bearer fallback removal" and §"D-09 alignment"
    - middleware.ts (current — for the surviving redirectToAuth signature reference)
  </read_first>
  <behavior>
    Per D-08, D-09, and FND-03 the middleware (or proxy.ts) only reads shop from `?shop=` and never derives it from a Bearer header. After this task, `__tests__/middleware.test.ts` MUST:
    - Retain the test "allows request when shop has offline session" (session present → status 200).
    - Retain the test "redirects to /api/auth when no offline session found" (session missing → 307 with Location containing '/api/auth').
    - Retain the test "redirects when no shop can be determined" (no `?shop=` and no headers → 307).
    - Add a new test: "redirects to /api/auth without shop param when shop query is missing" (asserts Location ends with '/api/auth' and does NOT contain '?shop=').
    - DELETE the test "extracts shop from valid App Bridge Bearer token" (lines 50-62 of current file).
    - DELETE the test "redirects when Bearer token is invalid" (lines 64-74 of current file).
    - REMOVE `decodeSessionToken: vi.fn()` from the mock at lines 4-11 (proxy/middleware no longer calls it).
    - Keep the import path stable for now (`from '../middleware'`) — Plan 07 will flip this to `'../proxy'` when it renames the file. Add a `// TODO(Plan 07): rename to ../proxy after Next.js 16 middleware → proxy migration` comment near the import.
  </behavior>
  <action>
    Edit `__tests__/middleware.test.ts` in place. Remove `decodeSessionToken: vi.fn()` from the `vi.mock('@/lib/shopify/client', ...)` block — leave only `getOfflineId: vi.fn()`. Delete the two Bearer-related `it(...)` blocks (`'extracts shop from valid App Bridge Bearer token'` and `'redirects when Bearer token is invalid'`). Add a new `it('redirects to /api/auth without shop param when shop query is missing', ...)` test that calls `makeRequest('/chat')` (no query, no headers), awaits `middleware(request)`, asserts `response.status === 307`, and asserts `response.headers.get('Location')` ends with `/api/auth` AND does NOT include `shop=`. Add a TODO comment above the `import { middleware } from '../middleware'` line: `// TODO(Plan 07): update to import { proxy } from '../proxy' after Next.js 16 migration (D-08).` This file will go RED against the *current* middleware (which still has Bearer fallback returning 200, not 307) — that's the intended state until Plan 07 lands the new middleware/proxy implementation.
  </action>
  <verify>
    <automated>bunx vitest run __tests__/middleware.test.ts 2>&1 | tail -30 ; grep -c "Bearer" __tests__/middleware.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File no longer contains the strings `'extracts shop from valid App Bridge Bearer token'` or `'redirects when Bearer token is invalid'`.
    - `grep -c "Bearer" __tests__/middleware.test.ts` returns 0.
    - `grep -c "decodeSessionToken" __tests__/middleware.test.ts` returns 0.
    - File contains a test whose name includes the literal `'without shop param'`.
    - File still contains tests for "allows request when shop has offline session" and "redirects to /api/auth when no offline session found".
    - File contains the TODO comment referencing Plan 07 and proxy.ts rename.
  </acceptance_criteria>
  <done>middleware.test.ts updated; Bearer fallback assertions removed; new shop-missing assertion added; file may report some failures against current middleware (RED state until Plan 07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → source modules | Tests intentionally fail until implementations land — no security exposure since this is local-only test code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Information Disclosure | Test fixtures referencing tokens | mitigate | Tests use the string 'broken' / 'good' / 'valid.jwt.token' as fixtures — never real tokens. No assertion logs token contents. |
| T-1-06 | Spoofing | Shop param in middleware tests | mitigate | Tests assert the middleware MUST require `?shop=` and MUST verify by loading offline session — preventing later regressions that re-introduce a Bearer-derived shop without DB verification. |
</threat_model>

<verification>
Wave 0 is intentionally RED. Verification is structural, not behavioral:
- All three files exist.
- Each contains the expected `describe`/`it` blocks and required string literals (see acceptance criteria).
- Vitest can at least *collect* the new files (no syntax errors).
- Existing tests not touched by this plan (e.g., `app/api/shopify/sync/__tests__/route.test.ts`, onboarding tests) still pass.

Quick sample: `bunx vitest run __tests__/middleware.test.ts app/api/shopify/sync/__tests__/route.test.ts lib/shopify/__tests__/auth.test.ts lib/db/repositories/__tests__/ProductRepository.test.ts 2>&1 | tail -40` — expect FAILs on auth.test.ts and ProductRepository.test.ts (import errors), expect some failures or passes on middleware.test.ts (current middleware is Bearer-permissive), expect all passes on sync/route.test.ts (untouched in Wave 0).
</verification>

<success_criteria>
- lib/shopify/__tests__/auth.test.ts exists with at least 8 `it(...)` blocks asserting the 5 error codes + happy path + 2 wrapper paths.
- lib/db/repositories/__tests__/ProductRepository.test.ts exists with cross-shop isolation tests.
- __tests__/middleware.test.ts no longer contains Bearer-fallback assertions and adds the shop-missing redirect test.
- All three files commit cleanly; no other source files modified.
- The downstream plans (02, 06, 07) have a concrete test surface they can drive GREEN.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` recording: files created/updated, count of `it()` blocks per file, which assertions are intentionally RED, and the next-plan handoffs (Plan 02 turns auth.test.ts GREEN; Plan 06 turns ProductRepository.test.ts GREEN; Plan 07 turns middleware.test.ts GREEN).
</output>
