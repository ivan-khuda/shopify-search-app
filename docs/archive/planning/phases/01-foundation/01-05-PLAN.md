---
phase: 01-foundation
plan: 05
type: execute
wave: 2
depends_on: ["01-03"]
files_modified:
  - app/generated/prisma/**
autonomous: false
requirements: [FND-01]
must_haves:
  truths:
    - "`bunx prisma migrate status` reports 'Database schema is up to date' (or equivalent — no pending migrations, no drift)"
    - "Inspecting the live DB confirms every product table has a `shop` column NOT NULL with a leading index on shop"
    - "`shopify_sessions` table still exists with the data it had before this plan ran"
    - "pgvector extension still exists (`SELECT extname FROM pg_extension WHERE extname='vector'` returns one row)"
    - "Generated Prisma client at `app/generated/prisma/` reflects the new models (TypeScript types for Product include `shop: string` field)"
  artifacts:
    - path: "app/generated/prisma/index.d.ts"
      provides: "Regenerated Prisma client types reflecting shop column + composite FK"
      contains: "shop"
  key_links:
    - from: "prisma/schema.prisma"
      to: "live Postgres database"
      via: "prisma migrate dev / migrate deploy"
      pattern: "bunx prisma migrate"
---

<objective>
[BLOCKING] Push the destructive migration authored in Plan 03 to the live database and regenerate the Prisma client. This task is mandatory — Phase 1 CANNOT verify FND-01, FND-04, or downstream phases without the live schema reflecting the new shop column + composite FK structure. Build and type checks would otherwise pass against the new schema while the DB stays on the old shape, creating a false-positive verification state.

Purpose: Make the structural multi-tenancy guarantee real in Postgres, not just in the Prisma schema file. Regenerate the Prisma client so Plan 06's `ProductRepository` can be implemented against accurate TypeScript types.
Output: Migration applied to local dev DB; `app/generated/prisma/` regenerated; `bunx prisma migrate status` clean; pgvector preserved; ShopifySession data intact.

This plan is `autonomous: false` because `bunx prisma migrate dev` is interactive (prompts for migration name when not pre-named; prompts to confirm data loss on destructive migrations on some configurations). The non-TTY fallback (`migrate deploy` after `migrate dev --create-only`) is provided.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@CLAUDE.md
@prisma/schema.prisma
@prisma/migrations/
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply migration + verify pgvector + verify ShopifySession survives</name>
  <read_first>
    - prisma/schema.prisma (post-Plan-03 — must already contain `@@unique([shop, id])` and composite FKs)
    - prisma/migrations/<ts>_add_shop_column_destructive/migration.sql (post-Plan-03 — the SQL Plan 03 authored)
    - .planning/phases/01-foundation/01-RESEARCH.md §Q5 "Migration mechanics" and §Q9 "Existing Migration Inventory"
    - .planning/phases/01-foundation/01-CONTEXT.md §D-01 (destructive single migration policy)
  </read_first>
  <what-built>
    Plan 03 authored the destructive migration but did NOT push it. Before this task: schema file and migration SQL exist on disk. After this task: the live local DB reflects the new schema; pgvector and shopify_sessions are intact; the Prisma client is regenerated.
  </what-built>
  <how-to-verify>
    Run the push and verify in this exact order. If the executor (Claude) can run all commands non-interactively, do so and treat the human checkpoint as a verification-only gate; otherwise the human runs the interactive prompt.

    Step 1 (preferred — interactive). Run:
      bunx prisma migrate dev --name add_shop_column_destructive
    The migration directory authored in Plan 03 already exists, so Prisma will detect it and apply it (do NOT regenerate the SQL). If Prisma asks to reset because of detected drift, accept — there is no production data (D-01).

    Step 1 (non-TTY fallback). If `prisma migrate dev` blocks on a TTY prompt, run:
      bunx prisma migrate deploy
    This applies pending migrations non-interactively against the configured DATABASE_URL.

    Step 2. Confirm migration status:
      bunx prisma migrate status
    Expected output: "Database schema is up to date" OR "Following migrations have not yet been applied: (none)" — no drift warnings.

    Step 3. Regenerate the Prisma client (required because the schema changed):
      bunx prisma generate
    Confirm `app/generated/prisma/index.d.ts` was updated (timestamp newer than the schema file). The generated `Product` type MUST include `shop: string`.

    Step 4. Verify pgvector extension survived:
      bunx prisma db execute --stdin <<< "SELECT extname FROM pg_extension WHERE extname='vector';"
    Expected: one row with `vector`.

    Step 5. Verify ShopifySession data preserved (must NOT have been touched by the destructive migration):
      bunx prisma db execute --stdin <<< "SELECT COUNT(*) FROM shopify_sessions;"
    Expected: returns a number (may be 0 in a fresh dev DB, but the table MUST exist — a "relation does not exist" error here means Pitfall 5 happened and ShopifySession was accidentally dropped — STOP and recover).

    Step 6. Verify shop column exists and is NOT NULL on each product table:
      bunx prisma db execute --stdin <<< "SELECT column_name, is_nullable FROM information_schema.columns WHERE table_name IN ('products','product_variants','product_images','product_options','product_embeddings') AND column_name='shop';"
    Expected: 5 rows, all with is_nullable='NO'.

    Step 7. Verify composite FK exists:
      bunx prisma db execute --stdin <<< "SELECT conname FROM pg_constraint WHERE conname LIKE '%productShop_productId_fkey';"
    Expected: >= 3 rows (product_variants, product_options, product_embeddings) — product_images may use a nullable composite, naming should still match the pattern; if it shows up the count is >=4.

    If any step fails:
    - Step 1 fails → investigate (likely a drift error from a previously-applied conflicting migration; `bunx prisma migrate reset --force` will replay all migrations from scratch since no production data exists — D-01).
    - Step 4 fails (pgvector missing) → the migration may have failed to `CREATE EXTENSION` due to permission issues; manually run `CREATE EXTENSION IF NOT EXISTS vector;` and re-run `bunx prisma migrate deploy`.
    - Step 5 fails (shopify_sessions gone) → Pitfall 5 fired. STOP the phase. The destructive migration accidentally dropped shopify_sessions. Recovery: restore from the `20260216181046_add_shop_table` and `20260502011528_update_shopify_session_schema` migrations via `bunx prisma migrate reset --force`. Then audit Plan 03's migration SQL for any stray reference to `shopify_sessions`.
  </how-to-verify>
  <resume-signal>Type "migration applied and verified" when steps 1-7 all pass, OR describe the failure encountered.</resume-signal>
  <acceptance_criteria>
    - `bunx prisma migrate status` exits 0 with "Database schema is up to date" (or equivalent — no drift).
    - `bunx prisma db execute --stdin <<< "SELECT extname FROM pg_extension WHERE extname='vector';"` returns one `vector` row.
    - `bunx prisma db execute --stdin <<< "SELECT COUNT(*) FROM shopify_sessions;"` succeeds (table exists).
    - `bunx prisma db execute --stdin <<< "SELECT column_name FROM information_schema.columns WHERE table_name='products' AND column_name='shop';"` returns one row.
    - `app/generated/prisma/index.d.ts` exists and contains the string `shop` in the Product type declaration: `grep -c "shop:" app/generated/prisma/index.d.ts` returns >= 5 (Product + 4 children all expose `shop`).
    - `git status app/generated/prisma/` shows changes (regenerated client).
  </acceptance_criteria>
  <done>Migration applied; pgvector survived; ShopifySession survived; Prisma client regenerated; the schema file, the migration SQL, and the live DB are all in sync. Downstream Plan 06 can now implement ProductRepository against accurate generated types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer terminal → local Postgres | The destructive migration deletes data. This is acceptable per D-01 (no production data exists) but the procedure must not exceed its blast radius (Pitfall 5). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Information Disclosure | Multi-tenant data model live | mitigate | Once this push applies, the database structurally rejects cross-shop child rows. Composite FK is the load-bearing guarantee. |
| (recovery) | Tampering | shopify_sessions | accept | If Pitfall 5 fires and shopify_sessions is dropped, Step 5 verification catches it before further damage. Recovery is `migrate reset` — no offline-session data is irrecoverable in dev. |
| Supply-chain | Tampering | npm installs | n/a | No package install in this plan — pure schema push using existing `prisma` 7.3.0 from package.json. |
</threat_model>

<verification>
The 7 verification steps above ARE the verification. The checkpoint is `blocking` because the rest of Phase 1 (Plan 06 ProductRepository + Plan 09 verification) depends on the live DB matching the schema. If this task isn't completed, the build will still pass (types are from schema.prisma, not the DB) but the app will throw at runtime when any Plan 06 method runs against the old schema.
</verification>

<success_criteria>
- Migration applied and `bunx prisma migrate status` is green.
- Prisma client regenerated; `app/generated/prisma/` modified.
- pgvector extension preserved.
- shopify_sessions table preserved.
- All 5 product tables have a `shop NOT NULL` column.
- Composite FK constraints exist on the child tables.
- Plan 06 can now be executed with confidence that `prisma.product.findFirst({ where: { shop, id } })` will compile AND run.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-05-SUMMARY.md` recording: which command was used (interactive `migrate dev` vs non-TTY `migrate deploy`), output of `prisma migrate status`, output of each verification SELECT (pgvector, shopify_sessions count, shop column inspection, composite FK list), regenerated client diff stat, and explicit handoff to Plan 06.
</output>
