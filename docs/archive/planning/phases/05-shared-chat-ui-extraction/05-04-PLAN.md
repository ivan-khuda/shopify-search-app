---
phase: 05-shared-chat-ui-extraction
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - app/(embedded)/chat/chat-shell.tsx
  - app/(embedded)/chat/page.tsx
  - app/(embedded)/chat/__tests__/chat-shell.test.tsx
  - app/(embedded)/chat/__tests__/page.test.tsx
  - app/prototype/prototype-data.ts
  - components/chat/chat.tsx
  - components/chat/chat-shell.tsx
  - components/chat/chat-message.tsx
  - components/chat/product-card.tsx
  - components/chat/history-panel.tsx
  - components/chat/saved-products-panel.tsx
  - components/chat/empty-state.tsx
  - components/chat/message-parts.tsx
  - components/chat/__tests__/product-card.test.tsx
  - components/chat/__tests__/history-panel.test.tsx
  - components/chat/__tests__/saved-products-panel.test.tsx
  - components/chat/__tests__/message-parts.test.tsx
  - components/chat/__tests__/chat-shell.test.tsx
  - components/chat/chat.integration-test.tsx
autonomous: true
requirements: [SHR-03, SHR-04]
tags: [refactor, embedded-surface, chat-ui, hard-cut]

must_haves:
  truths:
    - "Embedded admin chat page imports ChatPane/HistoryPanel/SavedProductsPanel exclusively from `@/lib/chat-ui` and EmbeddedAdapter from `@/lib/chat-ui/adapters/embedded` (SHR-03)"
    - "app/(embedded)/chat/chat-shell.tsx is a 'use client' surface shell that instantiates EmbeddedAdapter via useMemo, uses useHistoryStore(shop) + useSavedProductsStore(shop), and owns the surface-specific height classes (h-[calc(100vh-100px)] + h-[calc(100%-180px)])"
    - "app/(embedded)/chat/page.tsx imports ChatShell from './chat-shell' and passes shop prop derived from searchParams"
    - "Legacy components/chat/* and components/chat/__tests__/* files are DELETED entirely (hard cut per D-11)"
    - "`grep -rn '@/components/chat' app/ lib/ components/` returns ZERO matches"
    - "app/(embedded)/chat/__tests__/chat-shell.test.tsx is relocated and mocks `@/lib/chat-ui` with `ChatPane` named export key (not `default: Chat`)"
    - "app/(embedded)/chat/__tests__/page.test.tsx vi.mock target is updated to `./chat-shell` (relative) or new path"
    - "app/prototype/prototype-data.ts no longer contains the dead `// import { MOCK_PRODUCTS }` comment"
  artifacts:
    - path: "app/(embedded)/chat/chat-shell.tsx"
      provides: "Embedded surface shell consuming lib/chat-ui via EmbeddedAdapter + store hooks"
      contains: "EmbeddedAdapter"
    - path: "app/(embedded)/chat/page.tsx"
      provides: "RSC entry point importing the new shell location"
      contains: "from './chat-shell'"
    - path: "app/(embedded)/chat/__tests__/chat-shell.test.tsx"
      provides: "Relocated shell test mocking the new barrel"
      contains: "vi.mock('@/lib/chat-ui'"
  key_links:
    - from: "app/(embedded)/chat/chat-shell.tsx"
      to: "@/lib/chat-ui (barrel) + @/lib/chat-ui/adapters/embedded + @/lib/chat-ui/stores/hooks"
      via: "import statements"
      pattern: "from\\s+['\"]@/lib/chat-ui"
    - from: "app/(embedded)/chat/page.tsx"
      to: "./chat-shell"
      via: "import { ChatShell } from './chat-shell'"
      pattern: "from\\s+['\"]\\.\\/chat-shell"
---

<objective>
Wave 3: Hard-cut the embedded admin surface from the legacy `components/chat/*` imports to the new `@/lib/chat-ui/*` barrel. Rebuild `app/(embedded)/chat/chat-shell.tsx` as a `'use client'` surface shell that instantiates `EmbeddedAdapter`, consumes the store hooks, and owns surface-specific height classes. Relocate the shell test to live alongside `page.test.tsx`. Delete the entire `components/chat/` directory.

Purpose: This is the SHR-03 + SHR-04 delivery — embedded admin uses EmbeddedAdapter exclusively and zero `@/components/chat/*` imports remain anywhere in the repo. Per CONTEXT D-11, this is a single-commit hard cut, no shims.

Output:
- 1 modified RSC (`page.tsx`) — single import + prop change.
- 1 rebuilt surface shell (`app/(embedded)/chat/chat-shell.tsx`).
- 2 relocated tests under `app/(embedded)/chat/__tests__/` (chat-shell.test.tsx new, page.test.tsx existing edit).
- 1 dead-comment cleanup in `app/prototype/prototype-data.ts`.
- Deletion of 13 legacy files under `components/chat/` and `components/chat/__tests__/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-shared-chat-ui-extraction/05-CONTEXT.md
@.planning/phases/05-shared-chat-ui-extraction/05-RESEARCH.md
@.planning/phases/05-shared-chat-ui-extraction/05-PATTERNS.md
@.planning/phases/05-shared-chat-ui-extraction/05-UI-SPEC.md
@.planning/phases/05-shared-chat-ui-extraction/05-03-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Outputs from Plan 03 that this plan consumes. -->

lib/chat-ui/index.ts (barrel from Plan 03):
- export { ChatPane, ChatMessage, ProductCard, HistoryPanel, SavedProductsPanel, EmptyState }
- export type { ChatIdentityAdapter, HistoryStore, SavedProductsStore }
- export { useHistoryStore, useSavedProductsStore }

lib/chat-ui/adapters/embedded.ts (from Plan 02):
- export class EmbeddedAdapter implements ChatIdentityAdapter (endpoint '/api/chat', getAuthHeaders via shopify.idToken(), getRequestBody empty)

ChatPane props (from Plan 03):
- adapter: ChatIdentityAdapter
- savedProductIds: Set<string>
- onToggleSave: (product: ChatProduct) => void
- onHistoryAdd: (entry: ChatHistoryItem) => void
</interfaces>

<current_state>
<!-- Current legacy paths that this plan eliminates. -->

components/chat/ today contains (all to be DELETED at end of plan):
- chat.tsx (default-export Chat, used by chat-shell.tsx — Plan 03 ALREADY created the lifted lib/chat-ui/components/chat-pane.tsx)
- chat-shell.tsx (admin tab layout — replaced by the rebuilt app/(embedded)/chat/chat-shell.tsx in this plan)
- chat-message.tsx, product-card.tsx, history-panel.tsx, saved-products-panel.tsx, empty-state.tsx, message-parts.tsx (all lifted to lib/chat-ui/components/ in Plan 03)
- __tests__/product-card.test.tsx, history-panel.test.tsx, saved-products-panel.test.tsx, message-parts.test.tsx (relocated to lib/chat-ui/__tests__/ in Plan 03)
- __tests__/chat-shell.test.tsx (NOT yet relocated — this plan moves it to app/(embedded)/chat/__tests__/)
- chat.integration-test.tsx (relocated to lib/chat-ui/__tests__/chat-pane.integration-test.tsx in Plan 03)

app/(embedded)/chat/page.tsx today imports `import { ChatShell } from '@/components/chat/chat-shell';` — must change.
app/(embedded)/chat/__tests__/page.test.tsx today contains `vi.mock('@/components/chat/chat-shell', () => ({ ChatShell: ... }))` — must change.
app/prototype/prototype-data.ts line 1 contains the dead comment `// import { MOCK_PRODUCTS } from '@/components/chat/mock-products';` — delete (MOCK_PRODUCTS was removed in Phase 4).
</current_state>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild app/(embedded)/chat/chat-shell.tsx + update page.tsx + relocate chat-shell.test.tsx</name>
  <files>app/(embedded)/chat/chat-shell.tsx, app/(embedded)/chat/page.tsx, app/(embedded)/chat/__tests__/chat-shell.test.tsx, app/(embedded)/chat/__tests__/page.test.tsx</files>
  <read_first>
    - /Users/ikhuda/sites/personal/shopify-search-app/components/chat/chat-shell.tsx (legacy source — extract the byte-identical pieces to keep)
    - /Users/ikhuda/sites/personal/shopify-search-app/app/(embedded)/chat/page.tsx (current — only line 2 import and line 32 prop change)
    - /Users/ikhuda/sites/personal/shopify-search-app/app/(embedded)/chat/__tests__/page.test.tsx (current — only line 26 mock-target change)
    - /Users/ikhuda/sites/personal/shopify-search-app/components/chat/__tests__/chat-shell.test.tsx (legacy source — relocate + mock-path update)
    - /Users/ikhuda/sites/personal/shopify-search-app/.planning/phases/05-shared-chat-ui-extraction/05-PATTERNS.md (lines 534–710 — chat-shell, page, chat-shell.test, page.test instructions)
    - /Users/ikhuda/sites/personal/shopify-search-app/.planning/phases/05-shared-chat-ui-extraction/05-UI-SPEC.md (lines 56–62 — surface-specific heights belong HERE; lines 119–148 — D-12 hex map authoritative table)
    - /Users/ikhuda/sites/personal/shopify-search-app/.planning/phases/05-shared-chat-ui-extraction/05-RESEARCH.md (lines 608–635 — chat-shell.tsx code synthesis)
  </read_first>
  <behavior>
    - The rebuilt `app/(embedded)/chat/chat-shell.tsx`:
      - Is `'use client'`.
      - Accepts `{ shop }: { shop: string }`.
      - Instantiates `const adapter = useMemo(() => new EmbeddedAdapter(), []);`.
      - Calls `const history = useHistoryStore(shop);` and `const saved = useSavedProductsStore(shop);`.
      - Derives `const savedProductIds = useMemo(() => new Set(saved.items.map(p => p.id)), [saved.items]);`.
      - Renders the same Tabs / TabsList / TabsContent structure as the legacy file, with all hex literals UNCHANGED (per PATTERNS.md "Critical D-12 reality check" — the hex literals in legacy chat-shell.tsx are ALREADY arbitrary-value Tailwind classes `bg-[#008060]` etc., so D-12 is a verify-only step — confirm via `grep -E "bg-\[#|text-\[#|border-\[#" components/chat/chat-shell.tsx` before deciding what to change).
      - Adds the surface-specific height constraint (`h-[calc(100vh-100px)]`) on the outer wrapper of the shell (since it was removed from ChatPane in Plan 03 per UI-SPEC rule #4).
      - Wraps `<ChatPane>` inside `<TabsContent value="chat">` with `className="h-[calc(100%-180px)]"` (or equivalent — verify against PATTERNS.md line 593 instruction; the test that asserts ChatPane doesn't carry these classes will catch regressions).
      - Passes `adapter={adapter}`, `savedProductIds={savedProductIds}`, `onToggleSave={saved.toggle}`, `onHistoryAdd={history.add}` to ChatPane.
      - Passes `items={history.items}`, `onClear={history.clear}` to HistoryPanel.
      - Passes `products={saved.items}`, `onToggleSave={saved.toggle}` to SavedProductsPanel.
    - app/(embedded)/chat/page.tsx imports ChatShell from './chat-shell' (relative) and passes `shop={shop ?? ''}` derived from existing searchParams handling (per legacy page.tsx). All existing banner + RSC structure preserved per Phase 4 D-11.
    - app/(embedded)/chat/__tests__/chat-shell.test.tsx is RELOCATED from `components/chat/__tests__/` and updated to mock `@/lib/chat-ui` with named-export keys (NOT default-export).
    - app/(embedded)/chat/__tests__/page.test.tsx receives a SINGLE-LINE edit to the vi.mock target.
  </behavior>
  <action>
    Create `app/(embedded)/chat/chat-shell.tsx` with the structure synthesized from PATTERNS.md lines 538–596 + RESEARCH.md "Surface shell wiring (admin)" lines 608–635:

    1. First line: `'use client';`
    2. Imports (preserve order from RESEARCH lines 613–619):
       - `import { useMemo, useState } from 'react';`
       - `import { Tabs, TabsContent, TabsContents, TabsList, TabsTrigger } from '@/components/ui/tabs';` (preserve from legacy)
       - `import { Button } from '@/components/ui/button';` (preserve from legacy)
       - `import { cn } from '@/lib/utils';` (preserve from legacy)
       - `import { Bookmark, HistoryIcon, MessageSquare, PlusIcon, Sparkles } from 'lucide-react';` (preserve from legacy)
       - `import { ChatPane, HistoryPanel, SavedProductsPanel } from '@/lib/chat-ui';`
       - `import { EmbeddedAdapter } from '@/lib/chat-ui/adapters/embedded';`
       - `import { useHistoryStore, useSavedProductsStore } from '@/lib/chat-ui/stores/hooks';`
    3. Replace the legacy `export function ChatShell()` signature with: `export function ChatShell({ shop }: { shop: string }) { ... }`.
    4. Inside the function body (replacing the legacy `useState` for `history` and `savedProducts`):
       - `const [selectedTab, setSelectedTab] = useState<string>('chat');` (preserve from legacy)
       - `const adapter = useMemo(() => new EmbeddedAdapter(), []);`
       - `const history = useHistoryStore(shop);`
       - `const saved = useSavedProductsStore(shop);`
       - `const savedProductIds = useMemo(() => new Set(saved.items.map((p) => p.id)), [saved.items]);`
       - `const handleNewChat = () => setSelectedTab('chat');` (preserve from legacy)
    5. Preserve the legacy `<header>`, `<Tabs>` / `<TabsList>` / 3× `<TabsTrigger>`, and `<Button>` JSX BYTE-IDENTICALLY. The hex literals (`bg-[#008060]`, `text-[#008060]`, `border-[#e1e3e5]`, `text-[#6d7175]`, `text-[#202223]`) in the legacy file are already arbitrary-value Tailwind classes — UI-SPEC §"D-12 cleanup" maps each to the SAME class. KEEP them as-is. D-12 was misnamed; the cleanup is a no-op verify step. Confirm via `grep -E "bg-\[#|text-\[#|border-\[#" components/chat/chat-shell.tsx` and copy each match verbatim.
    6. Replace the legacy `<TabsContents>` block (legacy lines ~90–104) with:
       - `<TabsContents>`
       - `  <TabsContent value="chat" className="h-[calc(100%-180px)]"><ChatPane adapter={adapter} savedProductIds={savedProductIds} onToggleSave={saved.toggle} onHistoryAdd={history.add} /></TabsContent>`
       - `  <TabsContent value="history"><HistoryPanel items={history.items} onClear={history.clear} /></TabsContent>`
       - `  <TabsContent value="saved"><SavedProductsPanel products={saved.items} onToggleSave={saved.toggle} /></TabsContent>`
       - `</TabsContents>`
       - Verify the prop names `items` / `onClear` / `products` / `onToggleSave` against the actual lifted HistoryPanel + SavedProductsPanel signatures before finalizing. If the lifted props differ from this draft, use the lifted props verbatim (read `lib/chat-ui/components/history-panel.tsx` + `saved-products-panel.tsx` first).
    7. Add the outer surface-specific height: the shell's top-level wrapper `<div>` (whatever it is in the legacy file) gets `h-[calc(100vh-100px)]` appended to its className. Pick the exact insertion point that matches the legacy layout — the surface-height constraint must wrap the entire tab structure so the message-list within ChatPane gets its computed height context.
    8. NO `console.*` calls (security hygiene — CLAUDE.md).
    9. NO `any` casts in this file.

    Update `app/(embedded)/chat/page.tsx`:
    - Line 2: change `import { ChatShell } from '@/components/chat/chat-shell';` → `import { ChatShell } from './chat-shell';`
    - Line 32 (or whichever line renders `<ChatShell />`): change `<ChatShell />` → `<ChatShell shop={shop ?? ''} />`. The variable `shop` is already derived from `searchParams` in the existing RSC body (preserved from Phase 4 D-11) — do not introduce new searchParams handling.

    Relocate `components/chat/__tests__/chat-shell.test.tsx` to `app/(embedded)/chat/__tests__/chat-shell.test.tsx`:
    - Copy the file to the new path.
    - Update the `vi.mock` target on line ~31:
      - BEFORE: `vi.mock('@/components/chat/chat', () => ({ default: ({ onHistoryAdd, onToggleSave }: {...}) => (...) }));`
      - AFTER: `vi.mock('@/lib/chat-ui', () => ({ ChatPane: ({ onHistoryAdd, onToggleSave }: {...}) => (...), HistoryPanel: (props: {...}) => (...), SavedProductsPanel: (props: {...}) => (...) }));`
      - Note 1: the key changes from `default:` to `ChatPane:` (named export).
      - Note 2: HistoryPanel + SavedProductsPanel must also be mocked because the new chat-shell.tsx imports them from the same barrel — without these mocks the test will crash trying to render the real components.
    - Update the import on line ~51: `import { ChatShell } from '@/components/chat/chat-shell';` → `import { ChatShell } from '@/app/(embedded)/chat/chat-shell';` (or `'../chat-shell'` — both work; pick whichever is consistent with neighboring test files).
    - Update the render call on line ~55: `render(<ChatShell />)` → `render(<ChatShell shop="example.myshopify.com" />)` (the shop prop is now required).
    - Test body otherwise unchanged.

    Update `app/(embedded)/chat/__tests__/page.test.tsx`:
    - Single line edit (line ~26): change `vi.mock('@/components/chat/chat-shell', () => ({` → `vi.mock('./chat-shell', () => ({` (or `'../chat-shell'`, matching whatever import form the actual page.tsx uses post-edit).
    - Test body otherwise unchanged.
  </action>
  <verify>
    <automated>bunx vitest run app/\(embedded\)/chat/__tests__/</automated>
  </verify>
  <acceptance_criteria>
    - File `app/(embedded)/chat/chat-shell.tsx` exists (replacing or net-new).
    - `grep -c "'use client'" app/\(embedded\)/chat/chat-shell.tsx` returns at least 1.
    - `grep -c "EmbeddedAdapter" app/\(embedded\)/chat/chat-shell.tsx` returns at least 2 (import + instantiation).
    - `grep -c "useHistoryStore" app/\(embedded\)/chat/chat-shell.tsx` returns at least 2 (import + call).
    - `grep -c "useSavedProductsStore" app/\(embedded\)/chat/chat-shell.tsx` returns at least 2.
    - `grep -c "from '@/lib/chat-ui'" app/\(embedded\)/chat/chat-shell.tsx` returns 1 (barrel import).
    - `grep -c "h-\[calc(100vh-100px)\]" app/\(embedded\)/chat/chat-shell.tsx` returns at least 1 (surface height moved here).
    - `grep -c "from './chat-shell'" app/\(embedded\)/chat/page.tsx` returns 1.
    - `grep -c "@/components/chat" app/\(embedded\)/chat/page.tsx` returns 0.
    - `grep -c "shop={shop" app/\(embedded\)/chat/page.tsx` returns 1.
    - File `app/(embedded)/chat/__tests__/chat-shell.test.tsx` exists.
    - `grep -c "vi.mock('@/lib/chat-ui'" app/\(embedded\)/chat/__tests__/chat-shell.test.tsx` returns 1.
    - `grep -c "ChatPane:" app/\(embedded\)/chat/__tests__/chat-shell.test.tsx` returns at least 1 (mock keyed on named export).
    - `grep -c "default:" app/\(embedded\)/chat/__tests__/chat-shell.test.tsx` returns 0 (no legacy default-export mock).
    - `grep -c "vi.mock('@/components/chat" app/\(embedded\)/chat/__tests__/page.test.tsx` returns 0 (legacy mock path gone).
    - `bunx vitest run app/\(embedded\)/chat/__tests__/` exits 0 (chat-shell.test.tsx + page.test.tsx GREEN).
  </acceptance_criteria>
  <done>
    Embedded surface shell rebuilt; page.tsx updated; both embedded tests GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete legacy components/chat tree + cleanup prototype-data comment + verify grep gate</name>
  <files>components/chat/chat.tsx, components/chat/chat-shell.tsx, components/chat/chat-message.tsx, components/chat/product-card.tsx, components/chat/history-panel.tsx, components/chat/saved-products-panel.tsx, components/chat/empty-state.tsx, components/chat/message-parts.tsx, components/chat/__tests__/product-card.test.tsx, components/chat/__tests__/history-panel.test.tsx, components/chat/__tests__/saved-products-panel.test.tsx, components/chat/__tests__/message-parts.test.tsx, components/chat/__tests__/chat-shell.test.tsx, components/chat/chat.integration-test.tsx, app/prototype/prototype-data.ts</files>
  <read_first>
    - /Users/ikhuda/sites/personal/shopify-search-app/app/prototype/prototype-data.ts (line 1 — dead comment to delete)
    - /Users/ikhuda/sites/personal/shopify-search-app/.planning/phases/05-shared-chat-ui-extraction/05-CONTEXT.md (D-11 — hard cut, no shims)
    - /Users/ikhuda/sites/personal/shopify-search-app/.planning/phases/05-shared-chat-ui-extraction/05-PATTERNS.md (lines 711–717 — prototype-data.ts cleanup)
  </read_first>
  <behavior>
    - All 14 legacy files under `components/chat/` and `components/chat/__tests__/` are DELETED.
    - The `components/chat/` directory and its `__tests__/` subdirectory become empty and are also removed.
    - `app/prototype/prototype-data.ts` no longer contains the dead `// import { MOCK_PRODUCTS } ...` comment on line 1.
    - The grep gate `grep -rn '@/components/chat' app/ lib/ components/` returns ZERO matches.
  </behavior>
  <action>
    Delete each of the 14 legacy files listed in `<files>` above using `rm` (do NOT leave shims; D-11 is a hard cut). Then attempt to remove the now-empty directories via `rmdir components/chat/__tests__/` followed by `rmdir components/chat/` (rmdir is safe — it fails if non-empty, surfacing any missed file).

    Specifically:
    - `rm components/chat/chat.tsx components/chat/chat-shell.tsx components/chat/chat-message.tsx components/chat/product-card.tsx components/chat/history-panel.tsx components/chat/saved-products-panel.tsx components/chat/empty-state.tsx components/chat/message-parts.tsx components/chat/chat.integration-test.tsx`
    - `rm components/chat/__tests__/product-card.test.tsx components/chat/__tests__/history-panel.test.tsx components/chat/__tests__/saved-products-panel.test.tsx components/chat/__tests__/message-parts.test.tsx components/chat/__tests__/chat-shell.test.tsx`
    - `rmdir components/chat/__tests__/ components/chat/` (separate calls — `__tests__` first since it's nested).
    - If `components/chat/` has other unexpected files (e.g., a `mock-products.ts` that survived Phase 4), STOP and report — the directory should be empty after the 14-file removal.

    Update `app/prototype/prototype-data.ts`:
    - Delete line 1 (the dead `// import { MOCK_PRODUCTS } from '@/components/chat/mock-products';` comment). MOCK_PRODUCTS was removed in Phase 4 (per STATE.md); the dead comment would survive grep gates since it's a comment, not an import, but it must go for hygiene.
    - All other lines of `prototype-data.ts` UNCHANGED.

    After all deletions and the prototype-data cleanup, run the grep gate:
    - `grep -rn '@/components/chat' app/ lib/ components/ 2>/dev/null` MUST return ZERO matches. If any match remains, STOP and report which file still references the legacy path — that file needs an import update before the plan can be considered done.
  </action>
  <verify>
    <automated>set -e; test ! -d components/chat &amp;&amp; grep -rn '@/components/chat' app/ lib/ components/ 2>/dev/null; status=$?; [ $status -ne 0 ] &amp;&amp; echo OK || echo "FAIL: components/chat still exists or grep matched"</automated>
  </verify>
  <acceptance_criteria>
    - Directory `components/chat/` does NOT exist (`test ! -d components/chat` returns 0).
    - Directory `components/chat/__tests__/` does NOT exist.
    - `grep -rn '@/components/chat' app/ lib/ components/ 2>/dev/null` returns ZERO matches (the grep gate from D-11).
    - File `app/prototype/prototype-data.ts` exists.
    - `grep -c "MOCK_PRODUCTS" app/prototype/prototype-data.ts` returns 0 (dead comment removed).
    - `grep -c "@/components/chat/mock-products" app/prototype/prototype-data.ts` returns 0.
    - `bun build` exits 0 (no orphan imports broke the build).
    - `bunx vitest run lib/chat-ui/__tests__/ app/\(embedded\)/chat/__tests__/` exits 0 (post-deletion test suite stays GREEN).
  </acceptance_criteria>
  <done>
    Legacy components/chat tree deleted; prototype-data comment removed; grep gate clean; bun build clean; relevant tests still GREEN.
  </done>
</task>

</tasks>

<verification>
- `grep -rn '@/components/chat' app/ lib/ components/` returns ZERO matches (D-11 hard cut verified).
- `test ! -d components/chat` succeeds (directory deleted).
- `bunx vitest run lib/chat-ui/__tests__/ app/\(embedded\)/chat/__tests__/` exits 0.
- `bun build` exits 0 (TS strict + no orphan imports).
- The embedded admin chat page (post `bun dev`) still renders the same tabs, header, and chat surface — visual parity preserved per UI-SPEC §"File Move Parity Rules" rule #1 (manual verification deferred to Plan 05).
</verification>

<success_criteria>
- `app/(embedded)/chat/chat-shell.tsx` is the only ChatShell in the repo, uses EmbeddedAdapter + store hooks, owns surface-specific heights.
- `app/(embedded)/chat/page.tsx` imports ChatShell from `./chat-shell` and passes `shop` prop.
- All 14 legacy `components/chat/*` files DELETED. `components/chat/` directory removed.
- `app/(embedded)/chat/__tests__/chat-shell.test.tsx` mocks `@/lib/chat-ui` with `ChatPane:` named-export key.
- `app/(embedded)/chat/__tests__/page.test.tsx` vi.mock target updated.
- `app/prototype/prototype-data.ts` line 1 dead comment removed.
- Grep gate `grep -rn '@/components/chat' app/ lib/ components/` returns ZERO matches.
- `bun build` clean; relevant tests GREEN.
</success_criteria>

<output>
Create `.planning/phases/05-shared-chat-ui-extraction/05-04-SUMMARY.md` when done, recording:
- chat-shell.tsx rewrite summary (adapter instantiation, store hook wiring, surface-height insertion point)
- page.tsx + chat-shell.test.tsx + page.test.tsx diff summary (single-line edits each, except chat-shell.test.tsx mock rewrite)
- 14 deleted files (full list)
- prototype-data.ts cleanup confirmation
- Grep gate result (zero matches)
- bun build status
</output>
