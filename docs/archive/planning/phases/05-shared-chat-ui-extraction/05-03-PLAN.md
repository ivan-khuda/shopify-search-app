---
phase: 05-shared-chat-ui-extraction
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - lib/chat-ui/components/chat-pane.tsx
  - lib/chat-ui/components/chat-message.tsx
  - lib/chat-ui/components/product-card.tsx
  - lib/chat-ui/components/history-panel.tsx
  - lib/chat-ui/components/saved-products-panel.tsx
  - lib/chat-ui/components/empty-state.tsx
  - lib/chat-ui/components/message-parts.tsx
  - lib/chat-ui/index.ts
autonomous: true
requirements: [SHR-01, SHR-04]
tags: [refactor, components, chat-ui, ui]

must_haves:
  truths:
    - "D-01: Seven component files exist under lib/chat-ui/components/ as byte-identical lifts of components/chat/*.tsx EXCEPT the two UI-SPEC-locked deltas (user-bubble width clamp + chat-pane height removals) and intra-tree import-path updates; HistoryPanel + SavedProductsPanel ship as siblings in the barrel so any surface can opt in"
    - "D-10: ChatPane wires useChat via DefaultChatTransport with adapter.endpoint as `api`, adapter.getAuthHeaders as `headers`, adapter.getRequestBody as `body` (Resolvable functions — Pattern 1); imports continue to draw from @/components/ai-elements/* (ai-elements stays put at its current path)"
    - "D-02: ChatPane is prop-driven: accepts `adapter: ChatIdentityAdapter`, `savedProductIds: Set<string>`, `onToggleSave`, `onHistoryAdd`"
    - "D-01: lib/chat-ui/index.ts barrel exports ChatPane, ChatMessage, ProductCard, HistoryPanel, SavedProductsPanel, EmptyState, ChatIdentityAdapter, HistoryStore, SavedProductsStore, useHistoryStore, useSavedProductsStore"
    - "lib/chat-ui/index.ts does NOT export MessageParts and does NOT re-export any concrete `./adapters/` module (type-only re-export from `./adapters/types` IS permitted per D-04 — TS erases types at compile time — T-5-02 mitigation)"
    - "barrel-isolation.test.ts is fully GREEN (both `it` blocks pass; the type-only-exempt adapter regex authored in Plan 01 Task 1 is honored as-is, not mutated)"
    - "Four relocated component tests + chat-pane integration test exist under lib/chat-ui/__tests__/ and pass"
  artifacts:
    - path: "lib/chat-ui/components/chat-pane.tsx"
      provides: "ChatPane component (renamed from Chat) with adapter-driven transport"
      exports: ["ChatPane"]
    - path: "lib/chat-ui/components/chat-message.tsx"
      provides: "ChatMessage component (byte-lift + max-w-[min(448px,100%)] clamp)"
      exports: ["ChatMessage"]
    - path: "lib/chat-ui/components/product-card.tsx"
      provides: "ProductCard component (byte-lift)"
      exports: ["ProductCard"]
    - path: "lib/chat-ui/components/history-panel.tsx"
      provides: "HistoryPanel component (byte-lift + intra-tree import)"
      exports: ["HistoryPanel"]
    - path: "lib/chat-ui/components/saved-products-panel.tsx"
      provides: "SavedProductsPanel component (byte-lift + intra-tree imports)"
      exports: ["SavedProductsPanel"]
    - path: "lib/chat-ui/components/empty-state.tsx"
      provides: "EmptyState component (byte-lift)"
      exports: ["EmptyState"]
    - path: "lib/chat-ui/components/message-parts.tsx"
      provides: "MessageParts component (byte-lift + cross-tree path correction)"
      exports: ["MessageParts"]
    - path: "lib/chat-ui/index.ts"
      provides: "Barrel re-exports (named only)"
      exports: ["ChatPane", "ChatMessage", "ProductCard", "HistoryPanel", "SavedProductsPanel", "EmptyState", "ChatIdentityAdapter (type)", "HistoryStore (type)", "SavedProductsStore (type)", "useHistoryStore", "useSavedProductsStore"]
  key_links:
    - from: "lib/chat-ui/components/chat-pane.tsx"
      to: "ai/DefaultChatTransport + @ai-sdk/react/useChat"
      via: "useMemo(() => new DefaultChatTransport({ api, headers, body }))"
      pattern: "new DefaultChatTransport"
    - from: "lib/chat-ui/index.ts"
      to: "lib/chat-ui/components/* + lib/chat-ui/stores/hooks"
      via: "named re-export"
      pattern: "export\\s+\\{\\s*ChatPane"
---

<objective>
Wave 2: Lift seven chat components from `components/chat/` into `lib/chat-ui/components/`, apply the two locked UI-SPEC deltas (user-bubble width clamp + chat-pane surface-height removal), rename the legacy `Chat` default export to a named `ChatPane` export wired via `DefaultChatTransport`, relocate all unit + integration tests, and create the barrel `lib/chat-ui/index.ts` that exports only what D-04 permits.

Purpose: This is the SHR-01 + SHR-04 contract delivery. After this plan, the shared barrel exists and is consumable by surface shells. The embedded admin still imports from the legacy path — Plan 04 cuts that import over.

Output: 7 component files + 1 barrel + 5 relocated test files (4 unit tests + 1 integration test renamed to chat-pane.integration-test.tsx).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-shared-chat-ui-extraction/05-CONTEXT.md
@.planning/phases/05-shared-chat-ui-extraction/05-RESEARCH.md
@.planning/phases/05-shared-chat-ui-extraction/05-PATTERNS.md
@.planning/phases/05-shared-chat-ui-extraction/05-UI-SPEC.md
@.planning/phases/05-shared-chat-ui-extraction/05-02-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Contracts from Plan 02 (adapter + stores) and existing codebase that this plan consumes. -->

lib/chat-ui/adapters/types.ts (from Plan 02):
- export interface ChatIdentityAdapter { endpoint, getAuthHeaders, getRequestBody }

lib/chat-ui/stores/types.ts + hooks.ts (from Plan 02):
- HistoryStore, SavedProductsStore interfaces
- useHistoryStore(scope), useSavedProductsStore(scope) hooks

ai package (already installed @ 6.0.77):
- DefaultChatTransport class. Constructor accepts { api: string, headers?: Resolvable<Record<string,string>|Headers>, body?: Resolvable<object> }.
- Resolvable<T> = T | Promise<T> | (() => T | Promise<T>) — verified against node_modules/ai/dist/index.d.ts:3513-3577.

@ai-sdk/react package (already installed @ 3.0.75):
- useChat({ transport }): { messages, sendMessage, status, ... } — verified against node_modules/@ai-sdk/react/dist/index.d.ts.

types/product.ts:
- ChatProduct, ChatHistoryItem types (already cross-cutting; do NOT move).

components/ai-elements/* (stays at current path per D-10):
- PromptInput primitives, Response, Attachments — imported cross-tree.

components/ui/* (stays):
- Tabs, Button, TextShimmer — imported cross-tree.

lib/chat-ui/__tests__/barrel-isolation.test.ts (from Plan 01 — DO NOT MUTATE):
- Adapter-exemption regex `/from\s+['"]\.\/adapters\/(?!types['"])/` is authored from the start in Plan 01.
- Plan 03 honors this regex; the barrel's type-only re-export `export type { ChatIdentityAdapter } from './adapters/types'` is permitted by the regex's negative lookahead.
- Concrete adapter re-exports (`./adapters/embedded`, `./adapters/storefront`) remain forbidden.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lift the six pure components + create barrel</name>
  <files>lib/chat-ui/components/chat-message.tsx, lib/chat-ui/components/product-card.tsx, lib/chat-ui/components/history-panel.tsx, lib/chat-ui/components/saved-products-panel.tsx, lib/chat-ui/components/empty-state.tsx, lib/chat-ui/components/message-parts.tsx, lib/chat-ui/index.ts</files>
  <read_first>
    - /Users/ikhuda/sites/personal/shopify-search-app/components/chat/chat-message.tsx (source — byte-lift target)
    - /Users/ikhuda/sites/personal/shopify-search-app/components/chat/product-card.tsx (source — byte-lift target)
    - /Users/ikhuda/sites/personal/shopify-search-app/components/chat/history-panel.tsx (source — byte-lift target)
    - /Users/ikhuda/sites/personal/shopify-search-app/components/chat/saved-products-panel.tsx (source — byte-lift target)
    - /Users/ikhuda/sites/personal/shopify-search-app/components/chat/empty-state.tsx (source — byte-lift target)
    - /Users/ikhuda/sites/personal/shopify-search-app/components/chat/message-parts.tsx (source — byte-lift target)
    - /Users/ikhuda/sites/personal/shopify-search-app/.planning/phases/05-shared-chat-ui-extraction/05-UI-SPEC.md (lines 188–194 — width-clamp lock; lines 288–298 — File Move Parity Rules)
    - /Users/ikhuda/sites/personal/shopify-search-app/.planning/phases/05-shared-chat-ui-extraction/05-PATTERNS.md (lines 405–525 — per-file lift instructions with exact line-level import diffs)
    - /Users/ikhuda/sites/personal/shopify-search-app/lib/chat-ui/__tests__/barrel-isolation.test.ts (Plan 01 output — already authored with type-only adapter exemption; DO NOT MUTATE)
  </read_first>
  <behavior>
    - Each lifted component renders byte-identically to its `components/chat/` source except for two locked changes:
      (a) chat-message.tsx line 81 — replace `"max-w-md"` with `"max-w-[min(448px,100%)]"` per UI-SPEC rule #5.
      (b) Intra-tree import path updates per PATTERNS.md (e.g., `@/components/chat/empty-state` → `./empty-state`).
    - barrel lib/chat-ui/index.ts re-exports the named components + EmptyState + interfaces + store hooks. MessageParts is intentionally NOT re-exported (internal implementation of ChatMessage per RESEARCH §"Open Questions" #1). Concrete adapter modules are NOT re-exported (D-04 lock — T-5-02 mitigation). Type-only re-export of `./adapters/types` IS permitted (Plan 01 Task 1's regex already exempts it via negative lookahead; this plan does NOT mutate that test).
  </behavior>
  <action>
    For each of the 6 component files below, COPY the corresponding `components/chat/<file>.tsx` into `lib/chat-ui/components/<file>.tsx` byte-for-byte, then apply ONLY the per-file changes specified:

    1. `lib/chat-ui/components/empty-state.tsx`: byte-identical copy. ZERO import changes.

    2. `lib/chat-ui/components/product-card.tsx`: byte-identical copy. ZERO import changes (already uses `@/types/product`, `next/image`, `lucide-react` cross-tree).

    3. `lib/chat-ui/components/chat-message.tsx`: byte-identical copy EXCEPT line 81 — replace the string literal `"max-w-md"` with `"max-w-[min(448px,100%)]"` (UI-SPEC rule #5). Verify the `MessageParts` import stays `from './message-parts'` (relative — works at new path).

    4. `lib/chat-ui/components/history-panel.tsx`: byte-identical copy EXCEPT line 5 — change `from '@/components/chat/empty-state'` to `from './empty-state'`.

    5. `lib/chat-ui/components/saved-products-panel.tsx`: byte-identical copy EXCEPT lines 5–6 — change `from '@/components/chat/empty-state'` to `from './empty-state'` AND `from '@/components/chat/product-card'` to `from './product-card'`.

    6. `lib/chat-ui/components/message-parts.tsx`: byte-identical copy EXCEPT lines 6–8:
       - BEFORE: `import { TextShimmer } from "../ui/text-shimmer";` → AFTER: `import { TextShimmer } from "@/components/ui/text-shimmer";`
       - BEFORE: `import { Response } from "../ai-elements/response";` → AFTER: `import { Response } from "@/components/ai-elements/response";`
       - BEFORE: `import { ProductCard } from "@/components/chat/product-card";` → AFTER: `import { ProductCard } from "./product-card";`
       - Rationale: legacy `../ui/` and `../ai-elements/` worked because the file lived at `components/chat/`. After the move, relative depth changes — use `@/components/ui/*` for cross-tree and `./product-card` for sibling per UI-SPEC §"Permitted cross-tree dependency".

    7. Preserve `'use client'` directives EXACTLY as they appear in the source files (UI-SPEC rule #6). Verify with `head -1` on the source files BEFORE the move; chat-message.tsx does NOT have `'use client'` today (verified per PATTERNS.md cross-cutting table); the other five DO. Do not add or remove the directive.

    Create `lib/chat-ui/index.ts` (the barrel) with EXACTLY these exports — no more, no less:
    - `export { ChatPane } from './components/chat-pane';` (note: chat-pane.tsx is created in Task 2 of this plan; the barrel can reference it since Task 2 runs in the same plan)
    - `export { ChatMessage } from './components/chat-message';`
    - `export { ProductCard } from './components/product-card';`
    - `export { HistoryPanel } from './components/history-panel';`
    - `export { SavedProductsPanel } from './components/saved-products-panel';`
    - `export { EmptyState } from './components/empty-state';`
    - `export type { ChatIdentityAdapter } from './adapters/types';` — type-only re-export. Plan 01 Task 1's barrel-isolation regex `/from\s+['"]\.\/adapters\/(?!types['"])/` permits this via negative lookahead (TypeScript erases the type at compile time, so no runtime adapter code reaches the storefront bundle).
    - `export type { HistoryStore, SavedProductsStore } from './stores/types';`
    - `export { useHistoryStore, useSavedProductsStore } from './stores/hooks';`
    - Add a header comment block stating: "CONTRACT (D-04 + barrel-isolation.test.ts): re-exports components + interfaces + store hooks. DOES NOT re-export concrete adapter modules. The type-only re-export from `./adapters/types` is permitted because TypeScript erases type-only imports at compile time — no runtime adapter code reaches the storefront bundle. Consumers import concrete adapters via sub-paths (`@/lib/chat-ui/adapters/embedded` / `@/lib/chat-ui/adapters/storefront`)."

    DO NOT modify `lib/chat-ui/__tests__/barrel-isolation.test.ts`. The regex authored in Plan 01 Task 1 already exempts `./adapters/types` via negative lookahead — the barrel above is compatible with that test as-written. Mutating the Wave 0 RED contract mid-plan is forbidden.
  </action>
  <verify>
    <automated>bunx vitest run lib/chat-ui/__tests__/barrel-isolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 6 component files exist at `lib/chat-ui/components/`.
    - File `lib/chat-ui/index.ts` exists.
    - `grep -c "max-w-\[min(448px,100%)\]" lib/chat-ui/components/chat-message.tsx` returns 1 (the locked clamp).
    - `grep -c "max-w-md" lib/chat-ui/components/chat-message.tsx` returns 0 (legacy value gone).
    - Recursive check (W2): `! grep -rn '@/components/chat' lib/chat-ui/components/` exits zero (no matches anywhere under components/). Equivalently: `grep -rl '@/components/chat' lib/chat-ui/components/ 2>/dev/null` prints nothing.
    - `grep -c "export { ChatPane }" lib/chat-ui/index.ts` returns 1.
    - `grep -c "export { EmptyState }" lib/chat-ui/index.ts` returns 1.
    - `grep -c "MessageParts" lib/chat-ui/index.ts` returns 0 (intentionally not re-exported).
    - `grep -cE "export\s+(\\*|\\{[^}]*\\})\s+from\s+['\"]\\./adapters/(embedded|storefront)" lib/chat-ui/index.ts` returns 0 (no concrete adapter re-export — D-04 / T-5-02).
    - The barrel test file `lib/chat-ui/__tests__/barrel-isolation.test.ts` is UNCHANGED relative to Plan 01 output (no mid-plan edits): `git diff lib/chat-ui/__tests__/barrel-isolation.test.ts` shows zero changes within this task's commit scope beyond the Plan 01 commit.
    - `bunx vitest run lib/chat-ui/__tests__/barrel-isolation.test.ts` exits 0 (both assertions GREEN).
  </acceptance_criteria>
  <done>
    Six lifted component files exist with only the two locked changes; barrel created with correct exports (including type-only adapter-types re-export); barrel-isolation test GREEN without any test-file mutation in this plan.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lift chat.tsx → chat-pane.tsx with DefaultChatTransport wiring + relocate component tests</name>
  <files>lib/chat-ui/components/chat-pane.tsx, lib/chat-ui/__tests__/product-card.test.tsx, lib/chat-ui/__tests__/history-panel.test.tsx, lib/chat-ui/__tests__/saved-products-panel.test.tsx, lib/chat-ui/__tests__/message-parts.test.tsx, lib/chat-ui/__tests__/chat-pane.integration-test.tsx</files>
  <read_first>
    - /Users/ikhuda/sites/personal/shopify-search-app/components/chat/chat.tsx (source — byte-lift target + transport rewiring)
    - /Users/ikhuda/sites/personal/shopify-search-app/.planning/phases/05-shared-chat-ui-extraction/05-PATTERNS.md (lines 325–402 — chat-pane.tsx instructions: imports, props change, transport wiring, layout-height removals)
    - /Users/ikhuda/sites/personal/shopify-search-app/.planning/phases/05-shared-chat-ui-extraction/05-RESEARCH.md (Pattern 1 chat-pane code; Pitfalls 1, 2, 5)
    - /Users/ikhuda/sites/personal/shopify-search-app/.planning/phases/05-shared-chat-ui-extraction/05-UI-SPEC.md (lines 56–62 — surface-specific heights MUST NOT live in ChatPane)
    - /Users/ikhuda/sites/personal/shopify-search-app/components/chat/chat.integration-test.tsx (source for relocated integration test)
    - /Users/ikhuda/sites/personal/shopify-search-app/components/chat/__tests__/product-card.test.tsx (source for relocated unit test — one-line import change)
    - /Users/ikhuda/sites/personal/shopify-search-app/components/chat/__tests__/history-panel.test.tsx (source for relocated unit test)
    - /Users/ikhuda/sites/personal/shopify-search-app/components/chat/__tests__/saved-products-panel.test.tsx (source for relocated unit test)
    - /Users/ikhuda/sites/personal/shopify-search-app/components/chat/__tests__/message-parts.test.tsx (source for relocated unit test)
    - /Users/ikhuda/sites/personal/shopify-search-app/node_modules/ai/dist/index.d.ts (lines around 3513–3577 — confirm DefaultChatTransport constructor signature)
  </read_first>
  <behavior>
    - ChatPane is a named export (not default).
    - useChat is called as `useChat({ transport })` where `transport` is a useMemo'd `new DefaultChatTransport({ api: adapter.endpoint, headers: () => adapter.getAuthHeaders(), body: () => adapter.getRequestBody() })`. The functions are passed UNCALLED so Resolvable invokes them fresh on each sendMessage (Pitfall 2 avoidance).
    - ChatPane no longer derives `savedProductIds` internally — it accepts the Set directly (CONTEXT D-02).
    - Surface-specific heights are REMOVED from ChatPane outer div + message-list div (they move to the surface shell in Plan 04).
    - 4 relocated unit tests + 1 integration test pass. (Note: `components/chat/__tests__/chat-message.test.tsx` does not exist in the legacy tree per W1 audit — no chat-message unit test is relocated in this plan.)
    - barrel-isolation test stays GREEN.
  </behavior>
  <action>
    Create `lib/chat-ui/components/chat-pane.tsx` by copying `components/chat/chat.tsx` and applying these specific transforms:

    1. Preserve `'use client'` directive at line 1.
    2. Add new imports below the existing ones (preserve order):
       - `import { DefaultChatTransport } from 'ai';`
       - `import type { ChatIdentityAdapter } from '../adapters/types';`
    3. Update intra-tree import for `ChatMessage`:
       - BEFORE: `import { ChatMessage } from './chat-message';` (already relative — STAYS the same; no change needed)
    4. Replace the `ChatProps` interface and the default-export function with:
       - `interface ChatPaneProps { adapter: ChatIdentityAdapter; savedProductIds: Set<string>; onToggleSave: (product: ChatProduct) => void; onHistoryAdd: (entry: ChatHistoryItem) => void; }`
       - `export function ChatPane({ adapter, savedProductIds, onToggleSave, onHistoryAdd }: ChatPaneProps) { ... }`
       - The legacy `export default function Chat({ savedProducts, onToggleSave, onHistoryAdd }: ChatProps)` is REPLACED. There is NO `export default` in this file (CONTEXT discretion — named exports only).
    5. Remove the `savedProductIds = useMemo(() => new Set(savedProducts.map(p => p.id)), [savedProducts])` block (legacy chat.tsx lines 77–80). The Set is now a prop.
    6. Replace the legacy `const { messages, sendMessage, status } = useChat();` (legacy chat.tsx line 76) with:
       - `const transport = useMemo(() => new DefaultChatTransport({ api: adapter.endpoint, headers: () => adapter.getAuthHeaders(), body: () => adapter.getRequestBody() }), [adapter]);`
       - `const { messages, sendMessage, status } = useChat({ transport });`
       - The arrow functions `() => adapter.getAuthHeaders()` MUST be passed uncalled (Resolvable). Do NOT use `headers: await adapter.getAuthHeaders()` — that would resolve once and cache.
    7. Remove surface-specific heights (UI-SPEC rule #4):
       - BEFORE (legacy line 106): `<div className="flex flex-col w-full max-w-3xl h-[calc(100vh-100px)] mx-auto stretch gap-6 pt-3">`
       - AFTER:                    `<div className="flex flex-col w-full max-w-3xl mx-auto stretch gap-6 pt-3">`
       - BEFORE (legacy line 107): `<div className='h-[calc(100%-180px)] flex flex-col flex-1 gap-4 overflow-auto pr-4'>`
       - AFTER:                    `<div className='flex flex-col flex-1 gap-4 overflow-auto pr-4'>`
    8. Every other line of legacy `chat.tsx` is preserved byte-identically — the handleSubmit body, the empty-state inline `<p>Hello! I&apos;m...</p>`, the messages.map render, the PromptInputProvider block, all helper memo components (AttachmentItem, PromptInputAttachmentsDisplay) — all unchanged.
    9. NO `any` casts. NO `as unknown as` (SC #4).
    10. NO `console.*` calls.

    Relocate 4 component unit tests by copying their sources to `lib/chat-ui/__tests__/` and applying single-line import updates:
    - `lib/chat-ui/__tests__/product-card.test.tsx`: copy from `components/chat/__tests__/product-card.test.tsx`, change line 4 import to `import { ProductCard } from '@/lib/chat-ui';` (use barrel — ProductCard is a named export).
    - `lib/chat-ui/__tests__/history-panel.test.tsx`: copy from `components/chat/__tests__/history-panel.test.tsx`, change line 3 import to `import { HistoryPanel } from '@/lib/chat-ui';`.
    - `lib/chat-ui/__tests__/saved-products-panel.test.tsx`: copy from `components/chat/__tests__/saved-products-panel.test.tsx`, change line 3 import to `import { SavedProductsPanel } from '@/lib/chat-ui';`.
    - `lib/chat-ui/__tests__/message-parts.test.tsx`: copy from `components/chat/__tests__/message-parts.test.tsx`, change line 5 import to `import { MessageParts } from '@/lib/chat-ui/components/message-parts';` (deep-path — MessageParts is intentionally NOT in the barrel).

    (Note per W1 audit: `components/chat/__tests__/chat-message.test.tsx` does NOT exist in the legacy tree. There is no chat-message unit test to relocate. This task therefore creates 4 relocated unit-test files — not 5 — and does NOT reference `lib/chat-ui/__tests__/chat-message.test.tsx` in its files list or `files_modified`.)

    Create `lib/chat-ui/__tests__/chat-pane.integration-test.tsx` by copying `components/chat/chat.integration-test.tsx` and applying these transforms:
    - Rename file from `chat.integration-test.tsx` → `chat-pane.integration-test.tsx`.
    - Replace import line 3: `import Chat from '@/components/chat/chat';` → `import { ChatPane } from '@/lib/chat-ui';`
    - Add a stub adapter at the top of the test file (after imports):
      - `const mockAdapter: ChatIdentityAdapter = { endpoint: '/api/chat', getAuthHeaders: async () => ({}), getRequestBody: async () => ({}) };`
      - `import type { ChatIdentityAdapter } from '@/lib/chat-ui';`
    - Replace all `<Chat savedProducts={[TEST_PRODUCT]} ...>` render sites (legacy lines ~53–58, ~103–108) with `<ChatPane adapter={mockAdapter} savedProductIds={new Set([TEST_PRODUCT.id])} ...>` — the `savedProducts` → `savedProductIds` prop conversion per CONTEXT D-02.
    - Preserve the legacy `vi.mock('@ai-sdk/react', ...)` block (lines 31–37) UNCHANGED. The mock fully shadows useChat — the transport-prop change doesn't affect the mock surface.
    - The `as never` cast at legacy line 98 is permitted (test-file exception per CLAUDE.md).
  </action>
  <verify>
    <automated>bunx vitest run lib/chat-ui/__tests__/</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/chat-ui/components/chat-pane.tsx` exists.
    - `grep -c "export function ChatPane" lib/chat-ui/components/chat-pane.tsx` returns 1.
    - `grep -c "export default" lib/chat-ui/components/chat-pane.tsx` returns 0 (named exports only).
    - `grep -c "new DefaultChatTransport" lib/chat-ui/components/chat-pane.tsx` returns 1.
    - `grep -c "headers: () => adapter.getAuthHeaders()" lib/chat-ui/components/chat-pane.tsx` returns 1 (uncalled function — Resolvable).
    - `grep -c "body: () => adapter.getRequestBody()" lib/chat-ui/components/chat-pane.tsx` returns 1.
    - `grep -c "h-\[calc(100vh-100px)\]" lib/chat-ui/components/chat-pane.tsx` returns 0 (surface height removed).
    - `grep -c "h-\[calc(100%-180px)\]" lib/chat-ui/components/chat-pane.tsx` returns 0.
    - All 4 relocated unit test files exist at `lib/chat-ui/__tests__/` (product-card.test.tsx, history-panel.test.tsx, saved-products-panel.test.tsx, message-parts.test.tsx). The file `lib/chat-ui/__tests__/chat-message.test.tsx` is NOT created (no legacy source exists per W1 audit).
    - File `lib/chat-ui/__tests__/chat-pane.integration-test.tsx` exists.
    - `grep -c "from '@/lib/chat-ui'" lib/chat-ui/__tests__/chat-pane.integration-test.tsx` returns at least 1.
    - `grep -c "Chat from '@/components/chat/chat'" lib/chat-ui/__tests__/chat-pane.integration-test.tsx` returns 0 (legacy import gone).
    - `bunx vitest run lib/chat-ui/__tests__/` exits 0 (all GREEN including barrel-isolation).
    - `bunx vitest run lib/chat-ui/components/` exits 0 (any colocated tests pass — likely none after relocation).
    - `bun build` exits 0 (TS strict satisfied).
  </acceptance_criteria>
  <done>
    chat-pane.tsx created with adapter-driven transport; 4 unit tests + 1 integration test relocated; all `lib/chat-ui/` tests GREEN; bun build clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| barrel `lib/chat-ui/index.ts` → consumers | T-5-02: barrel must NOT re-export concrete adapter modules. Type-only re-export of `ChatIdentityAdapter` from `./adapters/types` is permitted (TS erases at compile time; Plan 01's regex authors this exemption via negative lookahead). |
| ChatPane → adapter | The transport invokes adapter.getAuthHeaders/getRequestBody on every send — no module-level caching can occur because the closure captures the adapter instance, not a resolved value. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-02 | Information Disclosure | lib/chat-ui/index.ts | mitigate | Barrel exports ONLY components + EmptyState + interfaces + store hooks (+ type-only re-export from `./adapters/types`). NO concrete `from './adapters/embedded'` or `from './adapters/storefront'` re-export. Enforced by barrel-isolation.test.ts (Plan 01's regex authored with type-only exemption from the start). Storefront bundle's tree-shaker drops embedded.ts entirely. |
| T-5-SC | Tampering | DefaultChatTransport headers/body | mitigate | Resolvable functions (`() => adapter.getAuthHeaders()`) passed UNCALLED so the AI SDK invokes them fresh on every sendMessage — short-lived JWT cannot go stale (Pitfall 2 / GitHub issue 7819 avoidance). |
</threat_model>

<verification>
- `bunx vitest run lib/chat-ui/__tests__/` shows all 4 relocated component test files + barrel-isolation + 3 adapter/store tests + 1 integration test ALL GREEN.
- Recursive check: `! grep -rn '@/components/chat' lib/chat-ui/` exits zero (no matches anywhere under lib/chat-ui/; intra-tree imports correctly use relative paths; cross-tree imports use `@/components/ai-elements`, `@/components/ui`, `@/types/product` only).
- `grep -rn "export default" lib/chat-ui/` returns ZERO matches (named exports only per discretion lock).
- `bun build` exits 0.
- The legacy `components/chat/` files still exist and the legacy imports in `app/(embedded)/chat/page.tsx` + `chat-shell.tsx` still resolve — Plan 04 cuts those over.
- The Wave 0 RED contract authored in Plan 01 Task 1 (`lib/chat-ui/__tests__/barrel-isolation.test.ts`) is UNCHANGED by this plan — Plan 03 honors it, does not mutate it.
</verification>

<success_criteria>
- 7 component files at `lib/chat-ui/components/`, byte-identical except for the two locked deltas + intra-tree import-path updates.
- `lib/chat-ui/index.ts` barrel created with the exact export list from PATTERNS.md (no MessageParts, no concrete adapter re-exports; type-only re-export from `./adapters/types` permitted).
- 4 component unit tests + 1 integration test relocated under `lib/chat-ui/__tests__/` with updated imports.
- All 8 tests under `lib/chat-ui/__tests__/` GREEN (barrel-isolation + 3 adapter/store from Plan 02 + 4 relocated component tests + 1 integration test).
- `bun build` clean (TS strict, no `any`).
- ZERO `export default` in `lib/chat-ui/`.
- ZERO `@/components/chat` references in `lib/chat-ui/` (recursive grep).
- `lib/chat-ui/__tests__/barrel-isolation.test.ts` content matches Plan 01 Task 1's output (no mid-plan mutation).
</success_criteria>

<output>
Create `.planning/phases/05-shared-chat-ui-extraction/05-03-SUMMARY.md` when done, recording:
- Component file move summary (sources → targets)
- UI-SPEC delta application (line numbers + before/after)
- Test relocation summary (5 files: 4 unit + 1 integration; import-line diffs)
- Barrel export list (exact verbatim)
- bun build status
- Confirmation that legacy `components/chat/*` still exists (Plan 04 deletes it after the import cut)
- Confirmation that `barrel-isolation.test.ts` was NOT modified in this plan
</output>
</output>
