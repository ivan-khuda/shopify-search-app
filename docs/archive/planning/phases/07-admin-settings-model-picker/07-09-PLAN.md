---
phase: 07-admin-settings-model-picker
plan: 09
type: execute
wave: 3
depends_on: ["07-08"]
files_modified:
  - app/(embedded)/EmbeddedProviders.tsx
autonomous: true
requirements: [ADM-03]
must_haves:
  truths:
    - "The embedded admin nav (s-app-nav block in EmbeddedProviders.tsx) renders an <s-link href='/settings'>Settings</s-link> entry alongside Search (Chat) and Onboarding (D-05)"
    - "The nav link order is: Search (rel='home'), Onboarding, Settings — keeps existing ordering and appends Settings at the end"
  artifacts:
    - path: app/(embedded)/EmbeddedProviders.tsx
      provides: "Embedded admin nav with the new Settings entry (D-05)"
      contains: "<s-link href=\"/settings\">"
  key_links:
    - from: app/(embedded)/EmbeddedProviders.tsx
      to: app/(embedded)/settings/page.tsx (Plan 08)
      via: <s-link href="/settings">
      pattern: "href=\"/settings\""
---

<objective>
Add a "Settings" entry to the embedded admin nav defined in `app/(embedded)/EmbeddedProviders.tsx`, completing D-05 (route is top-level `/settings`, adds nav entry alongside Chat/Onboarding). One-line change inside the existing `<s-app-nav>` block.

Purpose: Discoverability — without a nav entry, merchants would need to type `/settings` manually. This task is small but essential for SC1 ("Navigating to /settings shows a list...").

Output: One modified TSX file with one new `<s-link>` child.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-admin-settings-model-picker/07-CONTEXT.md
@.planning/phases/07-admin-settings-model-picker/07-RESEARCH.md

@app/(embedded)/EmbeddedProviders.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Settings nav entry to EmbeddedProviders.tsx</name>
  <files>app/(embedded)/EmbeddedProviders.tsx</files>
  <action>
    Edit `app/(embedded)/EmbeddedProviders.tsx`. The current file has:

    ```tsx
    <s-app-nav>
      <s-link href="/chat" rel="home">Search</s-link>
      <s-link href="/onboarding">Onboarding</s-link>
    </s-app-nav>
    ```

    Append one new `<s-link>` element BEFORE the closing `</s-app-nav>`:

    ```tsx
    <s-link href="/settings">Settings</s-link>
    ```

    Final block:

    ```tsx
    <s-app-nav>
      <s-link href="/chat" rel="home">Search</s-link>
      <s-link href="/onboarding">Onboarding</s-link>
      <s-link href="/settings">Settings</s-link>
    </s-app-nav>
    ```

    Do NOT change `rel="home"` on the Chat link. Do NOT reorder existing entries. Do NOT add `data-testid` (no test depends on the nav entry at this layer; the SettingsPage test stands on its own).
  </action>
  <verify>
    <automated>grep -c 'href="/settings"' app/(embedded)/EmbeddedProviders.tsx; echo "---"; bunx tsc --noEmit 2>&1 | grep -E "(EmbeddedProviders|error TS)" | head -5</automated>
  </verify>
  <done>`grep -c 'href="/settings"' app/(embedded)/EmbeddedProviders.tsx` returns 1. `tsc --noEmit` reports no new errors related to EmbeddedProviders.tsx. The `s-link` order is Search → Onboarding → Settings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Embedded admin shell → /settings route | Navigation is internal to the same origin; no cross-origin concerns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | Information disclosure | Nav link visible to non-admin users (i.e., users without a valid offline session) | accept | The /settings page itself is protected by the embedded shell's session-token Auth + middleware; the link rendering does not leak any sensitive data |
| T-07-09-02 | Tampering | A malicious nav reordering propagates an old href | mitigate | The verify step asserts the exact href string `"/settings"` and the nav order (Search → Onboarding → Settings) |
| T-07-09-SC | Tampering | npm/pip/cargo installs in this plan | accept | No new packages — one JSX edit; slopcheck N/A |
</threat_model>

<verification>
- `app/(embedded)/EmbeddedProviders.tsx` now contains the new s-link
- `tsc --noEmit` passes without new errors on this file
- Manual smoke: open the embedded app, observe the nav now shows three entries
</verification>

<success_criteria>
- One new line in `EmbeddedProviders.tsx`
- Nav entry order: Search, Onboarding, Settings
- No other files modified
</success_criteria>

<output>
Create `.planning/phases/07-admin-settings-model-picker/07-09-SUMMARY.md` documenting:
- The before/after of the s-app-nav block
- Confirmation of D-05 fulfillment (nav entry added)
- Wave 3 status: complete — Phase 7 ready for verification gate (Plan 10)
</output>
