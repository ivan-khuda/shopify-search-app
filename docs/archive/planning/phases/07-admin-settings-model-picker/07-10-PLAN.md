---
phase: 07-admin-settings-model-picker
plan: 10
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05", "07-06", "07-07", "07-08", "07-09"]
files_modified:
  - .planning/phases/07-admin-settings-model-picker/07-VERIFICATION.md
  - .planning/phases/07-admin-settings-model-picker/07-VALIDATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [ADM-03, ADM-04]
must_haves:
  truths:
    - "Full vitest suite is GREEN with all Phase 7 RED scaffolds from Plan 01 driven to GREEN (no regressions in Phases 1–6 tests)"
    - "Each of the 4 ROADMAP success criteria (SC1–SC4) has a documented automated or manual smoke verification with a pass/fail outcome"
    - "ADM-03 and ADM-04 are marked Complete in REQUIREMENTS.md traceability table"
    - "ROADMAP.md Phase 7 line is checked, Status column updated to Complete, Plans Complete count is 10/10"
    - "STATE.md current position advances; Phase 4 deferred items T-04-24 and T-04-25 are resolved (closed) with link to the resolutions in Plan 06's JSDoc"
    - "VALIDATION.md nyquist_compliant flips to true; the Per-Task Verification Map is fully populated with task IDs across all 10 plans"
  artifacts:
    - path: .planning/phases/07-admin-settings-model-picker/07-VERIFICATION.md
      provides: "Phase-end verification record: full-suite output, manual smoke results, gap list, sign-off"
      min_lines: 60
    - path: .planning/phases/07-admin-settings-model-picker/07-VALIDATION.md
      provides: "Per-Task Verification Map populated; nyquist_compliant: true"
    - path: .planning/ROADMAP.md
      provides: "Phase 7 marked complete; Plans Complete count updated"
    - path: .planning/REQUIREMENTS.md
      provides: "ADM-03 + ADM-04 marked Complete in traceability"
    - path: .planning/STATE.md
      provides: "Phase 7 entry under Accumulated Context with verified outcomes + closed Phase 4 deferred items"
  key_links:
    - from: .planning/STATE.md
      to: Phase 4 deferred items table
      via: closes T-04-24 + T-04-25 with reference to services/chat/getActiveChatModel.ts JSDoc
      pattern: "T-04-24|T-04-25"
---

<objective>
Run the Phase 7 verification gate: full vitest suite, manual smoke checklist for the two cross-route / cold-start behaviors that cannot run in jsdom, and planning-artifact updates. Mark ADM-03 and ADM-04 complete; flip nyquist_compliant; advance STATE.md; close Phase 4 deferred items T-04-24 + T-04-25.

Purpose: This is the gate that lets `/gsd-verify-work` declare Phase 7 done and unblocks Phase 8. Phases 1–6 each had an analogous verification plan (01-09, 02-11, 03-08, 04-06, 05-05, 06-14) — Plan 10 follows that template.

Output: 1 new VERIFICATION.md, 1 updated VALIDATION.md, 3 updated planning artifacts. No production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-admin-settings-model-picker/07-VALIDATION.md
@.planning/phases/07-admin-settings-model-picker/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated suite and capture pass/fail counts per Phase 7 file</name>
  <files>.planning/phases/07-admin-settings-model-picker/07-VERIFICATION.md</files>
  <action>
    Run `bun test` (full vitest suite). Capture:
    1. The final summary line (Test Files / Tests / Duration)
    2. Per-file pass count for each Phase 7 Wave-0 test file (model-catalog.test.ts, getActiveChatModel.test.ts, ShopSettingsRepository.test.ts, route.test.ts, page.test.tsx, settings-form.test.tsx)
    3. Any failures — including pre-existing flakes carried over from earlier phases (note in handoff)

    Create `.planning/phases/07-admin-settings-model-picker/07-VERIFICATION.md` with sections:
    - **Frontmatter:** phase, slug, status (in-progress at this point), verified_at: <today>
    - **Automated suite outcome:** full `bun test` summary + per-file table
    - **Phase 7 file count check:** Confirm 6 Phase 7 test files exist and are GREEN (the Wave 0 set from VALIDATION.md)
    - **SC traceability table:** SC1, SC2, SC3, SC4 each mapped to one or more it() blocks with status (✅ green / ⏭️ manual)
    - **Regression check:** Confirm Phases 1–6 tests still GREEN; list any newly-flaky tests
    - **Manual smoke** section — to be filled in by Task 2 (blocking human checkpoint)
    - **Phase 4 deferred items closure** — link T-04-24 + T-04-25 to the Plan 06 JSDoc resolution

    Do NOT mark the phase Complete in this task — that happens in Task 4 after manual smokes.
  </action>
  <verify>
    <automated>bun test 2>&1 | tail -10; echo "---"; ls .planning/phases/07-admin-settings-model-picker/07-VERIFICATION.md && wc -l .planning/phases/07-admin-settings-model-picker/07-VERIFICATION.md</automated>
  </verify>
  <done>Full `bun test` exit code is 0. 07-VERIFICATION.md exists with ≥60 lines including SC traceability table + automated outcomes + manual-smoke section placeholder. All 6 Phase 7 Wave-0 test files reported GREEN. Regression check confirms no new failures in non-Phase-7 files (any pre-existing flakes are documented but not blocking).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [HUMAN] Manual smoke — SC4 cross-route + D-03 cold-start fallback</name>
  <what-built>
    The /settings page renders, the PATCH endpoint accepts a save, the resolver picks up the new model on the next chat request, and the cold-start banner appears when the AI Gateway is unreachable. These two behaviors cannot be asserted in jsdom (per 07-VALIDATION.md Manual-Only Verifications) — they require a real embedded session and a real (or stubbed) gateway outage.
  </what-built>
  <how-to-verify>
    **Smoke 1 — SC4 cross-route playground update:**
    1. Start dev server: `bun dev` (with a valid `.env` and an installed dev shop session)
    2. Open the embedded admin app in your dev shop's Admin
    3. Navigate to /settings via the new nav link
    4. Confirm: the table shows ~10 BEST_FOR-keyed models, one is pre-selected (should be `google/gemini-2.5-flash` for a never-saved shop)
    5. Pick a different model (e.g., `anthropic/claude-sonnet-4.5`)
    6. Click Save in the ui-save-bar
    7. Confirm: toast appears reading `Model updated to Claude Sonnet 4.5`
    8. Navigate to /chat
    9. Confirm: the playground banner reads `Preview mode — using your real catalog · Model: Claude Sonnet 4.5` (em-dash + middle-dot from Phase 4)
    10. Send a test query to the chat; confirm it streams a response (proves the AI Gateway routing picked up the new model id end-to-end)

    **Smoke 2 — D-03 cold-start fallback:**
    1. Stop the dev server
    2. Edit `/etc/hosts` (or your shell HOSTALIASES) to add `127.0.0.1 ai-gateway.vercel.sh` (forces the catalog fetch to fail with connection refused), OR set `services/chat/model-catalog.ts` CATALOG_URL temporarily to an unreachable host for the smoke
    3. Restart dev server with cleared Node cache: `bun dev`
    4. Open /settings
    5. Confirm: a critical banner reads "Model catalog unavailable — showing default only"; the table shows ONE row (`google/gemini-2.5-flash`); the ui-save-bar is hidden (Save disabled per D-03 + Plan 08 saveDisabled prop)
    6. Restore the original `/etc/hosts` or CATALOG_URL value

    Record both outcomes (PASS / FAIL with notes) in the Manual smoke section of 07-VERIFICATION.md.
  </how-to-verify>
  <resume-signal>Type "both smokes PASS" once both flows are verified and recorded. Type "FAIL: <description>" to abort and trigger gap-closure planning.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Populate VALIDATION.md Per-Task Verification Map and flip nyquist_compliant</name>
  <files>.planning/phases/07-admin-settings-model-picker/07-VALIDATION.md</files>
  <action>
    Edit `07-VALIDATION.md`:

    1. Set frontmatter `nyquist_compliant: true` and `wave_0_complete: true` and `status: complete`
    2. Replace the placeholder row in the Per-Task Verification Map with one row per task across all 10 plans. Task ID format: `7-NN-MM` where NN = plan number, MM = task number within plan.
       For each row include: Task ID, Plan, Wave, Requirement (ADM-03 / ADM-04), Threat Ref (e.g., T-07-04-01), Secure Behavior (one-line summary), Test Type (unit / component / route integration / migration / human), Automated Command (the `<automated>` block from the plan, or `n/a (human)`), File Exists (✅), Status (✅ green)
    3. Tick all Wave 0 Requirements checkboxes
    4. Tick all Validation Sign-Off checkboxes
    5. Set Approval to `approved`

    The map will be roughly 18–22 rows (10 plans × ~1–2 tasks each, plus the human-checkpoint rows).
  </action>
  <verify>
    <automated>grep -c "nyquist_compliant: true" .planning/phases/07-admin-settings-model-picker/07-VALIDATION.md; grep -c "✅" .planning/phases/07-admin-settings-model-picker/07-VALIDATION.md | head -1</automated>
  </verify>
  <done>`nyquist_compliant: true` is set in frontmatter. The Per-Task Verification Map has ≥15 rows with ✅ green status. All Wave 0 Requirements checkboxes ticked. Sign-Off: approved.</done>
</task>

<task type="auto">
  <name>Task 4: Update ROADMAP.md, REQUIREMENTS.md, and STATE.md to reflect Phase 7 completion</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
    Edit `.planning/ROADMAP.md`:
    - Change the Phase 7 list entry from `- [ ] **Phase 7: ...**` to `- [x] **Phase 7: Admin Settings + Model Picker** - ShopSettings model, model picker UI, per-shop active model (completed YYYY-MM-DD)` (replacing the YYYY-MM-DD with today)
    - In the Progress table, update the Phase 7 row to `| 7. Admin Settings + Model Picker | 10/10 | Complete | YYYY-MM-DD |`
    - In the Phase 7 Phase Details section, replace `**Plans**: TBD` with `**Plans**: 10 plans` and append the 10 plan entries (each as a `- [x]` line listing plan filename + one-line objective)

    Edit `.planning/REQUIREMENTS.md`:
    - Change `- [ ] **ADM-03**: Settings screen at /settings ...` to `- [x] **ADM-03**: ...`
    - Change `- [ ] **ADM-04**: Default chat model is pre-selected ...` to `- [x] **ADM-04**: ...`
    - In the Traceability table, update ADM-03 row: `Status: Complete (Phase 7)`; ADM-04 row: `Status: Complete (Phase 7)`

    Edit `.planning/STATE.md`:
    - Update frontmatter `stopped_at: Phase 7 verified`, `last_updated: <today ISO>`, `last_activity: <today>`
    - Increment `progress.completed_phases` to 7
    - Update `progress.total_plans` and `progress.completed_plans` to current totals (was 53 — add 10)
    - Add a new entry under `### Decisions` with the Phase 7 verified summary (format mirrors the existing Phase 6 entry — 3–5 sentences covering: ShopSettings model added, /settings page with Polaris s-table, body-only swap of getActiveChatModel, defense-in-depth PATCH route, 10/10 plans complete, manual smokes PASS)
    - Update the **Deferred Items** table:
      - Remove (or mark resolved) the row "Phase 7 prerequisites | displayName XSS validation gate + searchParams.shop ↔ session.shop verification" — both resolved by Plan 06 (XSS safe by code-path inspection documented in getActiveChatModel.ts JSDoc; searchParams.shop asymmetry documented in settings/page.tsx JSDoc matching /chat pattern)
      - Append a new row if any Phase 7 work is genuinely deferred — e.g., the manual smoke for cold-start was performed via hosts-file edit; document this if any follow-up tooling is needed
    - Update `### Current Position` to indicate Phase 7 complete; Phase 8 ready to plan

    Use the Edit tool for each file. Do NOT use heredoc / cat << EOF / sed.
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*Phase 7" .planning/ROADMAP.md; grep -c "\[x\] \*\*ADM-03\*\*" .planning/REQUIREMENTS.md; grep -c "\[x\] \*\*ADM-04\*\*" .planning/REQUIREMENTS.md; grep -c "Phase 7 verified" .planning/STATE.md</automated>
  </verify>
  <done>Each grep returns ≥1. ROADMAP.md Progress table shows Phase 7 Complete. REQUIREMENTS.md traceability shows ADM-03 + ADM-04 Complete. STATE.md current position advances past Phase 7. The Phase 4 deferred items row for T-04-24 + T-04-25 is removed or marked resolved.</done>
</task>

<task type="auto">
  <name>Task 5: Commit the verification gate</name>
  <files>(git operation only — no new files)</files>
  <action>
    Stage and commit the verification artifacts:

    ```
    git add .planning/phases/07-admin-settings-model-picker/07-VERIFICATION.md \
            .planning/phases/07-admin-settings-model-picker/07-VALIDATION.md \
            .planning/ROADMAP.md \
            .planning/REQUIREMENTS.md \
            .planning/STATE.md
    git commit -m "docs(07): Phase 7 verification gate — ADM-03 + ADM-04 complete"
    ```

    Do NOT push. Do NOT amend. If pre-commit hooks fail, fix the issue and create a NEW commit.
  </action>
  <verify>
    <automated>git log --oneline -1</automated>
  </verify>
  <done>The most recent commit message starts with `docs(07): Phase 7 verification gate`. `git status` shows a clean working tree (or only unrelated noise).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification artifacts → future planners | Future phases read STATE.md decisions table; an incomplete or inaccurate entry would mislead Phase 8 |
| Manual smoke results → SC sign-off | Human attestation is the only evidence for SC4 cross-route flow and D-03 cold-start behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10-01 | Repudiation | Manual smoke result recorded without actually performing the steps | mitigate | Task 2 is a blocking human checkpoint; resume-signal requires explicit "both smokes PASS" — Claude cannot self-attest |
| T-07-10-02 | Information disclosure | Verification artifact accidentally captures a session token or session-ID from the dev shop | mitigate | Manual smoke notes describe behaviors and screenshots' presence/absence; never copy token strings into the .md |
| T-07-10-03 | Tampering | ROADMAP.md / REQUIREMENTS.md / STATE.md update applied to wrong phase rows | mitigate | Verify grep checks each row by exact string anchor (`\[x\] \*\*ADM-03\*\*`, etc.) — false update would not match |
| T-07-10-04 | Repudiation | Phase marked Complete despite a failed manual smoke | mitigate | Task 2 explicitly says "FAIL → abort and trigger gap-closure planning"; Tasks 3 + 4 + 5 only execute after both smokes PASS |
| T-07-10-SC | Tampering | npm/pip/cargo installs in this plan | accept | No installs — git + bash + Edit only; slopcheck N/A |
</threat_model>

<verification>
- Full `bun test` exits 0
- 07-VERIFICATION.md exists and documents SC1–SC4 outcomes
- VALIDATION.md frontmatter `nyquist_compliant: true`
- ROADMAP.md Phase 7 entry marked complete (`[x]`)
- REQUIREMENTS.md ADM-03 + ADM-04 marked complete (`[x]`)
- STATE.md Phase 4 deferred items table no longer references T-04-24 / T-04-25 as open
- One new git commit `docs(07): Phase 7 verification gate — ...`
</verification>

<success_criteria>
- Phase 7 is observably done: `bun test` GREEN, both manual smokes PASS, planning artifacts reflect completion
- Phase 4 deferred items closed (T-04-24 safe by inspection; T-04-25 asymmetry documented in two JSDoc comments)
- nyquist_compliant: true in VALIDATION.md
- Phase 8 has no inherited blockers from Phase 7
- The Settings page is accessible from the embedded admin nav; saving a model immediately changes the next chat request's model id
</success_criteria>

<output>
Create `.planning/phases/07-admin-settings-model-picker/07-10-SUMMARY.md` documenting:
- Final automated suite outcome (Test Files / Tests / Duration)
- SC1–SC4 status (✅ all)
- Both manual smoke outcomes (PASS with notes)
- Phase 4 deferred items closure references
- The completion commit hash
- Phase 8 readiness: confirmed Phase 7 introduces no new blockers; Phase 8 (Email + Hard Cap) depends only on Phase 2 (already complete in the roadmap dependency graph)
</output>
