---
phase: 07-admin-settings-model-picker
plan: 05
type: execute
wave: 2
depends_on: ["07-01", "07-03"]
files_modified:
  - lib/db/repositories/ShopSettingsRepository.ts
autonomous: true
requirements: [ADM-03]
must_haves:
  truths:
    - "ShopSettingsRepository exposes get(shop) returning ShopSettings | null and upsert(shop, activeChatModelId) returning ShopSettings"
    - "Every method filters by shop — multi-tenancy lock at the repository layer mirrors ProductRepository (FND-01 / D-04 convention)"
    - "The repository is the single mockable surface the resolver test (Plan 06) and route test (Plan 07) substitute against in unit tests"
  artifacts:
    - path: lib/db/repositories/ShopSettingsRepository.ts
      provides: "Type-safe get + upsert wrapper around prisma.shopSettings"
      exports: ["ShopSettingsRepository", "shopSettingsRepository"]
      min_lines: 25
  key_links:
    - from: lib/db/repositories/ShopSettingsRepository.ts
      to: prisma.shopSettings (via @/lib/db/client)
      via: import { prisma } from '@/lib/db/client'
      pattern: "prisma\\.shopSettings"
    - from: lib/db/repositories/ShopSettingsRepository.ts
      to: app/api/settings/model/route.ts (Plan 07)
      via: import { shopSettingsRepository }
      pattern: "shopSettingsRepository\\.upsert"
---

<objective>
Implement `lib/db/repositories/ShopSettingsRepository.ts` — the thin Prisma wrapper for the `shop_settings` table. Mirrors `ProductRepository.ts` shape: a class exposing `get` + `upsert` plus a singleton instance export. This file is consumed by Plan 07's PATCH route (for writes) and may optionally be used by Plan 06's resolver (the resolver can call `prisma.shopSettings.findUnique` directly to avoid an indirection — planner picks).

Purpose: Match the established repository pattern so the new write/read path is consistent with Phase 1's ProductRepository, gives Plan 07's route test a clean mock surface, and centralizes the shop-keyed query for future Phase 8 expansion.

Output: One new TypeScript module driving Plan 01's Wave-0 RED test `lib/db/repositories/__tests__/ShopSettingsRepository.test.ts` to GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-admin-settings-model-picker/07-CONTEXT.md
@.planning/phases/07-admin-settings-model-picker/07-RESEARCH.md
@CLAUDE.md

@lib/db/repositories/ProductRepository.ts
@lib/db/repositories/__tests__/ShopSettingsRepository.test.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ShopSettingsRepository.get + upsert mirroring ProductRepository</name>
  <files>lib/db/repositories/ShopSettingsRepository.ts</files>
  <behavior>
    Drives the Plan 01 Wave-0 test file `lib/db/repositories/__tests__/ShopSettingsRepository.test.ts` from RED to GREEN.

    Public surface:
    - `class ShopSettingsRepository`
      - `async get(shop: string): Promise<ShopSettings | null>` — calls `prisma.shopSettings.findUnique({ where: { shop } })`
      - `async upsert(shop: string, activeChatModelId: string): Promise<ShopSettings>` — calls `prisma.shopSettings.upsert({ where: { shop }, create: { shop, activeChatModelId }, update: { activeChatModelId } })`. The `updatedAt` field is Prisma-managed (`@updatedAt`) and updates automatically; do NOT set it manually in either branch.
    - `export const shopSettingsRepository = new ShopSettingsRepository();` — singleton instance for production callers

    Type import:
    - `import type { ShopSettings } from '@/app/generated/prisma/client';` — the generated Prisma client (Plan 03) exposes this type after migrate dev + generate.

    Constraints:
    - Match the shape and style of `lib/db/repositories/ProductRepository.ts`: class + singleton + named exports
    - No `console.*` calls (CLAUDE.md hard rule)
    - No `any` types — `ShopSettings` is the typed Prisma model
    - No transactions are needed — both operations are single-row writes against a shop PK; concurrency safety is handled by Postgres's PK constraint (a duplicate `shop` would collide deterministically — but `upsert` with `where: { shop }` is the idempotent path)
    - Do NOT include a `delete` or `list` method — Phase 7 has no use for either; YAGNI applies (deferred ideas list confirms no audit log, no per-shop list view)
    - Do NOT add a `bulkUpsert` — Phase 7 only writes one row at a time from the PATCH route
  </behavior>
  <action>
    Create `lib/db/repositories/ShopSettingsRepository.ts` matching the structure of `ProductRepository.ts` (class declaration + singleton export). Keep the file under 40 lines.

    Add a JSDoc header documenting:
    - The repository's contract (get + upsert; shop is the PK)
    - The relationship to the resolver (Plan 06): `getActiveChatModel(shop)` may call `prisma.shopSettings.findUnique` directly OR through this repository — Plan 06 documents which path it chose. Either is acceptable; the repository is mandatory only for the PATCH route's mockability.
    - The relationship to Plan 07's PATCH endpoint (the primary write site)

    Do NOT import from `@/services/chat/*` to avoid coupling. The catalog membership check lives in Plan 07's route handler, not in the repository.
  </action>
  <verify>
    <automated>bunx vitest run lib/db/repositories/__tests__/ShopSettingsRepository.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All it() blocks in `ShopSettingsRepository.test.ts` are GREEN. `grep -c 'console\.' lib/db/repositories/ShopSettingsRepository.ts` returns 0. `grep -c 'shopSettingsRepository' lib/db/repositories/ShopSettingsRepository.ts` returns ≥1 (the singleton export). The class has exactly two public methods (`get`, `upsert`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller (route handler) → repository | Caller must pre-derive `shop` from a verified session token; the repository takes shop on trust |
| Repository → Prisma client | Prisma escaping handles SQL injection at the parameter binding layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | Caller passes a shop string sourced from request body (bypassing the session-token shop) | mitigate | This is enforced at Plan 07's route handler — `withShopifySession` ctx is the only `shop` source. The repository documents this in its JSDoc and the route test asserts the multi-tenancy lock |
| T-07-05-02 | Elevation of privilege | Caller sets `activeChatModelId` to an arbitrary string | mitigate | Plan 07's PATCH route validates against the catalog before calling `upsert`; Zod max(200) caps length; defense-in-depth |
| T-07-05-03 | Repudiation | Concurrent writes from the same shop race | mitigate | Postgres serializes writes on the PK; `upsert` is idempotent at the SQL level; updatedAt timestamp reflects the last successful write |
| T-07-05-04 | Information disclosure | Repository accidentally logs the shop hostname or model id | mitigate | Zero `console.*` calls; the Prisma client itself does not log at the application level in production runtime |
| T-07-05-SC | Tampering | npm/pip/cargo installs in this plan | accept | No new packages — uses existing `@prisma/client` 7.3.0; slopcheck N/A |
</threat_model>

<verification>
- All Wave 0 repository tests GREEN
- Singleton instance `shopSettingsRepository` is the default consumer in Plan 07
- `grep -c 'class ShopSettingsRepository' lib/db/repositories/ShopSettingsRepository.ts` returns 1
- File is under 50 lines
</verification>

<success_criteria>
- All Plan 01 Wave-0 it() blocks in ShopSettingsRepository.test.ts GREEN
- Public surface is exactly `get(shop)` + `upsert(shop, activeChatModelId)` — no extra methods
- File mirrors ProductRepository.ts structural conventions (class + singleton + named exports)
- No `delete`, `list`, or bulk methods present
</success_criteria>

<output>
Create `.planning/phases/07-admin-settings-model-picker/07-05-SUMMARY.md` documenting:
- The two-method public surface and singleton export
- Confirmation that ProductRepository structure was mirrored
- Test pass count
- Handoff note for Plan 07 (PATCH route imports `shopSettingsRepository` and calls `.upsert`)
</output>
