---
phase: 07-admin-settings-model-picker
plan: 03
type: execute
wave: 1
depends_on: ["07-02"]
files_modified:
  - prisma/migrations/
  - app/generated/prisma/
autonomous: false
requirements: [ADM-03, ADM-04]
must_haves:
  truths:
    - "A new Prisma migration applied to the live dev database creates the shop_settings table with columns shop (PK), active_chat_model_id, updated_at"
    - "`bunx prisma generate` re-emits the typed client at app/generated/prisma/ with prisma.shopSettings.findUnique / upsert available"
    - "db/manual-indexes.sql is re-applied; no pgvector / GIN index drift introduced by the migration (Pitfall 3, project convention from CLAUDE.md)"
  artifacts:
    - path: prisma/migrations/{timestamp}_add_shop_settings/migration.sql
      provides: "Forward-only DDL creating the shop_settings table"
      contains: "CREATE TABLE \"shop_settings\""
    - path: app/generated/prisma/
      provides: "Regenerated Prisma client exposing prisma.shopSettings"
  key_links:
    - from: prisma/migrations/{timestamp}_add_shop_settings/migration.sql
      to: live PostgreSQL via DATABASE_URL / DIRECT_URL
      via: bunx prisma migrate dev
      pattern: "CREATE TABLE.*shop_settings"
    - from: app/generated/prisma/
      to: lib/db/repositories/ShopSettingsRepository.ts (Plan 05)
      via: import { PrismaClient } from '@/app/generated/prisma/client'
      pattern: "prisma\\.shopSettings"
---

<objective>
[BLOCKING] Apply the Phase 7 schema change to the live development database, regenerate the Prisma client, and re-apply the manual pgvector + GIN indexes per the project convention. This task gates EVERY downstream Wave 2 + Wave 3 plan — without the typed client, neither the repository (Plan 05), the resolver body swap (Plan 06), the PATCH route (Plan 07), nor the SSR settings page (Plan 08) will typecheck or run at runtime.

Purpose: Materialize the schema diff from Plan 02 into a real table and a real typed client. The migration is forward-only (no destructive operations); the table is empty by design (D-09 — never seed). Plan 02 + Plan 03 split mirrors Phases 1 (01-03 + 01-05), 2 (02-02 + 02-04), 3 (03-05), and 6 (06-02) — write schema, then apply.

Output: One new migration directory under `prisma/migrations/` and a regenerated `app/generated/prisma/` client. The dev DB has a new `shop_settings` table with zero rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-admin-settings-model-picker/07-CONTEXT.md
@.planning/phases/07-admin-settings-model-picker/07-RESEARCH.md
@CLAUDE.md
@prisma/schema.prisma
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING-HUMAN] Confirm DATABASE_URL / DIRECT_URL points to the intended dev database before applying the migration</name>
  <what-built>Plan 02 wrote the `ShopSettings` model to `prisma/schema.prisma`. Plan 03 now needs to APPLY this against the live database. Once applied, the migration directory is committed and the change is reproducible — but it does mutate a real Postgres database.</what-built>
  <how-to-verify>
    1. Run `echo "DATABASE_URL host: $(node -e 'console.log(new URL(process.env.DATABASE_URL).hostname)' 2>/dev/null)"` from a shell that has `.env` loaded; confirm the printed hostname is the expected dev/staging instance (e.g., `localhost` or your dev Prisma Accelerate endpoint), NOT a production host.
    2. Run `psql "$DIRECT_URL" -c "\dt" 2>/dev/null | grep shop_settings` — expect no match (the table does not yet exist).
    3. Confirm there is no active migration lock from a previous failed run: `ls prisma/migrations/ 2>&1 | tail -5` should show the expected list of prior milestones with no `_failed` suffix.
  </how-to-verify>
  <resume-signal>Type "approved — proceed with migrate dev" once the DB target is confirmed. If the DB host is wrong, abort and adjust .env before resuming.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run `bunx prisma migrate dev --name add_shop_settings` and regenerate client</name>
  <files>prisma/migrations/{timestamp}_add_shop_settings/migration.sql, app/generated/prisma/</files>
  <action>
    Run `bunx prisma migrate dev --name add_shop_settings`. Prisma will:
    1. Generate `prisma/migrations/{timestamp}_add_shop_settings/migration.sql` containing the CREATE TABLE DDL for `shop_settings`
    2. Apply it to the database at `DATABASE_URL` (Prisma Accelerate proxies to `DIRECT_URL` for migrations)
    3. Regenerate the Prisma client to `app/generated/prisma/` (the non-default output dir set in `prisma.config.ts`)

    If migrate dev prompts for confirmation in a non-interactive environment, set `PRISMA_NONINTERACTIVE=1` or pass `--name` (already provided) and `--create-only` is NOT what we want — we want apply.

    Expected SQL (Prisma will author this automatically — review the generated file for sanity):
    - `CREATE TABLE "shop_settings" ( "shop" TEXT NOT NULL, "activeChatModelId" TEXT NOT NULL, "updatedAt" TIMESTAMP(3) NOT NULL, CONSTRAINT "shop_settings_pkey" PRIMARY KEY ("shop") );`
    - No CREATE INDEX statements (the PK auto-creates a btree on shop; no other indexes per Plan 02)

    AFTER migrate dev completes successfully, explicitly run `bunx prisma generate` (Prisma usually runs this automatically as part of `migrate dev`, but rerun for safety in case of CI environments). Then sanity-check `app/generated/prisma/client.ts` (or `index.d.ts`) contains `shopSettings:` in the model namespace.

    Finally, re-apply the manual pgvector + GIN indexes per CLAUDE.md convention: `bun db:indexes`. The script is idempotent (verified by Phase 3 SC4) — it should print "OK" for each pre-existing index without errors. This guards against the rare case where `migrate dev` resets DB state.

    NO console.log additions, NO test changes, NO production code in this task — it is a CLI + DB orchestration step.
  </action>
  <verify>
    <automated>psql "$DIRECT_URL" -tA -c "SELECT column_name, data_type FROM information_schema.columns WHERE table_name = 'shop_settings' ORDER BY ordinal_position;" 2>&1; echo "---"; grep -l "shopSettings" app/generated/prisma/*.d.ts app/generated/prisma/client.ts 2>/dev/null | head -3; echo "---"; ls prisma/migrations/ | grep add_shop_settings</automated>
  </verify>
  <done>The psql query lists three columns (`shop` text, `activeChatModelId` text, `updatedAt` timestamp). The grep finds `shopSettings` in the generated client. A new migration directory `prisma/migrations/*_add_shop_settings/` exists with a `migration.sql` file inside. `bun db:indexes` exited 0 and printed OK for each pre-existing index.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke-test the new client by importing it in a one-off node script</name>
  <files>(none — temporary verification only)</files>
  <action>
    Run a one-shot smoke test confirming the generated client exposes `prisma.shopSettings` at runtime. Use bunx to execute a transient TypeScript snippet:

    ```
    bunx tsx -e "import { prisma } from '@/lib/db/client'; (async () => { const c = await prisma.shopSettings.count(); console.log('shop_settings rows:', c); await prisma.\$disconnect(); })();"
    ```

    Expected: the script prints `shop_settings rows: 0` (D-09 — table is empty). If it prints `Property 'shopSettings' does not exist on type 'PrismaClient'`, the generate step failed — rerun `bunx prisma generate` and re-test.

    Do NOT commit this snippet — it's a verify-only invocation. If the smoke test passes, proceed to commit only the new migration directory.
  </action>
  <verify>
    <automated>bunx tsx -e "import { prisma } from '@/lib/db/client'; (async () => { const c = await prisma.shopSettings.count(); console.log('OK rows=' + c); await prisma.\$disconnect(); })();" 2>&1 | grep -E "(OK rows=|Error)"</automated>
  </verify>
  <done>The smoke command prints `OK rows=0`. No `Error` line. Confirms the typed client is live and the table is empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local CLI → live DB | `bunx prisma migrate dev` mutates whatever database is at DATABASE_URL — a misconfigured env would migrate production |
| Prisma migration history → app/generated/prisma | The typed client must stay in sync with the applied migrations or runtime errors result |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | Migration applied against wrong DB (e.g., production via misconfigured DATABASE_URL) | mitigate | Task 1 is a blocking human checkpoint that asserts the DB host before any DDL runs; psql `\dt` lookup confirms target is the expected dev/staging instance |
| T-07-03-02 | Denial of service | Migration fails mid-apply, leaving the DB in a partial state | mitigate | Prisma migrate dev is wrapped in a transaction for the DDL; on failure it rolls back. The verify step asserts the table is fully created OR not at all |
| T-07-03-03 | Information disclosure | DATABASE_URL printed to a shared CI log | mitigate | Task 1's verify uses `node -e 'console.log(new URL(... hostname))'` to print only the hostname (no credentials); the migrate command itself does NOT echo the URL |
| T-07-03-04 | Tampering | Manual pgvector indexes accidentally dropped by an unrelated `prisma migrate reset` between Plan 02 and Plan 03 | mitigate | Task 2 re-runs `bun db:indexes` after migrate dev per CLAUDE.md convention; idempotent script reapplies HNSW + GIN as needed |
| T-07-03-05 | Repudiation | Phase 4 / 6 tests fail because the regenerated client changed an unrelated model's shape | mitigate | Migration is purely additive (CREATE TABLE only); no ALTER on existing tables; downstream tests should be unaffected — Phase 10 (verification gate) confirms full suite green |
| T-07-03-SC | Tampering | npm/pip/cargo installs in this plan | accept | No new packages — uses existing `prisma` 7.3.0 CLI, `tsx`, `psql` (system tool, not an npm package); slopcheck N/A |
</threat_model>

<verification>
- `prisma/migrations/{timestamp}_add_shop_settings/migration.sql` exists and is committed to git
- `app/generated/prisma/` regenerated artifacts include `shopSettings` accessor (consult `client.ts` and `index.d.ts`)
- `bun db:indexes` prints OK for each pre-existing index; no errors
- The smoke command `await prisma.shopSettings.count()` returns 0
- No production secrets, session tokens, or full database URLs appear in any CI/console output
</verification>

<success_criteria>
- The dev Postgres has a `shop_settings` table with columns `shop` (text, PK), `activeChatModelId` (text), `updatedAt` (timestamp)
- `bunx prisma generate` has produced a client with `prisma.shopSettings` accessor
- The smoke test passes (rows=0)
- The new migration directory + regenerated client are committed to git
- pgvector and GIN indexes from `db/manual-indexes.sql` remain intact (idempotent re-apply succeeded)
</success_criteria>

<output>
Create `.planning/phases/07-admin-settings-model-picker/07-03-SUMMARY.md` documenting:
- Migration directory name + commit hash
- Snippet of the auto-generated migration.sql showing the CREATE TABLE block
- Confirmation that `bun db:indexes` re-apply succeeded
- Wave 1 status: BLOCKING complete; Wave 2 plans (04, 05, 06) unblocked
</output>
