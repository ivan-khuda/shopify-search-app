---
phase: 07-admin-settings-model-picker
plan: 06
type: execute
wave: 2
depends_on: ["07-01", "07-03", "07-04"]
files_modified:
  - services/chat/getActiveChatModel.ts
autonomous: true
requirements: [ADM-03, ADM-04]
must_haves:
  truths:
    - "getActiveChatModel(shop) reads ShopSettings.activeChatModelId from the DB when a row exists for the shop (D-06)"
    - "getActiveChatModel(shop) returns DEFAULT_MODEL when no row exists OR shop is empty (D-09 — never-seeded fallback)"
    - "When the DB row's activeChatModelId is not in the catalog (or the catalog fetch fails), the resolver returns { id: row.activeChatModelId, displayName: <synthesized from id segment> } so chat never breaks (Open Question 3 + 4)"
    - "The function signature (shop: string) => Promise<ActiveChatModel> is UNCHANGED — body-only swap; /api/chat (line 64) and /api/proxy/chat continue to call .id without any modification (D-08 / Phase 4 contract)"
    - "Phase 4 deferred T-04-24 (XSS gate on displayName) is documented as safe by code-path inspection in the resolver's JSDoc — displayName only flows to text contexts (JSX text node, Polaris s-text); no dangerouslySetInnerHTML anywhere downstream"
  artifacts:
    - path: services/chat/getActiveChatModel.ts
      provides: "Phase 7 body of the active-model resolver (DB read + catalog hydration + fallback ladder)"
      exports: ["ActiveChatModel", "getActiveChatModel"]
      contains: "prisma.shopSettings.findUnique"
  key_links:
    - from: services/chat/getActiveChatModel.ts
      to: ShopSettings table via prisma
      via: prisma.shopSettings.findUnique({ where: { shop } })
      pattern: "prisma\\.shopSettings\\.findUnique"
    - from: services/chat/getActiveChatModel.ts
      to: services/chat/model-catalog.ts
      via: import { fetchModelCatalog } — for displayName hydration
      pattern: "fetchModelCatalog"
    - from: app/api/chat/route.ts (line 64) AND app/api/proxy/chat/route.ts
      to: services/chat/getActiveChatModel.ts
      via: getActiveChatModel(shop) — unchanged call sites
      pattern: "getActiveChatModel\\(shop\\)"
---

<objective>
Replace the body of `services/chat/getActiveChatModel.ts` with the Phase 7 implementation: read `prisma.shopSettings.findUnique({ where: { shop } })`, on hit hydrate `displayName` from the catalog (or synthesize from id segment when catalog lookup misses), on miss return `DEFAULT_MODEL`. The function signature stays `(shop: string) => Promise<ActiveChatModel>` — every caller (`/api/chat`, `/api/proxy/chat`, `/chat/page.tsx`) is unchanged. The export shape stays identical (interface `ActiveChatModel { id, displayName }`).

Purpose: Phase 4 designed this resolver as the single mutation point that turns Phase 7 into a body-only swap (D-08 / Phase 4 D-09 contract). This plan delivers the swap. Once GREEN, every per-request read in admin chat and storefront chat picks up the merchant's selected model on the very next request (SC4) without any caller code touching shared state.

Output: One modified TypeScript file driving Plan 01's updated `services/chat/__tests__/getActiveChatModel.test.ts` to GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-admin-settings-model-picker/07-CONTEXT.md
@.planning/phases/07-admin-settings-model-picker/07-RESEARCH.md
@CLAUDE.md

@services/chat/getActiveChatModel.ts
@services/chat/model-catalog.ts
@services/chat/__tests__/getActiveChatModel.test.ts
@app/api/chat/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace getActiveChatModel body with DB read + catalog hydration + fallback ladder</name>
  <files>services/chat/getActiveChatModel.ts</files>
  <behavior>
    Drives the updated Plan 01 Wave-0 test file `services/chat/__tests__/getActiveChatModel.test.ts` from RED to GREEN. The Phase 4 skipped block remains skipped (historical context).

    Public surface (UNCHANGED — contract anchor):
    - `export interface ActiveChatModel { id: string; displayName: string; }` — unchanged
    - `export async function getActiveChatModel(shop: string): Promise<ActiveChatModel>` — body changes

    Private (UNCHANGED):
    - `const DEFAULT_MODEL: ActiveChatModel = { id: 'google/gemini-2.5-flash', displayName: 'Gemini 2.5 Flash' };` — the canonical id literal stays in ONE place (Phase 4 D-09 / RESEARCH §Anti-Patterns)

    New body algorithm (per Open Question 3 + 4 resolutions):
    1. Guard: `if (!shop) return DEFAULT_MODEL;` — empty shop is a never-saved/installing state
    2. DB read: `const row = await prisma.shopSettings.findUnique({ where: { shop } });`
    3. DB miss: `if (!row) return DEFAULT_MODEL;` (D-09 — never explicitly seed)
    4. DB hit, catalog hydration (best-effort):
       ```
       try {
         const { models } = await fetchModelCatalog();
         const match = models.find((m) => m.id === row.activeChatModelId);
         if (match) return { id: match.id, displayName: match.displayName };
       } catch {
         /* fall through to synthesized displayName */
       }
       ```
    5. Last-ditch synthesized displayName (Open Question 3 + 4): `const segment = row.activeChatModelId.split('/')[1] ?? row.activeChatModelId; return { id: row.activeChatModelId, displayName: segment };`

    Imports to add:
    - `import { prisma } from '@/lib/db/client';`
    - `import { fetchModelCatalog } from './model-catalog';` (relative — both files in `services/chat/`)

    Imports to keep: none from the previous body. The `void shop;` line is removed (shop is now used).

    JSDoc updates (planner edits the existing JSDoc header):
    - Update the "ASYMMETRY" paragraph to reflect Phase 7 reality: Phase 4 returned the constant; Phase 7 reads `ShopSettings.activeChatModelId` and hydrates `displayName` from the catalog.
    - Add a new paragraph explicitly documenting the Phase 4 deferred items (T-04-24, T-04-25):
      - **T-04-24 (XSS gate on displayName):** RESOLVED safe by code-path inspection. `displayName` is consumed in three places: (a) admin chat banner `app/(embedded)/chat/page.tsx` (text inside JSX, auto-escaped), (b) admin settings table `app/(embedded)/settings/settings-form.tsx` (text inside `<s-table-cell>`, Polaris auto-escapes), (c) toast `Model updated to ${displayName}` (string interpolation into App Bridge toast API which renders text only). No `dangerouslySetInnerHTML`. The displayName source is the AI Gateway response (trusted) OR the synthesized id-segment (always alphanumeric + dashes per RESEARCH model id pattern `^[a-z-]+/[a-z0-9.-]+$`). No additional sanitization needed.
      - **T-04-25 (searchParams.shop ↔ session.shop):** ADDRESSED at the consumer level. The resolver itself takes a `shop: string` argument — it has no visibility into where the caller derived shop. `/api/chat` and `/api/proxy/chat` source shop from session-token / App Proxy HMAC (authenticated). `/chat/page.tsx` (Phase 4) and `/settings/page.tsx` (Plan 08) source shop from `searchParams` for SSR display purposes only. The write path (Plan 07's PATCH) sources shop strictly from `withShopifySession` — never from body/query. Plan 08's page documents the asymmetry; Plan 07's route enforces it.

    Constraints:
    - Zero `console.*` calls in this file (CLAUDE.md hard rule). The fetchModelCatalog failure is swallowed silently into the fallback.
    - The id literal `'google/gemini-2.5-flash'` appears EXACTLY ONCE — in `DEFAULT_MODEL`. Plan 04's model-catalog.ts cold-start fallback duplicates the literal (circular-import avoidance), but the chat code path treats `DEFAULT_MODEL` as canonical.
    - No `any` types. The Prisma return type for `findUnique` is `ShopSettings | null` — let TypeScript infer.
    - Do NOT import `ShopSettings` type — the structural use `row.activeChatModelId` is sufficient; importing adds an unnecessary dependency.
  </behavior>
  <action>
    Edit `services/chat/getActiveChatModel.ts`. Keep the file header banner explaining Phase 7's role; rewrite the body per the behavior contract. Update the JSDoc to reflect "Phase 7 has shipped" rather than "Phase 7 will swap". Add the imports for `prisma` and `fetchModelCatalog`. Remove the `void shop;` placeholder. File should stay under 100 lines including JSDoc.

    Per CONTEXT.md "Resolved Claude's-Discretion items" — the resolver calls `prisma.shopSettings.findUnique` directly rather than going through `shopSettingsRepository.get` from Plan 05. Rationale: the resolver is read-only and benefits from the typed inline query; the repository is the write-path tool. Document the choice in a short JSDoc note.
  </action>
  <verify>
    <automated>bunx vitest run services/chat/__tests__/getActiveChatModel.test.ts --reporter=verbose 2>&1 | tail -25; echo "---"; bunx vitest run app/api/chat/__tests__/route.test.ts app/api/proxy/chat/__tests__/route.test.ts 'app/(embedded)/chat/__tests__/' --reporter=default 2>&1 | tail -10</automated>
  </verify>
  <done>All non-skipped it() blocks in `getActiveChatModel.test.ts` are GREEN (Phase 4 skipped block stays skipped). The pre-existing Phase 4 chat and proxy chat route tests + the /chat page test remain GREEN (no regressions — body-only swap contract honored). `grep -c "void shop" services/chat/getActiveChatModel.ts` returns 0 (the placeholder is removed). `grep -c "prisma.shopSettings.findUnique" services/chat/getActiveChatModel.ts` returns ≥1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller (route handler / page) → resolver | Caller provides `shop` derived from an authenticated source (session token, App Proxy HMAC, or searchParams for read-only display) |
| Resolver → DB | Single shop-scoped findUnique; multi-tenancy enforced by shop being the PK |
| Resolver → catalog client | Catalog failure is non-fatal — synthesized displayName from id segment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Cross-site scripting (T-04-24 inherited) | `displayName` rendered in chat banner / settings table / toast | mitigate (RESOLVED safe) | Code-path inspection in RESEARCH §Security + JSDoc on the resolver documents that displayName flows to text contexts only; React + Polaris auto-escape; no dangerouslySetInnerHTML downstream |
| T-07-06-02 | Tampering (T-04-25 inherited) | `searchParams.shop` mismatch with the embedded session's shop on `/chat` and `/settings` SSR pages | mitigate | The resolver takes shop on trust; the write path (PATCH in Plan 07) derives shop strictly from withShopifySession; SSR pages document the read-only asymmetry. This matches the `/chat` Phase 4 pattern verbatim — D-08 acknowledges per-request reads are the playground update mechanism |
| T-07-06-03 | Information disclosure | Resolver logs the saved model id when it falls outside the catalog | mitigate | Zero `console.*` in this file; silent fallback is intentional (the settings page shows the user-facing warning banner per D-06) |
| T-07-06-04 | Tampering | DB row's `activeChatModelId` is a string outside the expected catalog (e.g., corrupted by a future bug or manual SQL edit) | mitigate | Catalog hydration is best-effort; on lookup miss, the synthesized displayName uses the id-segment (no code execution path triggered by the value). The chat stream still receives the literal id and the AI Gateway will surface a "model not found" error if it's truly bogus — failure is contained to the chat request, not the resolver |
| T-07-06-05 | Denial of service | Catalog fetch hangs, blocking every chat request | accept (bounded) | `fetchModelCatalog` has Next.js default fetch timeout; 15-min cache short-circuits 99.9% of calls. Acceptable per D-03 + RESEARCH §A5 |
| T-07-06-06 | Repudiation | A merchant changes the model and the next chat request still uses the old one | mitigate | Per-request resolver read (no caching at the resolver layer); D-08 explicitly forbids zustand/SWR/context plumbing for cross-route state |
| T-07-06-SC | Tampering | npm/pip/cargo installs in this plan | accept | No new packages — uses existing prisma client + the catalog module from Plan 04; slopcheck N/A |
</threat_model>

<verification>
- All Wave 0 resolver tests GREEN (DB-hit, DB-miss, unknown-id, empty-shop, catalog-throw cases)
- Phase 4 chat route + proxy chat route + chat page tests REMAIN GREEN (contract preserved)
- `grep -c 'console\.' services/chat/getActiveChatModel.ts` returns 0
- File contains exactly one occurrence of `'google/gemini-2.5-flash'` (in DEFAULT_MODEL)
- The function signature `getActiveChatModel(shop: string): Promise<ActiveChatModel>` is verbatim unchanged
</verification>

<success_criteria>
- All Plan 01 Wave-0 it() blocks in the Phase 7 describe block GREEN
- Phase 4 it() blocks remain SKIPPED, not deleted (historical record)
- No regressions in /api/chat, /api/proxy/chat, /chat/page tests
- The JSDoc header explicitly documents T-04-24 + T-04-25 resolutions
- The resolver passes a one-off integration check: running `bunx tsx -e "import { getActiveChatModel } from '@/services/chat/getActiveChatModel'; (async () => { const r = await getActiveChatModel('nonexistent-shop.myshopify.com'); console.log(r); })();"` prints the DEFAULT_MODEL (proving the function still works end-to-end against the live DB).
</success_criteria>

<output>
Create `.planning/phases/07-admin-settings-model-picker/07-06-SUMMARY.md` documenting:
- The body diff (before/after side-by-side)
- Confirmation that the signature is unchanged
- T-04-24 + T-04-25 resolution notes (links into the JSDoc)
- Test pass count from updated resolver suite
- Handoff note: Plan 07 (PATCH route) and Plan 08 (settings page) can now consume the new resolver behavior
</output>
