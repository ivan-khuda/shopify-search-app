---
phase: 07-admin-settings-model-picker
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - services/chat/__tests__/model-catalog.test.ts
  - services/chat/__tests__/getActiveChatModel.test.ts
  - lib/db/repositories/__tests__/ShopSettingsRepository.test.ts
  - app/api/settings/model/__tests__/route.test.ts
  - app/(embedded)/settings/__tests__/page.test.tsx
  - app/(embedded)/settings/__tests__/settings-form.test.tsx
autonomous: true
requirements: [ADM-03, ADM-04]
must_haves:
  truths:
    - "Wave 0 produces a RED suite that pins every Phase 7 behavior (SC1..SC4) before any implementation exists"
    - "All new test files are committed in failing state; subsequent Wave 1+ plans drive them to GREEN"
    - "Existing getActiveChatModel.test.ts is updated to assert the Phase 7 DB-hit + DB-miss + unknown-id contract (D-06, D-09)"
  artifacts:
    - path: services/chat/__tests__/model-catalog.test.ts
      provides: "Catalog client behavior contract (SC1, D-03 fallback ladder, $/M conversion)"
    - path: services/chat/__tests__/getActiveChatModel.test.ts
      provides: "Updated resolver contract (DB-hit hydrates displayName, DB-miss returns DEFAULT_MODEL, unknown-id falls back) per D-06/D-09"
    - path: lib/db/repositories/__tests__/ShopSettingsRepository.test.ts
      provides: "get(shop) + upsert(shop, modelId) contract"
    - path: app/api/settings/model/__tests__/route.test.ts
      provides: "PATCH endpoint contract: auth + Zod body validation + catalog membership + upsert + structured errors"
    - path: app/(embedded)/settings/__tests__/page.test.tsx
      provides: "Server Component contract: SSR catalog + active model pre-selection + cached/cold-start banners"
    - path: app/(embedded)/settings/__tests__/settings-form.test.tsx
      provides: "Client Component contract: sort, radio selection, dirty-state Save, ui-save-bar visibility"
  key_links:
    - from: services/chat/__tests__/model-catalog.test.ts
      to: services/chat/model-catalog.ts (Plan 04)
      via: import + vi.mock('global.fetch')
      pattern: "import .* from '@/services/chat/model-catalog'"
    - from: services/chat/__tests__/getActiveChatModel.test.ts
      to: services/chat/getActiveChatModel.ts (Plan 06)
      via: import + vi.mock prisma + vi.mock model-catalog
      pattern: "import .* from '@/services/chat/getActiveChatModel'"
    - from: app/api/settings/model/__tests__/route.test.ts
      to: app/api/settings/model/route.ts (Plan 07)
      via: import PATCH handler
      pattern: "import .* from '../route'"
---

<objective>
Create the Wave 0 RED test scaffolds for Phase 7 (per VALIDATION.md Wave 0 Requirements). Every behavior locked by D-01..D-10 and required by ADM-03/ADM-04/SC1..SC4 gets a failing test BEFORE any implementation lands. Subsequent plans drive these to GREEN.

Purpose: Pin the behavior contract before swapping `getActiveChatModel`'s body, adding the `ShopSettings` table, and shipping the `/settings` UI. Wave 0 prevents implementation drift and gives every Wave 1+ task an `<automated>` verify target.

Output: 5 new test files + 1 updated test file, all in failing state and committed to git.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-admin-settings-model-picker/07-CONTEXT.md
@.planning/phases/07-admin-settings-model-picker/07-RESEARCH.md
@.planning/phases/07-admin-settings-model-picker/07-VALIDATION.md

@services/chat/getActiveChatModel.ts
@services/chat/__tests__/getActiveChatModel.test.ts
@lib/db/repositories/__tests__/ProductRepository.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create catalog + repository + resolver test scaffolds (services + lib)</name>
  <files>services/chat/__tests__/model-catalog.test.ts, services/chat/__tests__/getActiveChatModel.test.ts, lib/db/repositories/__tests__/ShopSettingsRepository.test.ts</files>
  <behavior>
    services/chat/__tests__/model-catalog.test.ts (new) — must declare:
    - it('fetches /v1/models, filters type==="language", maps to CatalogModel, multiplies pricing by 1e6') — vi.stubGlobal('fetch') returning a fixture with one language model `google/gemini-2.5-flash` (pricing.input "0.0000003", pricing.output "0.0000025") AND one non-language entry; asserts result.models has exactly the language row with inputPricePerMillion === 0.3 and outputPricePerMillion === 2.5.
    - it('decorates rows with BEST_FOR descriptor, falls back to "General purpose"') — fixture includes one model NOT in BEST_FOR; assert that row.bestFor === 'General purpose'.
    - it('returns stale:true with last-known-good when fetch rejects after a successful prior call') — first call resolves, second call rejects; assert second result.stale === true and models matches first call's data.
    - it('returns coldStartFallback:true with DEFAULT_MODEL-only row when fetch fails on first call') — fetch rejects; assert models.length === 1 && models[0].id === 'google/gemini-2.5-flash' && result.coldStartFallback === true.
    - it('caches successful fetches for 15 minutes (vi.useFakeTimers + advanceTimersByTime)') — first call hits fetch, second call within 15min hits cache (fetch called once); after advancing >15min fetch is called again.
    - it('uses cache:"no-store" on every fetch (Pitfall 4 — Next.js data-cache leakage)') — assert the fetch mock's options arg includes cache:'no-store'.

    services/chat/__tests__/getActiveChatModel.test.ts (UPDATE existing) — keep the existing "shop-agnostic Phase 4 constant" tests as historical context but mark them with `it.skip(...)` and add the new Phase 7 contract:
    - it('returns DEFAULT_MODEL when no ShopSettings row exists for shop (D-09)') — vi.mock prisma.shopSettings.findUnique → null; assert returned id === 'google/gemini-2.5-flash' and displayName === 'Gemini 2.5 Flash'.
    - it('returns DEFAULT_MODEL when shop is empty string') — assert no prisma call is made.
    - it('reads ShopSettings row and hydrates displayName from catalog on DB hit (D-06)') — vi.mock prisma.shopSettings.findUnique → { shop, activeChatModelId: 'anthropic/claude-sonnet-4.5', updatedAt }; vi.mock fetchModelCatalog → catalog containing that id with displayName 'Claude Sonnet 4.5'; assert result === { id: 'anthropic/claude-sonnet-4.5', displayName: 'Claude Sonnet 4.5' }.
    - it('synthesizes displayName from id segment when catalog lookup misses (Open Question 3)') — DB row with id 'anthropic/claude-ghost-99'; catalog does NOT contain it; assert id is preserved and displayName === 'claude-ghost-99' (segment after slash).
    - it('synthesizes displayName from id when fetchModelCatalog throws (catalog outage)') — DB row present; catalog throws; same expectation as above.
    Use `vi.mock('@/lib/db/client', () => ({ prisma: { shopSettings: { findUnique: vi.fn() } } }))` and `vi.mock('@/services/chat/model-catalog', () => ({ fetchModelCatalog: vi.fn() }))`. Reset between tests.

    lib/db/repositories/__tests__/ShopSettingsRepository.test.ts (new) — mirror ProductRepository.test.ts structure:
    - vi.mock('@/lib/db/client', () => ({ prisma: { shopSettings: { findUnique: vi.fn(), upsert: vi.fn() } } }))
    - it('get(shop) returns null when no row exists') — findUnique → null; assert null.
    - it('get(shop) returns the row when present') — findUnique → row; assert returned row.activeChatModelId matches.
    - it('upsert(shop, id) creates a row when none exists (create branch wired)') — assert upsert called with where:{shop}, create:{shop, activeChatModelId:id}, update:{activeChatModelId:id}.
    - it('upsert(shop, id) updates only activeChatModelId on existing row') — same args; assert update branch payload contains exactly activeChatModelId.
    - it('upsert returns the (post-write) row including updatedAt') — mock returns row with updatedAt; assert that row is returned.
  </behavior>
  <action>Create the three new test files above. UPDATE `services/chat/__tests__/getActiveChatModel.test.ts` per behavior block (wrap existing three it() blocks in `describe.skip('Phase 4 contract (now obsolete)', ...)` and add a new `describe('Phase 7 contract', ...)` block). All imports use the `@/` alias. Use only `vitest` + `vi.mock`/`vi.fn`/`vi.stubGlobal` — do NOT import test runners or matchers from anywhere other than `vitest`. The tests reference modules and types (`fetchModelCatalog`, `CatalogModel`, `BEST_FOR`, `ShopSettingsRepository`) that do NOT yet exist; that is the RED state. Each test file must have an end-of-file note in a comment: `// RED: implementation lands in Plan {NN}`.</action>
  <verify>
    <automated>bunx vitest run services/chat/__tests__/model-catalog.test.ts services/chat/__tests__/getActiveChatModel.test.ts lib/db/repositories/__tests__/ShopSettingsRepository.test.ts --reporter=verbose 2>&1 | grep -E '(FAIL|PASS|Test Files)' | head -20</automated>
  </verify>
  <done>All three test files exist on disk. Running them produces a RED suite (failures expected because `services/chat/model-catalog.ts`, `lib/db/repositories/ShopSettingsRepository.ts`, and the Phase 7 body of `getActiveChatModel.ts` do not yet exist or do not yet implement the contract). The existing 3 Phase 4 assertions in `getActiveChatModel.test.ts` are wrapped in `describe.skip` and not counted as failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create route + page + settings-form test scaffolds</name>
  <files>app/api/settings/model/__tests__/route.test.ts, app/(embedded)/settings/__tests__/page.test.tsx, app/(embedded)/settings/__tests__/settings-form.test.tsx</files>
  <behavior>
    app/api/settings/model/__tests__/route.test.ts (new) — vi.mock '@/lib/shopify/auth' to bypass session-token verification by replacing `withShopifySession` with `(h) => async (req) => h({ shop: 'test-shop.myshopify.com', session: {} as never, req })`. vi.mock '@/lib/db/client' with `prisma.shopSettings.upsert`. vi.mock '@/services/chat/model-catalog' with `fetchModelCatalog` returning a catalog containing `google/gemini-2.5-flash` and `anthropic/claude-sonnet-4.5`.
    - it('returns 400 invalid_body when JSON body is not parseable') — POST with body 'not json'; assert 400 + body.error === 'invalid_body'.
    - it('returns 400 invalid_body when activeChatModelId is missing') — assert 400 + 'invalid_body'.
    - it('returns 400 invalid_body when activeChatModelId is > 200 chars') — assert 400 + 'invalid_body'.
    - it('returns 400 unknown_model_id when id is not in the catalog') — body { activeChatModelId: 'fake/model' }; assert 400 + 'unknown_model_id'; assert prisma.shopSettings.upsert NOT called.
    - it('upserts ShopSettings and returns 200 with displayName on valid request') — body { activeChatModelId: 'anthropic/claude-sonnet-4.5' }; assert upsert called with where:{shop:'test-shop.myshopify.com'}, create+update args set; assert response { ok: true, displayName: 'Claude Sonnet 4.5' }.
    - it('NEVER logs the Authorization header or session token in any code path') — spy on console.log/error/warn; expect zero calls across all branches above (CLAUDE.md hard rule).
    - it('derives shop from session ctx, not from request body (multi-tenancy lock)') — even if body includes shop: 'evil-shop', the upsert.where.shop is the session's shop.
    Imports: `import { PATCH } from '../route'`.

    app/(embedded)/settings/__tests__/page.test.tsx (new) — Use `@testing-library/react` + jsdom. vi.mock '@/services/chat/getActiveChatModel' returning DEFAULT_MODEL (or a custom id per test). vi.mock '@/services/chat/model-catalog' for `fetchModelCatalog`. Render the page as an async Server Component using `await SettingsPage({ searchParams: Promise.resolve({ shop: 'demo.myshopify.com' }) })` then `render()` the returned JSX.
    - it('renders catalog rows with name, provider, context window, $/M input, $/M output, best for columns (SC1)') — fixture catalog with 2 models; assert table cells contain each header text + each model's displayName, provider, formatted context window (use toLocaleString), and pricing strings prefixed with $.
    - it('pre-selects the active model row when activeId is present in catalog (SC3, D-06)') — activeModel.id matches a catalog row; assert that row's s-choice element has the `selected` attribute set.
    - it('renders warning banner when active id is not in catalog (D-06)') — activeModel.id NOT in catalog; assert s-banner with tone="warning" exists.
    - it('renders cached-banner when fetchModelCatalog returns stale: true (D-03)') — catalogResult.stale === true; assert s-banner with tone="warning" containing "cached".
    - it('renders cold-start banner + disables Save when coldStartFallback: true (D-03)') — assert s-banner tone="critical" + SettingsForm receives saveDisabled prop true.
    - it('does NOT crash when searchParams.shop is undefined; passes empty shop to resolver (Phase 4 deferred — matches /chat pattern)') — searchParams: {}; assert getActiveChatModel called with empty string.

    app/(embedded)/settings/__tests__/settings-form.test.tsx (new) — Use @testing-library/react + userEvent.
    - it('renders one s-choice per catalog row with value=row.id') — assert count + values.
    - it('toggles sort direction on header click (asc → desc → none) — Pitfall 1') — click context-window header; assert rows reorder by ascending; click again → descending; click again → original.
    - it('Save (ui-save-bar) is hidden until selection differs from activeId (D-07)') — initially no save bar; click a different radio → save bar visible.
    - it('Save (ui-save-bar) is hidden when saveDisabled prop is true (cold-start)') — saveDisabled=true; click different radio; save bar still hidden.
    - it('clicking Save calls PATCH /api/settings/model with the Bearer token and selected id') — stub window.shopify.idToken → 'tok'; stub global.fetch → 200 + { displayName: 'X' }; click Save; assert fetch called with method 'PATCH', headers Authorization 'Bearer tok', body JSON.stringify({ activeChatModelId: 'X-id' }).
    - it('shows toast on Save success with the new displayName (D-07)') — spy on window.shopify.toast.show; assert called with `Model updated to X`.
    - it('renders error banner with API error code on non-200 Save response') — fetch returns 400 + { error: 'unknown_model_id' }; assert s-banner tone="critical" containing "unknown_model_id".
    - it('renders the warning banner "previously-selected model is no longer available" when activeId is not in catalog (D-06)') — catalog without activeId; assert s-banner tone="warning".
    NB: Polaris s-* web components are not registered in jsdom; the test renders the React tree (s-* tags become unknown elements that still propagate props/events). Assertions use attribute presence (`element.getAttribute('selected')`) and text content, not Polaris-specific rendering.

    Stub the `window.shopify` global once in a shared `beforeEach` of settings-form.test.tsx: `Object.defineProperty(globalThis, 'shopify', { value: { idToken: vi.fn().mockResolvedValue('tok'), toast: { show: vi.fn() } }, configurable: true })`.
  </behavior>
  <action>Create the three test files above. Use `import { describe, it, expect, vi, beforeEach } from 'vitest'`, `import { render, screen, fireEvent } from '@testing-library/react'`, and `import userEvent from '@testing-library/user-event'`. Reference modules that do not yet exist (`../page`, `../settings-form`, `../route`) — that is the RED state. Each file ends with a comment: `// RED: implementation lands in Plan {NN}`. Do NOT add `dangerouslySetInnerHTML` or innerHTML assertions. Polaris s-* tags are treated as unknown HTML elements in jsdom — assertions read `getAttribute` or `textContent`.</action>
  <verify>
    <automated>bunx vitest run app/api/settings/model/__tests__/route.test.ts 'app/(embedded)/settings/__tests__/' --reporter=verbose 2>&1 | grep -E '(FAIL|PASS|Test Files)' | head -20</automated>
  </verify>
  <done>All three test files exist on disk. Suite runs and produces the expected RED state (failures because `app/api/settings/model/route.ts`, `app/(embedded)/settings/page.tsx`, and `settings-form.tsx` do not yet exist). No tests are accidentally green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → application code | Tests vi.mock prisma, fetch, App Bridge — assertions are the only enforcement against contract drift |
| Test fixtures → production assumptions | Hardcoded model ids in fixtures (`google/gemini-2.5-flash`, `anthropic/claude-sonnet-4.5`) must mirror the live AI Gateway catalog shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | Test fixtures encode the wrong pricing format (e.g., $/M instead of $/token) | mitigate | Fixtures use the literal `pricing.input: '0.0000003'` string format verified against the live AI Gateway response in RESEARCH.md Pitfall 5; tests assert the conversion (×1e6) explicitly |
| T-07-01-02 | Repudiation | Wave 0 tests committed green by accident (skipping the RED state) | mitigate | Verify step greps the runner output for FAIL/PASS counts; planner-execute checkpoint requires a non-zero failure count before Wave 0 is marked complete |
| T-07-01-03 | Information disclosure | Test mock for the PATCH route accidentally console.logs the session token via copy-paste from old patterns | mitigate | The route test explicitly spies on console.log/warn/error and asserts zero calls (CLAUDE.md hard rule); applies to test code too |
| T-07-01-SC | Tampering | npm/pip/cargo installs introduced in this plan | accept | No new packages — vitest + @testing-library/* already installed (per RESEARCH §Standard Stack); slopcheck not applicable |
</threat_model>

<verification>
After Task 2, run:
- `bunx vitest run services/chat lib/db/repositories app/api/settings 'app/(embedded)/settings' --reporter=default 2>&1 | tail -25`
- Confirm the expected RED count matches Wave 0 expectations (6 new test files; ~30+ failing it() blocks across them).
- No file outside the listed `files_modified` is touched.
</verification>

<success_criteria>
- 5 new test files + 1 updated test file committed to git in failing state
- Existing Phase 4 tests in `getActiveChatModel.test.ts` are skipped (not deleted) with a `describe.skip` wrapper preserving them as historical context
- Every test imports from `@/` path aliases (no relative `../../..` chains beyond `../route` / `../page` / `../settings-form` siblings)
- No production code (under `services/`, `lib/`, `app/api/`, `app/(embedded)/settings/page.tsx`, `settings-form.tsx`) is created by this plan
</success_criteria>

<output>
Create `.planning/phases/07-admin-settings-model-picker/07-01-SUMMARY.md` documenting:
- Each new test file path and the count of failing it() blocks
- Confirmation that the updated `getActiveChatModel.test.ts` preserves Phase 4 assertions via `describe.skip`
- Wave 0 status snapshot (X tests RED, ready for Wave 1)
</output>
