---
phase: 06-storefront-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/shopify/__tests__/app-proxy-auth.test.ts
  - lib/shopify/__tests__/app-proxy-auth.fuzz.test.ts
  - lib/rate-limit/__tests__/memory.test.ts
  - app/api/proxy/conversations/__tests__/route.test.ts
  - app/api/proxy/conversations/[id]/__tests__/route.test.ts
  - app/api/proxy/saved-products/__tests__/route.test.ts
  - app/api/proxy/saved-products/[productId]/__tests__/route.test.ts
  - app/api/proxy/chat/__tests__/route.test.ts
  - __tests__/merge-integration.test.ts
  - inngest/functions/__tests__/retention-sweep.test.ts
  - lib/chat-ui/stores/__tests__/db-backed.test.ts
  - lib/chat-ui/adapters/__tests__/storefront.test.ts
  - extensions/chat-drawer/__tests__/loader.test.ts
  - extensions/chat-drawer/src/components/__tests__/StorefrontDrawer.test.tsx
  - extensions/chat-drawer/src/components/__tests__/PromptChips.test.tsx
  - __tests__/app-embed-schema.test.ts
  - __tests__/shopify-toml.test.ts
  - __tests__/extension-structure.test.ts
  - __tests__/bundle-build.test.ts
  - lib/identity/__tests__/merge.test.ts
autonomous: true
requirements:
  - STR-01
  - STR-02
  - STR-03
  - STR-04
  - STR-06
  - STR-07
  - STR-08
  - IDN-02
  - IDN-04
  - IDN-05
  - IDN-06

must_haves:
  truths:
    - "Every requirement in Phase 6 has at least one RED test file that imports the (not-yet-existing) production module"
    - "Every test file uses `bunx vitest run <path>` form (no watch mode)"
    - "Tests fail with 'module not found' / unimplemented errors today; will go GREEN as Waves 2-3 ship"
  artifacts:
    - path: "lib/shopify/__tests__/app-proxy-auth.test.ts"
      provides: "STR-04 HMAC verification + IDN-02 logged_in_customer_id cross-check tests"
    - path: "lib/rate-limit/__tests__/memory.test.ts"
      provides: "D-08 sliding-window math tests"
    - path: "app/api/proxy/chat/__tests__/route.test.ts"
      provides: "STR-04 + D-19 onFinish + IDN-02 cross-check tests"
    - path: "__tests__/merge-integration.test.ts"
      provides: "IDN-06 merge transaction + VisitorCustomerLink idempotency tests (real test DB)"
    - path: "extensions/chat-drawer/__tests__/loader.test.ts"
      provides: "STR-07 designMode guard + D-15 skeleton test"
  key_links: []
---

<objective>
Wave 0 RED scaffolds. Create every Vitest test file enumerated in 06-VALIDATION.md so subsequent waves have failing tests to drive implementation against (Nyquist gate). Tests reference modules that do not yet exist; this plan is the first to commit them.

Purpose: Honor the project's Nyquist-validation guideline — no implementation task without a failing test scaffold already on disk.
Output: 20 test files (19 net-new + 1 extended existing storefront.test.ts) that drive Waves 2/3 to GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-storefront-surface/06-VALIDATION.md
@.planning/phases/06-storefront-surface/06-CONTEXT.md
@.planning/phases/06-storefront-surface/06-RESEARCH.md
@.planning/phases/06-storefront-surface/06-PATTERNS.md
@vitest.config.ts
@lib/chat-ui/adapters/storefront.ts
@lib/chat-ui/adapters/__tests__/storefront.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold auth, rate-limit, and route-handler RED tests</name>
  <files>
    lib/shopify/__tests__/app-proxy-auth.test.ts,
    lib/shopify/__tests__/app-proxy-auth.fuzz.test.ts,
    lib/rate-limit/__tests__/memory.test.ts,
    app/api/proxy/conversations/__tests__/route.test.ts,
    app/api/proxy/conversations/[id]/__tests__/route.test.ts,
    app/api/proxy/saved-products/__tests__/route.test.ts,
    app/api/proxy/saved-products/[productId]/__tests__/route.test.ts,
    app/api/proxy/chat/__tests__/route.test.ts,
    lib/identity/__tests__/merge.test.ts
  </files>
  <read_first>
    .planning/phases/06-storefront-surface/06-VALIDATION.md,
    .planning/phases/06-storefront-surface/06-RESEARCH.md,
    .planning/phases/06-storefront-surface/06-PATTERNS.md,
    lib/shopify/auth.ts,
    lib/shopify/__tests__/auth.test.ts,
    app/api/shopify/sync/status/route.ts,
    app/api/proxy/chat/route.ts
  </read_first>
  <behavior>
    - app-proxy-auth.test.ts: missing signature → 401 with code 'missing_signature'; invalid signature → 401 'invalid_signature'; non-myshopify domain → 401 'invalid_shop_domain'; valid no-delimiter alphabetical-sorted SHA-256 signature → returns { shop, query }; withAppProxyHmac surfaces AppProxyAuthError as JSON { error: code }
    - app-proxy-auth.fuzz.test.ts: tampered shop param after signing rejects; signature param with extra whitespace rejects; HMAC algorithm uses NO delimiter between key=value pairs (regression guard for the OAuth `&`-delimiter pitfall)
    - memory.test.ts: 30 chat hits within 5min for same visitor returns ok; 31st returns { ok:false, retryAfterSeconds:60 }; 60 read hits within 1min returns ok; 61st returns { ok:false, retryAfterSeconds:30 }; pruning drops timestamps older than window
    - conversations/__tests__/route.test.ts: GET without HMAC → 401; GET with HMAC + visitor_id returns paginated 20-item list ordered by lastMessageAt desc; POST creates Conversation with title = first 60 chars of firstMessage.text per D-18; POST title falls back to '(no title)' on empty text; DELETE bulk removes all rows for visitor_id (and customerId if linked)
    - conversations/[id]/__tests__/route.test.ts: GET returns full messages JSONB for matching shop; wrong shop → 403; missing id → 404; PATCH appends turn to messages JSONB atomically
    - saved-products/__tests__/route.test.ts: GET returns visitor+linked-customer rows; POST is idempotent (repeated POST yields the same row count due to ON CONFLICT DO NOTHING per D-20); POST without HMAC → 401; IDN-02 cross-check: body.customer_id != signed logged_in_customer_id → 403 'customer_id_mismatch'
    - saved-products/[productId]/__tests__/route.test.ts: DELETE removes single row matching shop+visitor+product; wrong shop → 403; missing visitor_id → 400
    - chat/__tests__/route.test.ts: replaces existing 501 stub test (existing file may already contain it — extend or recreate); POST without HMAC → 401; missing visitor_id body → 400; IDN-02 cross-check (body.customer_id !== signed logged_in_customer_id) → 403; rate limit exceeded → 429 + Retry-After; happy path returns a UI message stream Response (status 200, content-type text/event-stream or compatible); onFinish (D-19) writes assistant turn into Conversation.messages JSONB atomically with lastMessageAt = NOW(); mid-stream throw does NOT write
    - lib/identity/__tests__/merge.test.ts: mergeVisitorIntoCustomer(shop, visitorId, customerId) re-keys anon Conversation rows in place (UPDATE); INSERTs SavedProduct rows with customerId set, ON CONFLICT DO NOTHING dedupes; DELETEs visitor-only SavedProduct rows; INSERTs VisitorCustomerLink row; second invocation of mergeVisitorIntoCustomer with same triple is a no-op (idempotent)
  </behavior>
  <action>
    Create the test files listed. Each test file follows the project's Vitest pattern from `lib/shopify/__tests__/auth.test.ts` (describe/it/expect, vi.fn mocks for prisma, no watch mode). Tests MUST import from the (not-yet-existing) production paths so they fail with "Cannot find module" errors today:
    - `import { withAppProxyHmac, verifyAppProxyHmac, AppProxyAuthError } from '@/lib/shopify/app-proxy-auth';`
    - `import { rateLimit } from '@/lib/rate-limit/memory';`
    - `import { GET, POST, DELETE } from '@/app/api/proxy/conversations/route';` (per Next.js 16 named-export contract)
    - `import { GET, PATCH } from '@/app/api/proxy/conversations/[id]/route';`
    - `import { GET, POST } from '@/app/api/proxy/saved-products/route';`
    - `import { DELETE } from '@/app/api/proxy/saved-products/[productId]/route';`
    - `import { POST } from '@/app/api/proxy/chat/route';` (replaces 501 stub assertion)
    - `import { mergeVisitorIntoCustomer } from '@/lib/identity/merge';`
    HMAC-signing helper in fuzz + chat + conversations + saved-products tests uses the no-delimiter SHA-256 algorithm: `signature = HMAC_SHA256(SHOPIFY_API_SECRET, alphabeticallySortedKeys.map(k => k+'='+params[k]).join(''))` — NO `&` delimiter (D-21 RESEARCH §App Proxy specifics). Use `process.env.SHOPIFY_API_SECRET = 'test-secret'` in `beforeEach`. For the chat-route DB write test, mock `prisma.conversation.update` and assert it was called with `messages = messages || $newMsgJson` shape (loose match on `data.messages` containing user+assistant items + `lastMessageAt` Date). For merge.test.ts, mock `prisma.$transaction` and `prisma.visitorCustomerLink.findUnique/create` and `tx.$executeRaw`, asserting the exact sequence of D-11 statements. Do NOT inline production code in tests — only assertions and mocks.
  </action>
  <verify>
    <automated>bunx vitest run lib/shopify/__tests__/app-proxy-auth.test.ts lib/rate-limit/__tests__/memory.test.ts app/api/proxy/conversations/__tests__ app/api/proxy/saved-products/__tests__ app/api/proxy/chat/__tests__ lib/identity/__tests__/merge.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - All 9 listed files exist under their stated paths
    - Each file contains at least 1 `describe(...)` and ≥3 `it(...)` blocks
    - `bunx vitest run <files>` reports tests as failing with "Cannot find module '@/lib/shopify/app-proxy-auth'" or equivalent for every unimplemented module — proves RED state
    - No `vitest` watch-mode invocations in any task file (grep `vitest run` only)
    - HMAC test fixtures use NO `&` delimiter when concatenating signed params (regression guard for OAuth confusion)
    - Chat route test asserts IDN-02 cross-check: a request whose `body.customer_id` does not match the signed `query.logged_in_customer_id` returns 403 `customer_id_mismatch`
  </acceptance_criteria>
  <done>
    9 RED test files committed; vitest run lists them as failing module-not-found; no syntax errors in any test file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold store, adapter, retention-sweep, merge-integration tests</name>
  <files>
    lib/chat-ui/stores/__tests__/db-backed.test.ts,
    lib/chat-ui/adapters/__tests__/storefront.test.ts,
    inngest/functions/__tests__/retention-sweep.test.ts,
    __tests__/merge-integration.test.ts
  </files>
  <read_first>
    .planning/phases/06-storefront-surface/06-VALIDATION.md,
    .planning/phases/06-storefront-surface/06-PATTERNS.md,
    lib/chat-ui/stores/local-storage.ts,
    lib/chat-ui/stores/types.ts,
    lib/chat-ui/adapters/storefront.ts,
    lib/chat-ui/adapters/__tests__/storefront.test.ts,
    inngest/functions/sync-products.ts,
    inngest/functions/__tests__/sync-products.test.ts
  </read_first>
  <behavior>
    - db-backed.test.ts: DbBackedHistoryStore constructor throws on missing shop/visitorId per D-02; list() returns synchronous cache (useSyncExternalStore contract); refresh() fetches `/apps/smartdiscovery/conversations?visitor_id=...` and notifies subscribers; add() POSTs and optimistically updates cache; clear() issues DELETE bulk and resets cache; subscribe/unsubscribe pattern matches LocalStorage analog; DbBackedSavedProductsStore mirrors with toggle() POSTing to /apps/smartdiscovery/saved-products
    - storefront.test.ts (EXTEND existing — read existing file first; do NOT delete prior tests): adds tests asserting that when `window.Shopify.customer.id` is set to a numeric value, `getRequestBody()` returns `{ visitor_id, customer_id }` where customer_id is `String(window.Shopify.customer.id)` (BigInt precision preserved per Pitfall 7); when window.Shopify is undefined or customer is null, no customer_id key appears; STR-08: endpoint stays exactly `/api/proxy/chat` (no cross-origin URL)
    - retention-sweep.test.ts: uses `@inngest/test` per VALIDATION; mocks Conversation rows with lastMessageAt < now()-180d; asserts paginated step.run deletes 1000 per batch; asserts loop exits when batch returns 0; asserts cron trigger `'0 3 * * 0'` per RESEARCH §Pattern 3
    - merge-integration.test.ts: integration-style with real test DB or in-memory adapter — see existing integration test pattern in inngest/functions/__tests__; first call to mergeVisitorIntoCustomer creates VisitorCustomerLink + re-keys 3 anon Conversation rows + re-INSERTs 2 anon SavedProduct rows with customerId set + DELETEs the visitor-only rows; second call with same triple is a no-op (existing row short-circuits, zero writes)
  </behavior>
  <action>
    Create or extend the four test files. For `storefront.test.ts`, READ the existing file first; APPEND new `describe('IDN-02 customer_id', ...)` block — do NOT remove prior assertions. For `retention-sweep.test.ts`, import from `@/inngest/functions/retention-sweep` (not yet existing) — use the InngestTestEngine pattern from `inngest/functions/__tests__/sync-products.test.ts` to invoke and assert step.run output. For `merge-integration.test.ts`, the test may use Prisma's in-memory mock pattern OR mark itself with `it.skipIf(!process.env.TEST_DATABASE_URL)` so it runs in CI with a real DB but skips locally — match the existing `*.integration-test.ts` pattern in the repo if present. Mock `window.Shopify` in jsdom via `Object.defineProperty(window, 'Shopify', { value: { customer: { id: 5570080145486 } } })` for storefront.test.ts.
  </action>
  <verify>
    <automated>bunx vitest run lib/chat-ui/stores/__tests__/db-backed.test.ts lib/chat-ui/adapters/__tests__/storefront.test.ts inngest/functions/__tests__/retention-sweep.test.ts __tests__/merge-integration.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist
    - db-backed.test.ts imports `DbBackedHistoryStore, DbBackedSavedProductsStore from '@/lib/chat-ui/stores/db-backed'` — module not found until Wave 2
    - storefront.test.ts file size grew vs. prior commit (new tests appended, existing preserved)
    - retention-sweep.test.ts imports from `@/inngest/functions/retention-sweep` (module not found until Wave 2)
    - merge-integration.test.ts asserts second merge is a no-op (idempotency check on VisitorCustomerLink)
    - storefront.test.ts contains an assertion that `body.customer_id === '5570080145486'` (string-typed, BigInt precision)
  </acceptance_criteria>
  <done>
    4 test files updated/created; existing storefront.test.ts assertions still pass for already-shipped Phase 5 behavior; new tests fail with module-not-found.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffold extension, schema, toml, and bundle-build tests</name>
  <files>
    extensions/chat-drawer/__tests__/loader.test.ts,
    extensions/chat-drawer/src/components/__tests__/StorefrontDrawer.test.tsx,
    extensions/chat-drawer/src/components/__tests__/PromptChips.test.tsx,
    __tests__/app-embed-schema.test.ts,
    __tests__/shopify-toml.test.ts,
    __tests__/extension-structure.test.ts,
    __tests__/bundle-build.test.ts
  </files>
  <read_first>
    .planning/phases/06-storefront-surface/06-VALIDATION.md,
    .planning/phases/06-storefront-surface/06-UI-SPEC.md,
    .planning/phases/06-storefront-surface/06-RESEARCH.md,
    .planning/phases/06-storefront-surface/06-PATTERNS.md,
    shopify.app.toml,
    package.json
  </read_first>
  <behavior>
    - loader.test.ts: when `window.Shopify.designMode === true` is set AT click time, calling the FAB click handler does NOT call `import()` and does NOT mount the bundle (STR-07); when designMode false, fetches manifest then dynamically imports the bundle; first click paints skeleton container with class `sd-skeleton-open` before bundle resolves (D-15)
    - StorefrontDrawer.test.tsx: renders FAB with aria-label "Open SmartDiscovery AI chat" (UI-SPEC Copywriting); FAB click toggles drawer open/closed state; drawer aside contains tabs labeled "Chat", "History", "Saved" (UI-SPEC); Escape key closes drawer; close button has aria-label "Close chat drawer"
    - PromptChips.test.tsx: renders exactly 4 chips with EXACT label strings from UI-SPEC ("Show me your best sellers", "What's on sale right now?", "Help me find a gift under $50", "I'm looking for something warm and cozy"); clicking a chip invokes onSubmit prop with that exact text
    - app-embed-schema.test.ts: reads `extensions/chat-drawer/blocks/app_embed.liquid`; extracts the `{% schema %}` JSON; asserts settings include exactly three ids `enabled` (checkbox, default true), `accent_color` (color, default "#008060"), `fab_position` (select with options bottom_right/bottom_left, default bottom_right) per D-16
    - shopify-toml.test.ts: reads `shopify.app.toml`; parses TOML; asserts `[app_proxy]` block exists with `url`, `subpath = "smartdiscovery"`, `prefix = "apps"` per STR-03
    - extension-structure.test.ts: file existence assertions — `extensions/chat-drawer/shopify.extension.toml`, `extensions/chat-drawer/blocks/app_embed.liquid`, `extensions/chat-drawer/assets/loader.js`, `extensions/chat-drawer/assets/loader.css`, `extensions/chat-drawer/src/entry.tsx`
    - bundle-build.test.ts: runs `bun run prebuild` via execSync (skip on CI if BUN_NOT_AVAILABLE); asserts `public/storefront-bundle-*.js` matches a glob; asserts `public/storefront-manifest.json` parses to `{ bundle: '/storefront-bundle-X.js', version: string }`; asserts bundle size < 250KB minified
  </behavior>
  <action>
    Create all seven files. For TOML parsing in shopify-toml.test.ts, use Node's built-in TOML support via `fs.readFileSync` + a simple regex check (`/\[app_proxy\][^[]*url\s*=/`) since the project has no TOML parser dep — keep the assertion regex-based. For loader.test.ts, mount the loader script's IIFE manually by reading the (not-yet-existing) `extensions/chat-drawer/assets/loader.js` text and using `new Function(text)()` inside jsdom — alternatively, refactor loader to export a `paintFab(root)` function the test can call directly (planner leaves this to Wave 3 plan 12 author). For bundle-build.test.ts, use `execSync('bun run prebuild', { stdio: 'pipe' })` and gate with `it.skipIf(...)` if running in jsdom-only CI mode. UI-SPEC chip strings must match BYTE-PRECISE — use the exact U+2019 smart apostrophe in `"What's on sale right now?"` per UI-SPEC §Copywriting Contract (verify by reading 06-UI-SPEC.md line 200).
  </action>
  <verify>
    <automated>bunx vitest run extensions/chat-drawer/__tests__ extensions/chat-drawer/src/components/__tests__ __tests__/app-embed-schema.test.ts __tests__/shopify-toml.test.ts __tests__/extension-structure.test.ts __tests__/bundle-build.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files exist
    - PromptChips.test.tsx chip-string assertions use byte-identical UI-SPEC strings including U+2019 in chip 2
    - shopify-toml.test.ts uses `/\[app_proxy\]/` regex or equivalent string-presence check — does not require a TOML parser dependency
    - extension-structure.test.ts is a pure `fs.existsSync` test that will pass only after Wave 3 ships the scaffold
    - bundle-build.test.ts gates with `it.skipIf` when bun is not on PATH so the test does not hard-fail in non-bun environments
    - All tests fail today with module-not-found or file-not-found
  </acceptance_criteria>
  <done>
    7 RED test files committed; vitest run lists all newly-added tests as failing.
  </done>
</task>

</tasks>

<verification>
- `bunx vitest run` over all test paths listed in this plan reports all newly-added tests as FAILING (RED) — confirms the Wave 0 scaffold is in place and tests are written, but no implementation exists.
- Existing Phase 5 storefront.test.ts assertions still pass (additive extension only).
- `grep -rn 'vitest$' .planning/phases/06-storefront-surface/` returns no matches — never use watch mode.
</verification>

<success_criteria>
- 20 test files exist (19 new + 1 extended) covering every ❌ Wave 0 item in 06-VALIDATION.md
- All RED tests fail with module-not-found or file-not-found errors
- No production code changed in this plan (test files only)
- `06-VALIDATION.md` frontmatter can flip `nyquist_compliant: true` after this plan ships
</success_criteria>

<output>
Create `.planning/phases/06-storefront-surface/06-01-SUMMARY.md` when done listing the 20 test files committed.
</output>
