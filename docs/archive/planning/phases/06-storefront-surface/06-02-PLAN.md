---
phase: 06-storefront-surface
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - db/manual-indexes.sql
autonomous: false
requirements:
  - IDN-03
  - IDN-05
  - IDN-06

must_haves:
  truths:
    - "Three new models (Conversation, SavedProduct, VisitorCustomerLink) exist as Prisma tables in Postgres"
    - "Two partial unique indexes exist on saved_products: (shop, visitorId, productId) WHERE customerId IS NULL and (shop, customerId, productId) WHERE customerId IS NOT NULL"
    - "Re-running `bun db:indexes` is idempotent"
    - "Prisma client regenerated under app/generated/prisma/ exposes prisma.conversation, prisma.savedProduct, prisma.visitorCustomerLink"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Conversation, SavedProduct, VisitorCustomerLink model declarations"
      contains: "model Conversation"
    - path: "db/manual-indexes.sql"
      provides: "Two partial unique indexes for saved_products"
      contains: "saved_products_anon_unique_idx"
    - path: "prisma/migrations/<new>"
      provides: "Base migration for the three new tables"
  key_links:
    - from: "prisma/schema.prisma Conversation"
      to: "db/manual-indexes.sql saved_products_anon_unique_idx"
      via: "ON CONFLICT clause in D-11 merge SQL + saved-products POST"
      pattern: "saved_products_(anon|customer)_unique_idx"
---

<objective>
[BLOCKING] Add three new Prisma models (Conversation, SavedProduct, VisitorCustomerLink), append two partial unique indexes to db/manual-indexes.sql, run the Prisma migration + manual indexes, and regenerate the Prisma client. After this plan ships, every downstream plan's `prisma.conversation.*` / `prisma.savedProduct.*` / `prisma.visitorCustomerLink.*` call typechecks.

Purpose: Storefront persistence layer per D-01/D-11/D-17/D-20. Without this plan applied, every Wave 2 plan fails at compile time.
Output: Updated schema, applied migration, applied indexes, regenerated client.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-storefront-surface/06-CONTEXT.md
@.planning/phases/06-storefront-surface/06-RESEARCH.md
@.planning/phases/06-storefront-surface/06-PATTERNS.md
@CLAUDE.md
@prisma/schema.prisma
@db/manual-indexes.sql
@scripts/apply-manual-indexes.ts
</context>

<interfaces>
<!-- Pattern from prisma/schema.prisma existing models — every model carries `shop String` + `@@index([shop])`. -->
<!-- Snake_case @@map table names. Prisma cannot model partial unique indexes — those go in db/manual-indexes.sql per D-20 and the existing pgvector/GIN pattern. -->

From prisma/schema.prisma (existing Product model, lines 16-50): every merchant-data model declares `shop String` near the top, `@@index([shop])` near the bottom, and `@@map("snake_case_plural")` for table naming.

From db/manual-indexes.sql (existing pattern, lines 43-57): `CREATE INDEX IF NOT EXISTS "name" ON table USING ... ;` — every CREATE uses IF NOT EXISTS for idempotency.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add three new Prisma models to schema.prisma</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    prisma/schema.prisma,
    .planning/phases/06-storefront-surface/06-CONTEXT.md,
    .planning/phases/06-storefront-surface/06-RESEARCH.md,
    .planning/phases/06-storefront-surface/06-PATTERNS.md
  </read_first>
  <action>
    Append three new models after the existing WebhookEvent model in `prisma/schema.prisma` (per IDN-03, IDN-05, IDN-06, D-09, D-11, D-17, D-18):

    Conversation model (per D-03/D-04/D-17/D-18 — JSONB messages blob, 60-char title, indexed for cursor pagination over lastMessageAt desc):
    - `id String @id @default(cuid())`
    - `shop String`
    - `visitorId String`
    - `customerId String?` (nullable; gets set by D-11 merge)
    - `title String @db.VarChar(60)` (D-18 truncation enforced at app layer — column constraint provides defense in depth)
    - `messages Json @default("[]")` (UIMessage[] verbatim per D-17)
    - `lastMessageAt DateTime @default(now())`
    - `createdAt DateTime @default(now())`
    - Indexes: `@@index([shop])`, `@@index([shop, visitorId, lastMessageAt(sort: Desc)])`, `@@index([shop, customerId, lastMessageAt(sort: Desc)])`, `@@index([lastMessageAt])` (retention sweep)
    - `@@map("conversations")`

    SavedProduct model (per IDN-05, D-20 — partial unique indexes NOT modeled here; they ship in db/manual-indexes.sql):
    - `id String @id @default(cuid())`
    - `shop String`
    - `visitorId String`
    - `customerId String?`
    - `productId String` (Shopify product GID or numeric ID as string — Pitfall 7 says treat as opaque string)
    - `savedAt DateTime @default(now())`
    - Indexes: `@@index([shop])`, `@@index([shop, visitorId])`, `@@index([shop, customerId])`
    - NO `@@unique` declarations — those are partial indexes in raw SQL (D-20). Add a code comment: `// Partial unique indexes live in db/manual-indexes.sql (D-20)`
    - `@@map("saved_products")`

    VisitorCustomerLink model (per D-09 — composite unique, models per-pair merge marker):
    - `shop String`
    - `visitorId String`
    - `customerId String`
    - `mergedAt DateTime @default(now())`
    - `@@id([shop, visitorId, customerId])` (composite primary key — no surrogate id needed; the triple IS the identity per D-09)
    - `@@index([shop, visitorId])`
    - `@@index([shop, customerId])`
    - `@@map("visitor_customer_links")`

    Do NOT modify any existing model. Do NOT change generator block or datasource block. Follow the exact field ordering and indent style observed in the Product model.
  </action>
  <verify>
    <automated>bunx prisma format && bunx prisma validate 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^model Conversation\|^model SavedProduct\|^model VisitorCustomerLink' prisma/schema.prisma` returns 3
    - `bunx prisma validate` exits 0
    - `grep "VarChar(60)" prisma/schema.prisma` returns 1 match (Conversation.title)
    - `grep "Json.*default" prisma/schema.prisma` matches Conversation.messages
    - SavedProduct model contains NO `@@unique` line (verify via grep on lines belonging to the SavedProduct block)
    - VisitorCustomerLink declares `@@id([shop, visitorId, customerId])` (composite PK)
  </acceptance_criteria>
  <done>
    Three new models added; `bunx prisma validate` passes; no edits to pre-existing models.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append partial unique indexes to db/manual-indexes.sql</name>
  <files>db/manual-indexes.sql</files>
  <read_first>db/manual-indexes.sql, .planning/phases/06-storefront-surface/06-CONTEXT.md</read_first>
  <action>
    Append a new section to `db/manual-indexes.sql` after the existing GIN index block. Use the SAME header-comment style as the existing sections (===== separator + numbered section heading). Add two partial unique indexes per D-20:

    Section 4 — "SavedProduct anon-only uniqueness (D-20)":
    - `CREATE UNIQUE INDEX IF NOT EXISTS "saved_products_anon_unique_idx" ON saved_products (shop, "visitorId", "productId") WHERE "customerId" IS NULL;`

    Section 5 — "SavedProduct customer-linked uniqueness (D-20)":
    - `CREATE UNIQUE INDEX IF NOT EXISTS "saved_products_customer_unique_idx" ON saved_products (shop, "customerId", "productId") WHERE "customerId" IS NOT NULL;`

    Both use `IF NOT EXISTS` (idempotency contract per file header). Index names are STABLE identifiers — downstream merge SQL (D-11 ON CONFLICT clause) and tests grep these names exactly. Document in the comment block: "These indexes back the ON CONFLICT clauses in `/api/proxy/saved-products` POST (D-20) and the visitor→customer merge transaction (D-11). Do NOT add equivalent `@@unique` declarations in Prisma — Prisma cannot model partial indexes."
  </action>
  <verify>
    <automated>grep -c "saved_products_anon_unique_idx\|saved_products_customer_unique_idx" db/manual-indexes.sql</automated>
  </verify>
  <acceptance_criteria>
    - `db/manual-indexes.sql` ends with two new `CREATE UNIQUE INDEX IF NOT EXISTS` statements
    - Both use `WHERE "customerId" IS NULL` / `WHERE "customerId" IS NOT NULL` (partial-index syntax)
    - File still starts with `CREATE EXTENSION IF NOT EXISTS vector` (existing top-of-file unchanged)
    - `grep -v '^--' db/manual-indexes.sql | grep -c "IF NOT EXISTS"` returns ≥ 5 (vector ext + HNSW + GIN + 2 new partials)
  </acceptance_criteria>
  <done>
    Two partial unique indexes appended; file remains idempotent; downstream merge ON CONFLICT can reference these index names.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Apply Prisma migration + manual indexes + regenerate client</name>
  <action>Run the three migration commands listed in <how-to-verify> in order, then run the sanity-check Prisma client invocation. If any step fails, follow the error remediation notes in <how-to-verify>. Do not proceed to the next plan until all 5 verification steps succeed.</action>
  <what-built>
    Tasks 1 and 2 added schema models (Conversation, SavedProduct, VisitorCustomerLink) and partial unique indexes to db/manual-indexes.sql. To make these visible to downstream Wave 2 plans, the executor MUST run the Prisma migration and the manual-indexes script.
  </what-built>
  <how-to-verify>
    Run these commands IN ORDER from the project root:

    1. `bunx prisma migrate dev --name phase-06-storefront-models`
       - This generates a new migration file under `prisma/migrations/` and applies it to the dev DB.
       - Expected output: "Your database is now in sync with your schema. ✓ Generated Prisma Client".

    2. `bun db:indexes`
       - Applies db/manual-indexes.sql to the database.
       - Expected output: "manual indexes applied".
       - Re-run a second time; should also print "manual indexes applied" (idempotent).

    3. `bunx prisma generate`
       - Regenerates the typed client under app/generated/prisma/.
       - This step is also run automatically by `prisma migrate dev`; running it again is safe.

    4. Sanity check from the project root:
       - `bunx tsx -e "import { prisma } from './lib/db/client'; (async () => { await prisma.conversation.count(); await prisma.savedProduct.count(); await prisma.visitorCustomerLink.count(); console.log('OK'); await prisma.\$disconnect(); })()"`
       - Should print "OK" with no errors. This proves the new tables exist AND the typed client knows about them.

    5. Inspect the new partial indexes via psql or Prisma's introspection:
       - `bun db:indexes` re-run still passes
       - Optional: run `psql $DIRECT_URL -c "\d+ saved_products"` and confirm two unique indexes are listed with the WHERE clauses.

    If any step fails:
    - Step 1 errors usually mean DATABASE_URL / DIRECT_URL is misconfigured — see CLAUDE.md Environment Variables.
    - Step 2 errors usually mean a typo in db/manual-indexes.sql — re-read Task 2's file.
    - Step 4 errors with "Property 'conversation' does not exist on type 'PrismaClient'" mean the regenerated client did not pick up the changes — re-run `bunx prisma generate`.
  </how-to-verify>
  <resume-signal>Type "applied" if all 5 verification steps succeeded. Describe the error otherwise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Postgres | Schema + index DDL is applied via direct connection (DIRECT_URL). Must run only by trusted operators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Migration SQL | mitigate | Migration is generated by Prisma from schema.prisma — no hand-rolled SQL (low tamper risk). Manual-indexes.sql changes reviewed in PR. |
| T-06-02 | Information Disclosure | Conversation.messages JSONB | mitigate | 180-day retention sweep (D-07) bounds exposure window. PII never logged. |
| T-06-03 | Spoofing | SavedProduct uniqueness | mitigate | Partial unique indexes prevent duplicate save rows after merge — the constraint protects data integrity, not authentication. |
</threat_model>

<verification>
- `bunx prisma validate` passes (Task 1)
- `bun db:indexes` is idempotent (Task 2)
- Manual checkpoint task confirms migration applied + client regenerated + new tables queryable (Task 3)
- After this plan ships, every downstream `prisma.conversation.*` call typechecks
</verification>

<success_criteria>
- `prisma/schema.prisma` declares three new models with shop-scoping pattern matching existing models
- `db/manual-indexes.sql` ends with two partial unique indexes
- `bunx prisma migrate dev --name phase-06-storefront-models` ran successfully (new migration file under `prisma/migrations/`)
- `bun db:indexes` ran successfully and is idempotent on re-run
- `bunx prisma generate` regenerated client under `app/generated/prisma/`
- A direct Prisma client call (`prisma.conversation.count()`) succeeds against the dev DB
</success_criteria>

<output>
Create `.planning/phases/06-storefront-surface/06-02-SUMMARY.md` recording the migration filename, line count of the SQL appended, and a paste of the Task 3 checkpoint output.
</output>
