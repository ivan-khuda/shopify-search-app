---
phase: 06-storefront-surface
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - shopify.app.toml
autonomous: true
requirements:
  - STR-03

must_haves:
  truths:
    - "shopify.app.toml contains [app_proxy] block with url, subpath, prefix"
    - "Storefront fetches to `/apps/smartdiscovery/*` proxy to `/api/proxy/*` on this app"
  artifacts:
    - path: "shopify.app.toml"
      provides: "[app_proxy] declaration"
      contains: "[app_proxy]"
  key_links:
    - from: "shopify.app.toml [app_proxy].url"
      to: "app/api/proxy/* route handlers"
      via: "Shopify's signed-proxy infrastructure"
      pattern: "url\\s*=\\s*\"https://.*/api/proxy\""
---

<objective>
Add the `[app_proxy]` block to `shopify.app.toml` (STR-03). This block tells Shopify to route storefront requests to `/apps/smartdiscovery/*` through to our app's `/api/proxy/*` route, signing each request with the HMAC that withAppProxyHmac will validate.

Purpose: Without this TOML block, Shopify never proxies any requests — `extensions/chat-drawer` would have no backend to talk to. Plan 14 will run `shopify app deploy` to push this TOML to Shopify.
Output: Updated shopify.app.toml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-storefront-surface/06-CONTEXT.md
@.planning/phases/06-storefront-surface/06-RESEARCH.md
@.planning/phases/06-storefront-surface/06-PATTERNS.md
@shopify.app.toml
@__tests__/shopify-toml.test.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add [app_proxy] block to shopify.app.toml</name>
  <files>shopify.app.toml</files>
  <read_first>shopify.app.toml, .planning/phases/06-storefront-surface/06-RESEARCH.md, .planning/phases/06-storefront-surface/06-PATTERNS.md</read_first>
  <action>
    Append an `[app_proxy]` block to `shopify.app.toml` after the existing `[auth]` block. Per RESEARCH §App Proxy specifics and STR-03:

    - `url` MUST equal `application_url + "/api/proxy"` — the current `application_url` value is `"https://4b71-2a02-8309-518b-6900-6d0c-28a0-cf31-8de2.ngrok-free.app"` (read from the current file). Use the same dynamic hostname; do NOT hardcode a different URL. Pattern: `url = "<application_url>/api/proxy"`.
    - `subpath = "smartdiscovery"` (D-01 in CONTEXT — locks the App Proxy subpath that loaders fetch from)
    - `prefix = "apps"` (Shopify's mandatory top-level prefix for App Proxies)

    Do NOT modify any pre-existing block. Do NOT alter `application_url`, `client_id`, `scopes`, `redirect_urls`, or anything outside the new `[app_proxy]` section. The block is exactly three keys, no comments inline. Place after `[auth]` block, separated by a single blank line — match the spacing of pre-existing blocks.
  </action>
  <verify>
    <automated>grep -A4 '\[app_proxy\]' shopify.app.toml | tee /tmp/app_proxy_block; grep -c '^subpath = "smartdiscovery"\|^prefix = "apps"' shopify.app.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^\[app_proxy\]' shopify.app.toml` returns 1
    - The `url` value ends with `/api/proxy` (no trailing slash)
    - `subpath = "smartdiscovery"` appears verbatim (D-01 contract)
    - `prefix = "apps"` appears verbatim
    - `bunx vitest run __tests__/shopify-toml.test.ts` now passes (or at least the regex assertions on `[app_proxy]` presence pass — full test may still RED on other unrelated blocks)
    - `application_url` value unchanged from prior state (diff inspection)
  </acceptance_criteria>
  <done>
    `[app_proxy]` block added with correct url/subpath/prefix; no other config touched.
  </done>
</task>

</tasks>

<verification>
- `grep '\[app_proxy\]' shopify.app.toml` returns one match
- Diff vs. prior commit shows only the three-line block addition + leading blank line
- Plan 14 will run `shopify app deploy` to push this TOML to Shopify; this plan does NOT deploy
</verification>

<success_criteria>
- shopify.app.toml contains a valid `[app_proxy]` block (STR-03)
- `bunx vitest run __tests__/shopify-toml.test.ts` regex assertion on `[app_proxy]` presence passes
- No other configuration values changed
</success_criteria>

<output>
Create `.planning/phases/06-storefront-surface/06-03-SUMMARY.md` with a diff snippet showing the added block.
</output>
