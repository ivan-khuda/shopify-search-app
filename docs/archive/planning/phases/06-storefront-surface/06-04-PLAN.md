---
phase: 06-storefront-surface
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/shopify/app-proxy-auth.ts
  - lib/rate-limit/memory.ts
autonomous: true
requirements:
  - STR-04
  - STR-08
  - IDN-02

must_haves:
  truths:
    - "Every `/api/proxy/*` route can wrap its handler with `withAppProxyHmac` and receive { shop, query, req } only after HMAC validation"
    - "`shop` is derived ONLY from the signed query — never from raw request URL or body (CR-01 mitigation)"
    - "`rateLimit(visitorId, 'chat'|'rest')` returns ok or { ok:false, retryAfterSeconds } per D-08"
  artifacts:
    - path: "lib/shopify/app-proxy-auth.ts"
      provides: "withAppProxyHmac + verifyAppProxyHmac + AppProxyAuthError"
      exports: ["withAppProxyHmac", "verifyAppProxyHmac", "AppProxyAuthError", "AppProxyAuthErrorCode"]
    - path: "lib/rate-limit/memory.ts"
      provides: "rateLimit + BUCKETS constant"
      exports: ["rateLimit", "BUCKETS"]
  key_links:
    - from: "withAppProxyHmac"
      to: "shopifyClient.utils.validateHmac"
      via: "{ signator: 'appProxy' } option"
      pattern: "validateHmac\\(.*signator.*appProxy"
---

<objective>
Build the two reusable utilities every storefront route depends on:
1. `withAppProxyHmac` — HOC wrapper that validates Shopify App Proxy HMAC and forwards { shop, query, req } to the handler. Mirrors `withShopifySession` from `lib/shopify/auth.ts`.
2. `rateLimit` — module-scope in-memory sliding-window limiter per D-08 (30/5min for chat bucket, 60/min for rest bucket).

Purpose: Every Wave 2 route plan depends on these two utilities. Without them, every `/api/proxy/*` handler would re-implement HMAC + rate limit logic and risk forgetting one (CR-01 redux).
Output: Two files in `lib/shopify/` and `lib/rate-limit/`, fully unit-tested by Plan 01's RED scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-storefront-surface/06-RESEARCH.md
@.planning/phases/06-storefront-surface/06-PATTERNS.md
@CLAUDE.md
@lib/shopify/auth.ts
@lib/shopify/client.ts
@lib/shopify/__tests__/app-proxy-auth.test.ts
@lib/shopify/__tests__/app-proxy-auth.fuzz.test.ts
@lib/rate-limit/__tests__/memory.test.ts
</context>

<interfaces>
<!-- Exact contract that downstream route plans rely on. -->

From lib/shopify/auth.ts (analog — copy pattern verbatim, swap auth mechanism):
```typescript
export class ShopifyAuthError extends Error {
  public readonly status: 401 = 401;
  constructor(public readonly code: ShopifyAuthErrorCode) { ... }
}
export async function verifyShopSessionToken(req: Request): Promise<{ shop: string; session: Session }>;
export function withShopifySession(
  handler: (ctx: { shop: string; session: Session; req: Request }) => Promise<Response>
): (req: Request) => Promise<Response>;
```

Phase 6 parallel contracts:
```typescript
// lib/shopify/app-proxy-auth.ts
export type AppProxyAuthErrorCode =
  | 'missing_signature'
  | 'invalid_signature'
  | 'missing_shop'
  | 'invalid_shop_domain';
export class AppProxyAuthError extends Error { ... }
export async function verifyAppProxyHmac(
  req: Request
): Promise<{ shop: string; query: Record<string, string> }>;
export function withAppProxyHmac(
  handler: (ctx: { shop: string; query: Record<string, string>; req: Request }) => Promise<Response>
): (req: Request) => Promise<Response>;

// lib/rate-limit/memory.ts
export const BUCKETS: Record<'chat' | 'rest', { limitPerWindow: number; windowMs: number; retryAfterSeconds: number }>;
export function rateLimit(
  visitorId: string,
  bucket: 'chat' | 'rest',
): { ok: true } | { ok: false; retryAfterSeconds: number };
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement lib/shopify/app-proxy-auth.ts (withAppProxyHmac wrapper)</name>
  <files>lib/shopify/app-proxy-auth.ts</files>
  <read_first>
    lib/shopify/auth.ts,
    lib/shopify/client.ts,
    lib/shopify/__tests__/app-proxy-auth.test.ts,
    lib/shopify/__tests__/app-proxy-auth.fuzz.test.ts,
    .planning/phases/06-storefront-surface/06-RESEARCH.md,
    .planning/phases/06-storefront-surface/06-PATTERNS.md
  </read_first>
  <behavior>
    - Missing `?signature=` → throws AppProxyAuthError('missing_signature') → 401 JSON
    - `shopifyClient.utils.validateHmac(query, { signator: 'appProxy' })` returns false → throws AppProxyAuthError('invalid_signature') → 401
    - `query.shop` missing after validation → 'missing_shop' → 401
    - `query.shop` not ending with `.myshopify.com` → 'invalid_shop_domain' → 401
    - Happy path: forwards { shop: query.shop, query, req } to inner handler
    - withAppProxyHmac catches AppProxyAuthError and returns `NextResponse.json({ error: code }, { status: 401 })`; non-AppProxyAuthError exceptions rethrow (caller's responsibility)
    - NO console.log of signature, shop, raw query, or error stack (CLAUDE.md hard constraint — no secrets in logs)
  </behavior>
  <action>
    Mirror `lib/shopify/auth.ts` (file paths, class structure, exports). Key differences:

    - Replace `ShopifyAuthError` / `ShopifyAuthErrorCode` with `AppProxyAuthError` / `AppProxyAuthErrorCode` union of `'missing_signature' | 'invalid_signature' | 'missing_shop' | 'invalid_shop_domain'`.
    - `verifyAppProxyHmac(req)` algorithm:
      1. `const url = new URL(req.url); const query: Record<string, string> = {};`
      2. `url.searchParams.forEach((v, k) => { query[k] = v; });`
      3. If `!query.signature` → throw `new AppProxyAuthError('missing_signature')`.
      4. `const isValid = await shopifyClient.utils.validateHmac(query, { signator: 'appProxy' });` — if false, throw `'invalid_signature'`.
      5. Read `const shop = query.shop;` — undefined → throw `'missing_shop'`.
      6. `if (!shop.endsWith('.myshopify.com'))` → throw `'invalid_shop_domain'`.
      7. Return `{ shop, query }`.
    - `withAppProxyHmac(handler)` HOC: copy the try/catch shape from `withShopifySession` (lines 70-84 of auth.ts) — catch `AppProxyAuthError`, return `NextResponse.json({ error: err.code }, { status: err.status })`. Rethrow everything else.
    - Type the handler signature exactly as: `(ctx: { shop: string; query: Record<string, string>; req: Request }) => Promise<Response>`. Downstream routes destructure `{ shop, query, req }`.
    - Imports: `import { NextResponse } from 'next/server';` and `import { shopifyClient } from '@/lib/shopify/client';` (mirror auth.ts).
    - DO NOT add a `session` field to the returned context — App Proxy routes never have an offline session loaded (that's admin's pattern). Storefront identity is `visitor_id` in the request body, not a session.
    - DO NOT log anything in the catch block. Add a top-of-file comment block matching `lib/shopify/auth.ts` style explaining what this wrapper does and what it intentionally does NOT do.
  </action>
  <verify>
    <automated>bunx vitest run lib/shopify/__tests__/app-proxy-auth.test.ts lib/shopify/__tests__/app-proxy-auth.fuzz.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File exports `withAppProxyHmac`, `verifyAppProxyHmac`, `AppProxyAuthError`, `AppProxyAuthErrorCode`
    - `grep -c "console\\.\\(log\\|warn\\|error\\|info\\|debug\\)" lib/shopify/app-proxy-auth.ts` returns 0 (no secret logging)
    - `grep "validateHmac.*signator.*appProxy" lib/shopify/app-proxy-auth.ts` returns 1 match
    - `grep "createHmac\\|crypto\\.createHmac" lib/shopify/app-proxy-auth.ts` returns 0 — must NOT hand-roll SHA-256 (Pitfall 1 of RESEARCH §Common Pitfalls)
    - `bunx vitest run lib/shopify/__tests__/app-proxy-auth.test.ts` is GREEN
    - `bunx vitest run lib/shopify/__tests__/app-proxy-auth.fuzz.test.ts` is GREEN
    - `shop` is read ONLY from the validated `query` object — never from `req.url.searchParams` or `req.json()` body — verifiable by inspection (only one `query.shop` reference in code; no `body.shop` reference)
  </acceptance_criteria>
  <done>
    File ships, both unit tests pass (RED → GREEN), no console.log, no hand-rolled HMAC.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement lib/rate-limit/memory.ts (sliding-window limiter)</name>
  <files>lib/rate-limit/memory.ts</files>
  <read_first>
    lib/rate-limit/__tests__/memory.test.ts,
    .planning/phases/06-storefront-surface/06-RESEARCH.md,
    .planning/phases/06-storefront-surface/06-PATTERNS.md
  </read_first>
  <behavior>
    - First 30 calls to `rateLimit(visitorId, 'chat')` within a 5-minute window all return `{ ok: true }`
    - 31st call within the window returns `{ ok: false, retryAfterSeconds: 60 }`
    - After 5-minute window expires, count resets (sliding — drop timestamps older than now - windowMs)
    - For 'rest' bucket: 60 calls / 60_000ms, retryAfterSeconds: 30
    - Different visitorIds maintain independent counters
    - Pruning happens lazily on each call (no background interval)
    - Map entries are bounded — even an adversary repeatedly hitting can only accumulate `limitPerWindow` timestamps per (visitor, bucket) key
  </behavior>
  <action>
    Implement the module per RESEARCH §Pattern 2:

    - Top-of-file JSDoc comment block documenting the in-memory limitation (Vercel cold starts reset the Map; cross-instance enforcement impossible without shared store; Phase 8 RequestCounter supersedes). Cite CONTEXT D-08 and RESEARCH Pitfall 9.
    - Export `const BUCKETS` as `Record<'chat' | 'rest', { limitPerWindow: number; windowMs: number; retryAfterSeconds: number }>`:
      - `chat: { limitPerWindow: 30, windowMs: 5 * 60_000, retryAfterSeconds: 60 }`
      - `rest: { limitPerWindow: 60, windowMs: 60_000, retryAfterSeconds: 30 }`
    - Module-scope `const hits = new Map<string, number[]>();` keyed by `${visitorId}:${bucket}`, value = ascending unix-ms timestamps.
    - Export `function rateLimit(visitorId: string, bucket: keyof typeof BUCKETS): { ok: true } | { ok: false; retryAfterSeconds: number }`:
      1. `const cfg = BUCKETS[bucket];`
      2. `const now = Date.now(); const key = visitorId + ':' + bucket;`
      3. Read current array (default `[]`); `const fresh = arr.filter(t => now - t < cfg.windowMs);` (lazy prune)
      4. If `fresh.length >= cfg.limitPerWindow` → `hits.set(key, fresh); return { ok: false, retryAfterSeconds: cfg.retryAfterSeconds };`
      5. Else push `now`, save, return `{ ok: true }`.
    - DO NOT add a background `setInterval` sweeper — D-08 explicitly accepts the lazy-prune-on-access model.
    - DO NOT export `hits` directly — it's an internal implementation detail.
    - Add an inline `/* @internal */` clear function for tests if Plan 01's RED tests need to reset state between cases — accept this small API surface only if tests require it (read the test first to decide).
  </action>
  <verify>
    <automated>bunx vitest run lib/rate-limit/__tests__/memory.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File exports `rateLimit` and `BUCKETS`
    - `BUCKETS.chat.limitPerWindow === 30 && BUCKETS.chat.windowMs === 300000`
    - `BUCKETS.rest.limitPerWindow === 60 && BUCKETS.rest.windowMs === 60000`
    - `grep "setInterval\\|setTimeout" lib/rate-limit/memory.ts` returns 0 (no background timers)
    - `bunx vitest run lib/rate-limit/__tests__/memory.test.ts` is GREEN
    - File header JSDoc explicitly cites D-08 and notes the per-instance limitation
  </acceptance_criteria>
  <done>
    Limiter ships, unit tests pass, no background timers, documented limitations.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Storefront browser → Shopify App Proxy → Next.js | Untrusted query params arrive signed; HMAC validation is the trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Spoofing | shop param | mitigate | `shop` derived ONLY from validated query inside withAppProxyHmac; raw `req.url.searchParams.get('shop')` never used in any route handler (CR-01 redux prevention) |
| T-06-05 | Tampering | HMAC validation | mitigate | Use `shopifyClient.utils.validateHmac(query, { signator: 'appProxy' })` exclusively — no hand-rolled SHA-256 (Pitfall 1) |
| T-06-06 | Information Disclosure | Logs | mitigate | Zero console.log statements in app-proxy-auth.ts and memory.ts; CLAUDE.md hard constraint enforced |
| T-06-07 | Denial of Service | Storefront chat abuse | mitigate (partial) | rateLimit at 30/5min per visitor, in-memory only; Phase 8 DB-backed RequestCounter is the cross-instance layer (D-08 forward compat) |
| T-06-08 | Repudiation | HMAC replay | accept | Phase 6 ships WITHOUT replay protection; Shopify's timestamp field provides forensics; documented limitation |
</threat_model>

<verification>
- Both files exist and export the documented interfaces
- All RED tests (Plan 01 Task 1) for app-proxy-auth and rate-limit now pass GREEN
- `grep` audits confirm no secret logging and no hand-rolled HMAC anywhere in these files
</verification>

<success_criteria>
- `lib/shopify/app-proxy-auth.ts` ships and unit tests GREEN
- `lib/rate-limit/memory.ts` ships and unit tests GREEN
- Both files have zero `console.log` calls
- Both files have zero `crypto.createHmac` calls (must use library)
- Downstream Wave 2 plans can `import { withAppProxyHmac } from '@/lib/shopify/app-proxy-auth'` and `import { rateLimit } from '@/lib/rate-limit/memory'`
</success_criteria>

<output>
Create `.planning/phases/06-storefront-surface/06-04-SUMMARY.md` with the exported symbol list and a test pass summary.
</output>
