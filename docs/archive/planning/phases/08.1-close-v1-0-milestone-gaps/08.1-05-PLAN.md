---
phase: 08.1-close-v1-0-milestone-gaps
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/shopify/server-resolve-shop.ts
  - app/(embedded)/chat/page.tsx
  - app/(embedded)/chat/__tests__/page.test.tsx
autonomous: true
requirements:
  - ADM-05
must_haves:
  truths:
    - "When app/(embedded)/chat/page.tsx is rendered with a valid Authorization: Bearer <session-token> request header, the shop is resolved from the decoded session-token (payload.dest hostname) — NOT from searchParams"
    - "When the Authorization header is missing or invalid, the page falls back to searchParams.shop (preserves current behavior — does not regress direct-navigation refresh)"
    - "The banner JSX is byte-precise unchanged: 'Preview mode — using your real catalog · Model: <displayName>' with em-dash U+2014 and middle-dot U+00B7"
    - "getActiveChatModel(shop) is called with the resolved shop and the banner displayName matches the model the chat /api request will actually use"
  artifacts:
    - path: "lib/shopify/server-resolve-shop.ts"
      provides: "Server Component helper that decodes session-token from request headers and returns the verified shop hostname (or null)"
      exports: ["resolveShopFromRequest"]
    - path: "app/(embedded)/chat/page.tsx"
      provides: "Server Component that prefers session-resolved shop over searchParams.shop"
      contains: "resolveShopFromRequest"
  key_links:
    - from: "app/(embedded)/chat/page.tsx ChatPage"
      to: "lib/shopify/server-resolve-shop.ts resolveShopFromRequest"
      via: "await resolveShopFromRequest()"
      pattern: "resolveShopFromRequest\\(\\)"
    - from: "resolveShopFromRequest helper"
      to: "next/headers"
      via: "await headers() to read Authorization"
      pattern: "await headers\\(\\)"
    - from: "resolveShopFromRequest helper"
      to: "shopifyClient.session.decodeSessionToken"
      via: "Bearer token decode"
      pattern: "decodeSessionToken"
---

<objective>
Close W-2 by resolving the `shop` in the `/chat` Server Component from the embedded session-token header server-side, so the banner `displayName` reflects the model the chat request will actually invoke (not the model for whatever `?shop=` happened to be in the URL).

Purpose: Per the audit, `app/(embedded)/chat/page.tsx:16-19` reads `shop` from `searchParams ?? ''`; in a cross-shop edge case (or a stale-link refresh where `?shop=` doesn't match the active session), the banner shows `DEFAULT_MODEL.displayName` while the streamed chat request uses the session-bound shop's actual selection. T-04-25 documents this as accepted; W-2 closes it explicitly. Per CONTEXT D-13/D-14 and RESEARCH Pattern 2.

Output: A new `lib/shopify/server-resolve-shop.ts` helper that uses Next.js 16's `await headers()` API to decode the Bearer session-token (reusing `shopifyClient.session.decodeSessionToken` from `lib/shopify/auth.ts`'s pattern). `app/(embedded)/chat/page.tsx` consumes it and falls back to `searchParams.shop` only when the header is absent (preserves direct-navigation behavior; existing Phase 4 byte-precision banner tests continue to pass).

Critical contract preservation per CONTEXT D-14: the banner JSX is byte-precise. Em-dash U+2014, middle-dot U+00B7, exact text `Preview mode — using your real catalog · Model: ` — only the shop-resolution mechanism changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/08.1-close-v1-0-milestone-gaps/08.1-CONTEXT.md
@.planning/phases/08.1-close-v1-0-milestone-gaps/08.1-RESEARCH.md
@app/(embedded)/chat/page.tsx
@lib/shopify/auth.ts
@lib/shopify/client.ts
@services/chat/getActiveChatModel.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/shopify/server-resolve-shop.ts helper</name>
  <files>lib/shopify/server-resolve-shop.ts</files>
  <behavior>
    - `resolveShopFromRequest()` returns `Promise<string | null>`.
    - Reads `Authorization` header via `await headers()` from `next/headers` (Next.js 16 async API per RESEARCH Pitfall 4).
    - If header missing or does not start with `Bearer `, returns `null`.
    - Slices off the `Bearer ` prefix; calls `shopifyClient.session.decodeSessionToken(token)` (reuses the existing primitive from `lib/shopify/auth.ts:35`).
    - On decode failure (throw), returns `null` (silent — the page falls back to searchParams).
    - On success, parses `payload.dest` as a URL and returns `new URL(payload.dest).hostname` ONLY if the hostname ends with `.myshopify.com`. Otherwise returns `null`.
    - Returns `null` if `payload.dest` is missing or not a valid URL (catch parse error).
  </behavior>
  <action>Create the new helper file. Recommended shape (per RESEARCH Pattern 2):

```ts
import { headers } from 'next/headers';
import { shopifyClient } from '@/lib/shopify/client';

/**
 * Server Component shop resolver — decodes the embedded session-token from the
 * incoming request's Authorization header and returns the verified shop hostname.
 *
 * Returns null when the header is absent or the token fails to decode/validate,
 * letting callers fall back to searchParams.shop (preserves direct-navigation refresh).
 *
 * Mirrors the decode portion of `lib/shopify/auth.ts:verifyShopSessionToken` but
 * sources the header via Next.js 16's `await headers()` (Server Component idiom,
 * not the route-handler `Request` object).
 */
export async function resolveShopFromRequest(): Promise<string | null> {
  const h = await headers();
  const authHeader = h.get('authorization');
  if (!authHeader || !authHeader.startsWith('Bearer ')) return null;

  const token = authHeader.slice('Bearer '.length);
  let payload: { dest?: string };
  try {
    payload = await shopifyClient.session.decodeSessionToken(token);
  } catch {
    return null;
  }

  if (!payload.dest) return null;
  let shop: string;
  try {
    shop = new URL(payload.dest).hostname;
  } catch {
    return null;
  }
  return shop.endsWith('.myshopify.com') ? shop : null;
}
```

Per CONTEXT D-13 + Claude's Discretion: helper lives in `lib/shopify/` (NOT under `app/(embedded)/_lib/`) — co-located with `auth.ts` so the import graph cleanly groups all Shopify session-token decode primitives in one folder. Sub-paths under `app/(embedded)/_lib/` are an alternative; this plan goes with the lib/shopify path for consistency with the existing auth helper.

Do NOT export any additional functions in this file. No `console.*` (CLAUDE.md). No additional Shopify SDK imports beyond `shopifyClient`.</action>
  <verify>
    <automated>test -f lib/shopify/server-resolve-shop.ts && grep -q "export async function resolveShopFromRequest" lib/shopify/server-resolve-shop.ts && grep -q "await headers()" lib/shopify/server-resolve-shop.ts && bunx tsc --noEmit</automated>
  </verify>
  <done>Helper file exists, exports `resolveShopFromRequest`, uses `await headers()`, decodes via `shopifyClient.session.decodeSessionToken`, validates `.myshopify.com` suffix, returns null on every failure path; type check passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire app/(embedded)/chat/page.tsx to prefer session-resolved shop (RED-then-GREEN extension of Phase 4 test)</name>
  <files>app/(embedded)/chat/page.tsx, app/(embedded)/chat/__tests__/page.test.tsx</files>
  <behavior>
    - Locate the existing Phase 4 byte-precision banner test for `/chat/page.tsx` — RESEARCH lists it at `app/(embedded)/chat/__tests__/page.test.tsx`. If the actual filename differs (could be `page.test.tsx`, `chat-page.test.tsx`, or in a `__tests__/` subdirectory), use the one that matches the Phase 4 SUMMARY.md / VERIFICATION.md citation; if no Phase 4 test exists, create the appropriate file and explain why in the SUMMARY.
    - New test (added to the existing file): `it('resolves shop from Authorization Bearer session-token, ignoring searchParams.shop when both differ', ...)`. Mock `next/headers`'s `headers()` to return an object whose `.get('authorization')` returns a Bearer header. Mock `shopifyClient.session.decodeSessionToken` to return `{ dest: 'https://session-shop.myshopify.com/admin' }`. Mock `getActiveChatModel` so the call records which `shop` it received. Render `<ChatPage searchParams={Promise.resolve({ shop: 'query-shop.myshopify.com' })} />`. Assert `getActiveChatModel` was called with `'session-shop.myshopify.com'` (the session-resolved shop) — NOT `'query-shop.myshopify.com'`.
    - Second new test: `it('falls back to searchParams.shop when no Authorization header is present', ...)`. Mock `headers()` to return `{ get: () => null }`. Mock `getActiveChatModel` to record its arg. Render with `searchParams = { shop: 'fallback-shop.myshopify.com' }`. Assert it was called with `'fallback-shop.myshopify.com'`.
    - Third new test: `it('falls back to searchParams.shop when session-token decode throws', ...)`. Mock `headers()` to return a Bearer header; mock `decodeSessionToken` to throw. Assert fallback to `searchParams.shop`.
    - Existing Phase 4 banner byte-precision tests stay green (the banner JSX is unchanged).
  </behavior>
  <action>RED — write the three new tests first. Use the existing Phase 4 test file as a structural template (or create one if it doesn't exist; check via `ls app/(embedded)/chat/__tests__/` first). Mock setup: `vi.mock('next/headers', () => ({ headers: vi.fn() }))` and `vi.mock('@/lib/shopify/client', () => ({ shopifyClient: { session: { decodeSessionToken: vi.fn() } } }))` and `vi.mock('@/services/chat/getActiveChatModel', () => ({ getActiveChatModel: vi.fn() }))`. Per-test `(headers as Mock).mockResolvedValue(...)` overrides.

Then GREEN — edit `app/(embedded)/chat/page.tsx`:
1. Add import: `import { resolveShopFromRequest } from '@/lib/shopify/server-resolve-shop';`
2. Inside `ChatPage`, after `const { shop: shopFromQuery } = await searchParams;`, add `const shopFromSession = await resolveShopFromRequest();`.
3. Compute `const shop = shopFromSession ?? shopFromQuery ?? '';`.
4. Pass `shop` to `getActiveChatModel(shop)` and to `<ChatShell shop={shop} />`.
5. Banner JSX (the `<div role="status">` and its children) is BYTE-PRECISE unchanged — em-dash `—` (U+2014), middle-dot `·` (U+00B7), exact text "Preview mode — using your real catalog · Model: ". Do not edit this block beyond what's necessary.
6. The existing `bannerAriaLabel` template stays unchanged — it interpolates `displayName` which still comes from `model.displayName`.

After landing: all three new tests + existing byte-precision tests pass. `bunx tsc --noEmit` passes.</action>
  <verify>
    <automated>bunx vitest run app/\(embedded\)/chat/__tests__/ && bunx tsc --noEmit</automated>
  </verify>
  <done>ChatPage prefers session-resolved shop over searchParams; banner byte-shape is unchanged; new tests pin the contract; existing Phase 4 banner tests still green.</done>
</task>

<task type="auto">
  <name>Task 3: Full lint + adjacent suite sweep</name>
  <files></files>
  <action>1. `bun lint` — no new warnings.
2. `bunx vitest run app/(embedded)/chat/ services/chat/` — ensure the Phase 4 + Phase 7 model-resolver tests stay green (getActiveChatModel + DEFAULT_MODEL).
3. `bunx tsc --noEmit` — full project type check.
4. `grep -n "resolveShopFromRequest" app/(embedded)/chat/page.tsx` MUST return ≥1 (wiring verification).
5. `grep -n "searchParams\.shop\|shopFromQuery" app/(embedded)/chat/page.tsx` MUST still appear (fallback path retained).</action>
  <verify>
    <automated>bun lint && bunx vitest run app/\(embedded\)/chat/ services/chat/ && bunx tsc --noEmit && grep -q "resolveShopFromRequest" app/\(embedded\)/chat/page.tsx</automated>
  </verify>
  <done>Full lint + test sweep green; both the session-resolve wiring and the searchParams fallback are present in the source file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| embedded admin browser → `/chat` Server Component → session-token decode | The `Authorization: Bearer <session-token>` header is provided by App Bridge; the Server Component verifies its signature via `shopifyClient.session.decodeSessionToken` before trusting the embedded shop identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1.05-01 | Spoofing | resolveShopFromRequest | mitigate | Reuse `shopifyClient.session.decodeSessionToken` which verifies the JWS signature against `SHOPIFY_API_SECRET`. Never hand-roll JWT validation. On any decode failure, return null and fall back to searchParams (does not elevate trust — the banner displayName is informational; the actual chat request enforces its own auth at `/api/chat`). |
| T-08.1.05-02 | Information Disclosure | banner displays model.displayName | accept | Model displayName comes from a per-shop `getActiveChatModel(shop)` call; the resolved shop is the verified session shop. Worst-case (decode fails, fallback): the displayed model is for the searchParams shop, which is identical to today's behavior — no regression. |
| T-08.1.05-03 | Tampering | searchParams.shop fallback | accept | searchParams.shop has always been the data source on this page; falling back to it preserves current behavior. Mitigation: a stale-link refresh where ?shop= disagrees with the session token is the cross-shop edge case W-2 explicitly addresses — the new session-first resolution fixes it. |
| T-08.1.05-04 | Replay | session-token reuse | accept | Session-token expiry + JWS signature verification + offline-session presence-check (handled at `/api/chat` route, not this Server Component) form the existing Phase 1 FND-05 control. No new replay surface in this plan. |
</threat_model>

<verification>
- `bunx vitest run app/(embedded)/chat/` passes including existing byte-precision tests + 3 new tests.
- Banner text byte-equivalent: em-dash U+2014 + middle-dot U+00B7 + exact strings unchanged.
- `lib/shopify/server-resolve-shop.ts` is a standalone helper with one named export.
- The page falls back to searchParams.shop when the header is absent or invalid.
</verification>

<success_criteria>
- W-2 closed: ADM-05 status flips from `partial` to `satisfied`.
- Banner contract preserved (D-14).
- Existing direct-navigation refresh continues to work (searchParams fallback).
- No new external dependencies; no `console.*` added.
</success_criteria>

<output>
Create `.planning/phases/08.1-close-v1-0-milestone-gaps/08.1-05-SUMMARY.md` with:
- New file path `lib/shopify/server-resolve-shop.ts`.
- Decision recorded: helper placed under `lib/shopify/` (not `app/(embedded)/_lib/`) for symmetry with `auth.ts`.
- Final test counts (existing Phase 4 byte-precision + 3 new W-2 tests).
- Banner-byte-precision invariant confirmation (em-dash U+2014, middle-dot U+00B7).
</output>
