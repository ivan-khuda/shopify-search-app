---
phase: 08.1-close-v1-0-milestone-gaps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/chat-drawer/assets/loader.js
  - lib/chat-ui/adapters/storefront.ts
  - lib/chat-ui/__tests__/storefront-adapter.test.ts
autonomous: true
requirements:
  - STR-01
  - STR-08
  - EMB-07
must_haves:
  truths:
    - "extensions/chat-drawer/assets/loader.js fetches the bundle manifest at the App Proxy path /apps/smartdiscovery/_meta/bundle-url"
    - "StorefrontAdapter.endpoint is /apps/smartdiscovery/chat (the App Proxy path) — never /api/proxy/chat"
    - "storefront-adapter.test.ts asserts the corrected App Proxy endpoint in both assertion sites (the first describe block AND the IDN-02 describe block)"
    - "The misleading STR-08 comment that claimed endpoint=/api/proxy/chat was 'compliance' is replaced with an explanation of why /apps/smartdiscovery/chat is correct"
  artifacts:
    - path: "extensions/chat-drawer/assets/loader.js"
      provides: "Storefront loader that fetches the bundle manifest via App Proxy"
      contains: "/apps/smartdiscovery/_meta/bundle-url"
    - path: "lib/chat-ui/adapters/storefront.ts"
      provides: "StorefrontAdapter with App Proxy chat endpoint"
      contains: "/apps/smartdiscovery/chat"
    - path: "lib/chat-ui/__tests__/storefront-adapter.test.ts"
      provides: "Updated test pin asserting the App Proxy endpoint"
      contains: "/apps/smartdiscovery/chat"
  key_links:
    - from: "loader.js"
      to: "app/api/proxy/_meta/bundle-url/route.ts"
      via: "App Proxy URL /apps/smartdiscovery/_meta/bundle-url"
      pattern: "fetch\\('/apps/smartdiscovery/_meta/bundle-url'"
    - from: "StorefrontAdapter"
      to: "app/api/proxy/chat/route.ts"
      via: "App Proxy URL /apps/smartdiscovery/chat"
      pattern: "endpoint = '/apps/smartdiscovery/chat'"
---

<objective>
Close B-2, B-4, and W-3 — three coupled fixes on the storefront → backend bundle/chat pathway — in a single atomic plan so the storefront chat actually reaches the app backend through the Shopify App Proxy.

Purpose: Without this plan, the storefront FAB click never loads the React bundle (loader fetches a path the backend doesn't serve), and even if it did, the StorefrontAdapter would hit a same-origin path on the merchant's storefront instead of traversing the App Proxy. The test pin (W-3) ALSO actively reinforces the bug by asserting the wrong endpoint as "STR-08 compliance" in two separate `describe` blocks plus a misleading comment block — fixing the adapter without fixing the test would just turn red on commit. All three live on the same surface and must land together. Per CONTEXT D-05/D-06/D-07.

Output: Three files modified in a single coherent commit; existing test suite stays green; orphan routes `/apps/smartdiscovery/_meta/bundle-url` and `/apps/smartdiscovery/chat` become reachable end-to-end at the source-code level (live storefront smoke deferred to post-deploy operator step).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/08.1-close-v1-0-milestone-gaps/08.1-CONTEXT.md
@.planning/phases/08.1-close-v1-0-milestone-gaps/08.1-RESEARCH.md
@extensions/chat-drawer/assets/loader.js
@lib/chat-ui/adapters/storefront.ts
@lib/chat-ui/__tests__/storefront-adapter.test.ts
@shopify.app.toml
@app/api/proxy/_meta/bundle-url/route.ts
@app/api/proxy/chat/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the W-3 test pin in lib/chat-ui/__tests__/storefront-adapter.test.ts (RED-then-GREEN)</name>
  <files>lib/chat-ui/__tests__/storefront-adapter.test.ts</files>
  <behavior>
    - Test 1 (first describe, lines 16-19): `it('endpoint is /apps/smartdiscovery/chat (App Proxy path)', ...)` — `expect(adapter.endpoint).toBe('/apps/smartdiscovery/chat')`. Old assertion `'/api/proxy/chat'` MUST be gone.
    - Test 2 (IDN-02 describe, lines 108-119): the test currently named `'STR-08: endpoint remains /api/proxy/chat regardless of customer presence'` MUST be renamed to `'STR-08: endpoint is the App Proxy path regardless of customer presence'` and the assertion `expect(adapter.endpoint).toBe('/apps/smartdiscovery/chat')` MUST replace the old `'/api/proxy/chat'` assertion. The existing `expect(adapter.endpoint).not.toMatch(/^https?:\/\//)` line stays — same-origin invariant unchanged.
    - Comment block lines 51-55: the line `STR-08: endpoint stays exactly '/api/proxy/chat' — no cross-origin URL.` MUST be replaced with an explanation that App Proxy semantics require `/apps/smartdiscovery/chat`. Recommended replacement (per RESEARCH §"Code Examples" Example 2):
      ```
      // STR-08: endpoint is an App Proxy path '/apps/smartdiscovery/chat' so the
      // request stays same-origin from the storefront and Shopify HMAC-signs it
      // before forwarding to the app backend. IDN-02: when window.Shopify.customer.id
      // is present, getRequestBody() includes customer_id as a STRING (BigInt
      // precision preserved — Pitfall 7 from RESEARCH).
      ```
    - All other tests in the file stay byte-equivalent (visitor_id generation, customer_id BigInt-precision string, SSR guard, etc.).
  </behavior>
  <action>This is the RED phase — edit the test file FIRST so the test suite turns red against the current StorefrontAdapter source (`endpoint = '/api/proxy/chat'`). Two assertion sites + one comment block. Use the Edit tool with three separate replacements: (1) line 16-18 `it(...)` block — change the test description and the `toBe` argument; (2) lines 51-55 — replace the comment block; (3) line 108-117 `it(...)` block — change the test description string and the first `toBe` argument. Do NOT change the second `expect(...).not.toMatch(/^https?:\/\//)` invariant or the visitor_id / customer_id assertions. After the edit, `bunx vitest run lib/chat-ui/__tests__/storefront-adapter.test.ts` MUST fail with two assertion mismatches (proving RED). Commit as a temporary `test(08.1-02): pin storefront-adapter endpoint to App Proxy path` — this commit will turn green in Task 2.</action>
  <verify>
    <automated>grep -c "/apps/smartdiscovery/chat" lib/chat-ui/__tests__/storefront-adapter.test.ts | grep -qv "^0$" && grep -v '^[[:space:]]*//' lib/chat-ui/__tests__/storefront-adapter.test.ts | grep -v "not.toMatch" | grep -c "/api/proxy/chat" | grep -q "^0$"</automated>
  </verify>
  <done>Test file references `/apps/smartdiscovery/chat` in both assertion blocks; no executable references to `/api/proxy/chat` remain (commented-out lines or regex literals don't count); test suite is RED against the current adapter source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip the StorefrontAdapter endpoint to the App Proxy path (B-4 / GREEN)</name>
  <files>lib/chat-ui/adapters/storefront.ts</files>
  <behavior>
    - `new StorefrontAdapter().endpoint === '/apps/smartdiscovery/chat'`.
    - All other StorefrontAdapter behavior unchanged: getAuthHeaders returns {}, getRequestBody preserves visitor_id minting + persistence + customer_id BigInt-precision injection.
  </behavior>
  <action>Single-line change: at `lib/chat-ui/adapters/storefront.ts:6`, replace `readonly endpoint = '/api/proxy/chat';` with `readonly endpoint = '/apps/smartdiscovery/chat';`. Do NOT change the class name, the implements clause, the storage key, or any method body — those contracts are unchanged and are exercised by the rest of the test file. Per CONTEXT D-06: the path mirrors the DB-backed stores' patterns (`/apps/smartdiscovery/conversations`, `/apps/smartdiscovery/saved-products`); see shopify.app.toml `[app_proxy]` block confirming `prefix=apps, subpath=smartdiscovery, url=.../api/proxy`. After the edit, `bunx vitest run lib/chat-ui/__tests__/storefront-adapter.test.ts` MUST pass (GREEN).</action>
  <verify>
    <automated>bunx vitest run lib/chat-ui/__tests__/storefront-adapter.test.ts</automated>
  </verify>
  <done>StorefrontAdapter.endpoint is `/apps/smartdiscovery/chat`; the full storefront-adapter test suite passes (8 tests across two describe blocks).</done>
</task>

<task type="auto">
  <name>Task 3: Fix the loader.js bundle-manifest URL (B-2)</name>
  <files>extensions/chat-drawer/assets/loader.js</files>
  <action>At `extensions/chat-drawer/assets/loader.js:45`, replace `fetch('/apps/smartdiscovery/storefront-manifest.json', { method: 'GET', cache: 'no-store' })` with `fetch('/apps/smartdiscovery/_meta/bundle-url', { method: 'GET', cache: 'no-store' })`. Per CONTEXT D-05: this aligns the loader with the backend route at `app/api/proxy/_meta/bundle-url/route.ts` (Shopify App Proxy `/apps/smartdiscovery/_meta/bundle-url` → `/api/proxy/_meta/bundle-url`). The surrounding `.then(r => r.json()).then(m => import(m.bundle))` chain stays unchanged because the backend route returns the same `{ bundle: '<CDN URL>' }` shape the loader already destructures. The Liquid block comment elsewhere in the extension already documents this URL (per audit) so this change brings code into alignment with docs.

Do NOT touch any other line in loader.js (FAB paint logic, `Shopify.designMode` check, the customer-id dataset read, the mount call). Single string substitution; no behavior change beyond the URL.</action>
  <verify>
    <automated>grep -q "fetch('/apps/smartdiscovery/_meta/bundle-url'" extensions/chat-drawer/assets/loader.js && ! grep -q "storefront-manifest.json" extensions/chat-drawer/assets/loader.js</automated>
  </verify>
  <done>loader.js fetches `/apps/smartdiscovery/_meta/bundle-url`; the dead `storefront-manifest.json` URL is gone.</done>
</task>

<task type="auto">
  <name>Task 4: Full storefront-adapter + extension-loader test sweep + lint + tsc</name>
  <files></files>
  <action>Run the broader sanity checks to confirm none of the three coupled edits introduced regressions:
1. `bunx vitest run lib/chat-ui/__tests__/storefront-adapter.test.ts` — must pass (verifies Task 1 + Task 2).
2. `bunx vitest run extensions/chat-drawer/__tests__/` if loader-related tests exist; otherwise skip.
3. `bunx vitest run lib/chat-ui/__tests__/` — full chat-ui test suite must remain green (orphan stores, embedded adapter, components — none of these should be affected by this plan).
4. `bunx tsc --noEmit` — type check the whole project.
5. `bun lint` — no new warnings.

If any step fails: stop, diagnose, and report. Do not attempt to fix unrelated failures in this plan — surface them to the operator.</action>
  <verify>
    <automated>bunx vitest run lib/chat-ui/__tests__/ && bunx tsc --noEmit && bun lint</automated>
  </verify>
  <done>All checks pass; B-2, B-4, W-3 are closed at source-code level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storefront origin (`*.myshopify.com`) → Shopify App Proxy → app backend | Storefront fetches are signed and forwarded by Shopify; switching from same-origin-to-storefront to App Proxy path moves the boundary correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1.02-01 | Information Disclosure | StorefrontAdapter endpoint | mitigate | Endpoint flip from `/api/proxy/chat` (same-origin to storefront = wrong host) to `/apps/smartdiscovery/chat` (Shopify-signed proxy path) keeps all backend calls behind App Proxy HMAC verification (STR-04). |
| T-08.1.02-02 | Tampering | bundle-manifest fetch in loader.js | accept | The bundle-url endpoint returns an absolute CDN URL of the React bundle; Shopify App Proxy HMAC-verifies the request, and the loader subsequently does `import(m.bundle)` which the browser fetches with its own integrity checks. No new attack surface; this is the existing Phase 6 design — the loader was just calling the wrong URL. |
| T-08.1.02-03 | Tampering | misleading STR-08 test pin | mitigate (governance) | The misleading comment encoded the bug as a "contract" — fixing both the assertion and the explanatory comment prevents future regressions where someone "restores STR-08 compliance" by re-introducing the bug. |
</threat_model>

<verification>
- `bunx vitest run lib/chat-ui/__tests__/storefront-adapter.test.ts` passes (8 assertions across two describe blocks).
- Full `lib/chat-ui` vitest suite passes.
- `grep -rn "/api/proxy/chat" lib/chat-ui extensions/chat-drawer` returns no matches outside route handlers (the backend route file `app/api/proxy/chat/route.ts` is NOT in scope for this plan).
- `grep -rn "storefront-manifest.json" extensions/` returns no matches.
- `bun lint` passes; `bunx tsc --noEmit` passes.
</verification>

<success_criteria>
- B-2 closed: loader.js fetches the correct App Proxy manifest URL.
- B-4 closed: StorefrontAdapter.endpoint is the App Proxy path.
- W-3 closed: both test assertion sites + the misleading comment block reflect the corrected contract.
- STR-01, STR-08, EMB-07 source-code reachability restored (live storefront smoke deferred to post-deploy operator step per RESEARCH §"Runtime State Inventory").
</success_criteria>

<output>
Create `.planning/phases/08.1-close-v1-0-milestone-gaps/08.1-02-SUMMARY.md` documenting the three file edits, the vitest counts before/after, and a note that `app/api/proxy/_meta/bundle-url/route.ts` + `app/api/proxy/chat/route.ts` are now reachable end-to-end at the source-code level.
</output>
