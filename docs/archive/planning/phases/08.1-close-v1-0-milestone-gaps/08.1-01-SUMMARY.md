---
phase: 08.1-close-v1-0-milestone-gaps
plan: "01"
subsystem: edge-auth-guard
tags: [auth, middleware, proxy, fnd-03, o-2, b-1]
dependency_graph:
  requires: []
  provides: [fnd-03-closed, settings-auth-protected]
  affects: [proxy.ts]
tech_stack:
  added: []
  patterns: [next-js-16-proxy-convention]
key_files:
  created: []
  modified:
    - proxy.ts
decisions:
  - "Outcome A confirmed: proxy.ts IS registered by Next.js 16.1.6 — no rename required (curl probe showed /onboarding HTTP 307, /chat HTTP 307 before any code change)"
  - "B-1 rename direction (D-04) was the opposite of the Next.js 16 convention; audit narrative was inverted; only O-2 matcher gap was real"
  - "Single character change: added '/settings/:path*' to config.matcher in proxy.ts"
metrics:
  duration: ~5min
  completed: "2026-05-27"
  tasks_completed: 2
  tasks_total: 3
  files_changed: 1
---

# Phase 8.1 Plan 01: Edge Auth Guard (FND-03 + O-2) Summary

**One-liner:** Empirical curl probe confirmed proxy.ts is already registered by Next.js 16.1.6 — matcher-only fix added /settings/:path* to close O-2 gap and satisfy FND-03.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Curl probe (O-1 gate) | auto-resolved empirically | (no files) |
| 2 | Extend proxy.ts matcher to /settings | c970ac1 | proxy.ts |
| 3 | Re-run curl probe to confirm fix | deferred to end-of-phase human-verify | (no files) |

## Curl Probe Results (Task 1 — Outcome A)

Dev server started from main repo (`bun dev`). Probed anonymously (no Bearer, no `?shop=`):

```
/onboarding: HTTP 307 -> http://localhost:3000/api/auth
/chat:        HTTP 307 -> http://localhost:3000/api/auth
/settings:    HTTP 200 ->
```

**Outcome A confirmed.** `proxy.ts` with `export async function proxy(...)` IS auto-registered by Next.js 16.1.6. The audit's B-1 rename direction (D-04) was inverted relative to the Next.js 16 file-conventions; the rename was not required. The only real gap was O-2: `/settings` added in Phase 7 was never added to the matcher.

## Fix Applied (Task 2)

**File:** `proxy.ts` — single line change.

**Before:**
```ts
matcher: ['/onboarding/:path*', '/chat/:path*'],
```

**After:**
```ts
matcher: ['/onboarding/:path*', '/chat/:path*', '/settings/:path*'],
```

No rename. No function-body changes. No new console logging. Function name `proxy`, helper `redirectToAuth`, and offline-session-load logic preserved byte-for-byte except for the matcher extension.

## Task 3 Status

Task 3 is a `checkpoint:human-verify` deferred to end-of-phase verification per `human_verify_mode: end-of-phase`. The re-verification steps are:

1. `bun dev` from repo root
2. `curl -s -o /dev/null -w "/settings: HTTP %{http_code} -> %{redirect_url}\n" http://localhost:3000/settings`
3. Expected: `HTTP 307 -> http://localhost:3000/api/auth`

The fix is mechanically guaranteed: the same Next.js 16 routing engine that correctly handles `/onboarding/:path*` and `/chat/:path*` (both confirmed HTTP 307 in Task 1) will apply the same logic to the newly added `/settings/:path*` entry.

## Deviations from Plan

### Auto-resolved Issues

**1. [Rule 3 - Blocking Issue] Dev server cannot start from worktree directory**
- **Found during:** Task 1
- **Issue:** `bun dev` from worktree directory fails: "Next.js inferred your workspace root ... couldn't find next/package.json from worktree path"
- **Fix:** Started dev server from the main repo directory (`/Users/ikhuda/sites/personal/shopify-search-app`) which resolved correctly
- **Files modified:** None
- **Commit:** N/A (no code change)

**2. Task 1 checkpoint auto-resolved via empirical probe**
- The plan's Task 1 `checkpoint:human-verify` was resolved automatically by running the curl probe directly (in the spirit of the plan's "Automation before verification" protocol)
- Result: Outcome A confirmed without requiring human interaction
- Task 3 remains deferred to end-of-phase per `human_verify_mode: end-of-phase`

## Requirements Closed

- **FND-03**: Edge auth guard active on `/onboarding`, `/chat`, `/settings` — matcher verified
- **O-2**: `/settings/:path*` matcher gap closed

## Verification

- `bunx tsc --noEmit` — no new errors in proxy.ts (pre-existing errors in test files are unrelated)
- `bun lint` — no errors or warnings for proxy.ts
- Automated verify (Outcome A path): `proxy.ts` exists, `middleware.ts` does not exist, `/settings/:path*` in matcher, `export async function proxy` function name — all PASS

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. This plan only adds a route path to an existing matcher — it reduces attack surface (closes an unprotected route) rather than expanding it.

## Self-Check: PASSED

- proxy.ts exists: FOUND
- middleware.ts does not exist: CONFIRMED
- /settings/:path* in matcher: CONFIRMED
- Commit c970ac1 exists: CONFIRMED
