---
phase: 02-sync-pipeline
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - inngest/functions/__tests__/sync-products.test.ts
  - app/api/shopify/sync/status/__tests__/route.test.ts
  - app/api/shopify/webhook/__tests__/route.test.ts
  - services/shopify/__tests__/ShopifyProductService.test.ts
  - app/api/shopify/sync/__tests__/route.test.ts
  - app/(embedded)/__tests__/onboarding.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Inngest 4.4.0 and @inngest/test 1.0.0 are installed and lockfile reflects them"
    - "Four new RED test files exist with failing or skipped tests describing the not-yet-built behavior"
    - "Existing route + onboarding test files have been extended with idempotency, syncRunId, polling, and completion cases"
    - "All RED tests fail with a clear, expected reason (missing module / undefined export) — none accidentally pass"
  artifacts:
    - path: "inngest/functions/__tests__/sync-products.test.ts"
      provides: "RED stubs for SYN-03, SYN-06 (batch error policy + cursor persist)"
    - path: "app/api/shopify/sync/status/__tests__/route.test.ts"
      provides: "RED stubs for SYN-07 (shop-scoped 200 + cross-shop 403 + missing-syncRunId 400 + not-found 404)"
    - path: "app/api/shopify/webhook/__tests__/route.test.ts"
      provides: "RED stubs for SYN-10, SYN-11 (HMAC accept/reject, dedup, stale-updatedAt skip)"
    - path: "services/shopify/__tests__/ShopifyProductService.test.ts"
      provides: "RED stubs for SYN-01, SYN-02 (GraphQL paginate, map to ProductUpsertInput including updatedAtShopify)"
    - path: "app/api/shopify/sync/__tests__/route.test.ts"
      provides: "Extended cases: idempotency dedup + syncRunId returned"
    - path: "app/(embedded)/__tests__/onboarding.test.tsx"
      provides: "Extended cases: polling, progress bar, completion banner"
  key_links:
    - from: "inngest/functions/__tests__/sync-products.test.ts"
      to: "@inngest/test (InngestTestEngine)"
      via: "package import after `bun add -d @inngest/test`"
      pattern: "from '@inngest/test'"
    - from: "app/api/shopify/sync/__tests__/route.test.ts"
      to: "app/api/shopify/sync/route.ts (pending Plan 07 rewrite)"
      via: "import { POST } from '../route'"
      pattern: "syncRunId.*toBeDefined"
---

<objective>
Wave 0 RED test scaffolds — install `inngest` + `@inngest/test`, then create four new test files and extend two existing test files with the not-yet-implemented behaviors for Phase 2.

Purpose: Lock the executable contracts BEFORE any production code is written so every subsequent plan (02–10) has a precise definition-of-done.
Output: One commit installing dependencies + six test files (4 new + 2 extended) that all fail RED in a planned way.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sync-pipeline/02-CONTEXT.md
@.planning/phases/02-sync-pipeline/02-RESEARCH.md
@.planning/phases/02-sync-pipeline/02-PATTERNS.md
@.planning/phases/02-sync-pipeline/02-VALIDATION.md
@.planning/codebase/TESTING.md
@app/api/shopify/sync/__tests__/route.test.ts
@app/(embedded)/__tests__/onboarding.test.tsx

<interfaces>
Patterns to mirror in RED stubs (see 02-PATTERNS.md "Test files — Wave 0 RED stubs" section):

- `vi.mock(...)` blocks appear BEFORE any `import` of the module under test.
- `beforeEach(vi.clearAllMocks)`, `afterEach(vi.unstubAllGlobals + vi.restoreAllMocks)`.
- Cast mocked functions as `ReturnType<typeof vi.fn>` for `.mockResolvedValue()` chains.
- Inngest test harness: `new InngestTestEngine({ function: syncProductsFunction })` then `await t.execute({ events: [...] })`.
- Onboarding component tests stub `shopify` and `fetch` via `vi.stubGlobal` (already established pattern in the existing file).

Existing test file patterns to extend, not rewrite:
- `app/api/shopify/sync/__tests__/route.test.ts` — currently 6 tests; add 2 more (idempotency dedup + syncRunId return).
- `app/(embedded)/__tests__/onboarding.test.tsx` — add a polling test using `vi.useFakeTimers()` + `vi.advanceTimersByTime(2000)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Inngest packages</name>
  <files>package.json, bun.lock</files>
  <read_first>
    - package.json (current dependency set, confirm bun lockfile in use)
    - .planning/phases/02-sync-pipeline/02-RESEARCH.md §"Standard Stack" and §"Package Legitimacy Audit" (both inngest and @inngest/test verified VERIFIED via npm registry)
  </read_first>
  <action>
    Run `bun add inngest@4.4.0` then `bun add -d @inngest/test@1.0.0`. These versions are pinned in 02-RESEARCH.md and pass the Package Legitimacy Audit. Do NOT install `concurrently` (deferred per CONTEXT.md). Confirm both appear in package.json (one in `dependencies`, one in `devDependencies`).
  </action>
  <verify>
    <automated>node -e "const p = require('./package.json'); if (!p.dependencies.inngest) process.exit(1); if (!p.devDependencies['@inngest/test']) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies.inngest` is present and resolves to ^4.4.x.
    - `package.json` `devDependencies["@inngest/test"]` is present and resolves to ^1.0.x.
    - `bun.lock` updated (single coherent install, no merge conflict markers).
    - `bunx tsc --noEmit` still passes (no preexisting regressions; the import-only check on the new test files is fine to be missing modules at this point — those will fail in later tasks).
  </acceptance_criteria>
  <done>Both packages installed, lockfile updated, no other dependencies altered.</done>
</task>

<task type="auto">
  <name>Task 2: Create four new RED test files</name>
  <files>inngest/functions/__tests__/sync-products.test.ts, app/api/shopify/sync/status/__tests__/route.test.ts, app/api/shopify/webhook/__tests__/route.test.ts, services/shopify/__tests__/ShopifyProductService.test.ts</files>
  <read_first>
    - app/api/shopify/sync/__tests__/route.test.ts (existing analog: vi.mock-before-imports pattern, makeRequest helper, ReturnType<typeof vi.fn> cast)
    - .planning/phases/02-sync-pipeline/02-PATTERNS.md §"Test files — Wave 0 RED stubs and extensions" (each new file has a worked example)
    - .planning/phases/02-sync-pipeline/02-VALIDATION.md §"Per-Task Verification Map" rows 2-XX-INN, 2-XX-STAT, 2-XX-WH, 2-XX-SVC (specifies required behavior coverage per file)
    - .planning/phases/02-sync-pipeline/02-CONTEXT.md D-01..D-17 (test cases must encode the locked behaviors)
  </read_first>
  <action>
    Create four test files. Each starts `vi.mock(...)` blocks BEFORE imports per the project pattern. Use `import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'` even though globals are enabled — keeps the file self-documenting. Use `it.skip` or assertions against not-yet-existing exports so the suite reports a RED reason that disappears when production code lands (no `expect(true).toBe(true)` placeholders).

    1. `inngest/functions/__tests__/sync-products.test.ts` — mock `@/lib/db/client`, `@/lib/db/repositories/ProductRepository`, `@/services/shopify/ShopifyProductService`, `@/lib/shopify/client`, `@/lib/shopify/session-storage`. Import `InngestTestEngine` from `@inngest/test` and `syncProductsFunction` from `../sync-products` (file will not yet exist — import will fail RED). Write at least these test descriptions (skipped or asserting on undefined imports is acceptable): "processes a single batch and marks SyncRun succeeded", "persists cursor after each batch via step.run id pattern fetch-batch-${cursor}/upsert-batch-${cursor}/persist-cursor-${cursor}", "marks state=partial when at least one product upsert fails but batch is not 100% failed", "throws (triggers Inngest retry) when entire batch fails", "transitions queued→running at function start and writes finishedAt at end". Reference SYN-03, SYN-06 (per D-01, D-15).

    2. `app/api/shopify/sync/status/__tests__/route.test.ts` — copy the structure of the existing sync route test. Mock `@/lib/shopify/client`, `@/lib/shopify/session-storage`, `@/lib/db/client` with `prisma.syncRun.findFirst`. Import `GET` from `../route` (will fail RED). Helper `makeRequest(syncRunId?)`. Test descriptions: "returns 400 missing_sync_run_id when query param absent", "returns 404 sync_run_not_found when row does not exist for shop", "returns 403 wrong_shop when row exists but belongs to a different shop (cross-shop access blocked)", "returns 200 with {state, processedCount, totalCount, errors, startedAt, finishedAt} when row is found and shop matches", "401 invalid_token when session token decode fails (inherits from withShopifySession)". Reference SYN-07.

    3. `app/api/shopify/webhook/__tests__/route.test.ts` — mock `@/lib/shopify/client.shopifyClient.webhooks.validate`, `@/lib/db/client.prisma.webhookEvent.create`, `@/lib/db/repositories/ProductRepository.productRepository.{upsertProduct,deleteProduct,findByShopAndHandle}`. Import `POST` from `../route` (will fail RED). Test descriptions: "returns 401 invalid_hmac when shopifyClient.webhooks.validate returns valid:false", "returns 200 dedup when prisma.webhookEvent.create throws Prisma P2002 unique violation (replay)", "products/update calls productRepository.upsertProduct with mapped payload", "products/delete calls productRepository.deleteProduct", "stale updated_at (older than existing product.updatedAtShopify) returns 200 without re-upserting (SYN-11)", "reads rawBody via req.text() BEFORE JSON.parse (verified by spying on req.text)". Reference SYN-10, SYN-11.

    4. `services/shopify/__tests__/ShopifyProductService.test.ts` — mock `@/lib/shopify/client.shopifyClient.clients.Graphql` to return canned GraphQL responses. Import `fetchProductBatch`, `fetchTotalCount`, `mapToUpsertInput` from `../ShopifyProductService` (will fail RED). Test descriptions: "fetchProductBatch calls products(first: 100, after: cursor) GraphQL and returns {products, endCursor, hasNextPage}", "fetchTotalCount calls productsCount query and returns the count (or null on missing data)", "mapToUpsertInput maps GraphQL Product node to ProductUpsertInput including title, handle, description, vendor, productType, status, tags, variants (with parsed price), images, options, and the new updatedAtShopify field parsed from product.updatedAt". Reference SYN-01, SYN-02.

    Each file MUST execute (`bunx vitest run <file>`) and report a RED failure pointing at the missing import/module — not a flat passing test.
  </action>
  <verify>
    <automated>bunx vitest run inngest/functions/__tests__/sync-products.test.ts app/api/shopify/sync/status/__tests__/route.test.ts app/api/shopify/webhook/__tests__/route.test.ts services/shopify/__tests__/ShopifyProductService.test.ts 2>&1 | grep -E "(Cannot find|undefined|FAIL)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist at the listed paths.
    - Each file uses the `vi.mock(...)` before imports pattern.
    - Each file fails RED with a clear reason (missing module: `../sync-products`, `../route`, or undefined exports from `../ShopifyProductService`).
    - No test file accidentally passes by asserting `expect(true).toBe(true)` or similar.
    - File contents reference the SYN-XX requirement IDs in `describe` strings or comments so the requirement-coverage trace is visible.
    - `grep -c "vi.mock" <file>` ≥ 1 for each of the four files.
  </acceptance_criteria>
  <done>Four RED test files committed; running them produces a failure trace that names the not-yet-created production module.</done>
</task>

<task type="auto">
  <name>Task 3: Extend existing test files with Phase 2 cases</name>
  <files>app/api/shopify/sync/__tests__/route.test.ts, app/(embedded)/__tests__/onboarding.test.tsx</files>
  <read_first>
    - app/api/shopify/sync/__tests__/route.test.ts (existing 6 tests; preserve all, append 2 new)
    - app/(embedded)/__tests__/onboarding.test.tsx (existing tests; preserve all, append polling + completion cases)
    - .planning/phases/02-sync-pipeline/02-PATTERNS.md §"app/(embedded)/__tests__/onboarding.test.tsx extension" (vi.useFakeTimers polling pattern shown verbatim)
    - .planning/phases/02-sync-pipeline/02-CONTEXT.md D-05 (idempotency), D-13/D-14 (progress UI + completion banner)
  </read_first>
  <action>
    Edit (do not rewrite) the two existing test files. Preserve all existing tests verbatim.

    For `app/api/shopify/sync/__tests__/route.test.ts`, add a new `describe('POST /api/shopify/sync — Phase 2 behavior', ...)` block with:
    - "returns the existing syncRunId when a SyncRun with the same idempotencyKey exists in queued or running state (5-min idempotency window, D-05)"
    - "creates a new SyncRun row and calls inngest.send({ name: 'shopify/product.sync', data: { syncRunId, shop } }) when no existing run matches"
    - "responds with { syncRunId: <cuid-shape string> } within 2 seconds (SYN-05 latency contract — assert response.json structure)"

    Mock additions needed: `vi.mock('@/lib/db/client', () => ({ prisma: { syncRun: { findFirst: vi.fn(), create: vi.fn() } } }))` and `vi.mock('@/lib/inngest/client', () => ({ inngest: { send: vi.fn().mockResolvedValue({ ids: ['evt-1'] }) } }))`. These mocks will fail RED if the production route doesn't yet import them — that's the expected RED state.

    For `app/(embedded)/__tests__/onboarding.test.tsx`, add new test cases:
    - "starts polling /api/shopify/sync/status every 2000ms after sync POST returns syncRunId" — uses `vi.useFakeTimers()` + `vi.advanceTimersByTime(2000)` per pattern in 02-PATTERNS.md.
    - "renders <s-progress-bar> with value derived from processedCount/totalCount when state === 'running'" — query by `data-testid="progress-bar"` or by tag (s-progress-bar is a custom element; use `container.querySelector('s-progress-bar')`).
    - "renders state badge text matching SyncRun.state ('Queued', 'Running', 'Succeeded', 'Partial', 'Failed')"
    - "stops polling when state transitions to a terminal value (succeeded | partial | failed)"
    - "renders <s-banner tone='success'> + 'Open admin chat' CTA linking to /chat when state === 'succeeded' (D-14)"
    - "renders <s-banner tone='warning'> + Retry CTA when state === 'partial'"
    - "renders <s-banner tone='critical'> + Retry CTA when state === 'failed'"

    Use the existing `vi.stubGlobal('shopify', ...)` and `vi.stubGlobal('fetch', ...)` patterns at the top of the file — extend the `fetchMock.mockResolvedValueOnce` chain to enumerate POST → STATUS polling responses in order.
  </action>
  <verify>
    <automated>bunx vitest run app/api/shopify/sync/__tests__/route.test.ts "app/(embedded)/__tests__/onboarding.test.tsx" 2>&1 | grep -E "(Tests|FAIL|pass|fail)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - All previously-passing tests in both files still pass (no regression introduced by extension).
    - New tests fail RED with clear messages (cannot find inngest mock target, missing s-progress-bar in DOM, status route not yet implemented).
    - `grep -c "syncRunId" app/api/shopify/sync/__tests__/route.test.ts` ≥ 3 (existing + new cases).
    - `grep -c "useFakeTimers" "app/(embedded)/__tests__/onboarding.test.tsx"` ≥ 1.
    - `grep -c "progress-bar\\|s-banner\\|Open admin chat" "app/(embedded)/__tests__/onboarding.test.tsx"` ≥ 3.
  </acceptance_criteria>
  <done>Both files extended with Phase 2 test cases; existing tests preserved; new tests RED as planned.</done>
</task>

</tasks>

<verification>
After all three tasks:
1. `bun run test 2>&1 | tail -20` — shows X passing (preexisting) + N failing (new RED). No "tests not found" errors.
2. `bunx tsc --noEmit` — passes; new test files type-check against the (mocked-but-not-yet-implemented) production modules.
3. `grep -rn "@inngest/test\\|InngestTestEngine" inngest/ services/ app/` — confirms the new harness is referenced.
</verification>

<success_criteria>
- Both Inngest packages installed at the pinned versions from RESEARCH.md.
- Six test files in the state Wave 0 requires: 4 new with RED stubs, 2 extended with appended Phase 2 cases.
- Every RED failure points at a planned-but-not-yet-created file/export — no spurious failures.
- No production code touched in this plan (test scaffolding only).
</success_criteria>

<output>
Create `.planning/phases/02-sync-pipeline/02-01-SUMMARY.md` listing installed package versions, the six modified files, and a `grep -c` per-file count of how many RED tests describe Phase 2 behavior.
</output>
