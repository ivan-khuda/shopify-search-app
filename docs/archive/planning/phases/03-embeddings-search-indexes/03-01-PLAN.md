---
phase: 03-embeddings-search-indexes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/embeddings/__tests__/EmbeddingService.test.ts
  - services/search/__tests__/searchableText.test.ts
  - lib/db/__tests__/hnsw.test.ts
  - inngest/functions/__tests__/sync-products.test.ts
  - app/api/shopify/webhook/__tests__/route.test.ts
  - .env.example
autonomous: false
requirements: [EMB-01, EMB-02, EMB-03, EMB-06]
tags: [embeddings, search, vitest, env]
user_setup:
  - service: vercel-ai-gateway
    why: "AI_GATEWAY_API_KEY is required for EmbeddingService.embed/embedMany calls. Without it every embedding call (sync + webhook) throws."
    env_vars:
      - name: AI_GATEWAY_API_KEY
        source: "Vercel Dashboard → AI Gateway → Project keys"

must_haves:
  truths:
    - "Empty test files exist for every Wave 0 surface so downstream plans can fill them with real assertions"
    - "Developer has confirmed AI_GATEWAY_API_KEY is present in their local .env before any embedding-touching plan runs"
  artifacts:
    - path: services/embeddings/__tests__/EmbeddingService.test.ts
      provides: "RED scaffold for EmbeddingService unit tests (it.todo + vi.hoisted block)"
    - path: services/search/__tests__/searchableText.test.ts
      provides: "RED scaffold for buildSearchableText pure-function tests"
    - path: lib/db/__tests__/hnsw.test.ts
      provides: "RED scaffold for withHnswIterativeScan unit tests"
    - path: .env.example
      provides: "AI_GATEWAY_API_KEY documented as required env var"
  key_links:
    - from: services/embeddings/__tests__/EmbeddingService.test.ts
      to: services/embeddings/EmbeddingService.ts
      via: import { embed, embedBatch, embedAndStore } from '../EmbeddingService'
      pattern: "import .* from '\\.\\./EmbeddingService'"
    - from: lib/db/__tests__/hnsw.test.ts
      to: lib/db/hnsw.ts
      via: import { withHnswIterativeScan } from '../hnsw'
      pattern: "import .* from '\\.\\./hnsw'"
---

<objective>
Create Wave 0 RED test scaffolds for every new Phase 3 surface so downstream plans (`03-02`..`03-07`) can fill them with real assertions immediately and the Nyquist rule is satisfied without `MISSING` placeholders.

Purpose: Wave 0 establishes test file existence + `vi.hoisted` mock blocks for `ai`, `prisma`, `EmbeddingService`, `searchableText` — the canonical Phase 2 mock pattern (PATTERNS.md §"vi.hoisted + functional vi.mock"). Includes a blocking human-verify checkpoint confirming `AI_GATEWAY_API_KEY` exists in local `.env` (RESEARCH.md §Environment Availability flags this as MISSING).

Output: 5 test files with `it.todo(...)` stubs + `vi.hoisted` blocks; `.env.example` updated with `AI_GATEWAY_API_KEY` entry; human-verify checkpoint signed off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-embeddings-search-indexes/03-CONTEXT.md
@.planning/phases/03-embeddings-search-indexes/03-RESEARCH.md
@.planning/phases/03-embeddings-search-indexes/03-PATTERNS.md
@.planning/phases/03-embeddings-search-indexes/03-VALIDATION.md
@CLAUDE.md
@services/shopify/__tests__/ShopifyProductService.test.ts
@inngest/functions/__tests__/sync-products.test.ts
@app/api/shopify/webhook/__tests__/route.test.ts
@lib/db/repositories/__tests__/ProductRepository.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create services/search/__tests__/searchableText.test.ts scaffold</name>
  <read_first>
    - services/shopify/__tests__/ShopifyProductService.test.ts:36-50 (analog: `describe('toDecimal')` block — pure-function tests, no mocks, expect-only)
    - .planning/phases/03-embeddings-search-indexes/03-RESEARCH.md §"buildSearchableText (D-03)" lines 561-585 (target shape + asymmetry callout)
    - .planning/phases/03-embeddings-search-indexes/03-PATTERNS.md §"services/search/__tests__/searchableText.test.ts" (test cases: full input, missing optional, empty options, trimming)
    - .planning/phases/03-embeddings-search-indexes/03-CONTEXT.md D-03 (embed text format)
  </read_first>
  <behavior>
    - it.todo: "Title/Description/Tags/Vendor/Type/Options labels appear in output in that exact order"
    - it.todo: "Missing optional fields produce empty value after label (e.g. `Title: \n`)"
    - it.todo: "Empty options array produces `Options: ` with no trailing comma or dangling separator"
    - it.todo: "Leading/trailing whitespace on title/description/vendor/productType is trimmed"
    - it.todo: "options serialise as `name (v1/v2)` joined by `, `"
  </behavior>
  <action>Create the file with a single `describe('buildSearchableText', () => { ... })` block containing five `it.todo(...)` entries matching the behaviors above. Add an `import { buildSearchableText } from '../searchableText';` at the top (the import will fail to resolve until plan 03-02 creates the helper; that's intentional RED). No mocks needed (pure function). Match the no-vi.hoisted, no-beforeEach style of the `describe('toDecimal')` analog block.</action>
  <verify>
    <automated>test -f services/search/__tests__/searchableText.test.ts &amp;&amp; grep -c "it.todo" services/search/__tests__/searchableText.test.ts | xargs -I{} test {} -ge 5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path `services/search/__tests__/searchableText.test.ts`
    - Contains `import { buildSearchableText } from '../searchableText';`
    - Contains exactly one `describe('buildSearchableText', ...)` block
    - Contains at least 5 `it.todo(...)` entries covering the behaviors listed
    - No mocks (no `vi.mock`, no `vi.hoisted`)
  </acceptance_criteria>
  <done>The scaffold file exists; `bunx vitest run services/search/__tests__/searchableText.test.ts` reports the todos as skipped (not failures), even though the import resolves to a non-existent module — vitest treats `it.todo` as not-yet-implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create lib/db/__tests__/hnsw.test.ts scaffold</name>
  <read_first>
    - lib/db/repositories/__tests__/ProductRepository.test.ts:1-60 (analog: `$transaction` mock that invokes the callback inline; `vi.mock('@/lib/db/client')` shape)
    - .planning/phases/03-embeddings-search-indexes/03-RESEARCH.md §"Pattern 2: Iterative scan helper" lines 317-347 (target helper shape)
    - .planning/phases/03-embeddings-search-indexes/03-PATTERNS.md §"lib/db/__tests__/hnsw.test.ts" lines 247-277 (mock recipe + invocationCallOrder assertion)
    - .planning/phases/03-embeddings-search-indexes/03-CONTEXT.md D-11 (helper contract)
  </read_first>
  <behavior>
    - it.todo: "withHnswIterativeScan delegates to prisma.$transaction in callback form"
    - it.todo: "withHnswIterativeScan issues SET LOCAL hnsw.iterative_scan = 'relaxed_order' as the first statement inside the transaction"
    - it.todo: "withHnswIterativeScan invokes the user callback AFTER the SET LOCAL (verified via mock.invocationCallOrder)"
    - it.todo: "withHnswIterativeScan returns the value returned by the user callback"
  </behavior>
  <action>Create the file with a `vi.hoisted(() => ({ executeRawMock: vi.fn(), transactionMock: vi.fn() }))` block matching the destructure-pattern at ProductRepository.test.ts:7. Add `vi.mock('@/lib/db/client', () => ({ prisma: { $executeRaw: executeRawMock, $transaction: transactionMock } }))`. Add `beforeEach(() => vi.clearAllMocks())`. Inside a single `describe('withHnswIterativeScan')` block, place 4 `it.todo(...)` entries. Import `import { withHnswIterativeScan } from '../hnsw';` (will resolve when plan 03-03 creates `lib/db/hnsw.ts`).</action>
  <verify>
    <automated>test -f lib/db/__tests__/hnsw.test.ts &amp;&amp; grep -q "vi.hoisted" lib/db/__tests__/hnsw.test.ts &amp;&amp; grep -c "it.todo" lib/db/__tests__/hnsw.test.ts | xargs -I{} test {} -ge 4</automated>
  </verify>
  <acceptance_criteria>
    - File contains `vi.hoisted` block with `executeRawMock` and `transactionMock`
    - File contains `vi.mock('@/lib/db/client', ...)` calling those hoisted mocks
    - Imports `withHnswIterativeScan` from `'../hnsw'`
    - At least 4 `it.todo(...)` entries
  </acceptance_criteria>
  <done>Scaffold file exists; the four todos document the EMB-06 contract that plan 03-03 will fill.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create services/embeddings/__tests__/EmbeddingService.test.ts scaffold</name>
  <read_first>
    - services/shopify/__tests__/ShopifyProductService.test.ts:10-22 (analog: `vi.hoisted` destructure + functional `vi.mock` shape — for `ai` package mocking)
    - .planning/phases/03-embeddings-search-indexes/03-RESEARCH.md §"Pattern 1: AI Gateway embedMany call" lines 244-313 (target service shape)
    - .planning/phases/03-embeddings-search-indexes/03-RESEARCH.md §"AI Gateway Mock Pattern" lines 802-812 (exact mock shape for `ai` package)
    - .planning/phases/03-embeddings-search-indexes/03-PATTERNS.md §"EmbeddingService.test.ts" lines 87-138 (mock recipe)
    - .planning/phases/03-embeddings-search-indexes/03-CONTEXT.md D-09 (service shape) and D-08 (error policy)
  </read_first>
  <behavior>
    - it.todo: "embed(text) returns a number[] of length EMBEDDING_DIMENSIONS (1536)"
    - it.todo: "embed(text) throws when AI Gateway returns vector with length != EMBEDDING_DIMENSIONS"
    - it.todo: "embed(text) calls embed() from 'ai' with model = EMBEDDING_MODEL constant 'openai/text-embedding-3-small'"
    - it.todo: "embedBatch(texts) returns { ok: [{index, vector}], failed: [] } on full success"
    - it.todo: "embedBatch(texts) returns { ok: [], failed: [{index, message}] } when embedMany throws (one entry per input)"
    - it.todo: "embedBatch failed[].message contains err.message text only, never the full error object"
    - it.todo: "embedAndStore(shop, productId, text) calls embed then prisma.$executeRaw with the modelVersion equal to EMBEDDING_MODEL constant"
    - it.todo: "embedAndStore upsert SQL uses ON CONFLICT (shop, productShop, productId) DO UPDATE"
  </behavior>
  <action>Create the file. Add `vi.hoisted(() => ({ embedMock: vi.fn(), embedManyMock: vi.fn(), executeRawMock: vi.fn() }))` block. Add `vi.mock('ai', () => ({ embed: embedMock, embedMany: embedManyMock }))` and `vi.mock('@/lib/db/client', () => ({ prisma: { $executeRaw: executeRawMock } }))`. Add `beforeEach(() => vi.clearAllMocks())`. Single `describe('EmbeddingService')` with three nested describes (`describe('embed')`, `describe('embedBatch')`, `describe('embedAndStore')`) containing the 8 `it.todo` entries. Import `{ embed, embedBatch, embedAndStore, EMBEDDING_MODEL, EMBEDDING_DIMENSIONS } from '../EmbeddingService';`.</action>
  <verify>
    <automated>test -f services/embeddings/__tests__/EmbeddingService.test.ts &amp;&amp; grep -q "vi.mock('ai'" services/embeddings/__tests__/EmbeddingService.test.ts &amp;&amp; grep -c "it.todo" services/embeddings/__tests__/EmbeddingService.test.ts | xargs -I{} test {} -ge 8</automated>
  </verify>
  <acceptance_criteria>
    - File contains `vi.mock('ai', ...)` block using the hoisted `embedMock` and `embedManyMock`
    - File contains `vi.mock('@/lib/db/client', ...)` using hoisted `executeRawMock`
    - At least 8 `it.todo(...)` entries across the three nested describes
    - File imports EMBEDDING_MODEL and EMBEDDING_DIMENSIONS by name (forces plan 03-04 to export them)
  </acceptance_criteria>
  <done>Scaffold file exists; mocks compile (no `embedMock is not defined` errors); todos document the full EMB-01/EMB-03 contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Extend inngest/functions/__tests__/sync-products.test.ts with embed-batch step mock stubs</name>
  <read_first>
    - inngest/functions/__tests__/sync-products.test.ts:1-50 (existing vi.hoisted destructure — to extend without rewriting)
    - inngest/functions/__tests__/sync-products.test.ts:107-133 (existing partial-failure test — template for new embed-failure test)
    - .planning/phases/03-embeddings-search-indexes/03-RESEARCH.md §"Pattern 3: Embed step inside Inngest sync function" lines 349-443
    - .planning/phases/03-embeddings-search-indexes/03-PATTERNS.md §"inngest/functions/__tests__/sync-products.test.ts" lines 457-494
    - .planning/phases/03-embeddings-search-indexes/03-CONTEXT.md D-01 (step location) and D-08 (error policy)
  </read_first>
  <behavior>
    - it.todo: "sync run executes step embed-batch-${cursorKey} between upsert-batch and persist-cursor"
    - it.todo: "embed-batch step calls EmbeddingService.embedBatch with searchableText for each upserted product"
    - it.todo: "embed-batch step does NOT embed products whose upsert failed in the previous step"
    - it.todo: "partial embed failure (some products fail) is pushed into SyncRun.errors[] tagged stage:'embed' and run continues (state != 'failed')"
    - it.todo: "full-batch embed failure (all products fail) throws so Inngest retries"
  </behavior>
  <action>Extend the existing `vi.hoisted` destructure to add `embedBatchMock`, `executeRawMock`, `productFindUniqueMock`, `buildSearchableTextMock`. Add two new `vi.mock` calls: `vi.mock('@/services/embeddings/EmbeddingService', () => ({ embedBatch: embedBatchMock, EMBEDDING_MODEL: 'openai/text-embedding-3-small' }))` and `vi.mock('@/services/search/searchableText', () => ({ buildSearchableText: buildSearchableTextMock }))`. Extend the existing Prisma mock to include `$executeRaw: executeRawMock` and `product: { findUnique: productFindUniqueMock }`. Append a new `describe('embed-batch step (Phase 3)')` block at the bottom of the file with the 5 `it.todo(...)` entries. DO NOT modify the existing tests beyond extending the hoisted mocks.</action>
  <verify>
    <automated>grep -q "embedBatchMock" inngest/functions/__tests__/sync-products.test.ts &amp;&amp; grep -q "vi.mock('@/services/embeddings/EmbeddingService'" inngest/functions/__tests__/sync-products.test.ts &amp;&amp; bunx vitest run inngest/functions/__tests__/sync-products.test.ts 2>&amp;1 | grep -E "(passed|todo)"</automated>
  </verify>
  <acceptance_criteria>
    - Hoisted block adds `embedBatchMock`, `executeRawMock`, `productFindUniqueMock`, `buildSearchableTextMock` (without removing existing mocks)
    - `vi.mock('@/services/embeddings/EmbeddingService', ...)` present
    - `vi.mock('@/services/search/searchableText', ...)` present
    - New `describe('embed-batch step (Phase 3)')` block with at least 5 `it.todo(...)` entries
    - Existing tests still pass (no regression to Phase 2 behaviour)
  </acceptance_criteria>
  <done>Test file compiles; existing Phase 2 tests still pass; new todos document EMB-02 contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Extend app/api/shopify/webhook/__tests__/route.test.ts with embed-after-upsert stubs</name>
  <read_first>
    - app/api/shopify/webhook/__tests__/route.test.ts:1-40 (existing vi.hoisted destructure)
    - .planning/phases/03-embeddings-search-indexes/03-RESEARCH.md §"Pattern 4: Webhook re-embedding (D-02)" lines 445-466
    - .planning/phases/03-embeddings-search-indexes/03-PATTERNS.md §"app/api/shopify/webhook/__tests__/route.test.ts" lines 546-578
    - .planning/phases/03-embeddings-search-indexes/03-CONTEXT.md D-02 (inline synchronous webhook re-embedding)
  </read_first>
  <behavior>
    - it.todo: "products/create webhook calls embedAndStore(shop, upserted.id, buildSearchableText(mapped)) after upsertProduct"
    - it.todo: "products/update webhook calls embedAndStore once, with the local Product.id (not Shopify GID)"
    - it.todo: "products/delete webhook does NOT call embedAndStore (FK cascade handles row deletion)"
    - it.todo: "webhook returns 200 even when embedAndStore throws (Shopify must not retry on embed failure)"
    - it.todo: "embedAndStore failure path logs via console.error but does not include AI_GATEWAY_API_KEY or full error object"
  </behavior>
  <action>Extend the existing `vi.hoisted` destructure to add `embedAndStoreMock: vi.fn()` and `buildSearchableTextMock: vi.fn((p) => 'mocked-text')`. Add `vi.mock('@/services/embeddings/EmbeddingService', () => ({ embedAndStore: embedAndStoreMock }))` and `vi.mock('@/services/search/searchableText', () => ({ buildSearchableText: buildSearchableTextMock }))`. Update `upsertProductMock.mockResolvedValue` calls inside existing test bodies if needed to return `{ id: 42 }`-shaped Product (the new behaviour reads `upserted.id`). Append a new `describe('embedding integration (Phase 3)')` block with the 5 `it.todo(...)` entries. Existing tests must continue to pass.</action>
  <verify>
    <automated>grep -q "embedAndStoreMock" app/api/shopify/webhook/__tests__/route.test.ts &amp;&amp; bunx vitest run app/api/shopify/webhook/__tests__/route.test.ts 2>&amp;1 | grep -E "(passed|todo)"</automated>
  </verify>
  <acceptance_criteria>
    - Hoisted block adds `embedAndStoreMock` + `buildSearchableTextMock` (additive)
    - Both new `vi.mock` calls present
    - New `describe('embedding integration (Phase 3)')` block with at least 5 `it.todo(...)` entries
    - Existing webhook tests still pass
  </acceptance_criteria>
  <done>Test file compiles, existing tests green, new todos document D-02 contract.</done>
</task>

<task type="auto">
  <name>Task 6: Update .env.example with AI_GATEWAY_API_KEY + DIRECT_URL documentation</name>
  <read_first>
    - .env.example (current file — to extend)
    - CLAUDE.md §"Environment Variables" (current docs of required vars — for matching style)
    - .planning/phases/03-embeddings-search-indexes/03-RESEARCH.md §"Environment Availability" lines 739-747 (the two MISSING env vars)
    - .planning/phases/03-embeddings-search-indexes/03-RESEARCH.md §"Pitfall 6: DATABASE_URL is Accelerate" lines 547-552 (DIRECT_URL rationale)
  </read_first>
  <action>Append two lines to `.env.example`: `AI_GATEWAY_API_KEY=` (with comment `# Vercel AI Gateway key — required for EmbeddingService.embed / embedMany. Source: Vercel Dashboard → AI Gateway → Project keys.`) and `DIRECT_URL=` (with comment `# Direct postgres URL (postgresql://...) — required in production when DATABASE_URL is a Prisma Accelerate URL. Used by scripts/apply-manual-indexes.ts.`). Match the existing `KEY=` blank-value style of other entries in the file. DO NOT add values; secrets must come from the developer's Vercel dashboard (T-3-03 mitigation).</action>
  <verify>
    <automated>grep -q "^AI_GATEWAY_API_KEY=" .env.example &amp;&amp; grep -q "^DIRECT_URL=" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contains `AI_GATEWAY_API_KEY=` (empty value) preceded by an explanatory comment
    - `.env.example` contains `DIRECT_URL=` (empty value) preceded by an explanatory comment
    - No actual secret values committed (T-3-03)
    - `.env` is still in `.gitignore` (verify with `grep -q '^\.env$' .gitignore` — must succeed)
  </acceptance_criteria>
  <done>`.env.example` documents the new env vars; the developer can copy these to their local `.env` before plan 03-04 runs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Human verifies AI_GATEWAY_API_KEY is set in local .env</name>
  <action>Pause execution and present the verification checklist below to the operator. Do not proceed until the operator types "approved" — Plans 03-04 and 03-08 require AI_GATEWAY_API_KEY to be present in process.env at runtime. If the operator types "blocked", route to a follow-up plan that documents the blocker.</action>
  <what-built>Wave 0 RED test scaffolds created; `.env.example` updated with `AI_GATEWAY_API_KEY` + `DIRECT_URL` entries. Plans 03-04 (EmbeddingService) and 03-08 (verification gate) depend on the developer having a real `AI_GATEWAY_API_KEY` in their local `.env` — otherwise every embedding call throws and integration smoke tests cannot pass.</what-built>
  <how-to-verify>
    1. Open `.env` (NOT `.env.example`) in the project root.
    2. Confirm a line exists: `AI_GATEWAY_API_KEY=<a-non-empty-value-starting-with-vrcl_>` or similar Vercel-issued shape.
    3. If missing: open Vercel Dashboard → AI Gateway → Project keys → create or copy a key → paste into `.env`.
    4. (Optional) `DIRECT_URL=postgresql://...` — only required when deploying to a Prisma Accelerate setup; local dev with a direct `DATABASE_URL` can leave `DIRECT_URL` unset (the script falls back).
    5. Verify `.env` is NOT committed: `git status` should NOT show `.env` in tracked changes; `grep -q '^\.env$' .gitignore` should return 0.
    6. (Smoke test, optional) `bun -e "console.log(!!process.env.AI_GATEWAY_API_KEY)"` should print `true` after sourcing `.env`.
  </how-to-verify>
  <resume-signal>Type "approved" once AI_GATEWAY_API_KEY is set in `.env` AND `.env` is confirmed ignored by git. Type "blocked" if you cannot obtain a Vercel AI Gateway key.</resume-signal>
</task>

</tasks>

<verification>
- All 5 Wave 0 test files exist with at least the listed `it.todo` count.
- `.env.example` contains both `AI_GATEWAY_API_KEY` and `DIRECT_URL` documentation lines.
- `.env` is git-ignored (no token leak into git history — T-3-03).
- Existing Phase 2 tests (`sync-products.test.ts`, `webhook/route.test.ts`) still pass — Phase 3 mock extensions are additive only.
- `bunx vitest run services/embeddings/__tests__/EmbeddingService.test.ts services/search/__tests__/searchableText.test.ts lib/db/__tests__/hnsw.test.ts inngest/functions/__tests__/sync-products.test.ts app/api/shopify/webhook/__tests__/route.test.ts` exits 0 (todos count as not-failures).
- Human checkpoint signed off — `AI_GATEWAY_API_KEY` confirmed in local `.env`.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → process.env | AI_GATEWAY_API_KEY enters the process via dotenv at runtime |
| .env → git | .gitignore must keep .env out of version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-03 | Information Disclosure | .env file vs git history | mitigate | `.env.example` is the committed template; `.env` is ignored. Task 6 verifies `grep -q '^\.env$' .gitignore`; Task 7 (human-verify) confirms `git status` does not list `.env`. |
| T-3-EVN-01 | Information Disclosure | .env.example committed contents | mitigate | `.env.example` carries KEY= with empty values only (Task 6 acceptance criterion); no real keys ever committed. |
</threat_model>

<success_criteria>
- 5 Wave 0 test scaffold files exist at the exact paths in `files_modified`.
- `.env.example` updated with two new env var lines and explanatory comments.
- Human-verify checkpoint signed off (`AI_GATEWAY_API_KEY` set in local `.env`).
- Existing Phase 2 tests untouched in behaviour (regression check passes).
- All 5 new test files compile and report todos correctly.
</success_criteria>

<output>
Create `.planning/phases/03-embeddings-search-indexes/03-01-SUMMARY.md` recording: which scaffolds were created, the count of `it.todo` entries in each file, the `.env.example` lines added, and the human-verify outcome.
</output>
