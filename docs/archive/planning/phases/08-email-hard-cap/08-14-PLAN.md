---
phase: 08-email-hard-cap
plan: 14
type: execute
wave: 6
depends_on: ["08-09", "08-10"]
files_modified:
  - app/api/proxy/chat/route.ts
autonomous: true
requirements: [CAP-02, CAP-03]
tags: [phase-08, api, chat, storefront, proxy, cap-check]
must_haves:
  truths:
    - "app/api/proxy/chat/route.ts gains a tryConsumeRequest(shop) call inside the withAppProxyHmac callback, AFTER HMAC verification + customer-id match + rate-limit, BEFORE conversation create and streamText (D-14 — cap is the LAST gate before model invocation)"
    - "Replaces the existing D-21 step 4 stub comment with the real implementation"
    - "On allowed:false → capReachedResponse() (HTTP 200 + streamed limit message)"
    - "On allowed:true → existing Phase 6 D-21 flow (conversation lifecycle + merge + streamText) runs unchanged"
    - "Wave 0 storefront route it() blocks go GREEN"
  artifacts:
    - path: "app/api/proxy/chat/route.ts"
      provides: "Storefront chat route with cap-check between rate-limit and conversation lifecycle"
      contains: "tryConsumeRequest(shop)"
  key_links:
    - from: "withAppProxyHmac callback"
      to: "tryConsumeRequest(shop)"
      via: "After HMAC + customer-id + rate-limit, before conversation create"
      pattern: "tryConsumeRequest\\(shop\\)"
    - from: "allowed:false branch"
      to: "capReachedResponse()"
      via: "lib/chat/cap-reached-response — same helper as admin route for behavioral parity"
      pattern: "capReachedResponse\\(\\)"
---

<objective>
Insert the cap check into the storefront chat route. Unlike the admin route, this route has multiple upstream gates (HMAC, customer-id assertion, rate-limit) that must run FIRST so we don't accidentally bypass auth on a cap-rejected request. The cap is the LAST gate before the AI Gateway call — matches D-14 ("first action after auth/session resolution and before reaching the AI Gateway").

The existing handler at line 95 contains: `// D-21 step 4: hard-cap stub (Phase 8 fills in via DB-backed RequestCounter).` — this plan replaces the stub with the real call.

Purpose: Enforce CAP-02 / CAP-03 at the storefront surface. Storefront traffic is the higher-volume surface (visitor-driven, not merchant-driven), so cap enforcement here is the primary cost-protection mechanism.

Output: One file edit (stub line replaced with two lines) + Wave 0 storefront-route cap it() blocks go GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-email-hard-cap/08-CONTEXT.md
@.planning/phases/08-email-hard-cap/08-RESEARCH.md
@app/api/proxy/chat/route.ts
@app/api/proxy/chat/__tests__/route.test.ts
@services/chat/CapService.ts
@lib/chat/cap-reached-response.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the D-21 step 4 stub with the real cap check</name>
  <files>app/api/proxy/chat/route.ts</files>
  <behavior>
    - Add imports: `import { tryConsumeRequest } from '@/services/chat/CapService';` and `import { capReachedResponse } from '@/lib/chat/cap-reached-response';`
    - Locate line 95 — the stub comment `// D-21 step 4: hard-cap stub (Phase 8 fills in via DB-backed RequestCounter).` — and REPLACE it with:
      ```
      // D-21 step 4 / D-14: hard cap (CAP-02/03). Last gate before AI Gateway.
      const consume = await tryConsumeRequest(shop);
      if (!consume.allowed) return capReachedResponse();
      ```
    - All OTHER lines (HMAC wrapper on line 69, customer-id assert on lines 81-85, rate-limit on lines 87-93, conversation create on lines 97-116, merge on 118-120, streamText on 134-174) remain byte-identical
    - shop comes from withAppProxyHmac ctx — trusted; tryConsumeRequest contract trusts its arg
    - Update the existing top-of-file JSDoc "Hard cap (Phase 8): D-21 step 4 is a stub..." (lines 25-26) to read: "Hard cap (Phase 8): D-21 step 4 enforces CAP-02/03 via tryConsumeRequest after HMAC + customer-id + rate-limit gates, before conversation lifecycle."
  </behavior>
  <action>Edit app/api/proxy/chat/route.ts. Add the two imports (immediately after `import { hybridSearch } from '@/services/search/SearchService';` on line 42). Replace exactly the stub comment line 95 with the three-line block per behavior (the comment + the two code lines). Update the JSDoc on lines 25-26. Do not touch any other line. The injection point is intentional: after rate-limit (so a high-volume visitor cannot circumvent the cap by sending a flood that rate-limit drops — rate-limit goes first to absorb spam; cap goes second to enforce the monthly contract on legitimate traffic) and before conversation create (so we don't create empty conversations on cap-reached requests).</action>
  <verify>
    <automated>bunx vitest run app/api/proxy/chat/__tests__/route.test.ts 2>&1 | grep -E "✓|passed|hard cap|cap reached" | head -10; grep -n "tryConsumeRequest(shop)" app/api/proxy/chat/route.ts | head -3; ! grep -E "hard-cap stub" app/api/proxy/chat/route.ts</automated>
  </verify>
  <done>Wave 0 storefront-route Phase-8 it() blocks GREEN; existing Phase 6 storefront-route tests STILL GREEN (HMAC + customer-id + rate-limit + conversation lifecycle regression checks); stub comment gone; tryConsumeRequest appears once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storefront visitor → /api/proxy/chat | App Proxy HMAC validated by withAppProxyHmac (Phase 6) |
| withAppProxyHmac → CapService | shop is trusted (signed-query-derived); tryConsumeRequest contract trusts arg |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14-T1 | Tampering | shop tampering via signed query | mitigate | withAppProxyHmac validates signature — shop comes from signed query, not raw URL or body (Phase 6 lock) |
| T-08-14-T2 | Tampering / DoS | Visitor floods storefront chat to drain a merchant's cap | mitigate | rate-limit (Phase 6 sliding window) runs BEFORE cap-check — absorbs burst traffic; cap then enforces the monthly contract on remaining legitimate traffic |
| T-08-14-T3 | Tampering / DoS | Cap bypass via concurrent requests | mitigate | RequestCounterRepository.tryConsume is race-free (08-07) |
| T-08-14-I1 | Information disclosure | Cap-reached response leaking shop identity | mitigate | capReachedResponse is a static streamed message (08-09); identical bytes across shops |
| T-08-14-D1 | Denial of service | AI Gateway billing exposure | mitigate | Cap check is upstream of streamText — AI Gateway is never invoked on cap-reached requests (cost protection) |
</threat_model>

<verification>
- `bunx vitest run app/api/proxy/chat/__tests__/route.test.ts` GREEN
- `grep -n "tryConsumeRequest" app/api/proxy/chat/route.ts` shows exactly one match BETWEEN rate-limit and conversation create
- `grep -E "hard-cap stub" app/api/proxy/chat/route.ts` empty
</verification>

<success_criteria>
- CAP-02 storefront coverage: cap check fires before AI Gateway call
- CAP-03 storefront coverage: HTTP 200 + streamed limit message on cap-reached
- Phase 6 D-21 stub fully retired
- Behavioral parity with admin route (same helper, same response shape)
</success_criteria>

<output>
Create `.planning/phases/08-email-hard-cap/08-14-SUMMARY.md` summarizing the stub replacement and Wave 0 storefront-route status flip.
</output>
