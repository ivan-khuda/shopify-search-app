---
phase: 08-email-hard-cap
plan: 09
type: execute
wave: 4
depends_on: ["08-01"]
files_modified:
  - lib/chat/cap-reached-response.ts
autonomous: true
requirements: [CAP-03]
tags: [phase-08, chat, ai-sdk-v6, cap-reached]
must_haves:
  truths:
    - "lib/chat/cap-reached-response.ts exports capReachedResponse(): Response"
    - "Exports the constant CAP_REACHED_MESSAGE with the exact locked text"
    - "Synthesizes an AI SDK v6 UI message stream with chunks: start → text-start → text-delta → text-end → finish (Pattern 5 + Pitfall 5)"
    - "Returns HTTP 200 (D-10 / CAP-03) — not 4xx"
    - "Wave 0 cap-reached-response.test.ts goes GREEN"
  artifacts:
    - path: "lib/chat/cap-reached-response.ts"
      provides: "Single helper consumed by both /api/chat and /api/proxy/chat to return the limit-reached message"
      exports: ["capReachedResponse", "CAP_REACHED_MESSAGE"]
  key_links:
    - from: "capReachedResponse"
      to: "createUIMessageStreamResponse"
      via: "from 'ai' v6"
      pattern: "createUIMessageStreamResponse"
    - from: "writer.write"
      to: "v6 chunk taxonomy"
      via: "start → text-start → text-delta → text-end → finish (Pitfall 5)"
      pattern: "type:\\s*'(start|text-start|text-delta|text-end|finish)'"
---

<objective>
Author the single helper that synthesizes the cap-reached streamed response. This bypasses `streamText` (which always calls a model — Anti-Pattern 2 forbids that path here) and uses `createUIMessageStream` + `createUIMessageStreamResponse` from `ai` v6 to emit the documented v6 chunk taxonomy directly.

Per the additional_context locked decision: the cap-reached copy is a SINGLE constant shared between admin and storefront for V1. Future Phase 9 polish could specialize per-surface — but Open Question 1 recommends one constant in V1.

Purpose: Both chat routes (08-13, 08-14) import this helper so the cap-reached response shape is verifiably identical across surfaces.

Output: One file + Wave 0 test goes GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-email-hard-cap/08-CONTEXT.md
@.planning/phases/08-email-hard-cap/08-RESEARCH.md
@lib/chat/__tests__/cap-reached-response.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author lib/chat/cap-reached-response.ts</name>
  <files>lib/chat/cap-reached-response.ts</files>
  <behavior>
    - Module exports the CAP_REACHED_MESSAGE constant with EXACTLY this string: "You've reached this month's message limit. It resets on the 1st of the month. To raise your limit, contact support."
    - Exports capReachedResponse(): Response
    - Uses createUIMessageStream({ execute: async ({ writer }) => { ... } }) from 'ai' to write the 5-chunk sequence
    - First chunk: writer.write({ type: 'start', messageId: id }) where id = generateId() from 'ai'
    - Second: writer.write({ type: 'text-start', id })
    - Third: writer.write({ type: 'text-delta', id, delta: CAP_REACHED_MESSAGE })
    - Fourth: writer.write({ type: 'text-end', id })
    - Fifth: writer.write({ type: 'finish' })
    - Returns createUIMessageStreamResponse({ stream }) — HTTP 200 default
    - Zero console.* calls
  </behavior>
  <action>Implement verbatim per 08-RESEARCH.md §Code Examples "Cap-reached response helper" (lines 902-925). Imports: `import { createUIMessageStream, createUIMessageStreamResponse, generateId } from 'ai';`. CAP_REACHED_MESSAGE MUST be the exact string locked in additional_context — copy-paste byte-precise; do not paraphrase. The text "1st of the month" (not "1st of next month") — additional_context Resolved Items overrides the example in RESEARCH which used "1st of next month". Do NOT add any custom headers; createUIMessageStreamResponse sets the correct Content-Type by default. Note Pitfall 5: the chunk type names are v6-specific — never use v5's `text-delta` with different field names. The id passed to text-start / text-delta / text-end MUST be the same id from generateId() — chunk correlation is by id.</action>
  <verify>
    <automated>bunx vitest run lib/chat/__tests__/cap-reached-response.test.ts 2>&1 | grep -E "✓|passed" | head -5; grep -c "1st of the month" lib/chat/cap-reached-response.ts | grep -q "^1$"</automated>
  </verify>
  <done>cap-reached-response.test.ts GREEN; constant text is exact; 5 writer.write calls in correct order with shared id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route → helper | No external input — helper returns a synthetic stream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09-T1 | Tampering | v5/v6 chunk type mix crashes client | mitigate | Pitfall 5: use only v6 chunk types (start/text-start/text-delta/text-end/finish); Wave 0 test parses the stream and asserts the exact chunk-type order |
| T-08-09-D1 | Denial of service | Stream not closing | mitigate | finish chunk written last — createUIMessageStream wraps the writer in a proper ReadableStream that closes on execute() completion |
| T-08-09-I1 | Information disclosure | Leaking shop identity in the message | mitigate | CAP_REACHED_MESSAGE is a static string with no interpolation — zero risk |
</threat_model>

<verification>
- `bunx vitest run lib/chat/__tests__/cap-reached-response.test.ts` GREEN
- `grep -c "createUIMessageStreamResponse\\|createUIMessageStream\\b" lib/chat/cap-reached-response.ts` ≥ 2
</verification>

<success_criteria>
- CAP-03 / D-13 satisfied: graceful streamed response with locked copy
- Wave 0 test flips RED → GREEN
- Helper consumable by both 08-13 and 08-14 with identical behavior
</success_criteria>

<output>
Create `.planning/phases/08-email-hard-cap/08-09-SUMMARY.md` confirming the helper signature and exact constant value.
</output>
