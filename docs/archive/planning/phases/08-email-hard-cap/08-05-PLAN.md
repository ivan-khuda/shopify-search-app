---
phase: 08-email-hard-cap
plan: 05
type: execute
wave: 4
depends_on: ["08-01", "08-04"]
files_modified:
  - lib/email/templates/SyncSuccessEmail.tsx
  - lib/email/templates/SyncFailureEmail.tsx
autonomous: true
requirements: [NOT-01, NOT-02, NOT-03]
tags: [phase-08, email, react-email, templates]
must_haves:
  truths:
    - "Two React Email templates live under lib/email/templates/ per D-01 / NOT-03"
    - "SyncSuccessEmail renders productCount and adminUrl into the body + button href"
    - "SyncFailureEmail renders errorMessage as auto-escaped text (NO dangerouslySetInnerHTML — V5 Input Validation) + retryUrl into button"
    - "Templates use only @react-email/components primitives (Html, Head, Body, Container, Section, Text, Button, Hr)"
    - "Subject text + body copy matches D-07 minimal-transactional brief — no marketing copy"
    - "Wave 0 sync-success-email + sync-failure-email tests go GREEN"
  artifacts:
    - path: "lib/email/templates/SyncSuccessEmail.tsx"
      provides: "SyncSuccessEmail React Email component + SyncSuccessEmailProps interface"
      exports: ["SyncSuccessEmail", "SyncSuccessEmailProps"]
    - path: "lib/email/templates/SyncFailureEmail.tsx"
      provides: "SyncFailureEmail React Email component + SyncFailureEmailProps interface"
      exports: ["SyncFailureEmail", "SyncFailureEmailProps"]
  key_links:
    - from: "lib/email/templates/SyncSuccessEmail.tsx"
      to: "Button.href === adminUrl"
      via: "props.adminUrl prop drilled into <Button href={adminUrl}>"
      pattern: "href=\\{adminUrl\\}"
    - from: "lib/email/templates/SyncFailureEmail.tsx"
      to: "Button.href === retryUrl"
      via: "props.retryUrl prop drilled into <Button href={retryUrl}>"
      pattern: "href=\\{retryUrl\\}"
---

<objective>
Author the two React Email templates that the EmailService renders. Both files live at `lib/email/templates/` per D-01 / NOT-03 (LOCKED). Templates are pure, side-effect-free React function components — rendered to HTML at send time via `@react-email/render`.

Purpose: Provide the user-facing email content. Templates are unit-testable in isolation (no Resend mock needed) because rendering is decoupled from sending.

Output: Two TSX files + Wave-0 template tests go GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-email-hard-cap/08-CONTEXT.md
@.planning/phases/08-email-hard-cap/08-RESEARCH.md
@lib/email/templates/__tests__/sync-success-email.test.tsx
@lib/email/templates/__tests__/sync-failure-email.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author SyncSuccessEmail.tsx</name>
  <files>lib/email/templates/SyncSuccessEmail.tsx</files>
  <behavior>
    - Exports `SyncSuccessEmail` function component and `SyncSuccessEmailProps` interface
    - Props: { shop: string; productCount: number; adminUrl: string }
    - Renders an Html → Head → Body → Container hierarchy
    - Heading text contains the literal string "Catalog sync complete"
    - Body contains the productCount value AND the shop value as text
    - Section contains a Button with href={adminUrl} and text "View in admin"
    - Footer contains "SmartDiscovery AI · transactional notification" (middle-dot U+00B7 — match Phase 4 banner style)
    - No image / no marketing copy / no detailed stats per D-07
  </behavior>
  <action>Author the file verbatim per 08-RESEARCH.md §Code Examples "React Email template — Success". Import only from @react-email/components — DO NOT import lucide-react / radix / tailwind / next. Use inline style objects (React Email convention) — DO NOT use className. Style palette: backgroundColor '#f6f9fc' on Body, '#ffffff' on Container with 32px padding, max-width 480px, Button background '#008060' (SmartDiscovery brand color also used in Phase 5/6/7), color '#ffffff', borderRadius 6px. Footer Text styled fontSize 12px color '#9ca3af'. DO NOT embed any base64 images or external images — per D-07 "no embedded images beyond an inline SmartDiscovery wordmark" — and to keep the template simple, V1 omits the wordmark entirely (deferred to a polish iteration).</action>
  <verify>
    <automated>bunx vitest run lib/email/templates/__tests__/sync-success-email.test.tsx 2>&1 | grep -E "✓|passed" | head -5</automated>
  </verify>
  <done>sync-success-email.test.tsx GREEN; file imports only from @react-email/components; "Catalog sync complete" heading present; href={adminUrl} on the Button.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author SyncFailureEmail.tsx</name>
  <files>lib/email/templates/SyncFailureEmail.tsx</files>
  <behavior>
    - Exports `SyncFailureEmail` function component and `SyncFailureEmailProps` interface
    - Props: { shop: string; syncRunId: string; errorMessage: string; retryUrl: string }
    - Renders Html → Head → Body → Container
    - Heading text contains the literal string "Catalog sync failed" with color '#b91c1c'
    - Body contains the errorMessage as a Text node (auto-escaped by React Email — V5 Input Validation mitigation for email-injection per CONTEXT.md threat T-08-EI)
    - Section contains a Button with href={retryUrl} and text "Retry sync"
    - Footer mirrors SyncSuccessEmail's "SmartDiscovery AI · transactional notification"
    - NEVER use dangerouslySetInnerHTML — even though the failure-message comes from a thrown Error (not direct user input), it can contain GraphQL fragments or schema text that, if rendered raw, would risk XSS / template injection
  </behavior>
  <action>Author the file verbatim per 08-RESEARCH.md §Code Examples "React Email template — Failure". Same style palette as SyncSuccessEmail but the heading is red ('#b91c1c'). The errorMessage Text is styled with backgroundColor '#f3f4f6', padding 12px, borderRadius 4px to render as a code-block-ish preformatted block — keeps the error visually distinct from the marketing copy. retryUrl is constructed by the Inngest function (08-12) as `${process.env.HOST}/onboarding?retry=${syncRunId}` — this template just receives the string and drills it into Button href. The shop and syncRunId props are accepted in the type but only shop is rendered (syncRunId is reserved for analytics / observability — keep the prop for future surface).</action>
  <verify>
    <automated>bunx vitest run lib/email/templates/__tests__/sync-failure-email.test.tsx 2>&1 | grep -E "✓|passed" | head -5; ! grep -E "dangerouslySetInnerHTML" lib/email/templates/SyncFailureEmail.tsx</automated>
  </verify>
  <done>sync-failure-email.test.tsx GREEN; no dangerouslySetInnerHTML; href={retryUrl} on the Button.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| errorMessage (untrusted-ish) → rendered HTML | thrown Error.message can contain merchant-supplied GraphQL or shop names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-T1 | Tampering | Email-content injection via errorMessage | mitigate | React Email primitives auto-escape Text node children; no `dangerouslySetInnerHTML` anywhere; verify command greps for forbidden API |
| T-08-05-T2 | Tampering | Email-header injection via subject template | mitigate | Subject lines are static strings ("Catalog sync complete — ${productCount} products" / "Catalog sync failed") — no user-controllable input in headers |
| T-08-05-I1 | Information disclosure | Leaking PII into email body | accept | Email body intentionally contains shop hostname + productCount + admin URL — these are merchant-owned data, sent to merchant's own contactEmail; not a leak |
</threat_model>

<verification>
- `bunx vitest run lib/email/templates/__tests__` GREEN
- `grep -rE "dangerouslySetInnerHTML" lib/email/templates/` empty
- `grep -E "className=" lib/email/templates/` empty (React Email uses inline styles, not className)
</verification>

<success_criteria>
- NOT-03 satisfied: templates live at lib/email/templates/ as React Email components
- Subject + heading + button text match D-07 minimal-transactional brief
- Both Wave 0 template tests flip RED → GREEN
</success_criteria>

<output>
Create `.planning/phases/08-email-hard-cap/08-05-SUMMARY.md` confirming both templates compile, both tests are GREEN, and no dangerouslySetInnerHTML appears.
</output>
