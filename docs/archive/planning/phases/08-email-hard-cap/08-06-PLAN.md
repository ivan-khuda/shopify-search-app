---
phase: 08-email-hard-cap
plan: 06
type: execute
wave: 4
depends_on: ["08-01", "08-03"]
files_modified:
  - services/shopify/ShopifyShopService.ts
autonomous: true
requirements: [NOT-01, NOT-02]
tags: [phase-08, shopify, graphql]
must_haves:
  truths:
    - "services/shopify/ShopifyShopService.ts exports fetchShopContactEmail(session): Promise<string | null>"
    - "Function returns null on missing field, empty string, OR any thrown GraphQL error (D-05 — non-failing skip)"
    - "Function uses the existing shopifyClient.clients.Graphql plumbing (mirrors ShopifyProductService.ts pattern)"
    - "Wave 0 ShopifyShopService.test.ts goes GREEN"
  artifacts:
    - path: "services/shopify/ShopifyShopService.ts"
      provides: "fetchShopContactEmail + SHOP_CONTACT_EMAIL_QUERY constant"
      exports: ["fetchShopContactEmail", "SHOP_CONTACT_EMAIL_QUERY"]
  key_links:
    - from: "fetchShopContactEmail"
      to: "shopifyClient.clients.Graphql"
      via: "new shopifyClient.clients.Graphql({ session })"
      pattern: "new shopifyClient\\.clients\\.Graphql"
---

<objective>
Add a single new GraphQL helper to the existing Shopify service layer. The Admin GraphQL `Shop.contactEmail` field is typed `String!` in the schema, but in practice can be null or empty on dev stores (Pitfall 3) — the helper must return `null` defensively on missing / empty / error paths so the calling Inngest step can skip the email send without failing the sync (D-05).

Purpose: Decouple the GraphQL query from the Inngest function so it can be unit-tested in isolation and so the Inngest function stays focused on orchestration.

Output: One new file + Wave 0 test goes GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-email-hard-cap/08-CONTEXT.md
@.planning/phases/08-email-hard-cap/08-RESEARCH.md
@services/shopify/ShopifyProductService.ts
@lib/shopify/client.ts
@services/shopify/__tests__/ShopifyShopService.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author services/shopify/ShopifyShopService.ts</name>
  <files>services/shopify/ShopifyShopService.ts</files>
  <behavior>
    - Exports SHOP_CONTACT_EMAIL_QUERY GraphQL string literal: `query ShopContactEmail { shop { contactEmail } }`
    - Exports fetchShopContactEmail(session: Session): Promise<string | null>
    - Constructs `new shopifyClient.clients.Graphql({ session })` (mirrors ShopifyProductService pattern)
    - Calls `client.request<{ shop?: { contactEmail?: string | null } }>(SHOP_CONTACT_EMAIL_QUERY)`
    - Returns `email && email.length > 0 ? email : null` — non-empty string ONLY
    - Catches ANY thrown error and returns null (D-05: notifications are auxiliary — sync result is the contract)
    - Zero console.* calls (Pitfall 6 — contactEmail is PII; the catch block must NOT log)
  </behavior>
  <action>Implement verbatim per 08-RESEARCH.md §Code Examples "Shop contact-email fetch". Import shopifyClient from @/lib/shopify/client and type Session from @shopify/shopify-api. The try/catch swallows ALL errors and returns null — this is intentional per D-05 + Assumption A5 (failure-email send failure should not bubble; sync result is the contract). The catch arm is bare `catch {}` — do NOT bind the error to a variable that would be tempted to log (Pitfall 6). Open Question 2 resolution note as a JSDoc comment: the existing offline session scopes include read_products (Phase 2 dependency); Shop.contactEmail is readable with any granted scope per Shopify Admin GraphQL semantics. If verification surfaces a scope error at runtime, the verification gate (08-15) documents adding read_shop_data to the scopes config — but training-data evidence + Phase 2 working demo suggest this is unnecessary.</action>
  <verify>
    <automated>bunx vitest run services/shopify/__tests__/ShopifyShopService.test.ts 2>&1 | grep -E "✓|passed" | head -5; ! grep -E "console\\." services/shopify/ShopifyShopService.ts</automated>
  </verify>
  <done>Wave 0 test GREEN; happy path returns the email string; null contactEmail returns null; empty-string contactEmail returns null; thrown GraphQL error returns null without throwing; zero console.* calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inngest function → Shopify Admin GraphQL | Uses authenticated offline session; HTTPS by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-I1 | Information disclosure | Logging contactEmail (PII) | mitigate | Bare `catch {}` block — error is not bound; no console.* anywhere in file (Pitfall 6) |
| T-08-06-T1 | Tampering | GraphQL response shape skew | mitigate | Defensive coalescing: returns null on missing field, null value, OR empty string (Pitfall 3) |
| T-08-06-D1 | Denial of service | Sync hangs on Shopify GraphQL outage | mitigate | Exception → returns null → email step skips (D-05); sync result unaffected |
</threat_model>

<verification>
- `bunx vitest run services/shopify/__tests__/ShopifyShopService.test.ts` GREEN
- `grep -E "console\\." services/shopify/ShopifyShopService.ts` empty
</verification>

<success_criteria>
- fetchShopContactEmail returns null on every failure mode (graceful per D-05)
- Wave 0 test flips RED → GREEN
</success_criteria>

<output>
Create `.planning/phases/08-email-hard-cap/08-06-SUMMARY.md` confirming the file shape, exports, and Wave 0 status flip.
</output>
