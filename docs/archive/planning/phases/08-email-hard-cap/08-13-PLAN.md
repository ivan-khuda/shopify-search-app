---
phase: 08-email-hard-cap
plan: 13
type: execute
wave: 6
depends_on: ["08-09", "08-10"]
files_modified:
  - app/api/chat/route.ts
autonomous: true
requirements: [CAP-02, CAP-03]
tags: [phase-08, api, chat, admin, cap-check]
must_haves:
  truths:
    - "app/api/chat/route.ts gains a tryConsumeRequest(shop) call as the FIRST action inside the withShopifySession callback (D-14 — before streamText, before any model resolution)"
    - "On allowed:false, the route returns capReachedResponse() — HTTP 200 + streamed limit-reached message"
    - "On allowed:true, the existing Phase 4 streamText flow runs unchanged"
    - "Wave 0 admin route it() blocks go GREEN"
  artifacts:
    - path: "app/api/chat/route.ts"
      provides: "Admin chat route with cap-check inserted before model resolution"
      contains: "tryConsumeRequest(shop)"
  key_links:
    - from: "withShopifySession callback"
      to: "tryConsumeRequest(shop)"
      via: "First call inside the callback, before getActiveChatModel and streamText"
      pattern: "tryConsumeRequest\\(shop\\)"
    - from: "allowed:false branch"
      to: "capReachedResponse()"
      via: "lib/chat/cap-reached-response"
      pattern: "capReachedResponse\\(\\)"
---

<objective>
Insert the cap check at the top of the admin chat route. Per D-14: the call is the FIRST action inside the `withShopifySession` callback — before `req.json()` parsing (no need to parse the body if we're going to reject), before `getActiveChatModel`, and before any AI Gateway call. This minimizes wasted work on cap-reached requests.

Purpose: Enforce CAP-02 / CAP-03 at the admin chat surface. The injection point is symmetric with the storefront route (08-14) so the cap is uniformly enforced.

Output: One file edit + Wave 0 admin-route cap it() blocks go GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-email-hard-cap/08-CONTEXT.md
@.planning/phases/08-email-hard-cap/08-RESEARCH.md
@app/api/chat/route.ts
@app/api/chat/__tests__/route.test.ts
@services/chat/CapService.ts
@lib/chat/cap-reached-response.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject tryConsumeRequest as the first call in the withShopifySession callback</name>
  <files>app/api/chat/route.ts</files>
  <behavior>
    - Add imports: `import { tryConsumeRequest } from '@/services/chat/CapService';` and `import { capReachedResponse } from '@/lib/chat/cap-reached-response';`
    - Modify the existing `export const POST = withShopifySession(async ({ shop, req }) => { ... })` callback: BEFORE the current first line `const { messages }: { messages: UIMessage[] } = await req.json();`, insert:
      ```
      const consume = await tryConsumeRequest(shop);
      if (!consume.allowed) return capReachedResponse();
      ```
    - The rest of the handler (lines 62-101 — req.json, getActiveChatModel, streamText, toUIMessageStreamResponse) remains byte-identical
    - The shop variable comes from withShopifySession ctx — trusted (T-04 mitigation already in place; CapService trusts its arg by contract per 08-10)
  </behavior>
  <action>Edit app/api/chat/route.ts. Add the two imports near the top (immediately after existing `import { hybridSearch } from '@/services/search/SearchService';` on line 59). Inside the existing arrow function body, insert the two cap-check lines as the very first statements — before the existing `const { messages }: { messages: UIMessage[] } = await req.json();` on line 62. The existing JSDoc at the top of the file already mentions "Phase 8 hard cap (CAP-01/02/03)" in the DoS lock section (line 42) — update that comment to reference D-14 (cap check is the first action after auth resolves shop). No other code changes.</action>
  <verify>
    <automated>bunx vitest run app/api/chat/__tests__/route.test.ts 2>&1 | grep -E "✓|passed|hard cap|cap reached" | head -10; grep -n "tryConsumeRequest(shop)" app/api/chat/route.ts | head -3</automated>
  </verify>
  <done>Wave 0 admin-route Phase-8 it() blocks GREEN; existing Phase 4 admin-route tests STILL GREEN (allowed-path regression check); tryConsumeRequest appears exactly once before req.json().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/chat | Bearer session token validated by withShopifySession (Phase 1) |
| withShopifySession → CapService | shop is trusted (session-derived) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13-T1 | Tampering | shop spoofing via body / query bypassing the cap | mitigate | shop is sourced from withShopifySession ctx — never from req.json() / req.url; Phase 1 already established this trust boundary |
| T-08-13-T2 | Tampering / DoS | Cap bypass via concurrent requests | mitigate | RequestCounterRepository.tryConsume is race-free (08-07); cap check is upstream of streamText so AI Gateway is never called on cap-reached path (cost protection) |
| T-08-13-I1 | Information disclosure | Cap-reached response leaking other shops' data | mitigate | capReachedResponse returns a static streamed message (08-09); no per-shop content |
</threat_model>

<verification>
- `bunx vitest run app/api/chat/__tests__/route.test.ts` GREEN (Phase 4 + Phase 8 tests)
- `grep -n "tryConsumeRequest" app/api/chat/route.ts` shows exactly one match
- The match line number < line of `await req.json()` — confirms cap check is FIRST
</verification>

<success_criteria>
- CAP-02 admin coverage: cap check fires before AI Gateway call
- CAP-03 admin coverage: HTTP 200 + streamed limit message on cap-reached
- D-14: single helper invocation pattern shared with storefront route
</success_criteria>

<output>
Create `.planning/phases/08-email-hard-cap/08-13-SUMMARY.md` summarizing the two-line insertion and Wave 0 admin-route status flip.
</output>
