---
phase: 08-email-hard-cap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/email/templates/__tests__/sync-success-email.test.tsx
  - lib/email/templates/__tests__/sync-failure-email.test.tsx
  - services/email/__tests__/EmailService.test.ts
  - services/shopify/__tests__/ShopifyShopService.test.ts
  - lib/db/repositories/__tests__/RequestCounterRepository.test.ts
  - services/chat/__tests__/CapService.test.ts
  - lib/util/__tests__/period.test.ts
  - lib/chat/__tests__/cap-reached-response.test.ts
  - inngest/functions/__tests__/sync-products.test.ts
  - app/api/chat/__tests__/route.test.ts
  - app/api/proxy/chat/__tests__/route.test.ts
  - lib/db/repositories/__tests__/RequestCounterRepository.race.integration.test.ts
autonomous: true
requirements: [NOT-01, NOT-02, NOT-03, NOT-04, CAP-01, CAP-02, CAP-03]
tags: [phase-08, wave-0, tdd, email, hard-cap]
must_haves:
  truths:
    - "Every Wave 0 test file exists and runs RED (no implementation yet)"
    - "Each mandatory test asserts the exact behavior derived from the Req→Test map in 08-RESEARCH.md §Validation Architecture"
    - "Optional race-condition integration test exists, is skipped when INTEGRATION_DB_URL is unset, and runs when it is set"
  artifacts:
    - path: "lib/email/templates/__tests__/sync-success-email.test.tsx"
      provides: "RED test for SyncSuccessEmail — covers NOT-01 (template content + admin button URL)"
    - path: "lib/email/templates/__tests__/sync-failure-email.test.tsx"
      provides: "RED test for SyncFailureEmail — covers NOT-02 (template content + retry URL)"
    - path: "services/email/__tests__/EmailService.test.ts"
      provides: "RED test for sendSyncSuccess + sendSyncFailure — covers NOT-04 (env-scoped from), idempotencyKey shape, error propagation"
    - path: "services/shopify/__tests__/ShopifyShopService.test.ts"
      provides: "RED test for fetchShopContactEmail — happy / null / GraphQL-error paths"
    - path: "lib/db/repositories/__tests__/RequestCounterRepository.test.ts"
      provides: "RED test for tryConsume — asserts the $queryRaw template includes INSERT…ON CONFLICT…DO UPDATE…WHERE…RETURNING + cap-reached → empty rows"
    - path: "services/chat/__tests__/CapService.test.ts"
      provides: "RED test for tryConsumeRequest — env default 2000, env override parses positive int, falls back to 2000 on invalid env, calls repo with period from getCurrentPeriod"
    - path: "lib/util/__tests__/period.test.ts"
      provides: "RED test for getCurrentPeriod(now) — returns YYYY-MM UTC; deterministic under DI"
    - path: "lib/chat/__tests__/cap-reached-response.test.ts"
      provides: "RED test for capReachedResponse — HTTP 200, content-type text/event-stream, parses v6 chunk sequence (start → text-start → text-delta CAP_REACHED_MESSAGE → text-end → finish)"
    - path: "inngest/functions/__tests__/sync-products.test.ts"
      provides: "EXTENDED — appends: sends success email; sends failure email; skips when emailSentAt set; skips when contactEmail missing; distinct step IDs send-success-email vs send-failure-email"
    - path: "app/api/chat/__tests__/route.test.ts"
      provides: "EXTENDED — mocks CapService.tryConsumeRequest; allowed → streamText path reached; denied → HTTP 200 + cap-reached copy in stream"
    - path: "app/api/proxy/chat/__tests__/route.test.ts"
      provides: "EXTENDED — same dual-branch coverage after withAppProxyHmac"
    - path: "lib/db/repositories/__tests__/RequestCounterRepository.race.integration.test.ts"
      provides: "OPTIONAL race-condition stress test — gated on INTEGRATION_DB_URL env; N=200 concurrent tryConsume at count = cap - 1; exactly 1 wins"
  key_links:
    - from: "all 11 mandatory test files"
      to: "08-RESEARCH.md §Validation Architecture Phase Requirements → Test Map"
      via: "1:1 row mapping; every Req→Test row is realized by at least one it() block"
      pattern: "NOT-0\\d|CAP-0\\d|SC4|D-04|D-05"
---

<objective>
Author all Wave 0 RED test scaffolds for Phase 8 BEFORE any production code is written. Each test must fail with a meaningful "module not found" or "expected X, got undefined" error — never with a syntax error or import-of-undefined-symbol. This plan is the Nyquist contract: every subsequent plan (08-02 through 08-14) drives one or more of these files from RED to GREEN.

Purpose: Lock the test contract for Phase 8 per the Validation Architecture in 08-RESEARCH.md. The verification gate (08-15) aggregates the per-task `<automated>` commands referenced here into the Per-Task Verification Map of 08-VALIDATION.md.

Output: 11 mandatory test files + 1 optional gated integration test. All files RED on first run via `bunx vitest run <path>`. Coverage spans NOT-01..04, CAP-01..03, plus D-04 (idempotency) and D-05 (skip on missing contactEmail).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-email-hard-cap/08-CONTEXT.md
@.planning/phases/08-email-hard-cap/08-RESEARCH.md
@.planning/phases/08-email-hard-cap/08-VALIDATION.md

# Patterns to mirror (test style)
@app/api/chat/__tests__/route.test.ts
@inngest/functions/__tests__/sync-products.test.ts
@lib/db/repositories/__tests__
@services/chat/__tests__
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 8 standalone RED test files</name>
  <files>lib/email/templates/__tests__/sync-success-email.test.tsx, lib/email/templates/__tests__/sync-failure-email.test.tsx, services/email/__tests__/EmailService.test.ts, services/shopify/__tests__/ShopifyShopService.test.ts, lib/db/repositories/__tests__/RequestCounterRepository.test.ts, services/chat/__tests__/CapService.test.ts, lib/util/__tests__/period.test.ts, lib/chat/__tests__/cap-reached-response.test.ts</files>
  <action>Create each test file as a RED scaffold importing the (not-yet-existing) module under test. Use Vitest + @testing-library/react patterns identical to existing tests in app/api/chat/__tests__/route.test.ts and services/chat/__tests__/. Per file, author it() blocks covering exactly the behaviors enumerated in 08-RESEARCH.md §Validation Architecture Phase Requirements → Test Map and 08-VALIDATION.md Wave 0 Requirements: (a) sync-success-email.test.tsx — render template, assert productCount appears, admin button href === adminUrl prop, "Catalog sync complete" heading; (b) sync-failure-email.test.tsx — render template, assert errorMessage text node (auto-escaped per V5 Input Validation), retry button href === retryUrl prop, "Catalog sync failed" heading; (c) EmailService.test.ts — mock 'resend' module's Resend class; assert sendSyncSuccess calls resend.emails.send with from === process.env.RESEND_FROM_ADDRESS, subject === `Catalog sync complete — ${count} products`, second-arg options `{ idempotencyKey: 'sync-success/${syncRunId}' }`; assert thrown Error when result.error is set; mirror for sendSyncFailure with `sync-failure/${syncRunId}`; (d) ShopifyShopService.test.ts — mock shopifyClient.clients.Graphql; happy path returns 'owner@shop.com'; null contactEmail returns null; empty string returns null; thrown GraphQL error returns null (NOT throws, per D-05); (e) RequestCounterRepository.test.ts — mock prisma.$queryRaw with vi.spyOn; call tryConsume('shop.myshopify.com', '2026-05', 2000); assert the tagged-template strings array (1st arg) joined contains case-insensitive substrings 'INSERT INTO request_counter', 'ON CONFLICT (shop, period)', 'DO UPDATE', 'WHERE request_counter."requestCount" <', 'RETURNING'; assert returned { allowed: true, requestCount: 1 } when mock resolves [{ requestCount: 1 }] and { allowed: false } when mock resolves []; (f) CapService.test.ts — mock requestCounterRepository.tryConsume + getCurrentPeriod; unset env → cap === 2000; HARD_CAP_REQUESTS_PER_MONTH='500' → cap === 500; invalid env 'abc' or '-1' → cap === 2000; allowed:true / allowed:false pass-through; (g) period.test.ts — getCurrentPeriod(new Date('2026-05-01T00:00:00Z')) === '2026-05'; getCurrentPeriod(new Date('2026-12-31T23:59:59Z')) === '2026-12'; (h) cap-reached-response.test.ts — invoke capReachedResponse(); assert response.status === 200; parse the streaming body as text, assert it contains the literal CAP_REACHED_MESSAGE constant value, and the chunk types appear in order 'start', 'text-start', 'text-delta', 'text-end', 'finish'. All tests MUST import from @/ paths and use the exact module symbols that 08-04 through 08-09 will export — per the Resolved Claude's-Discretion items in CONTEXT.md (EmailService at services/email/EmailService.ts, period helper at lib/util/period.ts, cap-reached helper at lib/chat/cap-reached-response.ts).</action>
  <verify>
    <automated>bunx vitest run lib/email/templates/__tests__ services/email/__tests__ services/shopify/__tests__ lib/db/repositories/__tests__/RequestCounterRepository.test.ts services/chat/__tests__/CapService.test.ts lib/util/__tests__/period.test.ts lib/chat/__tests__/cap-reached-response.test.ts 2>&1 | tee /tmp/p8-w0.log; grep -E "Cannot find module|is not defined|expected" /tmp/p8-w0.log | head -20</automated>
  </verify>
  <done>All 8 files exist; running them produces RED output with "Cannot find module" or "is not a function" errors from the missing implementations. No test exits as PASS (would indicate the test file is empty / vacuous).</done>
</task>

<task type="auto">
  <name>Task 2: Extend the 3 existing test files + author 1 optional gated integration test</name>
  <files>inngest/functions/__tests__/sync-products.test.ts, app/api/chat/__tests__/route.test.ts, app/api/proxy/chat/__tests__/route.test.ts, lib/db/repositories/__tests__/RequestCounterRepository.race.integration.test.ts</files>
  <action>Append RED it() blocks to the 3 existing test files (DO NOT delete or rewrite existing tests). For sync-products.test.ts: add describe('Phase 8 — completion emails', ...) with 5 it() blocks — 'sends success email after finalize when emailSentAt is null', 'sends failure email inside onFailure when emailSentAt is null', 'skips success email when emailSentAt is already set', 'skips success email when contactEmail is null (per D-05) and does NOT throw', 'uses distinct step IDs send-success-email and send-failure-email (Pitfall 2)'. Each block mocks @/services/email/EmailService.sendSyncSuccess and sendSyncFailure (vi.fn), mocks @/services/shopify/ShopifyShopService.fetchShopContactEmail, and exercises the Inngest function via the existing @inngest/test harness pattern Phase 2 established. For app/api/chat/__tests__/route.test.ts: add describe('Phase 8 — hard cap', ...) with 2 blocks — 'allowed: true → streamText path reached (no cap-reached body)' and 'allowed: false → HTTP 200 + body contains CAP_REACHED_MESSAGE'; mock @/services/chat/CapService.tryConsumeRequest. For app/api/proxy/chat/__tests__/route.test.ts: mirror the same 2 blocks placed after the existing HMAC/rate-limit/customer-id-match coverage; the cap check fires after those gates per the injection contract. For the OPTIONAL race file: create lib/db/repositories/__tests__/RequestCounterRepository.race.integration.test.ts that calls describe.skipIf(!process.env.INTEGRATION_DB_URL)('race condition (real Postgres)', ...) with one it() that seeds (shop, period, requestCount = cap - 1), kicks off 200 Promise.all tryConsume(shop, period, cap) calls, and asserts exactly 1 returns allowed:true and 199 return allowed:false. Document at the top of the file: "Deferred-acceptable per 08-CONTEXT.md Open Question 3 resolution — runs when INTEGRATION_DB_URL is set, otherwise SKIPPED and the verification gate substitutes a manual-smoke entry."</action>
  <verify>
    <automated>bunx vitest run inngest/functions/__tests__/sync-products.test.ts app/api/chat/__tests__/route.test.ts app/api/proxy/chat/__tests__/route.test.ts 2>&1 | grep -E "FAIL|✗|expected" | head -10; bunx vitest run lib/db/repositories/__tests__/RequestCounterRepository.race.integration.test.ts 2>&1 | grep -E "skip|SKIP" | head -3</automated>
  </verify>
  <done>Phase-8 it() blocks in the 3 extended files fail RED with module-not-found / mock-target-missing errors. The race file is SKIPPED (no INTEGRATION_DB_URL) without crashing the suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → fs | Test scaffolds touch only `__tests__/` directories — no production source modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-T1 | Tampering | Test scaffolds shadowing real assertions | mitigate | Tests assert specific symbols / chunk types / SQL substrings — vacuous it.todo() forbidden |
| T-08-01-I1 | Information disclosure | Test fixtures containing real shop / contact emails | mitigate | Fixtures use literal strings 'test-shop.myshopify.com' / 'owner@example.com' only |
| T-08-01-SC | Tampering | npm/pip/cargo installs | n/a | No new packages installed in this plan (resend / @react-email/* installed in 08-04) |
</threat_model>

<verification>
- `bunx vitest run` — Phase-8 new/extended tests are RED; pre-existing Phase 1-7 tests remain GREEN
- File-existence grep matches all 12 paths in `files_modified`
</verification>

<success_criteria>
- 11 mandatory test files exist and are RED
- 1 optional gated test exists and is SKIPPED when INTEGRATION_DB_URL is unset
- No pre-existing test changes from RED to GREEN as a side effect of this plan
- 08-VALIDATION.md `Wave 0 Requirements` checklist items map 1:1 to created files
</success_criteria>

<output>
Create `.planning/phases/08-email-hard-cap/08-01-SUMMARY.md` when done, listing each created file + its anchoring requirement ID(s) + RED-status confirmation.
</output>
