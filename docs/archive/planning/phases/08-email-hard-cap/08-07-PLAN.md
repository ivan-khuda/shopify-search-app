---
phase: 08-email-hard-cap
plan: 07
type: execute
wave: 4
depends_on: ["08-01", "08-03"]
files_modified:
  - lib/db/repositories/RequestCounterRepository.ts
autonomous: true
requirements: [CAP-01, CAP-02, CAP-03]
tags: [phase-08, repository, prisma, atomic-sql, cap]
must_haves:
  truths:
    - "lib/db/repositories/RequestCounterRepository.ts exports class RequestCounterRepository + singleton requestCounterRepository"
    - "tryConsume(shop, period, cap) issues a single $queryRaw INSERT … ON CONFLICT … DO UPDATE … WHERE … RETURNING statement (race-free per Pattern 4)"
    - "Returns { allowed: true, requestCount } when the upsert succeeds; { allowed: false } when zero rows return"
    - "Class shape mirrors ShopSettingsRepository (Pattern 6 — typed methods + singleton export)"
    - "Wave 0 RequestCounterRepository.test.ts goes GREEN (SQL shape assertions on the tagged-template strings array)"
  artifacts:
    - path: "lib/db/repositories/RequestCounterRepository.ts"
      provides: "Atomic per-shop, per-period counter primitive — the single race-free entrypoint for cap enforcement"
      exports: ["RequestCounterRepository", "requestCounterRepository"]
  key_links:
    - from: "tryConsume"
      to: "prisma.$queryRaw"
      via: "Tagged-template literal with INSERT … ON CONFLICT (shop, period) DO UPDATE … WHERE … RETURNING"
      pattern: "ON CONFLICT \\(shop, period\\)\\s+DO UPDATE"
---

<objective>
Author the atomic counter primitive. This is the most security-critical file in Phase 8: a SELECT-then-UPDATE shape would create a race window allowing two concurrent requests to both pass the cap when the counter is at cap-1. The single Postgres `INSERT … ON CONFLICT … DO UPDATE … WHERE … RETURNING` statement (Pattern 4) is documented atomic per Postgres docs and is the only acceptable shape per CONTEXT.md D-11 + the Anti-Patterns block in RESEARCH.

Purpose: Provide the single race-free entry for cap enforcement. CapService (08-10) is a thin shell over this repository.

Output: One new file + Wave 0 SQL-shape test goes GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-email-hard-cap/08-CONTEXT.md
@.planning/phases/08-email-hard-cap/08-RESEARCH.md
@lib/db/repositories/ShopSettingsRepository.ts
@lib/db/repositories/ProductRepository.ts
@lib/db/repositories/__tests__/RequestCounterRepository.test.ts
@lib/db/client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author lib/db/repositories/RequestCounterRepository.ts</name>
  <files>lib/db/repositories/RequestCounterRepository.ts</files>
  <behavior>
    - Exports class RequestCounterRepository with method tryConsume(shop: string, period: string, cap: number): Promise<{ allowed: true; requestCount: number } | { allowed: false }>
    - Exports singleton requestCounterRepository = new RequestCounterRepository()
    - tryConsume issues prisma.$queryRaw with the tagged-template literal containing exactly: INSERT INTO request_counter (shop, period, "requestCount", "updatedAt") VALUES (${shop}, ${period}, 1, NOW()) ON CONFLICT (shop, period) DO UPDATE SET "requestCount" = request_counter."requestCount" + 1, "updatedAt" = NOW() WHERE request_counter."requestCount" < ${cap} RETURNING "requestCount"
    - Returns { allowed: true, requestCount: rows[0].requestCount } when rows.length > 0; { allowed: false } when rows.length === 0
    - Includes JSDoc citing Pattern 4 + Anti-Pattern 1 (no SELECT-then-UPDATE) + Postgres ON CONFLICT atomicity guarantee
    - No console.* calls
    - Optional get(shop, period) via prisma.requestCounter.findUnique for observability — keep tryConsume as the hot path
  </behavior>
  <action>Implement verbatim per 08-RESEARCH.md §Code Examples "RequestCounterRepository". Column names in the SQL MUST use the case-sensitive quoted form: "requestCount" + "updatedAt" — Prisma's @@map("request_counter") gives a snake_case table name but field names default to camelCase quoted identifiers in Postgres. Use $queryRaw — NOT $executeRaw — because we need RETURNING rows back (Pitfall: $executeRaw discards the result set). Interpolation MUST use the tagged-template form with ${shop}, ${period}, ${cap} — these are parameter-bound (Prisma docs confirm SQL-injection safe). Do NOT use Prisma.sql + concat patterns; do NOT use string concatenation; do NOT use prisma.requestCounter.upsert() (Anti-Pattern 5 — typed upsert cannot express WHERE count < cap). Add the singleton export at module bottom: export const requestCounterRepository = new RequestCounterRepository();</action>
  <verify>
    <automated>bunx vitest run lib/db/repositories/__tests__/RequestCounterRepository.test.ts 2>&1 | grep -E "✓|passed" | head -5; ! grep -E "console\\." lib/db/repositories/RequestCounterRepository.ts; ! grep -E "\\$executeRaw|prisma\\.requestCounter\\.upsert" lib/db/repositories/RequestCounterRepository.ts</automated>
  </verify>
  <done>Wave 0 test GREEN; the SQL substrings match the assertion regex; no $executeRaw and no typed upsert; zero console.* calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → tryConsume(shop, period, cap) | All three args are caller-supplied; shop is trusted by contract (CapService derives from authenticated session) |
| tryConsume → Postgres | $queryRaw tagged template is parameter-bound — SQL-injection safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-T1 | Tampering / DoS | Counter-bypass race at cap-1 | mitigate | Single-statement atomic INSERT … ON CONFLICT … DO UPDATE … WHERE … RETURNING (Pattern 4); Postgres serializes conflict resolution — verified by SC4 unit test + optional race integration test |
| T-08-07-T2 | Tampering | Cross-shop counter mutation | mitigate | Composite PK (shop, period) makes cross-shop writes structurally impossible — every UPDATE targets exactly the row for the bound (shop, period) tuple |
| T-08-07-T3 | Tampering | SQL injection via shop / period args | mitigate | Tagged-template $queryRaw parameter-binds all ${} interpolations (Prisma docs) |
| T-08-07-T4 | Tampering | $executeRaw silently discarding RETURNING | mitigate | Verify command greps explicitly for $executeRaw use — fails if present |
</threat_model>

<verification>
- bunx vitest run lib/db/repositories/__tests__/RequestCounterRepository.test.ts GREEN
- grep -E "ON CONFLICT \\(shop, period\\)" returns the file path
- grep -E "RETURNING" returns the file path
- No $executeRaw or prisma.requestCounter.upsert anywhere
</verification>

<success_criteria>
- CAP-01 satisfied: atomic per-shop, per-period counter primitive exists
- SC4 race-freeness assertion proven at SQL-shape level (Wave 0 test)
- Wave 0 test flips RED → GREEN
</success_criteria>

<output>
Create `.planning/phases/08-email-hard-cap/08-07-SUMMARY.md` listing the exports + SQL shape evidence + Wave 0 status flip.
</output>
