---
phase: 08-email-hard-cap
plan: 08
type: execute
wave: 4
depends_on: ["08-01"]
files_modified:
  - lib/util/period.ts
autonomous: true
requirements: [CAP-02]
tags: [phase-08, util, period]
must_haves:
  truths:
    - "lib/util/period.ts exports getCurrentPeriod(now?: Date): string"
    - "Default arg now = new Date() — DI-friendly for testing without vi.useFakeTimers (Pitfall 7)"
    - "Returns YYYY-MM in UTC via now.toISOString().slice(0, 7) — pure, no timezone library (D-12)"
    - "Wave 0 period.test.ts goes GREEN"
  artifacts:
    - path: "lib/util/period.ts"
      provides: "getCurrentPeriod helper — single source of truth for the YYYY-MM string derivation"
      exports: ["getCurrentPeriod"]
  key_links:
    - from: "getCurrentPeriod(now)"
      to: "now.toISOString().slice(0, 7)"
      via: "ISO format is UTC by construction — no local-time bug (Pitfall 7)"
      pattern: "toISOString\\(\\)\\.slice\\(0, 7\\)"
---

<objective>
Author the single pure helper that maps a Date to a YYYY-MM (UTC) string. Trivial in code; load-bearing in correctness — Pitfall 7 documents that a naive `getFullYear() + '-' + getMonth()` would slip a month near midnight UTC for non-UTC servers.

Purpose: Centralize period derivation so CapService (08-10) and any future per-shop usage analytics (out of V1 scope) consume the same logic.

Output: One file with one function + Wave 0 test goes GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-email-hard-cap/08-CONTEXT.md
@.planning/phases/08-email-hard-cap/08-RESEARCH.md
@lib/util/__tests__/period.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author lib/util/period.ts</name>
  <files>lib/util/period.ts</files>
  <behavior>
    - Exports getCurrentPeriod(now: Date = new Date()): string
    - Returns now.toISOString().slice(0, 7) — exactly 7 chars, "YYYY-MM" UTC
    - Pure / synchronous / no I/O / no Date.now() — only the arg
    - Default-arg DI pattern lets tests inject a fake `now` without vi.useFakeTimers
    - JSDoc comment cites Pitfall 7 (UTC-by-construction) and D-12 (calendar-month reset)
  </behavior>
  <action>Author the file: ```export function getCurrentPeriod(now: Date = new Date()): string { return now.toISOString().slice(0, 7); }``` with a JSDoc block above. NO timezone libraries (date-fns / dayjs / luxon) — pure native Date. The slice(0, 7) is byte-precise: ISO format is always "YYYY-MM-DDTHH:MM:SS.sssZ" so chars 0..6 are guaranteed YYYY-MM. No edge cases — Date.parse() of a slip date would still produce a valid ISO string; the function is total.</action>
  <verify>
    <automated>bunx vitest run lib/util/__tests__/period.test.ts 2>&1 | grep -E "✓|passed" | head -5</automated>
  </verify>
  <done>period.test.ts GREEN; file contains exactly one exported function; no imports beyond what's needed (likely zero imports).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → helper | now arg defaults to new Date() — no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-T1 | Tampering | Local-time-bug slipping a month at boundaries | mitigate | toISOString() is UTC by construction (Pitfall 7); test asserts boundary values 2026-12-31T23:59:59Z → '2026-12' |
</threat_model>

<verification>
- `bunx vitest run lib/util/__tests__/period.test.ts` GREEN
- File is < 20 LOC
</verification>

<success_criteria>
- D-12 satisfied: YYYY-MM UTC period derivation centralized
- Wave 0 test flips RED → GREEN
</success_criteria>

<output>
Create `.planning/phases/08-email-hard-cap/08-08-SUMMARY.md` (brief).
</output>
