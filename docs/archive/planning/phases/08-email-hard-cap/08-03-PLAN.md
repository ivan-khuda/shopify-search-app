---
phase: 08-email-hard-cap
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - prisma/migrations/{STAMP}_add_request_counter_and_email_sent_at/migration.sql
  - app/generated/prisma/**
autonomous: false
requirements: [CAP-01, NOT-01, NOT-02]
tags: [phase-08, migration, blocking, prisma-7-drift]
must_haves:
  truths:
    - "A new prisma/migrations/{STAMP}_add_request_counter_and_email_sent_at/ directory exists with hand-authored migration.sql"
    - "migration.sql contains ONLY: CREATE TABLE request_counter (...) + ALTER TABLE sync_runs ADD COLUMN emailSentAt — nothing else"
    - "Postgres has the new table and column applied (psql / Prisma Studio confirms)"
    - "Manual HNSW + GIN indexes from db/manual-indexes.sql remain in place AFTER the migration apply"
    - "bunx prisma generate produces a client that exposes prisma.requestCounter and SyncRun.emailSentAt"
    - "Existing automated tests that depend on Prisma client types still compile (bunx tsc --noEmit clean)"
  artifacts:
    - path: "prisma/migrations/{STAMP}_add_request_counter_and_email_sent_at/migration.sql"
      provides: "Hand-authored DDL for the Phase 8 schema delta"
      contains: "CREATE TABLE \"request_counter\""
  key_links:
    - from: "Option A migration pattern"
      to: "prisma/migrations/20260527161654_add_shop_settings/migration.sql"
      via: "Reuses the verbatim Phase 7 non-destructive sequence: prisma migrate diff → inspect → db execute → migrate resolve --applied → prisma generate → bun db:indexes"
      pattern: "prisma migrate resolve --applied"
---

<objective>
Apply the Phase 8 schema delta to Postgres via the Phase 7 Option A non-destructive migration pattern. The reason this plan is autonomous: false: Prisma 7.3 flags the manual HNSW + GIN indexes in `db/manual-indexes.sql` as drift on every `migrate dev` invocation. The wrong choice (`migrate dev` accept-reset) would DROP THE INDEXES. The right choice is the surgical 8-step sequence below, identical to what Phase 7 used for `ShopSettings`.

Purpose: Establish the `request_counter` table and the `sync_runs.emailSentAt` column so downstream plans (08-04 through 08-14) can compile against the regenerated Prisma client.

Output: New migration folder + applied DDL + regenerated Prisma client + re-affirmed manual indexes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/08-email-hard-cap/08-CONTEXT.md
@.planning/phases/08-email-hard-cap/08-RESEARCH.md
@prisma/schema.prisma
@prisma/migrations/20260527161654_add_shop_settings/migration.sql
@db/manual-indexes.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the surgical DDL diff and inspect it</name>
  <files>/tmp/p8/diff.sql</files>
  <action>Create /tmp/p8/ if missing. Run: `bunx prisma migrate diff --from-config-datasource --to-schema-datamodel prisma/schema.prisma --script > /tmp/p8/diff.sql`. Open the output and confirm it contains ONLY two statements: (1) `CREATE TABLE "request_counter" (...)` with composite PK `PRIMARY KEY ("shop", "period")`, and (2) `ALTER TABLE "sync_runs" ADD COLUMN "emailSentAt" TIMESTAMP(3)`. If the diff contains ANY other statement (DROP, CREATE INDEX on manual-indexes targets, ALTER on unrelated tables) — STOP. The schema is contaminated; revert and re-do 08-02. Note: the timestamp on the new column must be `TIMESTAMP(3)` not `TIMESTAMP` — Prisma 7 emits this for `DateTime?` fields.</action>
  <verify>
    <automated>test -f /tmp/p8/diff.sql && grep -cE 'CREATE TABLE "request_counter"|ALTER TABLE "sync_runs" ADD COLUMN "emailSentAt"' /tmp/p8/diff.sql | grep -q "^2$" && ! grep -E 'DROP|hnsw|gin|tsvector|product_embeddings' /tmp/p8/diff.sql</automated>
  </verify>
  <done>diff.sql contains exactly the two expected DDL statements and zero references to manual-index targets. Inspection complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Surgical DDL diff in /tmp/p8/diff.sql — staged but NOT yet applied to Postgres.</what-built>
  <how-to-verify>1. `cat /tmp/p8/diff.sql` — confirm the file contains ONLY the two expected statements (CREATE TABLE request_counter + ALTER TABLE sync_runs ADD COLUMN emailSentAt). 2. Confirm no extra DROP / CREATE INDEX / ALTER statements appear. 3. The operator running /gsd-execute-phase authorizes the apply step on the user's behalf — Phase 7 ran the same checkpoint successfully on 2026-05-27 (STATE.md Phase 7 entry). 4. Reply 'approved' to advance to Task 3.</how-to-verify>
  <resume-signal>Type "approved" to proceed with apply, or describe any unexpected DDL.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Create migration folder, apply DDL non-destructively, mark applied, regenerate client, re-affirm indexes</name>
  <files>prisma/migrations/{STAMP}_add_request_counter_and_email_sent_at/migration.sql, app/generated/prisma/**</files>
  <action>Execute the 6-step Option A sequence verbatim (matches Phase 7's pattern in prisma/migrations/20260527161654_add_shop_settings/migration.sql): (1) STAMP=$(date -u +%Y%m%d%H%M%S); mkdir -p prisma/migrations/${STAMP}_add_request_counter_and_email_sent_at; cp /tmp/p8/diff.sql prisma/migrations/${STAMP}_add_request_counter_and_email_sent_at/migration.sql. (2) Apply: `bunx prisma db execute --file prisma/migrations/${STAMP}_add_request_counter_and_email_sent_at/migration.sql --schema prisma/schema.prisma`. (3) Record: `bunx prisma migrate resolve --applied "${STAMP}_add_request_counter_and_email_sent_at"`. (4) Regenerate: `bunx prisma generate`. (5) Re-affirm manual indexes: `bun db:indexes` (idempotent — these are CREATE INDEX IF NOT EXISTS statements). (6) Verify the new symbols exist in the generated client: run a one-shot `bunx tsx -e "import { PrismaClient } from './app/generated/prisma/client'; const p = new PrismaClient(); console.log(typeof p.requestCounter); p.\\$disconnect();"` — must print `object` (not `undefined`). Also verify SyncRun.emailSentAt: `bunx tsx -e "import type { SyncRun } from './app/generated/prisma/client'; const r: SyncRun = {} as never; void r.emailSentAt;"` — should compile clean (use bunx tsc --noEmit if tsx -e is awkward).</action>
  <verify>
    <automated>ls prisma/migrations | grep -q "_add_request_counter_and_email_sent_at$" && bunx prisma migrate status 2>&1 | grep -q "Database schema is up to date" && bunx tsc --noEmit 2>&1 | grep -v "node_modules" | grep -E "error TS" | head -5 && bun db:indexes 2>&1 | tail -3</automated>
  </verify>
  <done>Migration folder exists; Postgres has new table + column; Prisma client regenerated with prisma.requestCounter symbol present; SyncRun TypeScript type has emailSentAt?: Date | null; manual HNSW + GIN indexes re-affirmed idempotently; tsc --noEmit clean across repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration script → live DB | Direct DDL execution against the development Postgres |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-T1 | Tampering / Destructive | Manual HNSW + GIN indexes | mitigate | NEVER run `prisma migrate dev` — Option A pattern uses `db execute` + `migrate resolve --applied` which leaves db/manual-indexes.sql targets untouched. Verified by Phase 7. |
| T-08-03-T2 | Tampering | Unintended DDL leakage | mitigate | Task 1 inspects /tmp/p8/diff.sql for non-target statements; Task 2 checkpoint requires operator approval |
| T-08-03-D1 | Denial of service | Prisma 7 drift forcing a future reset | accept | The Option A pattern documented in STATE.md keeps the manual indexes outside Prisma's history; trade-off accepted per Phase 3 decision (db/manual-indexes.sql idempotent re-apply) |
| T-08-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan |
</threat_model>

<verification>
- `bunx prisma migrate status` reports up-to-date
- `prisma.requestCounter` symbol exists in regenerated client
- `bun db:indexes` exits 0 with HNSW + GIN re-affirmed
- `bunx tsc --noEmit` clean
</verification>

<success_criteria>
- New migration folder exists with name pattern `{STAMP}_add_request_counter_and_email_sent_at`
- Postgres `\d+ request_counter` shows PK (shop, period), columns shop TEXT, period TEXT, requestCount INTEGER, updatedAt TIMESTAMP(3)
- Postgres `\d+ sync_runs` includes new column `emailSentAt TIMESTAMP(3)` nullable
- Manual HNSW + GIN indexes from db/manual-indexes.sql remain in place
</success_criteria>

<output>
Create `.planning/phases/08-email-hard-cap/08-03-SUMMARY.md` recording: actual STAMP value, the verified DDL, the prisma generate output, the bun db:indexes output, and a confirmation that manual indexes survived. This SUMMARY is the canonical record that 08-04 onwards can compile against the new Prisma client.
</output>
