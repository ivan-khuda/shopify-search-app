---
phase: 08-email-hard-cap
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - prisma/schema.prisma
autonomous: true
requirements: [CAP-01, NOT-01, NOT-02]
tags: [phase-08, schema, prisma]
must_haves:
  truths:
    - "prisma/schema.prisma declares a new RequestCounter model with composite PK (shop, period) per D-08"
    - "SyncRun model gains a nullable emailSentAt DateTime column per D-04"
    - "No migration is generated or applied in this plan — that is 08-03's [BLOCKING] responsibility"
    - "Prisma generate succeeds against the edited schema (syntax-valid)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "RequestCounter model + SyncRun.emailSentAt column"
      contains: "model RequestCounter"
  key_links:
    - from: "model RequestCounter"
      to: "@@id([shop, period])"
      via: "composite primary key — structurally makes shop scoping implicit (V4 Access Control mitigation)"
      pattern: "@@id\\(\\[shop, period\\]\\)"
    - from: "model SyncRun"
      to: "emailSentAt DateTime?"
      via: "additive nullable column — NULL is the 'email not yet sent' sentinel per D-04"
      pattern: "emailSentAt\\s+DateTime\\?"
---

<objective>
Edit `prisma/schema.prisma` ONLY. Add the `RequestCounter` model (per D-08 / CAP-01) and append a nullable `emailSentAt DateTime?` field inside the existing `SyncRun` model (per D-04). Do NOT touch any other model. Do NOT run `prisma migrate dev` — Plan 08-03 is the [BLOCKING] migration plan and is autonomous: false on purpose.

Purpose: Stage the Prisma schema delta so 08-03 can extract the surgical DDL via `prisma migrate diff --from-config-datasource --to-schema-datamodel`. Splitting schema-edit from migration apply avoids accidentally letting `prisma migrate dev` touch the manual HNSW + GIN indexes (Pitfall 1, see Phase 7 STATE.md note).

Output: Two diff hunks in `prisma/schema.prisma`. `bunx prisma format` must succeed; `bunx prisma generate` must succeed (no DB connection required, schema-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-email-hard-cap/08-CONTEXT.md
@.planning/phases/08-email-hard-cap/08-RESEARCH.md
@prisma/schema.prisma
@prisma/migrations/20260527161654_add_shop_settings/migration.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SyncRun.emailSentAt + RequestCounter model</name>
  <files>prisma/schema.prisma</files>
  <action>Edit prisma/schema.prisma. First hunk: locate the existing `model SyncRun { ... }` block (around line 183). Insert exactly one new field line `emailSentAt    DateTime?  // Phase 8 D-04 — NULL = email not yet sent` immediately AFTER the existing `finishedAt      DateTime?` line and BEFORE the closing `}`. Field name MUST be `emailSentAt` (camelCase in schema; Prisma maps to "emailSentAt" column in Postgres given no explicit @map). Second hunk: append at the END of the file (after `model ShopSettings { ... }`), a new model exactly: `// Phase 8 D-08 / CAP-01: per-shop monthly request counter.\n// Empty by design — first chat request of a (shop, period) INSERTs the row\n// via the atomic upsert primitive in RequestCounterRepository.tryConsume.\nmodel RequestCounter {\n  shop         String\n  period       String   // YYYY-MM UTC — D-12\n  requestCount Int      @default(0)\n  updatedAt    DateTime @updatedAt\n\n  @@id([shop, period])\n  @@map("request_counter")\n}`. Do NOT add `@@index([shop])` — the composite PK already covers shop-prefix lookups. Do NOT add any other field. After editing, run `bunx prisma format` to canonicalize whitespace.</action>
  <verify>
    <automated>bunx prisma format && bunx prisma validate && grep -c "model RequestCounter" prisma/schema.prisma | grep -q "^1$" && grep -E "emailSentAt\s+DateTime\?" prisma/schema.prisma | grep -q "Phase 8"</automated>
  </verify>
  <done>Schema validates; `model RequestCounter` appears exactly once; SyncRun gains exactly one `emailSentAt DateTime?` field; no other model is modified (`git diff prisma/schema.prisma` shows only these two hunks).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema → DB | Migration application happens in 08-03 — this plan only stages the model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-T1 | Tampering | Accidental drift on unrelated models | mitigate | Edit scope strictly limited to SyncRun + new RequestCounter; verification command checks `model RequestCounter` count and field shape; reviewer enforces `git diff` is two hunks |
| T-08-02-I1 | Information disclosure | Cross-shop counter read | mitigate | Composite PK `(shop, period)` — see V4 Access Control mitigation in 08-RESEARCH.md §Security Domain. No `@@index` exposed for cross-shop scans. |
| T-08-02-E1 | Elevation of privilege | Implicit cascade from FK | accept | No FK from RequestCounter to other tables — keeps the counter independent of shop installation lifecycle (counter rows survive uninstall by design; cleanup is a future operational task) |
</threat_model>

<verification>
- `bunx prisma format` clean
- `bunx prisma validate` passes (syntactic)
- `git diff prisma/schema.prisma` shows two and only two hunks
</verification>

<success_criteria>
- `RequestCounter` model present with the exact field shape from D-08
- `SyncRun.emailSentAt DateTime?` present and nullable
- No migration files created
</success_criteria>

<output>
Create `.planning/phases/08-email-hard-cap/08-02-SUMMARY.md` documenting the two hunks and the explicit non-action (no migration generated).
</output>
