---
phase: 08-email-hard-cap
plan: 12
type: execute
wave: 5
depends_on: ["08-04", "08-05", "08-06", "08-03"]
files_modified:
  - inngest/functions/sync-products.ts
autonomous: true
requirements: [NOT-02]
tags: [phase-08, inngest, sync, failure-email, on-failure]
must_haves:
  truths:
    - "inngest/functions/sync-products.ts onFailure handler signature changes to ({ event, error, step }) — adds the step parameter"
    - "onFailure body gains a step.run('send-failure-email', ...) block AFTER the existing prisma.syncRun.update marking the run as failed"
    - "Step body: load offline session (mirrors main handler), fetch contactEmail, skip when null, build retryUrl from HOST env var, sendSyncFailure, atomic emailSentAt stamp"
    - "Step ID is 'send-failure-email' — DISTINCT from 'send-success-email' (Pitfall 2: distinct step IDs to avoid memoization conflation)"
    - "Wave 0 Inngest failure-email it() blocks go GREEN"
  artifacts:
    - path: "inngest/functions/sync-products.ts"
      provides: "Phase 2 sync function extended with the failure-email step inside onFailure"
      contains: "step.run('send-failure-email'"
  key_links:
    - from: "onFailure: async ({ event, error, step })"
      to: "step.run('send-failure-email', ...)"
      via: "Inngest onFailure handler accepts step parameter for idempotent failure-side ops (Pattern 2)"
      pattern: "step\\.run\\('send-failure-email'"
    - from: "retryUrl"
      to: "${process.env.HOST}/onboarding?retry=${syncRunId}"
      via: "D-06 deep link"
      pattern: "onboarding\\?retry="
---

<objective>
Extend the existing onFailure handler in inngest/functions/sync-products.ts to send the failure-branch email. Pattern 2: onFailure already runs as its own Inngest function execution (`inngest/function.failed` system event) and exposes `step` for idempotent side operations.

Critical pitfall to avoid (Pitfall 2): the success and failure paths share Inngest's step-ID memoization namespace. Step IDs MUST be `send-success-email` and `send-failure-email` (NOT both `send-completion-email`) — conflating them would let the success path's cached result mask a real failure-path retry.

Purpose: Wire the failure-branch notification per D-03 / NOT-02. Failure email is auxiliary to the sync result (Assumption A5 — failure-email failure does NOT bubble back to the user; sync is already marked failed).

Output: One file edit + Wave 0 failure-email Inngest tests go GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-email-hard-cap/08-CONTEXT.md
@.planning/phases/08-email-hard-cap/08-RESEARCH.md
@inngest/functions/sync-products.ts
@inngest/functions/__tests__/sync-products.test.ts
@services/email/EmailService.ts
@services/shopify/ShopifyShopService.ts
@lib/shopify/client.ts
@lib/shopify/session-storage.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend onFailure with the failure-email step</name>
  <files>inngest/functions/sync-products.ts</files>
  <behavior>
    - onFailure signature updated from `async ({ event, error })` to `async ({ event, error, step })` — step parameter is destructured from Inngest's onFailure context per Pattern 2
    - Existing `prisma.syncRun.update({ ..., state: 'failed', ... })` block remains byte-identical and runs FIRST inside onFailure (sync state is the contract; email is auxiliary)
    - After the existing update, NEW step.run('send-failure-email', async () => { ... }) block:
      (1) const run = await prisma.syncRun.findUnique({ where: { id: original.syncRunId } }); if (!run || run.emailSentAt) return; — idempotency check
      (2) const offlineId = shopifyClient.session.getOfflineId(original.shop); const session = await sessionStorage.loadSession(offlineId); if (!session) return; — graceful skip if session purged (uninstall) — D-05 spirit
      (3) const contactEmail = await fetchShopContactEmail(session); if (!contactEmail) return; — D-05
      (4) const retryUrl = `${process.env.HOST}/onboarding?retry=${original.syncRunId}`; — D-06 deep link; HOST is Assumption A2
      (5) await sendSyncFailure({ to: contactEmail, shop: original.shop, syncRunId: original.syncRunId, errorMessage: String(error?.message ?? error), retryUrl });
      (6) await prisma.$executeRaw`UPDATE sync_runs SET "emailSentAt" = NOW() WHERE id = ${original.syncRunId} AND "emailSentAt" IS NULL`;
    - Step ID 'send-failure-email' is DISTINCT from 'send-success-email' (Pitfall 2 — verified by Wave 0 'uses distinct step IDs' it() block)
    - Zero console.* additions
  </behavior>
  <action>Edit inngest/functions/sync-products.ts. Update imports at the top to also include sendSyncFailure: `import { sendSyncSuccess, sendSyncFailure } from '@/services/email/EmailService';` (extend the import added in 08-11). Modify the onFailure handler at lines 30-40: change the destructure from `({ event, error })` to `({ event, error, step })`. After the existing `await prisma.syncRun.update({ ... })` block but before the closing `}` of the onFailure async fn, append the new step.run block per behavior. The existing destructure `const original = (event.data as { event: { data: SyncEventData } }).event.data;` at line 31 stays unchanged. Use the existing imports for shopifyClient (line 10) and sessionStorage (line 11) — no new imports needed beyond sendSyncFailure + fetchShopContactEmail (latter added in 08-11). Note: do NOT wrap the whole onFailure in try/catch — Inngest will surface step failures in its dashboard, and Assumption A5 says failure-email send failures are acceptable to log/observe rather than retry-loop infinitely.</action>
  <verify>
    <automated>bunx vitest run inngest/functions/__tests__/sync-products.test.ts -t "failure email" 2>&1 | grep -E "✓|passed" | head -5; bunx vitest run inngest/functions/__tests__/sync-products.test.ts -t "distinct step IDs" 2>&1 | grep -E "✓|passed" | head -3; grep -c "step.run('send-failure-email'" inngest/functions/sync-products.ts | grep -q "^1$"</automated>
  </verify>
  <done>Wave 0 failure-email + distinct-step-IDs it() blocks GREEN; one 'send-failure-email' block exists; existing Phase 2/3 + 08-11 success-email tests STILL GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| onFailure step → external services | Same as 08-11 (Resend HTTPS, Shopify GraphQL, Postgres) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12-T1 | Tampering | Step-ID conflation (Pitfall 2) | mitigate | Step IDs 'send-success-email' vs 'send-failure-email' — DISTINCT; verified by dedicated Wave 0 it() block |
| T-08-12-T2 | Tampering | Open redirect via retryUrl | mitigate | URL constructed from server-side HOST env + syncRunId from the authenticated event payload — no user input in URL path |
| T-08-12-T3 | Tampering | Email-content injection via error.message | mitigate | SyncFailureEmail renders errorMessage as auto-escaped Text node (08-05); no dangerouslySetInnerHTML; React Email primitives auto-escape children |
| T-08-12-D1 | Denial of service | onFailure infinite retry loop on Resend outage | accept | onFailure has no retries-after-retries semantics per Inngest docs — throwing here just logs to dashboard (Assumption A5); sync is already marked failed |
| T-08-12-I1 | Information disclosure | error.message containing secrets | accept | Phase 2 error messages are GraphQL / Prisma / network errors; no secret-shaped content expected. If a future change introduces sensitive errors, the failure-email path becomes a leak surface — flagged for revisit but accepted in V1 |
</threat_model>

<verification>
- `bunx vitest run inngest/functions/__tests__/sync-products.test.ts` GREEN (Phase 2/3 + 08-11 + 08-12 all pass)
- `grep -c "step.run('send-(success|failure)-email')" inngest/functions/sync-products.ts == 2`
</verification>

<success_criteria>
- NOT-02 satisfied: failed sync sends email with reason + retry link
- D-04 idempotency: failure-email step is no-op when emailSentAt set
- D-05 graceful skip on missing session / contactEmail
- Pitfall 2 mitigated: distinct step IDs
</success_criteria>

<output>
Create `.planning/phases/08-email-hard-cap/08-12-SUMMARY.md` summarizing the onFailure delta and Wave 0 status flips.
</output>
