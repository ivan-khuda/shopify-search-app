---
phase: 07-admin-settings-model-picker
plan: 02
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - prisma/schema.prisma
  - types/shopify-global.d.ts
autonomous: true
requirements: [ADM-03, ADM-04]
must_haves:
  truths:
    - "Prisma schema declares a ShopSettings model exactly as specified in D-10 (shop @id, activeChatModelId, updatedAt; @@map shop_settings)"
    - "TypeScript JSX intrinsic declarations cover every Polaris s-* / ui-* tag the /settings page renders (s-table family, s-choice-list, s-choice, ui-save-bar) so Plan 08's UI builds under tsc strict"
    - "No data backfill is required — the ShopSettings table is empty by design (D-09)"
  artifacts:
    - path: prisma/schema.prisma
      provides: "ShopSettings Prisma model — the read source for getActiveChatModel and the write target for the PATCH route"
      contains: "model ShopSettings"
    - path: types/shopify-global.d.ts
      provides: "JSX intrinsic augmentation for s-table-*, s-choice-list, s-choice, ui-save-bar (Polaris + App Bridge web components used on /settings)"
  key_links:
    - from: prisma/schema.prisma
      to: lib/db/repositories/ShopSettingsRepository.ts (Plan 05)
      via: prisma.shopSettings.findUnique / upsert
      pattern: "model ShopSettings"
    - from: types/shopify-global.d.ts
      to: app/(embedded)/settings/settings-form.tsx (Plan 08)
      via: React.JSX.IntrinsicElements augmentation
      pattern: "'s-table'"
---

<objective>
Add the `ShopSettings` Prisma model to the schema and augment the JSX intrinsic declarations for the Polaris s-table family + s-choice-list + ui-save-bar. This is a pure schema/types edit — no migration is applied here. Plan 03 is the [BLOCKING] task that runs `bunx prisma migrate dev` against the new schema and regenerates the client.

Purpose: Land the schema diff and TypeScript surface so Wave 2 (catalog client, repository, resolver body swap) and Wave 3 (route, page, settings-form) can typecheck and import `prisma.shopSettings` + render `<s-table>` etc. without "Property does not exist" errors. Splitting schema-write from schema-apply is the same `[BLOCKING]` pattern used in Plans 01-05, 02-04, 03-05, 06-02.

Output: 2 modified files (schema + types). No new files. No client regen yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-admin-settings-model-picker/07-CONTEXT.md
@.planning/phases/07-admin-settings-model-picker/07-RESEARCH.md

@prisma/schema.prisma
@types/shopify-global.d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append ShopSettings model to prisma/schema.prisma (D-10 exact shape)</name>
  <files>prisma/schema.prisma</files>
  <action>
    Append a new model block to `prisma/schema.prisma`. The exact shape is locked by D-10 in CONTEXT.md and verified against RESEARCH.md §Code Examples. Insert AFTER the final existing model (`VisitorCustomerLink`) to keep schema diff minimal.

    Required fields (no additions, no renames):
    - `shop String @id` — the shop hostname is the PK; one row per shop, multi-tenancy lock is structural (no need for `@@index([shop])` since it's already the PK)
    - `activeChatModelId String` — the AI Gateway model id (e.g., `google/gemini-2.5-flash`); length intentionally unconstrained at the schema level — Zod max(200) is the application-tier gate (Plan 07)
    - `updatedAt DateTime @updatedAt` — doubles as audit signal per D-10; no separate `createdAt`
    - `@@map("shop_settings")` — snake_case table name matches the project convention (`products`, `shopify_sessions`, etc.)

    Do NOT add: `customerId`, `createdAt`, `displayName` (catalog is the source of truth — RESEARCH §Pattern 3), `provider`, or any other column. Premature columns block the D-10 contract and complicate Phase 8.

    Do NOT add an index on `shop` — it is the PK and Postgres builds a btree automatically.
  </action>
  <verify>
    <automated>bunx prisma validate 2>&1 | tail -20</automated>
  </verify>
  <done>`prisma validate` reports success. `grep -n 'model ShopSettings' prisma/schema.prisma` returns exactly one match. The block contains `shop String @id`, `activeChatModelId String`, `updatedAt DateTime @updatedAt`, and `@@map("shop_settings")` — no extra fields.</done>
</task>

<task type="auto">
  <name>Task 2: Augment React.JSX.IntrinsicElements with Polaris s-table family + s-choice-list + ui-save-bar</name>
  <files>types/shopify-global.d.ts</files>
  <action>
    Append the following intrinsic tag declarations inside the existing `interface IntrinsicElements { ... }` block in `types/shopify-global.d.ts` (after the last `'s-banner'` line). Match the existing one-tag-per-line `PolarisIntrinsicProps` pattern.

    Tags to add (each typed as `PolarisIntrinsicProps`):
    - `'s-table'`
    - `'s-table-header-row'`
    - `'s-table-header'`
    - `'s-table-body'`
    - `'s-table-row'`
    - `'s-table-cell'`
    - `'s-choice-list'`
    - `'s-choice'`
    - `'ui-save-bar'`
    - `'s-text-field'` (defensive — referenced by Polaris docs in case the settings page or future Phase 8 surface needs it; cheap to add now)

    Do NOT alter the existing `PolarisIntrinsicProps` type, `ShopifyRuntimeGlobal`, or any other declaration. The augmentation must remain inside `namespace React.JSX`.

    Why this is a types-only file edit and not a code edit: Polaris web components are loaded at runtime via the CDN `<Script>` tag in `app/(embedded)/layout.tsx` (verified in RESEARCH.md §Standard Stack). The TypeScript compiler does not know about them unless we extend `JSX.IntrinsicElements`. Without this augmentation, `tsc --noEmit` on Plan 08's `settings-form.tsx` errors with `Property 's-table' does not exist on type 'JSX.IntrinsicElements'`.
  </action>
  <verify>
    <automated>bunx tsc --noEmit 2>&1 | grep -E "(error TS|Property 's-(table|choice|table-row)'|^$)" | head -20; echo "---"; grep -c "'s-table'" types/shopify-global.d.ts</automated>
  </verify>
  <done>`tsc --noEmit` does not error on the new declarations (existing errors unrelated to Phase 7 are acceptable; no new errors introduced). The grep returns ≥1 for `'s-table'`. All 10 new tags are present in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prisma schema → live DB | `bunx prisma migrate dev` (Plan 03) materializes the schema diff; until then no DB change exists |
| TypeScript declaration file → JSX rendering | Augmented intrinsics are advisory at compile time; runtime correctness depends on Polaris CDN script load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | Schema diff omits `@updatedAt`, allowing a write to bypass the audit timestamp | mitigate | The `@updatedAt` directive is Prisma-managed and cannot be overridden by the application layer; `prisma validate` enforces presence |
| T-07-02-02 | Repudiation | Adding `createdAt` against D-10 reduces the audit clarity (now two timestamps tell different stories) | mitigate | D-10 explicitly forbids `createdAt`; reviewer must confirm the model has exactly 3 columns |
| T-07-02-03 | Information disclosure | Schema-level `@unique` on `activeChatModelId` would force-share model selection across all shops if misapplied | mitigate | The schema uses `shop String @id` (the PK) — `activeChatModelId` is intentionally NOT unique; this is the multi-tenancy lock at the schema level |
| T-07-02-04 | Tampering | A misnamed `@@map` (e.g., `"shopsettings"` without underscore) silently creates a table the DB clients don't expect | mitigate | The exact string `"shop_settings"` is asserted by Plan 03's verify step (psql `\dt` lookup); Plan 02's verify catches it via `prisma validate` plus a grep |
| T-07-02-SC | Tampering | npm installs in this plan | accept | No new packages — schema + types edit only |
</threat_model>

<verification>
After both tasks:
- `bunx prisma validate` succeeds
- `bunx tsc --noEmit` does not report any new errors related to Phase 7 surfaces (some pre-existing errors from unrelated files such as `components/ai-elements/reasoning.tsx` are tracked separately and out of scope here)
- Schema diff (one model block appended) is reviewed in `git diff prisma/schema.prisma`
</verification>

<success_criteria>
- `prisma/schema.prisma` contains `model ShopSettings { shop String @id; activeChatModelId String; updatedAt DateTime @updatedAt; @@map("shop_settings") }` and nothing else under that block
- `types/shopify-global.d.ts` lists all 10 new JSX intrinsic tags
- No file outside `prisma/schema.prisma` and `types/shopify-global.d.ts` is touched
- No client regeneration is run in this plan — Plan 03 owns that step
</success_criteria>

<output>
Create `.planning/phases/07-admin-settings-model-picker/07-02-SUMMARY.md` documenting:
- The exact `ShopSettings` block appended (3-line model body)
- Confirmation of `prisma validate` success and the list of 10 JSX tags added
- Handoff note: Plan 03 is [BLOCKING] for everything in Wave 2 and beyond
</output>
