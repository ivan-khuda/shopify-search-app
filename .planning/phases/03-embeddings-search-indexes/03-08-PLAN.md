---
phase: 03-embeddings-search-indexes
plan: 08
type: execute
wave: 5
depends_on: ["03-02", "03-03", "03-04", "03-05", "03-06", "03-07"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/03-embeddings-search-indexes/03-VERIFICATION.md
autonomous: false
requirements: [EMB-01, EMB-02, EMB-03, EMB-04, EMB-06]
tags: [verification, gate, smoke-test, phase-completion]

must_haves:
  truths:
    - "Full vitest suite is green (Phase 1 + Phase 2 + Phase 3 — zero regressions)"
    - "TypeScript strict compile produces no NEW errors from Phase 3 files"
    - "EXPLAIN ANALYZE on a shop-scoped pgvector cosine query, wrapped in withHnswIterativeScan, shows Index Scan using product_embeddings_embedding_hnsw_idx (NOT Seq Scan)"
    - "Inside withHnswIterativeScan, SELECT current_setting('hnsw.iterative_scan', true) returns 'relaxed_order'"
    - "Re-running bun db:indexes after the verification smoke test still succeeds idempotently (EMB-04)"
    - "ROADMAP.md Phase 3 row is checked off; REQUIREMENTS.md status for EMB-01..04, EMB-06 is updated to Complete; STATE.md current_phase advances to 4"
    - "Human has visually confirmed (via psql or Prisma Studio) that at least one ProductEmbedding row exists with a populated modelVersion column equal to 'openai/text-embedding-3-small'"
  artifacts:
    - path: .planning/phases/03-embeddings-search-indexes/03-VERIFICATION.md
      provides: "Phase 3 verification report: test counts, EXPLAIN ANALYZE output, idempotency check output, GUC assertion output, manual sanity-check evidence"
    - path: .planning/ROADMAP.md
      provides: "Phase 3 marked [x] Complete with completion date"
    - path: .planning/REQUIREMENTS.md
      provides: "EMB-01..04, EMB-06 marked Complete in traceability table"
    - path: .planning/STATE.md
      provides: "current_phase=4, completed_phases incremented, last_activity updated"
  key_links:
    - from: .planning/phases/03-embeddings-search-indexes/03-VERIFICATION.md
      to: .planning/ROADMAP.md
      via: confirms all 4 Phase 3 success criteria are TRUE
      pattern: "EMB-01.*EMB-04.*EMB-06"
---

<objective>
Run the Phase 3 verification gate: automated tests + tsc + database smoke tests for EMB-04 (HNSW index used) and EMB-06 (iterative_scan GUC applied), idempotency re-check, and ROADMAP/STATE/REQUIREMENTS roll-forward. End the phase with a human-verify checkpoint confirming the operator has seen live ProductEmbedding rows with pinned modelVersion values.

Purpose: Phase 3 is infrastructure-only — there is no merchant-facing UI to "click through". The verification gate is the only mechanism that proves the four Phase 3 success criteria from ROADMAP.md are TRUE in the live system, not just in mocked unit tests. The DB-side smoke tests cover what unit tests structurally cannot (the GUC value inside a real transaction, the HNSW index actually being used by the planner).

Output: 03-VERIFICATION.md with full evidence; ROADMAP/STATE/REQUIREMENTS updated; human checkpoint signed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-embeddings-search-indexes/03-CONTEXT.md
@.planning/phases/03-embeddings-search-indexes/03-VALIDATION.md
@.planning/phases/03-embeddings-search-indexes/03-RESEARCH.md
@CLAUDE.md
@db/manual-indexes.sql
@scripts/apply-manual-indexes.ts
@lib/db/hnsw.ts
@services/embeddings/EmbeddingService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full vitest suite + tsc; record results in 03-VERIFICATION.md</name>
  <read_first>
    - .planning/phases/03-embeddings-search-indexes/03-VALIDATION.md (the validation contract — sampling rate, framework, manual-only items)
    - .planning/phases/03-embeddings-search-indexes/03-RESEARCH.md §"Validation Architecture" lines 756-815 (Phase Requirements → Test Map)
    - All Phase 3 SUMMARYs (03-01 through 03-07) to gather test counts per plan
  </read_first>
  <action>Create `.planning/phases/03-embeddings-search-indexes/03-VERIFICATION.md` with a header block (phase, gate_date, gate_status: in_progress). Run these commands in order, capturing exact output into the file:
    1. `bun test 2>&amp;1 | tee /tmp/phase3-suite.txt` — full vitest run. Record the final summary line (e.g. `Test Files  X passed (X)  Tests  Y passed (Y)`). Assert Y >= Phase 2 baseline of 95 tests + ~30 new Phase 3 tests = expect ≥ 120 tests passing; 0 failures, 0 skipped.
    2. `bunx tsc --noEmit 2>&amp;1 | tee /tmp/phase3-tsc.txt` — full type check. Filter out pre-existing ambient errors (`grep -v 'ambient\|reasoning.tsx'`). Assert zero NEW errors in any Phase 3 file: services/embeddings/, services/search/, lib/db/hnsw.ts, inngest/functions/sync-products.ts, app/api/shopify/webhook/route.ts, scripts/apply-manual-indexes.ts.
    3. `bunx vitest run services/embeddings services/search lib/db/__tests__/hnsw.test.ts inngest/functions/__tests__/sync-products.test.ts app/api/shopify/webhook/__tests__/route.test.ts --reporter=verbose 2>&amp;1 | tee /tmp/phase3-targeted.txt` — targeted run of Phase 3 surfaces only. Record exact pass count per file.
    Write the captured outputs into 03-VERIFICATION.md under `## Automated Verification` with subsections: Full Suite, TypeScript, Phase 3 Targeted. If ANY step fails, stop and write `gate_status: blocked` plus the failure transcript; do not proceed to Task 2.</action>
  <verify>
    <automated>bun test 2>&amp;1 | grep -E "Test Files.*passed" &amp;&amp; bunx tsc --noEmit 2>&amp;1 | grep -v "ambient\|reasoning.tsx" | grep -E "(services/embeddings|services/search|lib/db/hnsw|inngest/functions/sync-products|app/api/shopify/webhook/route|scripts/apply-manual-indexes)" | grep "error TS"; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - 03-VERIFICATION.md exists
    - Full suite report shows ALL tests passing (0 failed, 0 errored)
    - Targeted Phase 3 run shows: EmbeddingService.test.ts ≥8 tests, searchableText.test.ts ≥5 tests, hnsw.test.ts ≥4 tests, sync-products.test.ts new "embed-batch step" block ≥5 tests, webhook route.test.ts new "embedding integration (Phase 3)" block ≥5 tests
    - TypeScript reports zero NEW errors in Phase 3 files
    - Three transcripts (suite, tsc, targeted) embedded in 03-VERIFICATION.md
  </acceptance_criteria>
  <done>Automated test layer green; ready for DB-side smoke tests.</done>
</task>

<task type="auto">
  <name>Task 2: DB smoke tests — HNSW index usage + iterative_scan GUC + idempotency</name>
  <read_first>
    - .planning/phases/03-embeddings-search-indexes/03-VALIDATION.md §"Manual-Only Verifications" (3 items: idempotency, HNSW index usage, GUC value)
    - .planning/phases/03-embeddings-search-indexes/03-RESEARCH.md §"Open Questions" Q3 lines 732-735 (the smoke-test recipe)
    - lib/db/hnsw.ts (the helper to invoke)
    - db/manual-indexes.sql (the idempotency target)
    - scripts/apply-manual-indexes.ts (the runner)
  </read_first>
  <action>Run these smoke tests against the dev DB and record the EXACT command outputs in 03-VERIFICATION.md under `## Database Smoke Tests`:

  Smoke 1 — Idempotency (EMB-04): Run `bun db:indexes 2>&amp;1 | tee /tmp/phase3-indexes-1.txt`. Expect `manual indexes applied`. Then run `bun db:indexes 2>&amp;1 | tee /tmp/phase3-indexes-2.txt` AGAIN. Expect the SAME `manual indexes applied` output with no `ERROR`, no `already exists`, no exit code non-zero. Diff the two transcripts and confirm they are identical modulo timestamps. Record both transcripts.

  Smoke 2 — Indexes exist (EMB-04): Run `bunx tsx -e "import { prisma } from '@/lib/db/client'; const rows = await prisma.\$queryRaw\`SELECT indexname, tablename FROM pg_indexes WHERE indexname IN ('product_embeddings_embedding_hnsw_idx', 'products_searchVector_gin_idx') ORDER BY indexname\`; console.log(JSON.stringify(rows, null, 2)); await prisma.\$disconnect()"`. Expect 2 rows. Record output.

  Smoke 3 — iterative_scan GUC inside helper (EMB-06): Run `bunx tsx -e "import { withHnswIterativeScan } from '@/lib/db/hnsw'; const r = await withHnswIterativeScan(async (tx) => tx.\$queryRaw\`SELECT current_setting('hnsw.iterative_scan', true) AS guc\`); console.log(JSON.stringify(r))"`. Expect output containing `\"guc\":\"relaxed_order\"`. Record output. If the value is empty string or null, the SET LOCAL did not land — STOP and surface the error (this would mean Pitfall 1 escaped, or pgvector < 0.8.0 silently ignored the GUC — Pitfall 7).

  Smoke 4 — HNSW Index Scan (EMB-04 + EMB-06): Insert a synthetic embedding row first so EXPLAIN has something to plan against. Run:
  ```
  bunx tsx -e "
  import { prisma } from '@/lib/db/client';
  import { withHnswIterativeScan } from '@/lib/db/hnsw';
  const vec = '[' + new Array(1536).fill(0).map((_,i)=>(i%10)/10).join(',') + ']';
  await prisma.\$executeRawUnsafe(\`INSERT INTO product_embeddings (shop, \"productShop\", \"productId\", content, embedding, \"modelVersion\", \"searchableText\", \"createdAt\") SELECT 'smoke.myshopify.com', 'smoke.myshopify.com', id, 'smoke', '\${vec}'::vector, 'openai/text-embedding-3-small', 'smoke', NOW() FROM products LIMIT 1 ON CONFLICT (shop, \"productShop\", \"productId\") DO UPDATE SET embedding = EXCLUDED.embedding;\`);
  const plan = await withHnswIterativeScan(async (tx) => tx.\$queryRaw\`EXPLAIN ANALYZE SELECT \"productId\" FROM product_embeddings WHERE shop = 'smoke.myshopify.com' ORDER BY embedding <=> \${vec}::vector LIMIT 10\`);
  console.log(JSON.stringify(plan));
  await prisma.\$disconnect();
  "
  ```
  Inspect the plan output. SUCCESS criterion: at least one line contains `Index Scan using product_embeddings_embedding_hnsw_idx`. If the planner picked a Seq Scan (because only 1 row exists and the optimizer thinks seq beats hnsw on tiny tables), insert at least 20-30 synthetic rows first via the same script, then re-run EXPLAIN. Record the final plan output and the row count used. If after 100 rows the planner STILL refuses HNSW, the iterative_scan GUC may not be taking effect — surface the issue and STOP.

  Each smoke test's exact command + output is recorded in 03-VERIFICATION.md under `## Database Smoke Tests` with subsections matching the smoke numbers.</action>
  <verify>
    <automated>bun db:indexes 2>&amp;1 | grep -q "manual indexes applied" &amp;&amp; bun db:indexes 2>&amp;1 | grep -q "manual indexes applied"</automated>
  </verify>
  <acceptance_criteria>
    - Smoke 1: Two consecutive `bun db:indexes` runs both succeed and produce identical output (EMB-04 idempotency proved)
    - Smoke 2: pg_indexes query returns exactly 2 rows for the named indexes
    - Smoke 3: `current_setting('hnsw.iterative_scan', true)` inside `withHnswIterativeScan(...)` returns the literal string `'relaxed_order'` (EMB-06 proved against real Postgres)
    - Smoke 4: EXPLAIN ANALYZE output contains `Index Scan using product_embeddings_embedding_hnsw_idx` (or, if the planner picks a different but-still-index-using strategy on tiny tables, the output is annotated and the operator decides whether to accept)
    - All four smoke transcripts are pasted verbatim into 03-VERIFICATION.md
  </acceptance_criteria>
  <done>The Phase 3 success criteria 3 and 4 from ROADMAP.md are empirically true against the live dev DB.</done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP, REQUIREMENTS, STATE</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 3 row + Progress section)
    - .planning/REQUIREMENTS.md (Traceability table — EMB-01..04, EMB-06 status)
    - .planning/STATE.md (current position + progress numbers)
    - 03-VERIFICATION.md (Tasks 1-2 output — provides the evidence to cite)
  </read_first>
  <action>
    ROADMAP.md updates:
    1. Change `- [ ] **Phase 3: Embeddings + Search Indexes**` to `- [x] **Phase 3: Embeddings + Search Indexes**` in the Phases list.
    2. In the Phase 3 Details block, change `**Plans**: TBD` to `**Plans**: 8 plans` with the actual list of completed plans (`03-01-PLAN.md` through `03-08-PLAN.md` with their one-line objectives — see each plan's frontmatter `objective` first sentence).
    3. In the Progress table, change `| 3. Embeddings + Search Indexes | 0/TBD | Not started | - |` to `| 3. Embeddings + Search Indexes | 8/8 | Complete | 2026-05-25 |` (use today's date).

    REQUIREMENTS.md updates:
    4. In the Traceability table, change the `Status` cell for each of EMB-01, EMB-02, EMB-03, EMB-04, EMB-06 from `Pending` to `Complete (Phase 3)`. Do NOT touch EMB-05 / EMB-07 (Phase 4 — still Pending).
    5. In the section `### Embeddings + Hybrid Search`, change the unchecked `[ ]` for EMB-01..04, EMB-06 to checked `[x]`. EMB-05 and EMB-07 remain unchecked.

    STATE.md updates:
    6. Update frontmatter: `completed_phases: 3`, `completed_plans: 28` (was 20 — adds the 8 Phase 3 plans), `last_updated: <ISO timestamp now>`, `last_activity: 2026-05-25 -- Phase 3 verification gate passed`, `stopped_at: Phase 3 complete; ready for Phase 4 discussion`.
    7. Update `## Current Position`: `Phase: 4 (search-service-wire-chat) — READY FOR DISCUSSION`, `Plan: 0 of TBD`, `Status: Phase 3 complete`, `Last activity: 2026-05-25 -- Phase 3 verification gate passed`. Progress bar: `[███░░░░░░░] 37%` (3 of 8 phases complete = 37.5% rounded).
    8. Append to `### Decisions` a new bullet: `- Phase 3 (verified 2026-05-25): EMBEDDING_MODEL = 'openai/text-embedding-3-small' pinned via frozen constant; modelVersion column NOT NULL; HNSW + GIN indexes live in db/manual-indexes.sql (outside Prisma); withHnswIterativeScan helper consumed by Phase 4 SearchService.`
  </action>
  <verify>
    <automated>grep -q "\[x\] \*\*Phase 3: Embeddings" .planning/ROADMAP.md &amp;&amp; grep -q "EMB-01.*Phase 3.*Complete\|EMB-01 | Phase 3 | Complete" .planning/REQUIREMENTS.md &amp;&amp; grep -q "completed_phases: 3" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 3 row checked, plans count = 8, Progress table updated with completion date
    - REQUIREMENTS.md traceability shows EMB-01, EMB-02, EMB-03, EMB-04, EMB-06 as Complete (Phase 3); EMB-05 and EMB-07 still Pending
    - REQUIREMENTS.md section `### Embeddings + Hybrid Search` has `[x]` on the 5 Phase 3 requirements
    - STATE.md frontmatter shows completed_phases=3, completed_plans=28
    - STATE.md Current Position references Phase 4 as next
    - STATE.md Decisions section appended with the Phase 3 outcome bullet
  </acceptance_criteria>
  <done>Project tracking artifacts reflect Phase 3 completion; Phase 4 discussion can begin from a clean state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human visual confirmation of live ProductEmbedding rows + Phase 3 sign-off</name>
  <action>Pause execution and present the verification checklist below to the operator. The operator inspects 03-VERIFICATION.md, queries the live ProductEmbedding table for a populated modelVersion, optionally triggers a real sync and webhook re-embed against the dev shop, and reruns bun db:indexes once more to confirm idempotency. Only mark Phase 3 complete (and unlock /gsd:discuss-phase 4) once the operator types "approved".</action>
  <what-built>Plans 03-01 through 03-07 shipped:
  - 5 Wave 0 test scaffolds + 2 helper modules (`searchableText`, `withHnswIterativeScan`) + EmbeddingService (`embed/embedBatch/embedAndStore`) + schema additions + raw-SQL migration + manual-indexes.sql + apply script + db:indexes npm script + sync-products embed step + webhook re-embedding + CLAUDE.md docs.
  - Tasks 1-3 above ran the full test suite (green), the DB smoke tests (HNSW used, GUC = 'relaxed_order', indexes idempotent), and rolled forward ROADMAP/REQUIREMENTS/STATE.

  This checkpoint asks the operator to (a) visually inspect at least one live ProductEmbedding row to confirm modelVersion is populated with the pinned value, (b) confirm the verification evidence in 03-VERIFICATION.md is convincing, and (c) sign off on Phase 3 completion.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/03-embeddings-search-indexes/03-VERIFICATION.md` and read the three sections: Automated Verification, Database Smoke Tests, ROADMAP/REQUIREMENTS/STATE updates. Confirm test counts, EXPLAIN ANALYZE output, and GUC assertion match expectations.
    2. Optional but recommended — trigger a real sync against your dev shop:
       a. Visit the embedded admin onboarding flow (`/onboarding`) for your dev shop.
       b. Click "Start sync".
       c. Wait for the sync to complete (or for the first batch to finish — ~30s).
       d. Confirm no errors in Vercel function logs / Inngest dev server logs related to embedding.
    3. Inspect the live `product_embeddings` table for at least one row with a populated `modelVersion`:
       a. `bunx tsx -e "import { prisma } from '@/lib/db/client'; const r = await prisma.productEmbedding.findFirst({ select: { id: true, shop: true, productId: true, modelVersion: true } }); console.log(r); await prisma.\$disconnect()"`
       b. Expected output: a single row where `modelVersion: 'openai/text-embedding-3-small'`. If the column is null, EMB-03 is violated — REJECT this checkpoint.
       c. Alternative: open Prisma Studio (`bunx prisma studio`) → navigate to product_embeddings → spot-check the modelVersion column.
    4. Optional — trigger a webhook re-embed: edit any product's description in Shopify Admin, wait 10 seconds, then re-run the query above and confirm the same product's `searchableText` reflects the new description.
    5. Confirm `bun db:indexes` STILL works (run one more time): output `manual indexes applied`, exit code 0.
    6. Sign off:
       - If ALL checks pass: approve.
       - If ANY check fails: reject with details — the failing plan will need re-execution or a gap-closure plan.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 3 complete and unlock Phase 4 (`/gsd:discuss-phase 4` next). Type "blocked" plus the specific failure — name the smoke test or check that failed, and the executor will route to a gap-closure plan or revise the failing plan.</resume-signal>
</task>

</tasks>

<verification>
- 03-VERIFICATION.md exists with all three required sections fully populated (automated, DB smoke, roll-forward).
- `bun test` exits 0 with all test files passing.
- `bunx tsc --noEmit` shows no NEW errors in Phase 3 files.
- `bun db:indexes` runs twice consecutively, both succeed (EMB-04).
- `withHnswIterativeScan` smoke returns `'relaxed_order'` (EMB-06).
- EXPLAIN ANALYZE shows HNSW Index Scan (EMB-04 at planner level).
- ROADMAP / REQUIREMENTS / STATE updated and consistent.
- Human checkpoint signed off.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification gate → production confidence | Gate evidence must withstand audit; smoke test outputs are persisted in 03-VERIFICATION.md |
| dev DB write during smoke | Synthetic embedding row uses `shop = 'smoke.myshopify.com'` to avoid colliding with real shop data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Information Disclosure (multi-tenancy) | Smoke 4 synthetic insert | mitigate | Synthetic row uses shop literal `'smoke.myshopify.com'` — does not collide with real shop data. Cleanup (optional): `DELETE FROM product_embeddings WHERE shop = 'smoke.myshopify.com'` — operator runs after verification if desired. |
| T-3-V13-GATE | Tampering (false-positive verification) | gate decision authority | mitigate | The HUMAN checkpoint (Task 4) is the final authority — automated tests alone cannot prove EMB-06 (the GUC value inside a real transaction requires a real DB connection). The blocking checkpoint enforces operator review. |
| T-3-02 | Information Disclosure (V7) | smoke test outputs in 03-VERIFICATION.md | accept | Smoke outputs contain plan text, row IDs, and DDL strings — no secrets, no API keys, no PII. Embeddings (1536-float arrays) are not logged in full; only the plan output is captured. |
</threat_model>

<success_criteria>
- Full vitest suite green.
- TypeScript strict green for Phase 3 surfaces.
- 4 DB smoke tests pass: HNSW Index Scan visible, GUC = 'relaxed_order', indexes idempotent (twice), indexes exist.
- ROADMAP, REQUIREMENTS, STATE updated and consistent.
- Live ProductEmbedding row visually inspected with pinned modelVersion (human-verified).
- 03-VERIFICATION.md fully populated with command output evidence.
- Operator has signed off on Phase 3.
</success_criteria>

<output>
Create `.planning/phases/03-embeddings-search-indexes/03-08-SUMMARY.md` recording: full-suite test count and pass/fail summary, tsc error count (must be 0 new), DB smoke results (EXPLAIN line, GUC value, idempotency confirmation), the live-row modelVersion value seen during human-verify, ROADMAP/REQUIREMENTS/STATE diffs in summary form, and the operator's sign-off verbatim ("approved" or "blocked: <reason>").

Also confirm the 03-VERIFICATION.md frontmatter is set to `gate_status: passed` upon checkpoint approval.
</output>
