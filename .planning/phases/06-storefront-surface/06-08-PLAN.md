---
phase: 06-storefront-surface
plan: 08
type: execute
wave: 2
depends_on:
  - 06-02
  - 06-04
  - 06-01
files_modified:
  - app/api/proxy/saved-products/route.ts
  - app/api/proxy/saved-products/[productId]/route.ts
autonomous: true
requirements:
  - STR-04
  - STR-08
  - IDN-02
  - IDN-05

must_haves:
  truths:
    - "GET /api/proxy/saved-products returns visitor+linked-customer rows for shop"
    - "POST /api/proxy/saved-products is idempotent — repeated POST returns 200 with no row growth (D-20 ON CONFLICT DO NOTHING)"
    - "DELETE /api/proxy/saved-products/:productId removes a single row scoped to shop+visitor"
    - "body.customer_id is verified against signed query.logged_in_customer_id (IDN-02 critical security gate)"
  artifacts:
    - path: "app/api/proxy/saved-products/route.ts"
      provides: "GET (list), POST (toggle/save) handlers"
      exports: ["GET", "POST"]
    - path: "app/api/proxy/saved-products/[productId]/route.ts"
      provides: "DELETE (remove single)"
      exports: ["DELETE"]
  key_links:
    - from: "POST /api/proxy/saved-products"
      to: "saved_products_anon_unique_idx and saved_products_customer_unique_idx"
      via: "INSERT ... ON CONFLICT DO NOTHING (raw SQL)"
      pattern: "ON CONFLICT.*DO NOTHING"
---

<objective>
Implement the SavedProduct REST handlers under `app/api/proxy/saved-products/` per D-03, D-20, IDN-05. Each handler is HMAC-wrapped (Plan 04), rate-limited (D-08), and applies the IDN-02 logged_in_customer_id cross-check.

The POST endpoint relies on the partial unique indexes from Plan 02 (D-20) to drive an `INSERT ... ON CONFLICT DO NOTHING` upsert — idempotent toggle semantics.

Purpose: These endpoints back DbBackedSavedProductsStore (Plan 11). Without them, the Saved tab cannot persist across reload.
Output: Two files, three named HTTP-method exports total.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-storefront-surface/06-CONTEXT.md
@.planning/phases/06-storefront-surface/06-PATTERNS.md
@.planning/phases/06-storefront-surface/06-RESEARCH.md
@lib/shopify/app-proxy-auth.ts
@lib/rate-limit/memory.ts
@lib/db/client.ts
@app/api/proxy/conversations/route.ts
@app/api/proxy/saved-products/__tests__/route.test.ts
@app/api/proxy/saved-products/[productId]/__tests__/route.test.ts
@db/manual-indexes.sql
@inngest/functions/sync-products.ts
</context>

<interfaces>
GET /api/proxy/saved-products?visitor_id=...
  → 200 { items: Array<{ productId, savedAt }> }

POST /api/proxy/saved-products
  body: { visitor_id, customer_id?, product: ChatProduct }
  → 200 { ok: true, saved: true|false }  // saved=false when ON CONFLICT no-op

DELETE /api/proxy/saved-products/:productId?visitor_id=...
  → 200 { ok: true, deleted: number }

Error envelope: `{ error: '<code>' }` with appropriate status.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement app/api/proxy/saved-products/route.ts (GET + POST)</name>
  <files>app/api/proxy/saved-products/route.ts</files>
  <read_first>
    app/api/proxy/conversations/route.ts,
    lib/shopify/app-proxy-auth.ts,
    lib/rate-limit/memory.ts,
    app/api/proxy/saved-products/__tests__/route.test.ts,
    db/manual-indexes.sql,
    inngest/functions/sync-products.ts,
    .planning/phases/06-storefront-surface/06-PATTERNS.md,
    .planning/phases/06-storefront-surface/06-CONTEXT.md
  </read_first>
  <behavior>
    - GET: visitor_id from query (missing → 400 `missing_visitor_id`); rate-limit 'rest'; signed customerId from query.logged_in_customer_id (treat empty as null); query findMany where { shop, OR: [{ visitorId }, ...(customerId ? [{ customerId }] : [])] }; return 200 with productId+savedAt array.
    - POST: body { visitor_id, customer_id?, product: { id, ... } }; verify customer_id cross-check (mismatch → 403); rate-limit 'rest'; run raw INSERT with ON CONFLICT DO NOTHING per D-20 partial-index path:
      ```
      INSERT INTO "saved_products" (id, shop, "visitorId", "customerId", "productId", "savedAt")
      VALUES (gen_random_uuid()::text, ${shop}, ${visitorId}, ${customerId ?? null}, ${product.id}, NOW())
      ON CONFLICT DO NOTHING
      ```
      Postgres routes ON CONFLICT through whichever partial unique index matches the row predicate (anon vs customer-linked). Capture the rowCount from `$executeRaw` (Prisma returns affected row count); return { ok: true, saved: count === 1 }.
  </behavior>
  <action>
    Mirror the structure of `app/api/proxy/conversations/route.ts` (Plan 07). Imports: same set (NextResponse, withAppProxyHmac, rateLimit, prisma). Define the same private `assertCustomerMatch` helper as Plan 07 — V1 acceptable duplication, single file each.

    GET handler:
    - Read `visitor_id` from `new URL(req.url).searchParams`
    - Apply rate limit
    - Compute signed customerId from `query.logged_in_customer_id || null`
    - `await prisma.savedProduct.findMany({ where: { shop, OR: [{ visitorId }, ...(customerId ? [{ customerId }] : [])] }, select: { productId: true, savedAt: true } })`
    - Return `NextResponse.json({ items: rows })`

    POST handler:
    - `const body = await req.json();`
    - Validate `body.visitor_id` (string, required) and `body.product?.id` (string, required) — Zod-schema lightly inline OR a manual guard (V1: manual guard acceptable; if planner adds Zod, mirror inputSchema style from app/api/chat/route.ts)
    - Cross-check body.customer_id vs query.logged_in_customer_id; mismatch → 403 `customer_id_mismatch`
    - Apply rate limit
    - Run raw INSERT ON CONFLICT DO NOTHING (tagged-template `$executeRaw`):
      - Bind: `shop`, `body.visitor_id`, `body.customer_id ?? null`, `body.product.id`
      - Use `gen_random_uuid()::text` for the id column — pgcrypto extension provided by Plan 06
    - `$executeRaw` returns affected row count; respond `{ ok: true, saved: count === 1 }`
    - On Prisma error `P2003`/`P2010` (constraint failure or missing partial index), surface as 500 `db_error` — log NO body or shop value

    Do NOT add any console.log statements. Do NOT include the ChatProduct's title/description/image in the row — those come from the live catalog on the next /api/proxy/chat call via hybridSearch. We only store productId, savedAt, and the identity scoping fields (per Plan 02 schema).
  </action>
  <verify>
    <automated>bunx vitest run app/api/proxy/saved-products/__tests__/route.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exports `GET` and `POST`
    - Both wrapped with `withAppProxyHmac`
    - POST uses raw SQL `INSERT INTO "saved_products" ... ON CONFLICT DO NOTHING` with tagged-template `$executeRaw`
    - `grep "\\$executeRawUnsafe" app/api/proxy/saved-products/route.ts` returns 0
    - `grep "console\\." app/api/proxy/saved-products/route.ts` returns 0
    - `grep "logged_in_customer_id" app/api/proxy/saved-products/route.ts` returns ≥ 1 (cross-check)
    - `bunx vitest run app/api/proxy/saved-products/__tests__/route.test.ts` GREEN
    - Repeat POST with identical body produces no new row (count stable; rowCount = 0 on second invocation)
  </acceptance_criteria>
  <done>
    GET + POST ship; idempotent via ON CONFLICT; tests GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement app/api/proxy/saved-products/[productId]/route.ts (DELETE)</name>
  <files>app/api/proxy/saved-products/[productId]/route.ts</files>
  <read_first>
    app/api/proxy/saved-products/route.ts,
    app/api/proxy/conversations/[id]/route.ts,
    lib/shopify/app-proxy-auth.ts,
    app/api/proxy/saved-products/[productId]/__tests__/route.test.ts
  </read_first>
  <behavior>
    - DELETE: productId from URL path; visitor_id from query; missing → 400. Rate-limit 'rest'. Cross-check signed customerId. Issues `prisma.savedProduct.deleteMany({ where: { shop, productId, OR: [{ visitorId }, ...(customerId ? [{ customerId }] : [])] } })`. Returns 200 { ok: true, deleted: result.count }.
  </behavior>
  <action>
    Mirror Plan 07 Task 2's URL-segment extraction approach:
    `const productId = new URL(req.url).pathname.split('/').filter(Boolean).pop();`

    Imports same as Task 1.

    `export const DELETE = withAppProxyHmac(async ({ shop, query, req }) => { ... })`:
    - Extract productId; visitor_id from query
    - Apply rate limit 'rest'
    - Compute signed customerId
    - `await prisma.savedProduct.deleteMany({ where: { shop, productId, OR: [...visitor and customer scoping] } })`
    - Return `NextResponse.json({ ok: true, deleted: result.count })`

    Defense in depth: even if a visitor crafts a request with a productId that belongs to another visitor's row, the where-clause scoping prevents matching. NO `404` if deleted = 0 (matches REST DELETE convention for idempotency).

    NO console.log.
  </action>
  <verify>
    <automated>bunx vitest run app/api/proxy/saved-products/[productId]/__tests__/route.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File exports `DELETE`
    - Wrapped with `withAppProxyHmac`
    - `deleteMany` scopes by `shop`, `productId`, AND owner (visitor OR linked customer)
    - `grep "console\\." app/api/proxy/saved-products/[productId]/route.ts` returns 0
    - `bunx vitest run app/api/proxy/saved-products/[productId]/__tests__/route.test.ts` GREEN
  </acceptance_criteria>
  <done>
    DELETE ships; HMAC + rate-limit + owner scoping enforced; tests GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Storefront browser → App Proxy → these handlers | Same as Plan 07; identical security model. The CRITICAL gate here is IDN-02 cross-check on POST — without it an attacker could bind a different customer's saved-products to their own visitor_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | Spoofing | body.customer_id ≠ signed logged_in_customer_id (Critical per RESEARCH §Security Domain) | mitigate | assertCustomerMatch helper on POST; 403 customer_id_mismatch |
| T-06-16 | Tampering | Partial unique index missing | mitigate | Plan 02 ships indexes; ON CONFLICT clause depends on them; documented dependency in code header |
| T-06-17 | Data integrity | Duplicate save rows post-merge | mitigate | saved_products_customer_unique_idx prevents (D-20) — POST ON CONFLICT routes to it |
</threat_model>

<verification>
- Both test files GREEN
- `grep $executeRawUnsafe` returns 0 in both files
- IDN-02 cross-check present on POST (Task 1)
- Idempotency: repeated POST with same body produces stable row count
</verification>

<success_criteria>
- `app/api/proxy/saved-products/route.ts` ships with GET + POST
- `app/api/proxy/saved-products/[productId]/route.ts` ships with DELETE
- Plan 01 RED tests for both files are GREEN
- All three handlers wrapped with withAppProxyHmac + rateLimit
- IDN-02 cross-check enforced on POST
</success_criteria>

<output>
Create `.planning/phases/06-storefront-surface/06-08-SUMMARY.md` with file paths, exported HTTP methods, and a grep audit summary (especially `$executeRawUnsafe = 0`).
</output>
