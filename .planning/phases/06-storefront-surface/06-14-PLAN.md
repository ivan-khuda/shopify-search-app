---
phase: 06-storefront-surface
plan: 14
type: execute
wave: 4
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
  - 06-05
  - 06-06
  - 06-07
  - 06-08
  - 06-09
  - 06-10
  - 06-11
  - 06-12
  - 06-13
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/06-storefront-surface/06-VERIFICATION.md
autonomous: false
requirements:
  - STR-01
  - STR-02
  - STR-03
  - STR-04
  - STR-05
  - STR-06
  - STR-07
  - STR-08
  - IDN-01
  - IDN-02
  - IDN-03
  - IDN-04
  - IDN-05
  - IDN-06

must_haves:
  truths:
    - "Full vitest suite GREEN across the whole codebase"
    - "`bun build` succeeds (no TypeScript errors)"
    - "`bun run prebuild` produces a valid storefront bundle artifact"
    - "`shopify app deploy` pushed the new [app_proxy] block + chat-drawer extension to Shopify (operator-verified)"
    - "Manual smoke against a real dev store passes the 5 ROADMAP Phase 6 success criteria (FAB renders → click → drawer mounts → real catalog query → reload → history persists)"
  artifacts:
    - path: ".planning/phases/06-storefront-surface/06-VERIFICATION.md"
      provides: "Verification report with pass/fail for each ROADMAP success criterion"
  key_links: []
---

<objective>
Final verification gate for Phase 6. Confirms every Plan 01-13 deliverable is wired, every requirement is satisfied, and the storefront surface works end-to-end on a real dev store.

Steps:
1. Run the full automated suite (`bun test` + `bun build`)
2. Build the storefront bundle (`bun run prebuild`)
3. Apply DB schema + manual indexes if not already (idempotent re-apply)
4. Run `shopify app deploy` (blocking human checkpoint — requires CLI auth)
5. Manual smoke against a Dawn dev store (the 5 ROADMAP success criteria)
6. Author `06-VERIFICATION.md` with pass/fail per criterion
7. Update ROADMAP.md / STATE.md / REQUIREMENTS.md to mark Phase 6 complete

Purpose: Closes Phase 6. After this plan, the storefront drawer is live on the dev store; admin chat (Phase 4) and storefront chat (Phase 6) both serve real catalog results.
Output: Verification report + updated planning artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-storefront-surface/06-CONTEXT.md
@.planning/phases/06-storefront-surface/06-VALIDATION.md
@.planning/phases/06-storefront-surface/06-RESEARCH.md
@.planning/phases/06-storefront-surface/06-UI-SPEC.md
@shopify.app.toml
@extensions/chat-drawer/
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run full automated verification suite</name>
  <files></files>
  <read_first>.planning/phases/06-storefront-surface/06-VALIDATION.md</read_first>
  <action>
    Run, in order, from the project root:

    1. `bun test 2>&1 | tail -50` — full vitest suite. Expected: GREEN. Capture the final summary line.
    2. `bun build 2>&1 | tail -30` — Next.js production build. Expected: no TS errors. Note the deprecated-file build error from Phase 5 retrospective (`components/ai-elements/reasoning.tsx` external `@jenius/ui`) — if it persists, document as a known pre-existing failure NOT introduced by Phase 6.
    3. `bun run prebuild 2>&1 | tail -10` — storefront bundle build. Expected: writes `public/storefront-bundle-*.js` + manifest.
    4. `bun db:indexes 2>&1 | tail -5` — idempotent re-apply of manual indexes. Expected: prints "manual indexes applied" (or equivalent success).
    5. Grep audits (must each return 0):
       - `grep -rn "console\\.\\(log\\|warn\\|info\\|debug\\)" lib/shopify/app-proxy-auth.ts lib/rate-limit/memory.ts lib/identity/merge.ts app/api/proxy/ 2>&1 | grep -v "__tests__"` — no secret logging in Phase 6 production code
       - `grep -rn '\\$executeRawUnsafe' lib/identity/merge.ts app/api/proxy/ 2>&1` — no unsafe raw SQL
       - `grep -rn "toAIStreamResponse" app/api/proxy/ 2>&1` — no v5 API leakage
       - `grep -rn "query\\.shop\\b" app/api/proxy/ 2>&1 | grep -v "__tests__"` — no raw query.shop reads in production routes (defense-in-depth)

    Capture all results into the verification report (Task 5).
  </action>
  <verify>
    <automated>bun test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `bun test` exit code 0 (all tests GREEN)
    - `bun build` exit code 0 (or only the pre-existing Phase 5 reasoning.tsx external-package failure, documented)
    - `bun run prebuild` exit code 0; outputs exist
    - `bun db:indexes` exit code 0; idempotent
    - All four grep audits return 0 (no logging, no unsafe SQL, no v5 API, no raw query.shop reads in production routes)
  </acceptance_criteria>
  <done>
    Automated suite GREEN; audits clean; bundle artifact present.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Run `shopify app deploy` to push extension + [app_proxy] block</name>
  <action>Run `shopify app deploy` from the project root as described in <how-to-verify>. Confirm the App Proxy block and extension appear in the Partner Dashboard, then enable the App Embed on a dev store and verify the FAB renders.</action>
  <what-built>
    Plans 03 and 12 added the `[app_proxy]` block to `shopify.app.toml` and the `extensions/chat-drawer/` package. These are LOCAL changes — Shopify does not know about them until `shopify app deploy` pushes the config + extension assets to the Shopify Partner Dashboard.

    `shopify app deploy` requires:
    - Shopify CLI authenticated (`shopify auth login`)
    - The correct Partner / app context loaded (interactive prompt on first run)
    - Network access to Shopify Partners API
  </what-built>
  <how-to-verify>
    Run from project root:

    1. `shopify app deploy` — interactive. The CLI will:
       - Detect the new `[app_proxy]` block in `shopify.app.toml` and ask to confirm pushing the change.
       - Detect the new `extensions/chat-drawer/` extension and ask to confirm registering / uploading.
       - Print a "Deployed successfully" line on completion.

    2. After deploy, verify in the Shopify Partner Dashboard:
       - App → Configuration → App Proxy: shows URL = `<your APP URL>/api/proxy`, subpath = `smartdiscovery`, prefix = `apps`.
       - App → Extensions: shows "SmartDiscovery AI Chat Drawer" (chat-drawer) listed and active.

    3. From a dev store:
       - Online Store → Themes → Customize → Theme settings → App embeds → Enable "SmartDiscovery AI" → Save.
       - Visit the storefront. Expected: FAB visible bottom-right with #008060 accent color (no merchant override yet).

    If deploy fails:
    - "Not authenticated" → run `shopify auth login`.
    - "Extension validation failed" → check `extensions/chat-drawer/shopify.extension.toml` schema; re-read Plan 12 Task 2 output.
    - "App Proxy URL not reachable" → confirm `application_url` in `shopify.app.toml` matches the currently-running app (ngrok / Vercel preview URL).
  </how-to-verify>
  <resume-signal>Type "deployed" once the App Proxy block and extension show in the Partner Dashboard and the FAB renders on the dev store. Describe errors otherwise.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Manual smoke test — 5 ROADMAP success criteria on Dawn dev store</name>
  <action>Execute the 5 ROADMAP success-criterion smoke tests (SC1-SC5) listed in <how-to-verify> against a Dawn dev store. Capture PASS/FAIL per scenario; collect screenshots for any FAIL. Report results before Task 4 authors the verification report.</action>
  <what-built>
    All 13 implementation plans have shipped. This checkpoint validates the 5 ROADMAP Phase 6 success criteria end-to-end against a Dawn theme dev store with real catalog data.
  </what-built>
  <how-to-verify>
    Run each scenario in order. Mark PASS / FAIL inline. Capture screenshots for the verification report if any criterion fails.

    **SC1 — App Embed toggle + FAB renders**:
    1. Open the Dawn dev store theme customizer.
    2. Toggle "SmartDiscovery AI" App Embed ON. Save.
    3. Reload any storefront page.
    4. Expected: FAB renders bottom-right, 56px circle, #008060 accent.
    5. Click FAB → drawer slides in from right (desktop) or bottom (mobile).
    6. Header reads "SmartDiscovery AI" / "Shopify Assistant".
    7. Tabs visible: Chat / History / Saved.
    → PASS / FAIL

    **SC2 — Visitor identity persistence**:
    1. Open Chrome DevTools → Application → Local Storage → check `smartdiscovery.visitor_id` exists with a UUID.
    2. Type a chat message → see streaming response with product cards (from real catalog).
    3. Close drawer; reload page.
    4. Re-open drawer → History tab → see the previous conversation listed.
    → PASS / FAIL

    **SC3 — Customer login merge**:
    1. Log into the dev store with a customer account.
    2. Open drawer. Expected: any prior anon History + Saved merges into the customer-keyed record (transparent — no UI affordance per UI-SPEC).
    3. Log out, log into the SAME customer on a different browser (incognito or another device). Expected: same conversations/saved appear.
    4. Optional verification: query Postgres `SELECT * FROM visitor_customer_links WHERE shop=...` and confirm the merge marker row exists.
    → PASS / FAIL

    **SC4 — App Proxy same-origin**:
    1. In DevTools Network tab, filter for "smartdiscovery".
    2. Send a chat message.
    3. Expected: all requests go to `<storefront-domain>/apps/smartdiscovery/*` — same-origin, no `cors`/`preflight` rows.
    → PASS / FAIL

    **SC5 — Z-index + designMode safety**:
    1. Test on Dawn, Sense, Craft themes (install each via theme library on the dev store).
    2. On each: FAB and drawer sit above sticky headers, cart drawers, mobile menus. No visual clipping.
    3. Open the theme customizer (Theme Editor) → expected: FAB visible, but clicking it does NOT open the drawer (designMode guard at click time).
    4. Confirm via Network tab in editor mode: the storefront bundle (`/storefront-bundle-*.js`) is NEVER requested (loader short-circuits before fetching manifest).
    → PASS / FAIL
  </how-to-verify>
  <resume-signal>Reply "all 5 PASS" with optional screenshot links, OR list FAIL items with details for follow-up.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Write 06-VERIFICATION.md</name>
  <files>.planning/phases/06-storefront-surface/06-VERIFICATION.md</files>
  <read_first>
    .planning/phases/06-storefront-surface/06-VALIDATION.md,
    .planning/phases/06-storefront-surface/06-CONTEXT.md,
    .planning/ROADMAP.md
  </read_first>
  <action>
    Author the canonical verification report. Structure (mirror Phase 5's `05-VERIFICATION.md` if present, otherwise use this skeleton):

    ```markdown
    ---
    phase: 06
    slug: storefront-surface
    status: <verified | verified-with-deferred-smoke | failed>
    verified_at: <YYYY-MM-DD>
    ---

    # Phase 6 — Verification Report

    ## ROADMAP Success Criteria

    | # | Criterion | Status | Evidence |
    |---|-----------|--------|----------|
    | 1 | Merchant toggles App Embed; FAB renders bottom-right; drawer opens with Chat/History/Saved tabs | <PASS|FAIL> | Manual smoke Task 3 SC1; screenshots: <links> |
    | 2 | First-time anon visitor gets UUID visitor_id; conversation persists across close+reload | <PASS|FAIL> | Manual smoke SC2 |
    | 3 | Customer login merges anon history + saved; same on different device | <PASS|FAIL> | Manual smoke SC3 + visitor_customer_links row |
    | 4 | All storefront-to-backend via App Proxy /apps/smartdiscovery/* (no CORS) | <PASS|FAIL> | Manual smoke SC4 + Network tab |
    | 5 | No z-index collision on Dawn/Sense/Craft; designMode prevents auto-open | <PASS|FAIL> | Manual smoke SC5 |

    ## Requirements Coverage

    | Req | Status | Plan(s) |
    |-----|--------|---------|
    | STR-01 | <PASS> | 12, 05, 13 |
    | STR-02 | <PASS> | 12 |
    | STR-03 | <PASS> | 03 |
    | STR-04 | <PASS> | 04, 07, 08, 09 |
    | STR-05 | <PASS> | 12, 13 |
    | STR-06 | <PASS> | 13 |
    | STR-07 | <PASS> | 12, 13 |
    | STR-08 | <PASS> | 04, 11, 12, 13 |
    | IDN-01 | <PASS> | 11 (existing Phase 5 + edit) |
    | IDN-02 | <PASS> | 04, 07, 08, 09, 11 |
    | IDN-03 | <PASS> | 02, 07 |
    | IDN-04 | <PASS> | 07, 11, 13 |
    | IDN-05 | <PASS> | 02, 08 |
    | IDN-06 | <PASS> | 02, 06, 09 |

    ## Automated Suite Summary

    - `bun test`: <N passed / M failed> — capture from Task 1 output
    - `bun build`: <status> — note Phase 5 pre-existing reasoning.tsx failure separately if it occurred
    - `bun run prebuild`: <bundle path + size>
    - `bun db:indexes`: idempotent re-apply success

    ## Grep Audit Summary

    - console.log/warn/info/debug in Phase 6 production paths: 0
    - $executeRawUnsafe usages: 0
    - toAIStreamResponse (v5 API) usages: 0
    - Raw query.shop reads in production routes: 0

    ## Security Acceptance

    - withAppProxyHmac wraps every /api/proxy/* route (STR-04)
    - logged_in_customer_id cross-check on /api/proxy/chat, /api/proxy/saved-products POST, /api/proxy/conversations POST/PATCH/DELETE (RESEARCH §Security Domain critical requirement)
    - rateLimit applied per visitor on chat (30/5min) and rest (60/min)
    - Schema multi-tenancy lock: every Conversation, SavedProduct, VisitorCustomerLink query filters by shop derived from HMAC

    ## Deferred / Known Limitations

    - In-memory rate limiter is per-instance; Phase 8 RequestCounter supersedes (D-08)
    - HMAC replay protection NOT in V1 (RESEARCH §Common Pitfalls — accepted)
    - Bundle SRI not in V1 (RESEARCH §Security Domain — accepted)
    - Mid-stream chat abort discards user message (D-19 / Pitfall 3 — accepted; client-side useChat retains input for retry)
    - Saved-products "clear all" is client-cache only (no bulk DELETE endpoint per Plan 08) — UI does not surface this affordance, accepted

    ## Sign-Off

    Verified by: <claude-checker / operator name>
    Date: <YYYY-MM-DD>
    ```

    Fill in actual values from Tasks 1-3. If any SC failed, capture the failure detail in an "Outstanding Items" section and DOWNGRADE status to `failed` or `verified-with-deferred-smoke`.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-storefront-surface/06-VERIFICATION.md && head -10 .planning/phases/06-storefront-surface/06-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/06-storefront-surface/06-VERIFICATION.md`
    - Frontmatter has `phase: 06`, `slug: storefront-surface`, `status:` (verified or verified-with-deferred-smoke)
    - ROADMAP Success Criteria table has 5 rows, each PASS or FAIL with evidence
    - Requirements Coverage table has 14 rows (8 STR + 6 IDN)
    - Grep audit summary present
    - Security acceptance section present
  </acceptance_criteria>
  <done>
    Verification report committed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Update ROADMAP.md / STATE.md / REQUIREMENTS.md</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>.planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md, .planning/phases/06-storefront-surface/06-VERIFICATION.md</read_first>
  <action>
    1. **ROADMAP.md**:
       - Mark Phase 6 as completed: `- [x] **Phase 6: Storefront Surface** - ...`
       - Update Phase 6's "Plans" line to "14 plans" with the actual plan filenames listed
       - Update the bottom progress table row for Phase 6: status "Complete" (or "Complete with deferred smoke" if any SC failed), completion date today.

    2. **STATE.md**:
       - Update frontmatter `last_updated` to today's ISO timestamp
       - `last_activity: "<today> -- Phase 06 verification gate closed"`
       - Update progress numbers: `completed_phases: 6`, `total_plans: 53` (39 + 14), `completed_plans: 53`, `percent: 75`
       - Append a Phase 6 line to the "Decisions" subsection under Accumulated Context: a 4-6 line dense paragraph summarizing what Phase 6 shipped (3 new models, withAppProxyHmac wrapper, in-memory rate limiter, 8 App Proxy REST routes including replaced 501 chat stub, identity merge transaction, retention sweep cron, Theme App Extension + App Embed block, lazy-loaded bundle pipeline, drawer React shell composing lib/chat-ui via DbBacked stores, IDN-02 logged_in_customer_id cross-check, all 5 ROADMAP SCs verified).
       - Update "Current Position" block: `Phase: 06 (storefront-surface) — VERIFIED`, etc.

    3. **REQUIREMENTS.md**:
       - Flip 14 requirement status fields (STR-01..08, IDN-01..06) from `[ ]` to `[x]` and update the Traceability table's `Status` column for each from `Pending` to `Complete (Phase 6)`.
       - Update the "Last updated" footer line with today's date and a note: "STR-01..08, IDN-01..06 marked complete after Phase 6 verification gate".
  </action>
  <verify>
    <automated>grep -c "Complete (Phase 6)" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md marks Phase 6 as complete; "Plans" subsection updated with 14 entries
    - STATE.md progress numbers updated; new Phase 6 decision summary added; current position reflects VERIFIED
    - REQUIREMENTS.md shows 14 newly-completed requirements
    - `grep -c "Complete (Phase 6)" .planning/REQUIREMENTS.md` returns ≥ 14
  </acceptance_criteria>
  <done>
    Planning artifacts updated to reflect Phase 6 completion.
  </done>
</task>

</tasks>

<verification>
- All Plan 01-13 outputs verified by Task 1 (automated suite)
- `shopify app deploy` pushed config to Shopify (Task 2)
- 5 ROADMAP success criteria validated on real dev store (Task 3)
- 06-VERIFICATION.md committed (Task 4)
- ROADMAP / STATE / REQUIREMENTS updated (Task 5)
</verification>

<success_criteria>
- 5 ROADMAP Phase 6 success criteria all PASS (or known-deferred with operator approval)
- 14 requirements (STR-01..08, IDN-01..06) marked Complete
- Phase 6 status: verified (or verified-with-deferred-smoke if any SC failed)
- Bundle artifact lives in public/; storefront drawer renders on dev store
</success_criteria>

<output>
Create `.planning/phases/06-storefront-surface/06-14-SUMMARY.md` with the final verification status, plan-completion timestamps, and any deferred items captured for Phase 7+ backlog.
</output>
