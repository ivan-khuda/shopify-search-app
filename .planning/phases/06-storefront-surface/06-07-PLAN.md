---
phase: 06-storefront-surface
plan: 07
type: execute
wave: 2
depends_on:
  - 06-02
  - 06-04
  - 06-01
files_modified:
  - app/api/proxy/conversations/route.ts
  - app/api/proxy/conversations/[id]/route.ts
autonomous: true
requirements:
  - STR-04
  - STR-08
  - IDN-03
  - IDN-04

must_haves:
  truths:
    - "GET /api/proxy/conversations returns paginated conversations (20/page, cursor by lastMessageAt desc) scoped to shop+visitor (and linked customer)"
    - "POST /api/proxy/conversations creates a Conversation row, sets title = first user message truncated to 60 chars (D-18)"
    - "GET /api/proxy/conversations/:id returns full messages JSONB; row.shop mismatch → 403"
    - "PATCH /api/proxy/conversations/:id appends a turn to messages JSONB atomically + bumps lastMessageAt"
    - "DELETE /api/proxy/conversations?visitor_id=... bulk-deletes for visitor + linked customer (D-06 — hard delete, no undo)"
    - "Every route rejects requests without valid HMAC (STR-04)"
  artifacts:
    - path: "app/api/proxy/conversations/route.ts"
      provides: "GET (list), POST (create), DELETE (bulk) handlers"
      exports: ["GET", "POST", "DELETE"]
    - path: "app/api/proxy/conversations/[id]/route.ts"
      provides: "GET (single conversation), PATCH (append turn)"
      exports: ["GET", "PATCH"]
  key_links:
    - from: "/api/proxy/conversations handlers"
      to: "withAppProxyHmac"
      via: "POST = withAppProxyHmac(async ({ shop, query, req }) => ...)"
      pattern: "withAppProxyHmac\\(async"
---

<objective>
Implement the four Conversation REST handlers under `app/api/proxy/conversations/` per D-03/D-04/D-05/D-06/D-17/D-18. Each handler:
1. Wraps with `withAppProxyHmac` (Plan 04) — never reads `shop` from raw query/body
2. Applies `rateLimit(visitorId, 'rest')` (Plan 04) — 60/min reads + writes
3. Verifies body.customer_id matches signed `query.logged_in_customer_id` (security gate from RESEARCH §Security Domain)
4. Performs shop-scoped Prisma operations (T-06-04 multi-tenancy lock — every where clause filters by shop)

Purpose: These endpoints back the DbBackedHistoryStore (Plan 11) and the History tab resume affordance (IDN-04).
Output: Two files with five named HTTP-method exports total.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-storefront-surface/06-CONTEXT.md
@.planning/phases/06-storefront-surface/06-PATTERNS.md
@.planning/phases/06-storefront-surface/06-RESEARCH.md
@lib/shopify/app-proxy-auth.ts
@lib/rate-limit/memory.ts
@lib/db/client.ts
@app/api/shopify/sync/route.ts
@app/api/shopify/sync/status/route.ts
@app/api/proxy/conversations/__tests__/route.test.ts
@app/api/proxy/conversations/[id]/__tests__/route.test.ts
</context>

<interfaces>
<!-- Wire format the DbBackedHistoryStore (Plan 11) expects: -->

GET /api/proxy/conversations?visitor_id=...&cursor=<conversationId>
  → 200 { items: Array<{ id, title, lastMessageAt, createdAt }>, nextCursor: string | null }

POST /api/proxy/conversations
  body: { visitor_id: string, customer_id?: string, firstMessage: { text: string } }
  → 201 { conversation_id: string, title: string }

DELETE /api/proxy/conversations?visitor_id=...
  → 200 { deleted: number }

GET /api/proxy/conversations/:id?visitor_id=...
  → 200 { id, title, messages, lastMessageAt, createdAt }
  → 404 if id not found
  → 403 if row.shop !== shop OR row's visitorId/customerId does not match the requesting visitor

PATCH /api/proxy/conversations/:id
  body: { visitor_id, customer_id?, newMessages: UIMessage[] }
  → 200 { ok: true }

Standard error envelope: `{ error: '<code>' }` with appropriate 4xx/5xx status.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement app/api/proxy/conversations/route.ts (GET/POST/DELETE collection handlers)</name>
  <files>app/api/proxy/conversations/route.ts</files>
  <read_first>
    app/api/shopify/sync/route.ts,
    app/api/shopify/sync/status/route.ts,
    lib/shopify/app-proxy-auth.ts,
    lib/rate-limit/memory.ts,
    lib/db/client.ts,
    app/api/proxy/conversations/__tests__/route.test.ts,
    .planning/phases/06-storefront-surface/06-PATTERNS.md,
    .planning/phases/06-storefront-surface/06-CONTEXT.md
  </read_first>
  <behavior>
    - GET: extracts visitor_id from query; missing → 400 `missing_visitor_id`. Applies rate limit 'rest'. Builds where = { shop, OR: [{ visitorId }, ...(customerId ? [{ customerId }] : [])] } where customerId = signed query.logged_in_customer_id (null if anon). Queries findMany take: 21 + cursor pagination; returns first 20 items + nextCursor = items[20]?.id.
    - POST: parses body { visitor_id, customer_id?, firstMessage: { text } }; missing visitor_id → 400; verifies body.customer_id === signed query.logged_in_customer_id (IDN-02 cross-check) — mismatch → 403 `customer_id_mismatch`. Applies rate limit. Computes title = firstMessage.text.trim().slice(0, 60) || '(no title)' per D-18. Creates Conversation row with messages = [] (D-17 — JSONB blob populated by /api/proxy/chat onFinish). Returns 201 { conversation_id, title }.
    - DELETE: extracts visitor_id from query; missing → 400. Applies rate limit. Cross-check customer_id from query.logged_in_customer_id (no body for DELETE per D-06). Issues prisma.conversation.deleteMany({ where: { shop, OR: [{ visitorId }, ...(customerId ? [{ customerId }] : [])] } }). Returns 200 { deleted: result.count }. D-06 explicitly: irreversible by design.
  </behavior>
  <action>
    File starts with a JSDoc explaining the role (per-resource App Proxy endpoints; D-03/D-04/D-05/D-06; STR-04; T-06-04 multi-tenancy lock — shop derived from withAppProxyHmac only). Imports:
    - `import { NextResponse } from 'next/server';`
    - `import { withAppProxyHmac } from '@/lib/shopify/app-proxy-auth';`
    - `import { rateLimit } from '@/lib/rate-limit/memory';`
    - `import { prisma } from '@/lib/db/client';`

    Implementation:
    - `export const GET = withAppProxyHmac(async ({ shop, query, req }) => { ... })` — extract visitor_id and cursor from `new URL(req.url).searchParams`; rate-limit; query the DB per "behavior" above; return NextResponse.json.
    - `export const POST = withAppProxyHmac(async ({ shop, query, req }) => { ... })` — parse body via `await req.json()`; verify customer_id matches signed `query.logged_in_customer_id` (treat empty string as null on both sides); rate-limit; create row; return NextResponse.json(payload, { status: 201 }).
    - `export const DELETE = withAppProxyHmac(async ({ shop, query, req }) => { ... })` — extract visitor_id from url searchParams; rate-limit; deleteMany; return NextResponse.json.

    customer_id cross-check helper (shared across this file and Plan 09 chat route — keep inline first; if duplication grows, planner refactors into withAppProxyHmac context):
    ```
    function assertCustomerMatch(bodyCustomerId: string | undefined, signed: string | undefined): true | 'customer_id_mismatch' {
      const a = bodyCustomerId || null;
      const b = signed || null;
      if (a === null && b === null) return true;
      if (a !== null && b !== null && a === b) return true;
      return 'customer_id_mismatch';
    }
    ```
    Keep helper private (not exported) — Plan 09 will duplicate the same logic. This is acceptable for V1 (two call sites).

    Defense-in-depth: every Prisma query filters by `shop` explicitly. NEVER trust `query.shop` after withAppProxyHmac — use the `shop` variable destructured from the wrapper closure.

    NO console.log of any of: visitor_id, customer_id, body, query, shop. PROJECT.md hard constraint.

    Cursor pagination implementation: pass `cursor: cursor ? { id: cursor } : undefined` and `skip: cursor ? 1 : 0` to findMany. Use `take: 21` then slice the 21st as nextCursor (avoids a count query). Return shape: `{ items: items.slice(0, 20).map(c => ({ id, title, lastMessageAt, createdAt })), nextCursor: items[20]?.id ?? null }` — strip the messages JSONB from the list payload (history list does not need full messages; resume uses GET /:id).

    Rate limit: return `NextResponse.json({ error: 'rate_limited' }, { status: 429, headers: { 'Retry-After': String(rl.retryAfterSeconds) } });` on `!rl.ok`.
  </action>
  <verify>
    <automated>bunx vitest run app/api/proxy/conversations/__tests__/route.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exports `GET`, `POST`, `DELETE`
    - Every export is wrapped with `withAppProxyHmac(...)` — `grep "export const \\(GET\\|POST\\|DELETE\\) = withAppProxyHmac" app/api/proxy/conversations/route.ts` returns 3 matches
    - `grep "rateLimit" app/api/proxy/conversations/route.ts` returns ≥ 3 matches (one per handler)
    - `grep "logged_in_customer_id" app/api/proxy/conversations/route.ts` returns ≥ 1 match (cross-check helper)
    - `grep "console\\." app/api/proxy/conversations/route.ts` returns 0
    - `grep "query.shop\\b" app/api/proxy/conversations/route.ts` returns 0 — shop comes from wrapper closure, not directly from query
    - POST title computation uses `.slice(0, 60)` and `|| '(no title)'` fallback (D-18)
    - `bunx vitest run app/api/proxy/conversations/__tests__/route.test.ts` GREEN
  </acceptance_criteria>
  <done>
    Three exports ship; all HMAC-wrapped; rate-limited; cross-check enforced; tests GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement app/api/proxy/conversations/[id]/route.ts (GET single, PATCH append)</name>
  <files>app/api/proxy/conversations/[id]/route.ts</files>
  <read_first>
    app/api/shopify/sync/status/route.ts,
    app/api/proxy/conversations/route.ts,
    lib/shopify/app-proxy-auth.ts,
    app/api/proxy/conversations/[id]/__tests__/route.test.ts,
    .planning/phases/06-storefront-surface/06-PATTERNS.md
  </read_first>
  <behavior>
    - GET: extracts id from the URL pathname (Next.js 16 dynamic segment); validates visitor_id query param; fetches Conversation; null → 404 `conversation_not_found`; row.shop !== shop → 403 `wrong_shop`; row.visitorId !== visitor_id AND row.customerId !== signed_customer_id → 403 `not_owner` (defense in depth). Returns 200 { id, title, messages, lastMessageAt, createdAt }.
    - PATCH: same id extraction; same shop/owner checks; body { newMessages: UIMessage[] }; appends via atomic raw SQL `UPDATE "conversations" SET messages = messages || $jsonb, "lastMessageAt" = NOW() WHERE id = ${id} AND shop = ${shop}`. Returns 200 { ok: true }.
    - All handlers rate-limited 'rest'.
    - HMAC verified by withAppProxyHmac.
  </behavior>
  <action>
    Next.js 16 dynamic-segment handler signature: `withAppProxyHmac` does not pass `params` through its current contract (Plan 04 only forwards `{ shop, query, req }`). Two acceptable routes:
    A. Extract the id from `req.url` directly: `const id = new URL(req.url).pathname.split('/').filter(Boolean).pop();` — accept this small ergonomics tradeoff (the id is the last path segment).
    B. Rewrite `withAppProxyHmac` to accept a generic `extras` parameter that the inner handler signature can opt into. NOT REQUIRED in this plan — keep wrapper minimal (Plan 04 lock).

    Choose option A. Document the choice in a small file-header comment.

    Handler skeleton mirrors `app/api/shopify/sync/status/route.ts` (lines 5-28 — pattern for single-row fetch + shop guard + 404/403 envelope):
    - `export const GET = withAppProxyHmac(async ({ shop, query, req }) => { /* id from URL, visitor_id from query, signed customerId from query.logged_in_customer_id */ ... })`
    - `export const PATCH = withAppProxyHmac(async ({ shop, query, req }) => { ... })`

    For PATCH atomic append:
    - Read `const body = await req.json();`
    - Verify customer_id cross-check (private helper or duplicate from Task 1 — V1 acceptable)
    - `await prisma.$executeRaw\`UPDATE "conversations" SET messages = messages || ${JSON.stringify(body.newMessages)}::jsonb, "lastMessageAt" = NOW() WHERE id = ${id} AND shop = ${shop}\`` — this is the JSONB concat operator `||`. Prisma's tagged-template parameterization handles the JSON value safely (no string concat).
    - Check result count; if 0 rows updated, return 404 `conversation_not_found`.

    All responses use NextResponse.json. Errors follow the same `{ error: code }` envelope as Task 1.

    Visitor/customer owner check: after Prisma fetch, verify row.visitorId === visitorIdFromQuery OR row.customerId === signedCustomerIdFromQuery — if neither matches, return 403. This is the defense-in-depth gate: even with a valid HMAC for the shop, a visitor cannot read another visitor's history.

    NO console.log statements.
  </action>
  <verify>
    <automated>bunx vitest run app/api/proxy/conversations/[id]/__tests__/route.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exports `GET` and `PATCH`
    - Both wrapped with withAppProxyHmac
    - PATCH uses `$executeRaw` with `messages || ${...}::jsonb` tagged-template (no $executeRawUnsafe — verifiable by grep)
    - 404 on missing row; 403 on shop or owner mismatch
    - `grep "console\\." app/api/proxy/conversations/[id]/route.ts` returns 0
    - `bunx vitest run app/api/proxy/conversations/[id]/__tests__/route.test.ts` GREEN
  </acceptance_criteria>
  <done>
    Both handlers ship; HMAC + rate-limit + owner checks enforced; tests GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Storefront browser → App Proxy → these route handlers | Signed query is the auth boundary; visitor_id and customer_id in body/query are NOT auth — they are scoping identifiers backed by the partial-index unique constraints and the IDN-02 cross-check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Spoofing | Visitor reads another visitor's conversation | mitigate | Defense in depth: HMAC scopes to shop, owner check filters by visitorId OR signed customerId before returning row contents |
| T-06-13 | Spoofing | Body customer_id ≠ signed logged_in_customer_id | mitigate | Cross-check helper on POST/DELETE/PATCH; 403 on mismatch (RESEARCH §Security Domain critical requirement) |
| T-06-14 | DoS | Cursor pagination skip | mitigate | take: 21 (no count); no offset-based pagination; bounded memory |
</threat_model>

<verification>
- `bunx vitest run app/api/proxy/conversations/__tests__` is GREEN for both files
- `grep $executeRawUnsafe app/api/proxy/conversations/` returns 0
- `grep "query.shop\b" app/api/proxy/conversations/` returns 0 in either file (shop only from wrapper closure)
</verification>

<success_criteria>
- All five HTTP method exports ship across the two files
- All wrapped with `withAppProxyHmac`
- All apply `rateLimit` with bucket='rest'
- Cross-check (body.customer_id === signed logged_in_customer_id) enforced on POST/PATCH/DELETE
- Multi-tenancy: every Prisma query filters by `shop` from wrapper closure
- Plan 01 RED tests for these two files are now GREEN
</success_criteria>

<output>
Create `.planning/phases/06-storefront-surface/06-07-SUMMARY.md` with the file paths, exported HTTP methods, and a grep audit summary.
</output>
