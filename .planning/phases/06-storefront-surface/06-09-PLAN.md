---
phase: 06-storefront-surface
plan: 09
type: execute
wave: 2
depends_on:
  - 06-02
  - 06-04
  - 06-06
  - 06-01
files_modified:
  - app/api/proxy/chat/route.ts
autonomous: true
requirements:
  - STR-04
  - STR-08
  - IDN-02
  - IDN-06

must_haves:
  truths:
    - "POST /api/proxy/chat is HMAC-authenticated (STR-04); shop is derived from signed query only"
    - "body.customer_id is verified against signed query.logged_in_customer_id (IDN-02 critical security gate); mismatch → 403"
    - "Rate-limit 'chat' (30/5min per visitor) enforced; 429 with Retry-After: 60 on exceed"
    - "If body.customer_id present, mergeVisitorIntoCustomer is called before streamText (D-21 step 7)"
    - "streamText is called with the SAME searchCatalog tool registration as /api/chat (D-21 step 8)"
    - "onFinish (D-19) atomically appends user+assistant turns into Conversation.messages JSONB and bumps lastMessageAt"
    - "Mid-stream abort does NOT write to the DB (only onFinish writes)"
    - "Response is streamText.toUIMessageStreamResponse() (Vercel AI SDK v6 — NOT v5 toAIStreamResponse)"
  artifacts:
    - path: "app/api/proxy/chat/route.ts"
      provides: "Full storefront streaming chat endpoint replacing 501 stub"
      exports: ["POST"]
  key_links:
    - from: "/api/proxy/chat onFinish"
      to: "Conversation.messages JSONB"
      via: "atomic UPDATE messages = messages || $newJson"
      pattern: "messages = messages \\|\\|"
    - from: "/api/proxy/chat handler"
      to: "mergeVisitorIntoCustomer"
      via: "called before streamText when body.customer_id present"
      pattern: "mergeVisitorIntoCustomer\\("
---

<objective>
Replace the existing 501 stub at `app/api/proxy/chat/route.ts` with the full D-21 implementation:

D-21 checklist (verbatim from CONTEXT):
1. HMAC validation via `withAppProxyHmac` (Plan 04) — `shop` from signed query, NEVER from raw param
2. Extract `visitor_id` from body; reject if missing
3. Apply `rateLimit(visitorId, 'chat')` (Plan 04 — 30/5min); 429 + Retry-After: 60
4. Hard-cap stub (Phase 8 fills in — leave as a no-op `void await checkHardCap?.(shop);` or equivalent placeholder)
5. Resolve `customer_id` from body if present
6. If first message of session (no body.conversation_id): create Conversation row via `prisma.conversation.create`; otherwise locate by conversation_id
7. If body.customer_id present: call `mergeVisitorIntoCustomer(shop, visitor_id, customer_id)` (Plan 06) before streamText
8. `streamText({ model, system, messages, tools: { searchCatalog }, stopWhen: stepCountIs(3), onFinish })` — exact pattern mirror of `app/api/chat/route.ts`
9. `onFinish: async ({ response }) => { atomic UPDATE conversations SET messages = messages || $newTurns, lastMessageAt = NOW() WHERE id = $conversationId AND shop = $shop }`
10. Return `result.toUIMessageStreamResponse()`

Add the critical security gate from RESEARCH §Security Domain: verify `body.customer_id === query.logged_in_customer_id` (treat empty/null equivalent on both sides) — mismatch → 403 `customer_id_mismatch`.

Purpose: This is the last piece for STR-08 (storefront-to-backend traffic), CR-01 closure (no raw shop param), and the closing of D-19 (atomic onFinish persistence). After Plan 09 ships, the storefront drawer can actually talk to the AI.
Output: Replace the contents of `app/api/proxy/chat/route.ts` (delete the 501 short-circuit; keep no legacy imports).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-storefront-surface/06-CONTEXT.md
@.planning/phases/06-storefront-surface/06-PATTERNS.md
@.planning/phases/06-storefront-surface/06-RESEARCH.md
@CLAUDE.md
@app/api/proxy/chat/route.ts
@app/api/chat/route.ts
@lib/shopify/app-proxy-auth.ts
@lib/rate-limit/memory.ts
@lib/identity/merge.ts
@lib/db/client.ts
@services/search/SearchService.ts
@services/chat/getActiveChatModel.ts
@app/api/proxy/chat/__tests__/route.test.ts
</context>

<interfaces>
<!-- Wire format mirrors /api/chat with added storefront fields -->

POST /api/proxy/chat (signed by Shopify App Proxy)
  body: {
    messages: UIMessage[],
    visitor_id: string,           // required
    customer_id?: string,         // optional; must match signed logged_in_customer_id when present
    conversation_id?: string,     // optional; absent on first message of session (per D-04)
  }
  → 200 UI message stream (text/event-stream-like; toUIMessageStreamResponse()'s native shape)
  → 400 { error: 'missing_visitor_id' }
  → 401 (HMAC failure handled by withAppProxyHmac)
  → 403 { error: 'customer_id_mismatch' }
  → 429 { error: 'rate_limited' } with Retry-After: 60

<!-- Calls into: -->
- streamText, tool, convertToModelMessages, stepCountIs, UIMessage from 'ai' (v6)
- z from 'zod'
- dedent from 'dedent'
- withAppProxyHmac from '@/lib/shopify/app-proxy-auth'
- rateLimit from '@/lib/rate-limit/memory'
- mergeVisitorIntoCustomer from '@/lib/identity/merge'
- prisma from '@/lib/db/client'
- hybridSearch from '@/services/search/SearchService'
- getActiveChatModel from '@/services/chat/getActiveChatModel'
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace 501 stub with full /api/proxy/chat implementation</name>
  <files>app/api/proxy/chat/route.ts</files>
  <read_first>
    app/api/proxy/chat/route.ts,
    app/api/chat/route.ts,
    lib/shopify/app-proxy-auth.ts,
    lib/rate-limit/memory.ts,
    lib/identity/merge.ts,
    lib/db/client.ts,
    services/search/SearchService.ts,
    services/chat/getActiveChatModel.ts,
    app/api/proxy/chat/__tests__/route.test.ts,
    .planning/phases/06-storefront-surface/06-CONTEXT.md,
    .planning/phases/06-storefront-surface/06-RESEARCH.md,
    .planning/phases/06-storefront-surface/06-PATTERNS.md
  </read_first>
  <behavior>
    - Missing HMAC signature → 401 (handled by withAppProxyHmac)
    - Missing body.visitor_id → 400 `missing_visitor_id`
    - body.customer_id present AND body.customer_id !== query.logged_in_customer_id → 403 `customer_id_mismatch`
    - rateLimit(visitor_id, 'chat') ok=false → 429 `rate_limited` + Retry-After: 60
    - No body.conversation_id → create new Conversation row; title = body.messages[0]?.parts find user text or .content trimmed to 60 chars OR '(no title)'
    - body.conversation_id present → load Conversation by id+shop; null → 404 `conversation_not_found`; row.shop !== shop → 403
    - body.customer_id present (and matched) → call mergeVisitorIntoCustomer (idempotent — Plan 06)
    - streamText with: model.id from getActiveChatModel(shop), same system prompt as admin chat (dedent block from app/api/chat/route.ts lines 64-72), convertToModelMessages(body.messages), searchCatalog tool (identical inputSchema + execute closure capturing shop), stopWhen: stepCountIs(3), onFinish: write turns to DB
    - onFinish writes:
      ```
      tx.$executeRaw`UPDATE "conversations"
                       SET messages = messages || ${JSON.stringify([lastUserMsg, ...response.messages])}::jsonb,
                           "lastMessageAt" = NOW()
                       WHERE id = ${conversationId} AND shop = ${shop}`
      ```
      Single UPDATE — atomic per D-19. Mid-stream failure means onFinish never fires → no write → user prompt remains populated client-side for retry (Pitfall 3 documented).
    - Return result.toUIMessageStreamResponse() — NOT toAIStreamResponse (v5 — must not appear in this file per RESEARCH §State of the Art)
  </behavior>
  <action>
    DELETE the entire existing contents of `app/api/proxy/chat/route.ts` (the 501 stub + comment block + `void hybridSearch`). Replace with a full implementation. Structure:

    1. Top-of-file JSDoc (mirror `app/api/chat/route.ts` lines 1-41 style) — explain D-21, security model (shop from HMAC, customer_id cross-check, rate limit, hard-cap stub), D-19 onFinish contract, and Pitfall 3 (mid-stream failure discards user message intentionally).

    2. Imports (consolidated, single import block at top):
       - `import { convertToModelMessages, stepCountIs, streamText, tool, type UIMessage } from 'ai';`
       - `import dedent from 'dedent';`
       - `import { z } from 'zod';`
       - `import { withAppProxyHmac } from '@/lib/shopify/app-proxy-auth';`
       - `import { rateLimit } from '@/lib/rate-limit/memory';`
       - `import { mergeVisitorIntoCustomer } from '@/lib/identity/merge';`
       - `import { prisma } from '@/lib/db/client';`
       - `import { getActiveChatModel } from '@/services/chat/getActiveChatModel';`
       - `import { hybridSearch } from '@/services/search/SearchService';`

    3. Inline private helper `assertCustomerMatch(bodyCustomerId, signed)` — same shape as Plan 07/08 (V1 acceptable duplication; do NOT extract yet).

    4. `export const POST = withAppProxyHmac(async ({ shop, query, req }) => { ... })`:
       a. Parse body: `const body: { messages: UIMessage[]; visitor_id: string; customer_id?: string; conversation_id?: string } = await req.json();`
       b. `if (!body.visitor_id) return Response.json({ error: 'missing_visitor_id' }, { status: 400 });`
       c. Cross-check customer_id; mismatch → 403 `customer_id_mismatch`
       d. `const rl = rateLimit(body.visitor_id, 'chat'); if (!rl.ok) return Response.json({ error: 'rate_limited' }, { status: 429, headers: { 'Retry-After': String(rl.retryAfterSeconds) } });`
       e. D-21 step 4: hard-cap stub — add the comment `// D-21 step 4: hard-cap stub (Phase 8 fills in via DB-backed RequestCounter)`. Do NOT add any actual check.
       f. D-21 steps 6 + 7: resolve `conversationId` and run merge:
          - If `body.conversation_id`: load and verify ownership (`row.shop === shop`); if not found → 404 `conversation_not_found`
          - Else: derive `title` from the first user UIMessage's text content — find the last user message in body.messages (the freshly-typed one), extract its text. Helper: `function extractUserText(msg: UIMessage): string` — for AI SDK v6 UIMessages, scan `msg.parts` for `{ type: 'text', text }` chunks and concatenate. Trim and slice to 60 chars; empty → `'(no title)'`. Then `const created = await prisma.conversation.create({ data: { shop, visitorId: body.visitor_id, customerId: body.customer_id ?? null, title, messages: [] } });` and `const conversationId = created.id;`
          - If body.customer_id present (and cross-check passed): `await mergeVisitorIntoCustomer(shop, body.visitor_id, body.customer_id);` BEFORE the streamText call — accept the slight latency cost; merge is idempotent so repeated calls are cheap O(1)
       g. `const model = await getActiveChatModel(shop);` — exactly as admin route does
       h. `const system = dedent\`... same as app/api/chat/route.ts lines 64-72 ...\`` — copy the system prompt VERBATIM (admin and storefront prompts are intentionally identical for V1; future phases may diverge)
       i. `const result = streamText({ model: model.id, system, messages: await convertToModelMessages(body.messages), tools: { searchCatalog: tool({ description: ..., inputSchema: z.object({ query, priceMin?, priceMax? }), execute: async ({ query, priceMin, priceMax }) => hybridSearch(shop, query, { priceMin, priceMax }) }) }, stopWhen: stepCountIs(3), onFinish: async ({ response }) => { ... } });` — mirror admin route's tool definition byte-for-byte; the only difference from admin is the wrapper (withAppProxyHmac) and the onFinish DB-write.
       j. onFinish body:
          ```
          // D-19 atomic single-row append. Build the new turn JSON: last user msg + response.messages assistant turns.
          // Use Vercel AI SDK v6 response.messages shape — assistant messages emitted during this call.
          const lastUserMessage = [...body.messages].reverse().find(m => m.role === 'user');
          const newTurns = [];
          if (lastUserMessage) newTurns.push(lastUserMessage);
          newTurns.push(...response.messages);  // assistant turns
          await prisma.$executeRaw`UPDATE "conversations"
                                       SET messages = messages || ${JSON.stringify(newTurns)}::jsonb,
                                           "lastMessageAt" = NOW()
                                       WHERE id = ${conversationId} AND shop = ${shop}`;
          ```
          NOTE: if `prisma.$executeRaw` from inside an async `onFinish` is blocked by the SDK's response lifecycle (it shouldn't be — RESEARCH cites "supports async DB writes via Promise<void> return"), the executor falls back to wrapping the write in a `try/catch` that logs to a server-side error channel WITHOUT logging body contents (no console.log of messages — only the error code if any).
       k. `return result.toUIMessageStreamResponse();`

    5. ABSOLUTELY DO NOT include the legacy `void hybridSearch;` or the 501 fallback. The new route fully uses hybridSearch via the tool's execute closure — the EMB-07 grep gate (which scans for the import) continues to be satisfied because the import remains in the file's first line block.

    6. Zero `console.log` statements anywhere.

    7. Multi-tenancy lock: every Prisma operation uses `shop` from the wrapper closure. Never re-read `query.shop` for SQL. The merge helper accepts shop as a parameter; the create and the update both pin to `shop`.

    Pattern reference: copy 80%+ of the structure from `app/api/chat/route.ts` — the only differences are (a) wrapper, (b) added body fields, (c) added cross-check + rate-limit + conversation lifecycle + merge calls + onFinish DB write.
  </action>
  <verify>
    <automated>bunx vitest run app/api/proxy/chat/__tests__/route.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/proxy/chat/route.ts` no longer contains the 501 stub (`grep -c "501\\|not_implemented" app/api/proxy/chat/route.ts` returns 0)
    - File exports `POST` wrapped with `withAppProxyHmac`
    - `grep "toAIStreamResponse" app/api/proxy/chat/route.ts` returns 0 (v5 banned per State of the Art lock)
    - `grep "toUIMessageStreamResponse" app/api/proxy/chat/route.ts` returns 1 match
    - `grep "logged_in_customer_id\\|customer_id_mismatch" app/api/proxy/chat/route.ts` returns ≥ 2 matches (cross-check helper + check site)
    - `grep "rateLimit.*'chat'" app/api/proxy/chat/route.ts` returns 1 match
    - `grep "mergeVisitorIntoCustomer" app/api/proxy/chat/route.ts` returns 1 match
    - `grep "messages = messages \\|\\|\\|messages || " app/api/proxy/chat/route.ts` matches the JSONB concat onFinish UPDATE (escape variations acceptable)
    - `grep "console\\." app/api/proxy/chat/route.ts` returns 0
    - `grep "query\\.shop\\b" app/api/proxy/chat/route.ts` returns 0 — shop only from wrapper closure
    - `grep "hybridSearch\\(shop," app/api/proxy/chat/route.ts` returns 1 — verifies shop comes from wrapper closure, not tool args
    - `bunx vitest run app/api/proxy/chat/__tests__/route.test.ts` GREEN
    - `bunx tsc --noEmit app/api/proxy/chat/route.ts` reports no errors
  </acceptance_criteria>
  <done>
    501 stub replaced; full D-21 implementation ships; tests GREEN; v6 lock honored; no secret logging; no raw shop param read.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Storefront browser → Shopify App Proxy → /api/proxy/chat | The most sensitive endpoint in Phase 6 — combines HMAC auth + cross-customer identity gate + streaming AI + DB write + merge transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-18 | Spoofing | Body customer_id ≠ signed logged_in_customer_id (CRITICAL per RESEARCH §Security Domain) | mitigate | assertCustomerMatch on every POST; 403 customer_id_mismatch |
| T-06-19 | Spoofing | shop param hijack (CR-01 redux) | mitigate | shop derived only from withAppProxyHmac wrapper closure; never from raw query |
| T-06-20 | Tampering | Tool-arg shop hallucination (T-04-07/-09/-13 carry-over) | mitigate | searchCatalog inputSchema does NOT declare shop; execute closure shadows any hallucinated arg by capturing the wrapper's shop variable |
| T-06-21 | DoS | Storefront chat abuse | mitigate (partial) | rateLimit 'chat' 30/5min per visitor; AI Gateway provider rate limits layer atop; Phase 8 hard cap is the cross-instance backstop |
| T-06-22 | Information Disclosure | Conversation row leak via wrong-shop / wrong-owner | mitigate | All Prisma queries scope by `shop`; resumed conversation_id verified for shop ownership |
| T-06-23 | Repudiation | Mid-stream DB inconsistency | accept | D-19 explicitly: onFinish only writes on success; mid-stream abort discards user message; client-side useChat retains prompt input for retry |
| T-06-24 | Information Disclosure | Logs leak secrets | mitigate | Zero console.log; CLAUDE.md hard constraint |
</threat_model>

<verification>
- `bunx vitest run app/api/proxy/chat/__tests__/route.test.ts` GREEN (HMAC + cross-check + onFinish DB write + 429 rate limit assertions)
- `bunx tsc --noEmit` passes
- `grep "toAIStreamResponse\\|501\\|not_implemented\\|console\\.\\|query.shop" app/api/proxy/chat/route.ts` returns 0 for each banned token
</verification>

<success_criteria>
- `app/api/proxy/chat/route.ts` ships full D-21 implementation, 501 stub deleted
- IDN-02 logged_in_customer_id cross-check enforced (critical security gate from RESEARCH)
- D-19 atomic onFinish write to Conversation.messages JSONB
- Vercel AI SDK v6 API contract maintained (toUIMessageStreamResponse, inputSchema)
- Multi-tenancy lock: shop only from wrapper closure
- Plan 01 RED test for this file is now GREEN
</success_criteria>

<output>
Create `.planning/phases/06-storefront-surface/06-09-SUMMARY.md` with the replaced file's size, the exported POST handler's auth wrapper, and a grep audit summary.
</output>
