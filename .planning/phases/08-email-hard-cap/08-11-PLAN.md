---
phase: 08-email-hard-cap
plan: 11
type: execute
wave: 5
depends_on: ["08-04", "08-05", "08-06", "08-03"]
files_modified:
  - inngest/functions/sync-products.ts
autonomous: true
requirements: [NOT-01]
tags: [phase-08, inngest, sync, success-email]
must_haves:
  truths:
    - "inngest/functions/sync-products.ts gains exactly one new step.run('send-success-email', ...) immediately AFTER the existing finalize step"
    - "The step reloads SyncRun, skips when emailSentAt is non-null OR state === 'failed' (failure branch handles its own send), skips when contactEmail is null/empty (D-05)"
    - "On success, calls sendSyncSuccess with adminUrl built from SHOPIFY_APP_HANDLE env var; then atomic $executeRaw stamps emailSentAt with WHERE emailSentAt IS NULL guard (D-04)"
    - "All existing Phase 2/3 steps remain byte-identical (git diff shows only one added step.run block)"
    - "Wave 0 Inngest success-email it() blocks go GREEN"
  artifacts:
    - path: "inngest/functions/sync-products.ts"
      provides: "Phase 2 sync function extended with the success-email step"
      contains: "step.run('send-success-email'"
  key_links:
    - from: "step.run('send-success-email', ...)"
      to: "sendSyncSuccess"
      via: "from @/services/email/EmailService"
      pattern: "sendSyncSuccess\\("
    - from: "step.run('send-success-email', ...)"
      to: "fetchShopContactEmail"
      via: "from @/services/shopify/ShopifyShopService"
      pattern: "fetchShopContactEmail\\("
    - from: "emailSentAt stamp"
      to: "atomic UPDATE … WHERE emailSentAt IS NULL"
      via: "$executeRaw template literal"
      pattern: "emailSentAt.*IS NULL"
---

<objective>
Append a single `step.run('send-success-email', ...)` block to the main handler of the existing Phase 2 Inngest sync function, immediately after the `finalize` step. The step does the 6-action sequence from RESEARCH §System Architecture: reload SyncRun → idempotency skip → contact-email fetch → render → send → atomic stamp.

Three-layer idempotency defense in depth (D-04 + Pattern 1):
1. Inngest step.run memoization keyed by 'send-success-email'
2. Application-level SyncRun.emailSentAt stamp (atomic UPDATE … WHERE emailSentAt IS NULL)
3. Resend platform-level idempotencyKey 'sync-success/{syncRunId}' (24h server-side)

Purpose: Wire the success-branch notification into the canonical sync outcome (D-03). The email send must not fail the sync — D-05.

Output: One file edit + Wave 0 success-email Inngest tests go GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-email-hard-cap/08-CONTEXT.md
@.planning/phases/08-email-hard-cap/08-RESEARCH.md
@inngest/functions/sync-products.ts
@inngest/functions/__tests__/sync-products.test.ts
@services/email/EmailService.ts
@services/shopify/ShopifyShopService.ts
@lib/db/client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append step.run('send-success-email', ...) after the finalize step</name>
  <files>inngest/functions/sync-products.ts</files>
  <behavior>
    - New step.run('send-success-email', async () => { ... }) inserted immediately AFTER the existing `return await step.run('finalize', ...)` block — restructured so finalize returns into a local const, then the email step runs, then the function returns the captured finalize result. The function's return value MUST be preserved (existing tests assert on the finalize return shape).
    - Step body: (1) const run = await prisma.syncRun.findUnique({ where: { id: syncRunId } }); (2) if (!run || run.emailSentAt || run.state === 'failed') return; — the 'failed' guard prevents the success branch from sending when finalize marked the run partial-with-100%-errors (defensive); (3) const contactEmail = await fetchShopContactEmail(session); if (!contactEmail) return; — D-05 graceful skip; (4) const shopSlug = shop.replace('.myshopify.com', ''); const handle = process.env.SHOPIFY_APP_HANDLE; const adminUrl = handle ? `https://admin.shopify.com/store/${shopSlug}/apps/${handle}` : `https://admin.shopify.com/store/${shopSlug}`; — Assumption A1 defensive fallback; (5) await sendSyncSuccess({ to: contactEmail, shop, productCount: run.processedCount, adminUrl, syncRunId }); (6) await prisma.$executeRaw`UPDATE sync_runs SET "emailSentAt" = NOW() WHERE id = ${syncRunId} AND "emailSentAt" IS NULL`;
    - Zero console.* — error inside step.run propagates to Inngest for retry (Assumption A4)
    - finalize's existing return value must remain the function-level return
  </behavior>
  <action>Edit inngest/functions/sync-products.ts. At the top, add imports: `import { sendSyncSuccess } from '@/services/email/EmailService';` and `import { fetchShopContactEmail } from '@/services/shopify/ShopifyShopService';`. Restructure the existing `return await step.run('finalize', ...)` (lines 186-199): capture the result into `const finalizeResult = await step.run('finalize', async () => { ... });` — body unchanged, just change `return await` to `const finalizeResult = await` AND wrap the existing inner function body identically. Then append the new step.run block per the behavior section above. Function returns `finalizeResult` at the end. All existing Phase 2/3 steps stay byte-identical — only the finalize call site changes from `return await` to `const finalizeResult = await` + new return at function bottom + new email step inserted between them. Use the existing `session` variable (already loaded on line 46) and the existing `syncRunId` + `shop` destructured on line 43. Use the existing prisma + sessionStorage imports — no new prisma import needed.</action>
  <verify>
    <automated>bunx vitest run inngest/functions/__tests__/sync-products.test.ts 2>&1 | grep -E "✓|sends success email" | head -10; grep -c "step.run('send-success-email'" inngest/functions/sync-products.ts | grep -q "^1$"; ! grep -E "console\\." inngest/functions/sync-products.ts</automated>
  </verify>
  <done>Inngest sync-products.test.ts success-email it() blocks GREEN; existing Phase 2/3 tests STILL GREEN (regression check); exactly one 'send-success-email' step exists; no console.* in file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inngest step → Resend API | Outbound HTTPS via SDK |
| Inngest step → Shopify GraphQL | Authenticated offline session |
| Inngest step → Postgres | Stamp uses $executeRaw with parameter-bound syncRunId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11-T1 | Tampering / Spoofing | Replay → duplicate success email | mitigate | Three-layer defense (Pattern 1): step.run memoization + emailSentAt stamp + Resend idempotencyKey (24h) |
| T-08-11-T2 | Tampering | emailSentAt double-write race (two concurrent function retries) | mitigate | Atomic $executeRaw with WHERE emailSentAt IS NULL — second update is a no-op (0 rows affected) |
| T-08-11-I1 | Information disclosure | Logging contactEmail | mitigate | Zero console.* in file; fetchShopContactEmail also has bare-catch (no log) |
| T-08-11-D1 | Denial of service | Sync failure due to email send error | mitigate | D-05 + bare catch in fetchShopContactEmail returns null → step skips gracefully; sendSyncSuccess throw propagates to Inngest step retry (Assumption A4) — sync result already committed in finalize |
| T-08-11-T3 | Tampering | Open redirect via adminUrl | mitigate | adminUrl built from server-side env (SHOPIFY_APP_HANDLE) + shop slug derived from the authenticated session — no user input in URL path |
</threat_model>

<verification>
- `bunx vitest run inngest/functions/__tests__/sync-products.test.ts` GREEN (incl. Phase 2/3 pre-existing)
- `grep -c "step.run('send-success-email'" inngest/functions/sync-products.ts == 1`
- Phase 2/3 finalize return shape unchanged (tests assert this)
</verification>

<success_criteria>
- NOT-01 satisfied: successful sync sends email with productCount + admin link
- D-04 idempotency: re-running the step is a no-op when emailSentAt is set
- D-05 graceful skip when contactEmail is missing
</success_criteria>

<output>
Create `.planning/phases/08-email-hard-cap/08-11-SUMMARY.md` summarizing the surgical edit and Wave 0 status flips.
</output>
