---
phase: 08-email-hard-cap
plan: 04
type: execute
wave: 4
depends_on: ["08-01", "08-03"]
files_modified:
  - services/email/EmailService.ts
  - package.json
  - bun.lock
autonomous: true
requirements: [NOT-04, NOT-01, NOT-02]
tags: [phase-08, email, resend, services]
user_setup:
  - service: resend
    why: "Transactional email provider (CLAUDE.md lock)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard → API Keys → Create API key (re_xxxxx)"
      - name: RESEND_FROM_ADDRESS
        source: "Verified sending domain in Resend → e.g., noreply@smartdiscovery.app"
    dashboard_config:
      - task: "Add and verify the sending domain (DNS records for SPF + DKIM + DMARC)"
        location: "Resend Dashboard → Domains → Add Domain"
must_haves:
  truths:
    - "EmailService exports sendSyncSuccess and sendSyncFailure"
    - "Each function calls render(...) on the template + resend.emails.send({...}, { idempotencyKey: '<event>/<syncRunId>' })"
    - "from address read from process.env.RESEND_FROM_ADDRESS at module load (NOT-04 — env-scoped)"
    - "Throws Error when result.error is truthy — does not silently swallow Resend failures (Assumption A4)"
    - "Module contains zero console.* calls (CLAUDE.md hard rule + Pitfall 6 — no contactEmail or RESEND_API_KEY logging)"
    - "Wave 0 RED tests for EmailService go GREEN"
  artifacts:
    - path: "services/email/EmailService.ts"
      provides: "sendSyncSuccess(args) + sendSyncFailure(args) — typed wrappers over resend.emails.send"
      exports: ["sendSyncSuccess", "sendSyncFailure", "SendSyncSuccessArgs", "SendSyncFailureArgs"]
  key_links:
    - from: "services/email/EmailService.ts"
      to: "lib/email/templates/SyncSuccessEmail.tsx"
      via: "render(SyncSuccessEmail(args))"
      pattern: "render\\(SyncSuccessEmail"
    - from: "services/email/EmailService.ts"
      to: "lib/email/templates/SyncFailureEmail.tsx"
      via: "render(SyncFailureEmail(args))"
      pattern: "render\\(SyncFailureEmail"
    - from: "resend.emails.send"
      to: "Resend platform-level idempotency"
      via: "second-arg { idempotencyKey } per Pitfall 4 (NOT headers)"
      pattern: "idempotencyKey:\\s*`sync-(success|failure)/"
---

<objective>
Install the Resend + React Email packages and create `services/email/EmailService.ts` (location decision: services/ not lib/ — mirrors services/shopify/ + services/chat/ for runtime-orchestrated I/O wrappers, per Resolved Claude's-Discretion items in additional_context).

Purpose: Provide a thin, typed wrapper over `resend.emails.send` so the Inngest function (08-11, 08-12) can fire success / failure emails without inlining SDK initialization or idempotency key construction.

Output: New service file + 3 new packages in package.json + Wave 0 EmailService.test.ts goes GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-email-hard-cap/08-CONTEXT.md
@.planning/phases/08-email-hard-cap/08-RESEARCH.md
@services/email/__tests__/EmailService.test.ts
@services/chat/getActiveChatModel.ts
@services/shopify/ShopifyProductService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Resend + React Email packages</name>
  <files>package.json, bun.lock</files>
  <action>Run `bun add resend@^6.12.4 @react-email/components@^1.0.12 @react-email/render@^2.0.8`. Per 08-RESEARCH.md §Package Legitimacy Audit, all three are vendor-owned (github.com/resend/*), have no postinstall scripts, and are LOCKED by CLAUDE.md + NOT-03 — no [ASSUMED]/[SUS] checkpoint required. After install, run `bun install --frozen-lockfile` to confirm lockfile integrity. Commit only package.json + bun.lock changes (no source code yet — 08-05 authors the templates that consume @react-email/components).</action>
  <verify>
    <automated>grep -E '"resend":|"@react-email/components":|"@react-email/render":' package.json | wc -l | grep -q "^3$" && bun install --frozen-lockfile 2>&1 | grep -qE "Saved lockfile|done"</automated>
  </verify>
  <done>All three packages present in package.json; bun.lock updated; no other dependency changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author services/email/EmailService.ts</name>
  <files>services/email/EmailService.ts</files>
  <behavior>
    - sendSyncSuccess({ to, shop, productCount, adminUrl, syncRunId }) calls render(SyncSuccessEmail({...})) then resend.emails.send with from=process.env.RESEND_FROM_ADDRESS, to=args.to, subject=`Catalog sync complete — ${productCount} products`, html=rendered, and second-arg options { idempotencyKey: `sync-success/${syncRunId}` }
    - sendSyncFailure({ to, shop, syncRunId, errorMessage, retryUrl }) mirrors with subject='Catalog sync failed' and idempotencyKey: `sync-failure/${syncRunId}`
    - Throws Error('Resend send failed: ...') when result.error is truthy (Assumption A4 — Inngest must see failures so step.run retries)
    - Module-top: const resend = new Resend(process.env.RESEND_API_KEY!) — `!` non-null assertion is acceptable here because missing key is a deploy-time misconfiguration that should fail loud (env validation is a deploy concern, not a runtime concern)
    - FROM constant: const FROM = process.env.RESEND_FROM_ADDRESS! — same rationale
    - Zero console.* calls (CLAUDE.md + Pitfall 6); no logging of args.to or args.errorMessage values
  </behavior>
  <action>Implement EmailService.ts verbatim per 08-RESEARCH.md §Code Examples "EmailService wrapper". Exports MUST be: sendSyncSuccess, sendSyncFailure, SendSyncSuccessArgs, SendSyncFailureArgs (interfaces). Import paths: `import { Resend } from 'resend';`, `import { render } from '@react-email/render';`, `import { SyncSuccessEmail } from '@/lib/email/templates/SyncSuccessEmail';`, `import { SyncFailureEmail } from '@/lib/email/templates/SyncFailureEmail';`. Module loads the Resend client and FROM constant at module scope so unit tests can mock the 'resend' module via vi.mock('resend', ...). NOTE: the template imports will be unresolved until 08-05 lands — that is expected; this plan and 08-05 are independent within Wave 4 (parallel), and the type-check at Wave 4 end is fine because module-level imports are still type-resolvable from the test mocks. If you need to land 08-04 strictly before 08-05 lands, the type-only forward references are fine because the templates are React function components — only their signatures are needed. Add a top-of-file JSDoc block explaining the Pitfall 4 idempotency-key contract (second-arg, not headers).</action>
  <verify>
    <automated>bunx vitest run services/email/__tests__/EmailService.test.ts 2>&1 | grep -E "✓|passed" | head -5; grep -E "console\\." services/email/EmailService.ts | grep -v "^\\s*\\*" || echo "OK no console.* in shipped code"</automated>
  </verify>
  <done>EmailService.test.ts goes GREEN; zero console.* calls in services/email/EmailService.ts; both exports present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EmailService → Resend | Outbound HTTPS to api.resend.com via SDK (TLS by default — V9 Communications) |
| EmailService → process.env | RESEND_API_KEY + RESEND_FROM_ADDRESS read at module load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-T1 | Tampering | Replay → duplicate email | mitigate | `idempotencyKey: 'sync-{success,failure}/{syncRunId}'` second-arg form (Pitfall 4); 24h retention server-side; defense-in-depth with Inngest step memoization + SyncRun.emailSentAt |
| T-08-04-I1 | Information disclosure | Logging contactEmail or RESEND_API_KEY | mitigate | Zero console.* in this file (CLAUDE.md + Pitfall 6); error throws use only result.error.message — never args.to |
| T-08-04-T2 | Tampering | Header-form idempotency that doesn't actually dedup | mitigate | Use second-arg options form, NOT headers (Pitfall 4); test asserts the SDK call shape |
| T-08-04-D1 | Denial of service | Resend outage causing sync to hang | accept | Inngest step.run retry handles transient outages; Assumption A4 — throw on result.error |
| T-08-04-SC | Tampering | npm install of resend / react-email | mitigate | All vendor-owned (github.com/resend/*); no postinstall scripts; LOCKED by CLAUDE.md + NOT-03 — no slopcheck gate needed |
</threat_model>

<verification>
- `bunx vitest run services/email/__tests__/EmailService.test.ts` GREEN
- `grep -E "console\\." services/email/EmailService.ts` returns no shipped-code lines
- `package.json` includes resend, @react-email/components, @react-email/render at the pinned versions
</verification>

<success_criteria>
- NOT-04 satisfied: from address comes from env, not per-shop config
- sendSyncSuccess + sendSyncFailure exports exist and are typed
- Wave 0 EmailService.test.ts goes from RED to GREEN
</success_criteria>

<output>
Create `.planning/phases/08-email-hard-cap/08-04-SUMMARY.md` listing the installed package versions, the exports, and the Wave-0 test status flip.
</output>
