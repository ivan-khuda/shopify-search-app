---
phase: 08-email-hard-cap
plan: 10
type: execute
wave: 5
depends_on: ["08-07", "08-08"]
files_modified:
  - services/chat/CapService.ts
autonomous: true
requirements: [CAP-02, CAP-03]
tags: [phase-08, services, cap, chat]
must_haves:
  truths:
    - "services/chat/CapService.ts exports tryConsumeRequest(shop): Promise<{ allowed: boolean }>"
    - "Reads env HARD_CAP_REQUESTS_PER_MONTH (default 2000); invalid values fall back to 2000 (CAP-02 lock)"
    - "Composes getCurrentPeriod() + requestCounterRepository.tryConsume — thin resolver, mirrors getActiveChatModel.ts style"
    - "Wave 0 CapService.test.ts goes GREEN"
  artifacts:
    - path: "services/chat/CapService.ts"
      provides: "tryConsumeRequest helper consumed by both chat routes"
      exports: ["tryConsumeRequest"]
  key_links:
    - from: "services/chat/CapService.ts"
      to: "lib/db/repositories/RequestCounterRepository.ts"
      via: "requestCounterRepository.tryConsume(shop, period, cap)"
      pattern: "requestCounterRepository\\.tryConsume"
    - from: "services/chat/CapService.ts"
      to: "lib/util/period.ts"
      via: "getCurrentPeriod()"
      pattern: "getCurrentPeriod\\(\\)"
---

<objective>
Author the thin per-request resolver that both chat routes (08-13, 08-14) consume. Mirrors the structural style of `services/chat/getActiveChatModel.ts` (Phase 7 pattern reference) — a small synchronous-ish composer that wires env + helper + repository.

Purpose: Centralize cap-check logic. Routes never inline env reads or period derivation; they get a single allowed/denied verdict.

Output: One file + Wave 0 test goes GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-email-hard-cap/08-CONTEXT.md
@.planning/phases/08-email-hard-cap/08-RESEARCH.md
@services/chat/__tests__/CapService.test.ts
@services/chat/getActiveChatModel.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author services/chat/CapService.ts</name>
  <files>services/chat/CapService.ts</files>
  <behavior>
    - Module-private constant DEFAULT_CAP = 2000 (D-09 locked default)
    - Module-private function readCap(): number — parses process.env.HARD_CAP_REQUESTS_PER_MONTH via parseInt(., 10); returns DEFAULT_CAP when missing, NaN, ≤ 0, or non-finite
    - Exports tryConsumeRequest(shop: string): Promise<{ allowed: boolean }>
    - Function body: const period = getCurrentPeriod(); const cap = readCap(); const r = await requestCounterRepository.tryConsume(shop, period, cap); return { allowed: r.allowed };
    - Zero console.* calls
    - JSDoc explains: empty shop is not specifically guarded here because both chat routes derive shop from the authenticated session (withShopifySession / withAppProxyHmac) — passing an empty shop is a caller bug, not a security gap
  </behavior>
  <action>Implement verbatim per 08-RESEARCH.md §Code Examples "CapService" (lines 867-886). Imports: `import { requestCounterRepository } from '@/lib/db/repositories/RequestCounterRepository';`, `import { getCurrentPeriod } from '@/lib/util/period';`. Return shape is intentionally `{ allowed: boolean }` (not the discriminated union from the repository) — routes only branch on allowed; they do not need the requestCount value. JSDoc cites D-14 (single-helper co-location) and CAP-02 (env-driven cap).</action>
  <verify>
    <automated>bunx vitest run services/chat/__tests__/CapService.test.ts 2>&1 | grep -E "✓|passed" | head -5; ! grep -E "console\\." services/chat/CapService.ts</automated>
  </verify>
  <done>Wave 0 test GREEN; env default 2000; env override parses positive int; invalid env falls back to 2000; zero console.* calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route → CapService | shop is trusted (caller-derived from authenticated session) |
| CapService → process.env | HARD_CAP_REQUESTS_PER_MONTH read per request (cheap; allows operator rollout via env var rotation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10-T1 | Tampering | env var rotated to 0 / -1 / 'abc' bypassing the cap | mitigate | readCap() guards via Number.isFinite + > 0 check; falls back to DEFAULT_CAP=2000 |
| T-08-10-T2 | Tampering | shop spoofing via body / query | mitigate | Mitigation is at the route layer (08-13 uses withShopifySession ctx.shop; 08-14 uses withAppProxyHmac signed query) — CapService trusts its arg by contract; tests assert this trust boundary |
</threat_model>

<verification>
- `bunx vitest run services/chat/__tests__/CapService.test.ts` GREEN
- `grep -E "console\\." services/chat/CapService.ts` empty
</verification>

<success_criteria>
- CAP-02 satisfied: env-driven cap with safe defaults
- Wave 0 test flips RED → GREEN
- Both chat routes can import a single function and get a boolean
</success_criteria>

<output>
Create `.planning/phases/08-email-hard-cap/08-10-SUMMARY.md` (brief).
</output>
