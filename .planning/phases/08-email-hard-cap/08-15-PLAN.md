---
phase: 08-email-hard-cap
plan: 15
type: execute
wave: 7
depends_on: ["08-11", "08-12", "08-13", "08-14"]
files_modified:
  - .planning/phases/08-email-hard-cap/08-VALIDATION.md
  - .planning/phases/08-email-hard-cap/08-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [NOT-01, NOT-02, NOT-03, NOT-04, CAP-01, CAP-02, CAP-03]
tags: [phase-08, verification-gate, nyquist, sign-off]
must_haves:
  truths:
    - "Full vitest suite runs GREEN (bunx vitest run)"
    - "bunx tsc --noEmit is clean"
    - "All 7 requirement IDs from this phase (NOT-01..04, CAP-01..03) traced to passing tests in 08-VALIDATION.md Per-Task Verification Map"
    - "08-VALIDATION.md nyquist_compliant frontmatter flag flips to true"
    - "ROADMAP.md Phase 8 status flips from [ ] to [x] (or [-] verified-with-deferred-smoke if manual smokes deferred)"
    - "REQUIREMENTS.md status for the 7 phase IDs flips to Complete (Phase 8) or Complete-deferred-smoke"
    - "STATE.md Decisions block gains a Phase 8 entry summarizing the verified state"
    - "08-VERIFICATION.md document exists capturing: automated evidence, manual-smoke instructions, deferred items list, security disposition"
  artifacts:
    - path: ".planning/phases/08-email-hard-cap/08-VERIFICATION.md"
      provides: "Phase 8 verification record — automated + manual + deferred"
      contains: "## Automated Evidence"
    - path: ".planning/phases/08-email-hard-cap/08-VALIDATION.md"
      provides: "Per-Task Verification Map fully populated"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "08-VERIFICATION.md"
      to: "ROADMAP.md Phase 8 status"
      via: "Verification gate is the contract for flipping the roadmap checkbox"
      pattern: "verified|verified-with-deferred-smoke"
---

<objective>
Run the full verification gate for Phase 8: aggregate automated evidence from all preceding plans, populate the Per-Task Verification Map in 08-VALIDATION.md, document manual-smoke procedures + their deferral status, and update the project planning artifacts (ROADMAP, REQUIREMENTS, STATE).

Per the Phase 4 / Phase 7 precedent (both passed with "verified-with-deferred-smoke" status), this gate is autonomous: false because:
1. The Per-Task Verification Map is filled in based on actual file IDs from execution — judgment is required when test IDs don't line up 1:1 with the plan's anticipated tasks
2. Manual-smoke items (real Resend send to a dev shop; cap-reached cross-route smoke) require operator action and may be deferred per past precedent
3. The "verified" vs "verified-with-deferred-smoke" status decision belongs to the operator

Output: 4 planning files updated + 1 new 08-VERIFICATION.md authored.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-email-hard-cap/08-CONTEXT.md
@.planning/phases/08-email-hard-cap/08-RESEARCH.md
@.planning/phases/08-email-hard-cap/08-VALIDATION.md

# Reference precedent for verification gate format
@.planning/phases/07-admin-settings-model-picker
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated verification suite</name>
  <files>/tmp/p8/verify-suite.log, /tmp/p8/tsc.log</files>
  <action>Run: `bunx vitest run 2>&1 | tee /tmp/p8/verify-suite.log` — full suite must be GREEN (zero FAIL, zero failed it() blocks). Then run `bunx tsc --noEmit 2>&1 | tee /tmp/p8/tsc.log` — output must be empty (no "error TS" lines). Also run the targeted Phase 8 sampling: `bunx vitest run services/email services/shopify services/chat lib/db/repositories lib/email lib/util lib/chat lib/inngest 'app/api/chat' 'app/api/proxy/chat' 2>&1 | tee /tmp/p8/verify-phase-8.log`. Also run `bun lint` — must exit 0. Also run `grep -rE "console\\." services/email services/shopify/ShopifyShopService.ts services/chat/CapService.ts lib/db/repositories/RequestCounterRepository.ts lib/util/period.ts lib/chat/cap-reached-response.ts lib/email/templates/ inngest/functions/sync-products.ts | grep -v "^\\s*\\*" | grep -v "__tests__"` — must return empty (CLAUDE.md hard rule + Pitfall 6). Then run `grep -rE "dangerouslySetInnerHTML" lib/email/templates/` — empty. Collect all log outputs for inclusion in 08-VERIFICATION.md.</action>
  <verify>
    <automated>tail -5 /tmp/p8/verify-suite.log | grep -E "passed|failed"; tail -3 /tmp/p8/tsc.log | wc -l; bun lint 2>&1 | tail -3</automated>
  </verify>
  <done>Full suite GREEN with explicit pass count; tsc clean; lint clean; no console.* / no dangerouslySetInnerHTML in shipped files.</done>
</task>

<task type="auto">
  <name>Task 2: Populate 08-VALIDATION.md Per-Task Verification Map + flip nyquist_compliant flag</name>
  <files>.planning/phases/08-email-hard-cap/08-VALIDATION.md</files>
  <action>Edit 08-VALIDATION.md. For each Phase 8 task that produced a `<automated>` verify command, add a row to the Per-Task Verification Map with: Task ID (08-NN-T1 / T2), Plan number, Wave, Requirement ID, Threat Ref (T-08-NN-XX from each plan's threat_model), Secure Behavior summary, Test Type (unit / integration / static), Automated Command (verbatim from the plan), File Exists (✅ if present), Status (✅ green / ❌ red / ⚠️ flaky / ⬜ pending). The Validation Sign-Off checklist at the bottom must be filled: all 6 boxes checked, "Approval: gate-passed" (or "gate-passed-with-deferred-smoke" if any manual smoke is deferred). Frontmatter: flip `nyquist_compliant: false` to `nyquist_compliant: true`, set `wave_0_complete: true`, update `status: verified` (or `verified-with-deferred-smoke`).</action>
  <verify>
    <automated>grep -c "08-0[0-9]-T" .planning/phases/08-email-hard-cap/08-VALIDATION.md; grep "nyquist_compliant: true" .planning/phases/08-email-hard-cap/08-VALIDATION.md; grep "Approval:" .planning/phases/08-email-hard-cap/08-VALIDATION.md</automated>
  </verify>
  <done>≥ 15 task-rows populated; nyquist_compliant flipped; Approval line populated.</done>
</task>

<task type="auto">
  <name>Task 3: Author .planning/phases/08-email-hard-cap/08-VERIFICATION.md</name>
  <files>.planning/phases/08-email-hard-cap/08-VERIFICATION.md</files>
  <action>Author a new file matching the Phase 4 / Phase 7 verification-record format. Sections: ## Automated Evidence (full-suite pass count, tsc clean line, lint clean line, console.* / dangerouslySetInnerHTML grep results, key Phase-8 it() block names + their pass status); ## Requirements Coverage (table mapping NOT-01..04 + CAP-01..03 to plan IDs + test IDs + verified-by command); ## Security Disposition (table copy/paste of all STRIDE threats from the 14 prior plans' threat_model blocks with their disposition status — every threat must be 'mitigated', 'accepted', or 'n/a'); ## Manual-Smoke Checklist (verbatim from 08-VALIDATION.md Manual-Only Verifications table — 4 items: real success email, real failure email + retry, cap-reached cross-route smoke, optional race integration test); for each item: mark ✅ verified (if operator ran it during this gate) OR ⬜ deferred-to-operator (with rationale: operator-only / requires-verified-domain / requires-dev-shop); ## Deferred Items (any deferred-smoke entries get added to STATE.md Deferred Items table format for inclusion in Task 5); ## Resolved Open Questions (cite resolutions from 08-RESEARCH.md and additional_context: Open Q1 → one cap-reached constant for V1; Open Q2 → contactEmail readable without extra scopes [or document scope addition if discovered]; Open Q3 → two-tier race testing with optional integration test); ## Sign-Off (operator name + date + status: 'verified' OR 'verified-with-deferred-smoke').</action>
  <verify>
    <automated>test -f .planning/phases/08-email-hard-cap/08-VERIFICATION.md; grep -c "^## " .planning/phases/08-email-hard-cap/08-VERIFICATION.md | awk '$1 >= 6 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>File exists with at least 6 top-level sections; manual-smoke items each have a status; sign-off line present.</done>
</task>

<task type="auto">
  <name>Task 4: Update ROADMAP.md Phase 8 status</name>
  <files>.planning/ROADMAP.md</files>
  <action>Edit .planning/ROADMAP.md. In the Phases checklist near the top, change `- [ ] **Phase 8: Email + Hard Cap** - ...` to `- [x] **Phase 8: Email + Hard Cap** - ... (completed YYYY-MM-DD)` (or use `[-]` + "verified-with-deferred-smoke" footnote if manual smokes deferred, matching Phase 4 / Phase 7 precedent — see the existing footnote pattern on Phase 4 / Phase 7 lines). In ## Progress table, update the Phase 8 row: Plans Complete = "15/15", Status = "Complete" or "Complete (passed-with-deferred-smoke)", Completed = today's date in YYYY-MM-DD UTC. In ### Phase 8: Email + Hard Cap section, append a `**Plans**: 15 plans` line listing all 15 plans by ID + brief objective (mirror the Phase 6 / Phase 7 plan list format).</action>
  <verify>
    <automated>grep -E "\\[(x|-)\\] \\*\\*Phase 8" .planning/ROADMAP.md; grep -E "08-15-PLAN" .planning/ROADMAP.md</automated>
  </verify>
  <done>Roadmap shows Phase 8 checked; 15 plans listed; progress table updated.</done>
</task>

<task type="auto">
  <name>Task 5: Update REQUIREMENTS.md + STATE.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>Edit REQUIREMENTS.md Traceability table: change each of NOT-01..04 + CAP-01..03 rows from `Pending` to `Complete (Phase 8)` (or `Complete-deferred-smoke (Phase 8)` matching the actual verification status). Increment the Coverage block at the bottom if needed (counts already align at 54). Also flip the `- [ ]` checkboxes to `- [x]` for the 7 IDs in their requirement sections. Edit STATE.md: update frontmatter (last_updated, last_activity, status: verifying → planning if next phase TBD else this is the final phase per ROADMAP — set status: "complete-v1-milestone"; stopped_at: "Phase 8 verification gate passed"; progress.completed_phases 7 → 8; progress.percent 88 → 100). Append a new Decisions entry: `- Phase 8 (verified [or verified-with-deferred-smoke] YYYY-MM-DD): Email + Hard Cap complete (15/15 plans, NOT-01..04 + CAP-01..03 satisfied). [Summarize the key implementation facts in 2-3 sentences: EmailService + React Email templates at lib/email/templates/; Resend send with second-arg idempotencyKey; Inngest function gained send-success-email step after finalize + send-failure-email step inside onFailure with DISTINCT step IDs (Pitfall 2); SyncRun.emailSentAt nullable column applied via Option A non-destructive migration; RequestCounter model with composite PK (shop, period) + atomic $queryRaw INSERT...ON CONFLICT...DO UPDATE WHERE count < cap RETURNING; CapService.tryConsumeRequest co-located helper; both /api/chat and /api/proxy/chat call capReachedResponse() returning HTTP 200 streamed v6 UI message on cap. Manual smokes deferred per [list].]`. Add any deferred items to the Deferred Items table.</action>
  <verify>
    <automated>grep -E "NOT-0[1-4]\\s*\\|.*Complete" .planning/REQUIREMENTS.md | wc -l | grep -q "^4$"; grep -E "CAP-0[1-3]\\s*\\|.*Complete" .planning/REQUIREMENTS.md | wc -l | grep -q "^3$"; grep "Phase 8" .planning/STATE.md | head -3</automated>
  </verify>
  <done>All 7 phase requirements flipped to Complete in traceability + checkbox; STATE.md gets Phase 8 decisions entry + frontmatter updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 8 verification gate ready for sign-off — all automated evidence collected, all planning artifacts updated, manual-smoke status documented.</what-built>
  <how-to-verify>1. Inspect /tmp/p8/verify-suite.log — confirm "Tests N passed" with zero failures (current Phase 7 baseline was 354/358 passed with 4 skipped historical). 2. Inspect /tmp/p8/tsc.log — confirm empty (no TS errors). 3. Inspect .planning/phases/08-email-hard-cap/08-VERIFICATION.md — confirm sign-off section, manual-smoke status, deferred items list. 4. Inspect ROADMAP.md Phase 8 row — confirm checked status + date. 5. Optionally run manual smokes from 08-VALIDATION.md against a dev shop + Resend dashboard (real success email, real failure email + retry, cap-reached on both surfaces). If running smokes: update 08-VERIFICATION.md to flip ⬜ deferred → ✅ verified. If deferring: status is verified-with-deferred-smoke. 6. Reply 'verified' or 'verified-with-deferred-smoke' to advance.</how-to-verify>
  <resume-signal>Type "verified" (full sign-off) or "verified-with-deferred-smoke" (manual smokes deferred to operator) or describe any failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 7: Commit verification artifacts</name>
  <files>.planning/phases/08-email-hard-cap/08-VERIFICATION.md, .planning/phases/08-email-hard-cap/08-VALIDATION.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>Stage and commit the 5 files in a single commit: `git add .planning/phases/08-email-hard-cap/08-VERIFICATION.md .planning/phases/08-email-hard-cap/08-VALIDATION.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md` then commit with message `docs(08): phase 8 verification gate (status from Task 6 — verified or verified-with-deferred-smoke)`. Do NOT push without operator approval.</action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -E "phase 8 verification"</automated>
  </verify>
  <done>Single commit landed with the 5 files; commit message matches pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification gate → planning artifacts | Documentation updates only — no production code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15-T1 | Tampering | Premature gate pass without manual smoke evidence | mitigate | Task 6 is autonomous:false — requires operator to consciously choose "verified" vs "verified-with-deferred-smoke" (matches Phase 4 / Phase 7 precedent) |
| T-08-15-I1 | Information disclosure | Verification logs leaking secrets | mitigate | Verify suite logs to /tmp/p8/ (gitignored); 08-VERIFICATION.md includes only structural evidence (pass counts, file paths, line numbers) — no env values |
| T-08-15-R1 | Repudiation | Sign-off without traceable evidence | mitigate | 08-VERIFICATION.md Requirements Coverage table maps every NOT-/CAP- ID to a specific test command; STATE.md Decisions entry includes verification date |
</threat_model>

<verification>
- Full vitest suite GREEN
- bunx tsc --noEmit clean
- bun lint clean
- No console.* / dangerouslySetInnerHTML in shipped Phase-8 files
- All 7 requirement IDs traced to passing tests
- All 4 planning artifacts updated + committed
</verification>

<success_criteria>
- All 7 Phase 8 requirement IDs verified
- All STRIDE threats from the 14 prior plans dispositioned
- ROADMAP / REQUIREMENTS / STATE reflect the verified state
- Manual smokes either run or explicitly deferred with rationale
- v1 milestone complete (or complete-with-deferred-smoke)
</success_criteria>

<output>
Create `.planning/phases/08-email-hard-cap/08-15-SUMMARY.md` summarizing the gate outcome: status (verified / verified-with-deferred-smoke), suite pass count, deferred items count, sign-off timestamp.
</output>
