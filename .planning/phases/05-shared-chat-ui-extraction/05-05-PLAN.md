---
phase: 05-shared-chat-ui-extraction
plan: 05
type: execute
wave: 5
depends_on: ["05-04"]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SHR-01, SHR-02, SHR-03, SHR-04]
tags: [verification, gate, chat-ui]

must_haves:
  truths:
    - "SHR-01 satisfied: lib/chat-ui/ barrel exports the locked component set with zero @shopify/* / window.shopify / App Bridge imports outside lib/chat-ui/adapters/ (asserted by barrel-isolation.test.ts GREEN)"
    - "SHR-02 satisfied: ChatIdentityAdapter interface exists at lib/chat-ui/adapters/types.ts and is implemented by both EmbeddedAdapter and StorefrontAdapter (asserted by embedded-adapter.test.ts + storefront-adapter.test.ts GREEN)"
    - "SHR-03 satisfied: embedded admin chat-shell uses EmbeddedAdapter exclusively; storefront drawer surface (Phase 6) will use StorefrontAdapter — adapter file exists and is unit-tested (storefront-adapter.test.ts GREEN)"
    - "SHR-04 satisfied: app/(embedded)/chat/chat-shell.tsx imports ChatPane, HistoryPanel, SavedProductsPanel from @/lib/chat-ui (single barrel); zero @/components/chat/* references remain anywhere"
    - "D-12: hex literals in chat-shell are arbitrary-value Tailwind classes; verify-only no-op grep confirms no bare hex literals appear outside `[#...]` class syntax"
    - "TS strict build passes (bun build exits 0); zero `any` / `as any` / `as unknown as` in lib/chat-ui/components/ + lib/chat-ui/adapters/ + lib/chat-ui/stores/"
    - "Full vitest suite GREEN (bun test exits 0)"
    - ".planning/STATE.md, ROADMAP.md, REQUIREMENTS.md reflect Phase 5 completion with SHR-01..04 marked done"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Updated Current Position + Accumulated Context — Phase 5 verified"
      contains: "Phase 05 (shared-chat-ui-extraction) — VERIFIED"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 marked [x] complete with verification note"
      contains: "[x] **Phase 5: Shared Chat-UI Extraction**"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SHR-01..04 marked [x] in v1 Requirements + Traceability"
      contains: "SHR-01"
  key_links:
    - from: "verification gate"
      to: "all 4 success criteria in ROADMAP Phase 5"
      via: "automated checks below"
      pattern: "exits 0"
---

<objective>
Final verification gate for Phase 5. Run the four success-criteria checks from ROADMAP §"Phase 5: Shared Chat-UI Extraction" and update the project planning artifacts (STATE.md, ROADMAP.md, REQUIREMENTS.md) to mark Phase 5 complete and SHR-01..04 satisfied.

Purpose: Convert Plans 01–04's structural delivery into a verified milestone. No new source code is written here — this plan is the executable proof that all 4 SHR requirements are satisfied.

Output: A pass/fail report against the 4 ROADMAP success criteria + updated STATE.md/ROADMAP.md/REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-shared-chat-ui-extraction/05-CONTEXT.md
@.planning/phases/05-shared-chat-ui-extraction/05-VALIDATION.md
@.planning/phases/05-shared-chat-ui-extraction/05-01-PLAN.md
@.planning/phases/05-shared-chat-ui-extraction/05-02-PLAN.md
@.planning/phases/05-shared-chat-ui-extraction/05-03-PLAN.md
@.planning/phases/05-shared-chat-ui-extraction/05-04-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification gate (4 ROADMAP success criteria)</name>
  <files>(no source files modified — verification commands only)</files>
  <read_first>
    - /Users/ikhuda/sites/personal/shopify-search-app/.planning/ROADMAP.md (Phase 5 success criteria — the 4 truths)
    - /Users/ikhuda/sites/personal/shopify-search-app/.planning/REQUIREMENTS.md (SHR-01..04 definitions)
    - /Users/ikhuda/sites/personal/shopify-search-app/.planning/phases/05-shared-chat-ui-extraction/05-VALIDATION.md (sampling rate + Wave 0 requirements)
  </read_first>
  <action>
    Execute the verification gate as a series of commands. Capture each command's exit code AND a one-line summary. Format the output as a 4-row table mapping each ROADMAP success criterion to its automated check.

    SC #1: `lib/chat-ui/` exports `ChatPane`, `ChatMessage`, `ProductCard`, `HistoryPanel`, `SavedProductsPanel` with zero `window.shopify` / App Bridge / Shopify-SDK imports.
    - Check (a): `grep -cE "^export\s+\{\s*(ChatPane|ChatMessage|ProductCard|HistoryPanel|SavedProductsPanel|EmptyState)" lib/chat-ui/index.ts` returns at least 6.
    - Check (b): `bunx vitest run lib/chat-ui/__tests__/barrel-isolation.test.ts` exits 0.
    - Check (c): `grep -rE "from\s+['\"]@shopify\b" lib/chat-ui/ | grep -v "/adapters/"` returns no matches.
    - Check (d): `grep -rE "window\.shopify|window\.Shopify|\bshopify\.idToken\b" lib/chat-ui/ | grep -v "/adapters/embedded" | grep -v "__tests__"` returns no matches.

    SC #2: `ChatIdentityAdapter` interface is the sole surface-specific seam; `EmbeddedAdapter` provides session-token Bearer; `StorefrontAdapter` provides visitor_id from localStorage.
    - Check (a): `test -f lib/chat-ui/adapters/types.ts` AND `grep -c "interface ChatIdentityAdapter" lib/chat-ui/adapters/types.ts` returns 1.
    - Check (b): `grep -c "class EmbeddedAdapter implements ChatIdentityAdapter" lib/chat-ui/adapters/embedded.ts` returns 1.
    - Check (c): `grep -c "class StorefrontAdapter implements ChatIdentityAdapter" lib/chat-ui/adapters/storefront.ts` returns 1.
    - Check (d): `bunx vitest run lib/chat-ui/__tests__/embedded-adapter.test.ts lib/chat-ui/__tests__/storefront-adapter.test.ts` exits 0.

    SC #3: Embedded admin chat page imports exclusively from `lib/chat-ui/` — no direct imports from `components/chat/` remain.
    - Check (a): `grep -rn "@/components/chat" app/ lib/ components/ 2>/dev/null` returns ZERO matches (D-11 grep gate).
    - Check (b): `test ! -d components/chat` succeeds (directory deleted in Plan 04).
    - Check (c): `grep -c "@/lib/chat-ui" app/\(embedded\)/chat/chat-shell.tsx` returns at least 1.
    - Check (d): `grep -c "EmbeddedAdapter" app/\(embedded\)/chat/chat-shell.tsx` returns at least 2 (import + instantiation).

    SC #4: TypeScript strict-mode build passes with no `any` casts in the shared barrel or either adapter.
    - Check (a): `bun build` exits 0.
    - Check (b): `grep -rnE ':\s*any\b|<any[,>]|as\s+any\b|as\s+unknown\s+as' lib/chat-ui/components/ lib/chat-ui/adapters/ lib/chat-ui/stores/ | grep -v "__tests__"` returns no matches (type-position `any` and `as any`/`as unknown as` casts forbidden in production; test-file `as never` casts permitted per CLAUDE.md; `bun build` exit 0 above is the authoritative TS-strict gate).

    Aggregate check: `bun test` (full suite) exits 0. Capture pass/fail counts.

    Produce a markdown table in the SUMMARY of this plan:
    | SC | Description | Check | Result |
    |----|-------------|-------|--------|
    | 1  | Barrel exports + no Shopify SDK imports | a/b/c/d | PASS/FAIL |
    | 2  | ChatIdentityAdapter seam + concrete implementations | a/b/c/d | PASS/FAIL |
    | 3  | Embedded admin uses lib/chat-ui exclusively | a/b/c/d | PASS/FAIL |
    | 4  | TS strict + no any | a/b | PASS/FAIL |
    | —  | Full vitest suite | bun test | PASS/FAIL |

    If ANY check FAILs, STOP and report which check failed with the command's output snippet. Do not proceed to Task 2 (planning artifacts update) until all 5 rows are PASS.
  </action>
  <verify>
    <automated>set -e; bun test &amp;&amp; bun build &amp;&amp; ! grep -rn "@/components/chat" app/ lib/ components/ 2>/dev/null &amp;&amp; echo "ALL GATES OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 SC rows + the aggregate "Full vitest suite" row report PASS.
    - `bun test` exits 0; output captured in the summary.
    - `bun build` exits 0; output captured in the summary.
    - `grep -rn '@/components/chat' app/ lib/ components/` returns ZERO matches.
    - `grep -rnE ':\s*any\b|<any[,>]|as\s+any\b|as\s+unknown\s+as' lib/chat-ui/components/ lib/chat-ui/adapters/ lib/chat-ui/stores/ | grep -v "__tests__"` returns ZERO matches.
    - `grep -rE "from\s+['\"]@shopify\b" lib/chat-ui/ | grep -v "/adapters/"` returns ZERO matches.
  </acceptance_criteria>
  <done>
    The 5-row verification table is built; all rows PASS; the table is ready to embed in 05-05-SUMMARY.md and STATE.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update planning artifacts (STATE.md, ROADMAP.md, REQUIREMENTS.md)</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - /Users/ikhuda/sites/personal/shopify-search-app/.planning/STATE.md (current — Current Position + Progress + Accumulated Context blocks to update)
    - /Users/ikhuda/sites/personal/shopify-search-app/.planning/ROADMAP.md (current — Phase 5 line + Progress table to update)
    - /Users/ikhuda/sites/personal/shopify-search-app/.planning/REQUIREMENTS.md (current — SHR-01..04 checkboxes + Traceability table to update)
  </read_first>
  <behavior>
    - STATE.md frontmatter reflects Phase 5 completion (status, last_activity, progress counters).
    - STATE.md "Current Position" section shows Phase 5 verified and Phase 6 as next focus.
    - STATE.md "Accumulated Context > Decisions" appends a Phase 5 entry summarizing the lift outcome.
    - ROADMAP.md Phase 5 line changes from `- [ ]` to `- [x]`.
    - ROADMAP.md Phase 5 section "Plans:" list is populated with the 5 plan filenames + brief objectives (markers [x] for completed).
    - ROADMAP.md Progress table row for Phase 5 updates to `5/5 | Complete | 2026-05-26`.
    - REQUIREMENTS.md SHR-01..04 checkboxes change from `- [ ]` to `- [x]`.
    - REQUIREMENTS.md Traceability table SHR-01..04 status changes from `Pending` to `Complete (Phase 5)`.
  </behavior>
  <action>
    Update `.planning/STATE.md`:
    1. Frontmatter:
       - `status: planning` (or whatever was set) → `status: phase-complete`
       - `stopped_at: Phase 5 UI-SPEC approved` → `stopped_at: Phase 5 verification gate closed`
       - `last_updated:` → ISO8601 timestamp of now
       - `last_activity: 2026-05-26 -- Phase 04 verification gate closed (manual smoke deferred)` → `last_activity: 2026-05-26 -- Phase 05 verification gate closed (lib/chat-ui barrel + adapters live)`
       - `progress.completed_phases: 4` → `5`
       - `progress.total_plans: 34` → `39` (34 + 5 new Phase 5 plans)
       - `progress.completed_plans: 34` → `39`
       - `progress.percent: 50` → `62` (5 / 8 rounded down)
    2. "Current Position" body:
       - `Phase: 05 (shared-chat-ui-extraction) — READY FOR DISCUSSION` → `Phase: 05 (shared-chat-ui-extraction) — VERIFIED`
       - `Plan: 0 of TBD` → `Plan: 5 of 5`
       - `Status: ...` → `Status: Phase 05 verified (4/4 success criteria PASS; bun build + bun test green)`
       - `Last activity:` → `2026-05-26 -- Phase 05 verification gate closed`
       - Progress bar update: `Progress: [█████░░░░░] 50%` → `Progress: [██████░░░░] 62%`
    3. "Accumulated Context > Decisions" — APPEND a new bullet (do NOT delete existing entries):
       - `- Phase 5 (verified 2026-05-26): `lib/chat-ui/` barrel ships ChatPane (renamed from Chat, named export, adapter-driven DefaultChatTransport with Resolvable headers/body), ChatMessage, ProductCard, HistoryPanel, SavedProductsPanel, EmptyState. ChatIdentityAdapter interface in lib/chat-ui/adapters/types.ts implemented by EmbeddedAdapter (App Bridge runtime global, ZERO @shopify/* imports) and StorefrontAdapter (localStorage 'smartdiscovery.visitor_id' + crypto.randomUUID, SSR-safe). HistoryStore + SavedProductsStore interfaces in lib/chat-ui/stores/types.ts with LocalStorage default implementations namespaced by scope (T-5-01 empty-scope throw guard). useHistoryStore + useSavedProductsStore React hooks via useSyncExternalStore with SSR snapshot. Embedded surface shell app/(embedded)/chat/chat-shell.tsx instantiates EmbeddedAdapter + store hooks; legacy components/chat/ tree fully deleted. UI-SPEC locked deltas: chat-message.tsx user bubble clamp `max-w-[min(448px,100%)]`; ChatPane no longer carries surface-specific heights. Barrel-isolation static-grep test (lib/chat-ui/__tests__/barrel-isolation.test.ts) enforces SHR-01 with adapter sub-path exemption (D-04).`
    4. "Session Continuity" — update `Stopped at:` and `Resume file:` to point at this plan's SUMMARY.

    Update `.planning/ROADMAP.md`:
    1. Phase 5 line in the top "Phases" checklist: `- [ ] **Phase 5: Shared Chat-UI Extraction** - lib/chat-ui barrel, adapter pattern, persistence models` → `- [x] **Phase 5: Shared Chat-UI Extraction** - lib/chat-ui barrel, adapter pattern, persistence models`
    2. Phase 5 detail section "**Plans**: TBD" → "**Plans**: 5 plans" followed by:
       ```
       Plans:
       - [x] 05-01-PLAN.md — Wave 0 RED test scaffolds (barrel isolation + adapter unit + store unit)
       - [x] 05-02-PLAN.md — ChatIdentityAdapter + EmbeddedAdapter + StorefrontAdapter + store interfaces + LocalStorage*Store + useSyncExternalStore hooks
       - [x] 05-03-PLAN.md — Lift 7 components (chat-pane.tsx with DefaultChatTransport) + barrel index.ts + relocate 5 tests (4 unit + 1 integration)
       - [x] 05-04-PLAN.md — Rebuild app/(embedded)/chat/chat-shell.tsx with EmbeddedAdapter + delete legacy components/chat/ tree
       - [x] 05-05-PLAN.md — Phase 5 verification gate + planning artifact updates
       ```
    3. Progress table row: `| 5. Shared Chat-UI Extraction | 0/TBD | Not started | - |` → `| 5. Shared Chat-UI Extraction | 5/5 | Complete | 2026-05-26 |`

    Update `.planning/REQUIREMENTS.md`:
    1. v1 Requirements "Shared Chat-UI Package" section:
       - `- [ ] **SHR-01**` → `- [x] **SHR-01**`
       - `- [ ] **SHR-02**` → `- [x] **SHR-02**`
       - `- [ ] **SHR-03**` → `- [x] **SHR-03**`
       - `- [ ] **SHR-04**` → `- [x] **SHR-04**`
    2. Traceability table:
       - `| SHR-01 | Phase 5 | Pending |` → `| SHR-01 | Phase 5 | Complete (Phase 5) |`
       - `| SHR-02 | Phase 5 | Pending |` → `| SHR-02 | Phase 5 | Complete (Phase 5) |`
       - `| SHR-03 | Phase 5 | Pending |` → `| SHR-03 | Phase 5 | Complete (Phase 5) |`
       - `| SHR-04 | Phase 5 | Pending |` → `| SHR-04 | Phase 5 | Complete (Phase 5) |`
    3. Footer `*Last updated:*` → `*Last updated: 2026-05-26 — SHR-01..04 marked complete after Phase 5 verification gate*`

    NO source code changes. NO changes to `lib/chat-ui/` or `app/`. This is a metadata-only task.
  </action>
  <verify>
    <automated>grep -c "Phase 05 .* VERIFIED" .planning/STATE.md &amp;&amp; grep -cE "\[x\] \*\*Phase 5:" .planning/ROADMAP.md &amp;&amp; grep -cE "\[x\] \*\*SHR-0[1-4]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "VERIFIED" .planning/STATE.md` returns at least 1 referring to Phase 5.
    - `grep -c "completed_phases: 5" .planning/STATE.md` returns 1.
    - `grep -c "\[x\] \*\*Phase 5:" .planning/ROADMAP.md` returns 1.
    - `grep -cE "05-0[1-5]-PLAN\.md" .planning/ROADMAP.md` returns 5 (all 5 plan filenames listed in Phase 5 section).
    - `grep -cE "5/5 \| Complete" .planning/ROADMAP.md` returns 1 (Progress table row updated).
    - `grep -cE "\[x\] \*\*SHR-0[1-4]" .planning/REQUIREMENTS.md` returns 4.
    - `grep -cE "SHR-0[1-4] \| Phase 5 \| Complete" .planning/REQUIREMENTS.md` returns 4.
    - Phase 1–4 historical entries in STATE.md "Accumulated Context > Decisions" are UNTOUCHED (verified by counting bullet lines before/after).
  </acceptance_criteria>
  <done>
    STATE.md, ROADMAP.md, REQUIREMENTS.md reflect Phase 5 completion + SHR-01..04 satisfaction.
  </done>
</task>

</tasks>

<verification>
- All Task 1 success-criteria rows PASS.
- `bun test` exits 0 (full vitest suite).
- `bun build` exits 0 (TS strict).
- Planning artifacts updated: STATE.md frontmatter + Current Position + Decisions; ROADMAP.md Phase 5 [x] + Plans list + Progress table; REQUIREMENTS.md SHR-01..04 [x] + Traceability rows.
- `git status` shows the three planning files modified plus the new 05-05-SUMMARY.md.
</verification>

<success_criteria>
- All 4 ROADMAP Phase 5 success criteria verified PASS via automated checks (see Task 1 table).
- `bun test` GREEN; `bun build` GREEN.
- `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` reflect Phase 5 completion.
- SHR-01, SHR-02, SHR-03, SHR-04 all marked complete in REQUIREMENTS.md (checkbox + Traceability table).
- Phase 5 line in ROADMAP.md marked `- [x]` with 5/5 plans complete on 2026-05-26.
- The phase is ready for `/gsd:verify-phase 05` formal verification gate (this plan is the structural close; the verify command provides external sign-off).
</success_criteria>

<output>
Create `.planning/phases/05-shared-chat-ui-extraction/05-05-SUMMARY.md` when done, containing:
- The full 5-row verification table from Task 1 with PASS/FAIL results and command snippets
- Total counts: tests run, tests passing, files in lib/chat-ui/, files deleted from components/chat/
- Frontmatter diff summary for STATE.md / ROADMAP.md / REQUIREMENTS.md
- Confirmation that SHR-01..04 are all marked Complete
- Next phase pointer: Phase 6 (Storefront Surface) depends on Phase 5
</output>
