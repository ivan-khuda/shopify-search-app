---
phase: 02-sync-pipeline
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/<timestamp>_add_sync_pipeline/migration.sql
autonomous: true
requirements: [SYN-04]
must_haves:
  truths:
    - "prisma/schema.prisma declares SyncState enum with exactly 5 values: queued, running, succeeded, failed, partial"
    - "SyncRun model exists with all D-03 fields and a unique constraint on idempotencyKey"
    - "WebhookEvent model exists with eventId as @id and (shop, topic) indexed"
    - "Product model gains updatedAtShopify DateTime? column (D-17)"
    - "Migration SQL is ADDITIVE only — no DROP TABLE, no DROP COLUMN, no DROP CONSTRAINT against Phase 1 tables"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "SyncState enum + SyncRun model + WebhookEvent model + Product.updatedAtShopify"
      contains: "enum SyncState, model SyncRun, model WebhookEvent, updatedAtShopify"
    - path: "prisma/migrations/<timestamp>_add_sync_pipeline/migration.sql"
      provides: "Additive SQL: CREATE TYPE, CREATE TABLE × 2, ALTER TABLE products ADD COLUMN, CREATE INDEX × 3"
      contains: "CREATE TYPE \"SyncState\", CREATE TABLE \"sync_runs\", CREATE TABLE \"webhook_events\", ALTER TABLE \"products\" ADD COLUMN \"updatedAtShopify\""
  key_links:
    - from: "prisma/migrations/<timestamp>_add_sync_pipeline/migration.sql"
      to: "prisma/schema.prisma"
      via: "matching CREATE TABLE shape vs model declaration"
      pattern: "CREATE TABLE \"sync_runs\""
---

<objective>
Additive Prisma schema rewrite — add `SyncState` enum, `SyncRun` model, `WebhookEvent` model, and `Product.updatedAtShopify` column. Author the SQL for the migration; the actual `bunx prisma migrate dev` invocation happens in Plan 04 (blocking checkpoint).

Purpose: Establish the persistent contract every Phase 2 route, Inngest function, and webhook handler will read/write.
Output: schema.prisma updated + migration.sql authored (not yet applied).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-sync-pipeline/02-CONTEXT.md
@.planning/phases/02-sync-pipeline/02-PATTERNS.md
@.planning/phases/02-sync-pipeline/02-RESEARCH.md
@prisma/schema.prisma
@prisma/migrations/20260523011257_add_shop_column_destructive/migration.sql

<interfaces>
Target schema additions per 02-PATTERNS.md §"Prisma schema additions" and §"Migration SQL":

```prisma
enum SyncState {
  queued
  running
  succeeded
  failed
  partial
}

model SyncRun {
  id             String    @id @default(cuid())
  shop           String
  state          SyncState @default(queued)
  processedCount Int       @default(0)
  totalCount     Int?
  errors         String[]  @default([])
  cursor         String?
  idempotencyKey String    @unique
  startedAt      DateTime  @default(now())
  finishedAt     DateTime?

  @@index([shop])
  @@map("sync_runs")
}

model WebhookEvent {
  eventId    String   @id
  shop       String
  topic      String
  receivedAt DateTime @default(now())

  @@index([shop])
  @@map("webhook_events")
}

// Inside existing Product model — add ONLY this field:
updatedAtShopify DateTime?
```

Migration SQL skeleton from 02-PATTERNS.md (CREATE TYPE, CREATE TABLE × 2, ALTER TABLE products ADD COLUMN, CREATE INDEX × 3).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SyncState enum + SyncRun + WebhookEvent + Product.updatedAtShopify to schema</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (the file being modified — note existing enum style, model style, `@@map` snake-case convention, and Product model definition position)
    - .planning/phases/02-sync-pipeline/02-PATTERNS.md §"Prisma schema additions" (verbatim target syntax)
    - .planning/phases/02-sync-pipeline/02-CONTEXT.md §D-03 and §D-17 (locked schema decisions)
  </read_first>
  <action>
    Edit `prisma/schema.prisma`. Do NOT touch any existing model. Add:

    1. `enum SyncState` — exactly 5 values: `queued, running, succeeded, failed, partial`. Place near other enums (or end of file if no enums yet).

    2. `model SyncRun` — fields per D-03: `id String @id @default(cuid())`, `shop String`, `state SyncState @default(queued)`, `processedCount Int @default(0)`, `totalCount Int?`, `errors String[] @default([])`, `cursor String?`, `idempotencyKey String @unique`, `startedAt DateTime @default(now())`, `finishedAt DateTime?`. Add `@@index([shop])` and `@@map("sync_runs")`. (Implementer's call from D-15: use `String[]` for `errors`; the simpler type, with each entry being a JSON-encoded `{shopifyId, message}` string — documented in the migration header comment.)

    3. `model WebhookEvent` — per D-07: `eventId String @id`, `shop String`, `topic String`, `receivedAt DateTime @default(now())`. Add `@@index([shop])` and `@@map("webhook_events")`.

    4. Inside the existing `Product` model, add one new optional field: `updatedAtShopify DateTime?`. Do NOT change any other Product field. Position the new field near other `DateTime?` fields if any exist; otherwise near the end of the model body.

    Run `bunx prisma format` after editing to normalize indentation. Do NOT run `bunx prisma migrate dev` — that is Plan 04's job.
  </action>
  <verify>
    <automated>grep -c "enum SyncState" prisma/schema.prisma; grep -c "model SyncRun" prisma/schema.prisma; grep -c "model WebhookEvent" prisma/schema.prisma; grep -c "updatedAtShopify" prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "enum SyncState" prisma/schema.prisma` returns 1.
    - `grep -c "model SyncRun" prisma/schema.prisma` returns 1.
    - `grep -c "model WebhookEvent" prisma/schema.prisma` returns 1.
    - `grep -c "updatedAtShopify" prisma/schema.prisma` returns 1.
    - `grep -c "idempotencyKey String @unique" prisma/schema.prisma` returns 1.
    - `grep -c "@@map(\"sync_runs\")" prisma/schema.prisma` returns 1.
    - `grep -c "@@map(\"webhook_events\")" prisma/schema.prisma` returns 1.
    - All 5 enum values present: `bash -c 'for v in queued running succeeded failed partial; do grep -c "^  $v$" prisma/schema.prisma || exit 1; done'` succeeds.
    - `bunx prisma validate` passes (no syntax errors).
    - All Phase 1 models remain unchanged — `git diff prisma/schema.prisma` shows additions only, no deletions of Phase 1 declarations.
  </acceptance_criteria>
  <done>schema.prisma compiles via `prisma validate`; all four schema additions present; no Phase 1 declarations modified.</done>
</task>

<task type="auto">
  <name>Task 2: Author additive migration SQL file</name>
  <files>prisma/migrations/&lt;timestamp&gt;_add_sync_pipeline/migration.sql</files>
  <read_first>
    - prisma/migrations/20260523011257_add_shop_column_destructive/migration.sql (Phase 1 migration — style reference for column alignment, CONSTRAINT naming, comment header)
    - .planning/phases/02-sync-pipeline/02-PATTERNS.md §"Migration SQL (migration, additive)" (target SQL verbatim)
  </read_first>
  <action>
    Create the migration directory `prisma/migrations/<UTC-timestamp>_add_sync_pipeline/` where `<UTC-timestamp>` is `YYYYMMDDHHMMSS` matching the moment of authoring. Inside, create `migration.sql` containing — in this order:

    1. Header comment block: "ADDITIVE migration. Creates SyncState enum, sync_runs table, and webhook_events table. Adds updatedAtShopify column to products. No existing tables are dropped. Safe to run on any Phase 1+ database."

    2. `CREATE TYPE "SyncState" AS ENUM ('queued', 'running', 'succeeded', 'failed', 'partial');`

    3. `CREATE TABLE "sync_runs"` with all fields matching the Prisma model: `id TEXT NOT NULL`, `shop TEXT NOT NULL`, `state "SyncState" NOT NULL DEFAULT 'queued'`, `processedCount INTEGER NOT NULL DEFAULT 0`, `totalCount INTEGER`, `errors TEXT[] DEFAULT ARRAY[]::TEXT[]`, `cursor TEXT`, `idempotencyKey TEXT NOT NULL`, `startedAt TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP`, `finishedAt TIMESTAMP(3)`, with `CONSTRAINT "sync_runs_pkey" PRIMARY KEY ("id")`.

    4. `CREATE TABLE "webhook_events"` with `eventId TEXT NOT NULL`, `shop TEXT NOT NULL`, `topic TEXT NOT NULL`, `receivedAt TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP`, `CONSTRAINT "webhook_events_pkey" PRIMARY KEY ("eventId")`.

    5. `ALTER TABLE "products" ADD COLUMN "updatedAtShopify" TIMESTAMP(3);` — additive only, nullable, no default (existing rows stay NULL until the next sync/webhook updates them).

    6. `CREATE UNIQUE INDEX "sync_runs_idempotencyKey_key" ON "sync_runs"("idempotencyKey");`
    7. `CREATE INDEX "sync_runs_shop_idx" ON "sync_runs"("shop");`
    8. `CREATE INDEX "webhook_events_shop_idx" ON "webhook_events"("shop");`

    Match Phase 1 migration formatting: column types right-padded for visual alignment, blank lines between sections, inline `-- CreateTable`/`-- CreateEnum`/`-- AlterTable`/`-- CreateIndex` comments.

    The file MUST contain ZERO occurrences of `DROP`. Verify with grep before committing.
  </action>
  <verify>
    <automated>bash -c 'F=$(ls prisma/migrations/*_add_sync_pipeline/migration.sql); test -f "$F" && grep -c "CREATE TABLE \"sync_runs\"" "$F" && grep -c "CREATE TABLE \"webhook_events\"" "$F" && grep -c "ADD COLUMN \"updatedAtShopify\"" "$F" && ! grep -q "DROP" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `prisma/migrations/<14-digit-timestamp>_add_sync_pipeline/migration.sql`.
    - `grep -c "CREATE TYPE \"SyncState\"" <file>` returns 1.
    - `grep -c "CREATE TABLE \"sync_runs\"" <file>` returns 1.
    - `grep -c "CREATE TABLE \"webhook_events\"" <file>` returns 1.
    - `grep -c "ADD COLUMN \"updatedAtShopify\"" <file>` returns 1.
    - `grep -c "CREATE UNIQUE INDEX \"sync_runs_idempotencyKey_key\"" <file>` returns 1.
    - `grep -c "CREATE INDEX \"sync_runs_shop_idx\"" <file>` returns 1.
    - `grep -c "CREATE INDEX \"webhook_events_shop_idx\"" <file>` returns 1.
    - `grep -c "DROP" <file>` returns 0.
    - Header comment includes the word "ADDITIVE" so the intent is visible to reviewers.
  </acceptance_criteria>
  <done>migration.sql authored and ready for Plan 04 to apply via `bunx prisma migrate dev --name add_sync_pipeline`.</done>
</task>

</tasks>

<verification>
1. `bunx prisma validate` passes.
2. `bunx prisma format --check` returns clean (or `bunx prisma format` produces no diff after this plan).
3. No production code or routes touched — only `prisma/` directory.
4. Phase 1 Wave 0 tests still pass: `bunx vitest run lib/db/repositories/__tests__/ProductRepository.test.ts lib/shopify/__tests__/auth.test.ts __tests__/proxy.test.ts` — green (schema-only changes shouldn't break repository unit tests because they mock prisma anyway).
</verification>

<success_criteria>
- schema.prisma has all four Phase 2 additions (enum + 2 models + 1 column).
- Migration SQL exists, is additive-only, and matches the schema declarations exactly.
- Plan 04 can apply the migration without further authoring.
</success_criteria>

<output>
Create `.planning/phases/02-sync-pipeline/02-02-SUMMARY.md` listing the schema additions and migration file path. Note that the migration has NOT been applied — Plan 04 owns that.
</output>
