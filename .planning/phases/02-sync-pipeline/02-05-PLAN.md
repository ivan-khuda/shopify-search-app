---
phase: 02-sync-pipeline
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-04"]
files_modified:
  - services/shopify/ShopifyProductService.ts
  - lib/db/repositories/ProductRepository.ts
autonomous: true
requirements: [SYN-01, SYN-02]
must_haves:
  truths:
    - "fetchProductBatch(session, cursor, batchSize) returns {products, endCursor, hasNextPage} from a real Shopify GraphQL call"
    - "fetchTotalCount(session) returns the productsCount integer (or null on missing data)"
    - "mapToUpsertInput(node) deterministically maps a GraphQL Product node to ProductUpsertInput including the new updatedAtShopify field (parsed from product.updatedAt)"
    - "ProductUpsertInput TypeScript interface gains an `updatedAtShopify?: Date | null` field"
    - "All RED tests in services/shopify/__tests__/ShopifyProductService.test.ts transition GREEN"
  artifacts:
    - path: "services/shopify/ShopifyProductService.ts"
      provides: "Real Shopify GraphQL product fetching + Shopify→local mapping"
      exports: ["fetchProductBatch", "fetchTotalCount", "mapToUpsertInput", "PRODUCTS_QUERY"]
    - path: "lib/db/repositories/ProductRepository.ts"
      provides: "ProductUpsertInput extended with updatedAtShopify field"
      contains: "updatedAtShopify"
  key_links:
    - from: "services/shopify/ShopifyProductService.ts"
      to: "lib/shopify/client.ts (shopifyClient.clients.Graphql)"
      via: "new shopifyClient.clients.Graphql({ session })"
      pattern: "shopifyClient\\.clients\\.Graphql"
    - from: "services/shopify/ShopifyProductService.ts"
      to: "lib/db/repositories/ProductRepository.ts (ProductUpsertInput)"
      via: "import type { ProductUpsertInput }"
      pattern: "ProductUpsertInput"
---

<objective>
ShopifyProductService rewrite — replace the stub with a real implementation that paginates Shopify Admin GraphQL, returns typed `{products, endCursor, hasNextPage}`, fetches total count, and maps each GraphQL Product node to the local `ProductUpsertInput` shape (with the new `updatedAtShopify` field for SYN-11 conflict resolution).

Purpose: Provide the data-extraction layer Plan 06's Inngest function will call inside `step.run('fetch-batch-...')`.
Output: One service file rewritten + one repository interface extended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-sync-pipeline/02-CONTEXT.md
@.planning/phases/02-sync-pipeline/02-PATTERNS.md
@.planning/phases/02-sync-pipeline/02-RESEARCH.md
@services/shopify/ShopifyProductService.ts
@lib/db/repositories/ProductRepository.ts
@lib/shopify/client.ts
@services/shopify/__tests__/ShopifyProductService.test.ts

<interfaces>
Function contracts to implement (from 02-PATTERNS.md §"services/shopify/ShopifyProductService.ts rewrite" and 02-RESEARCH.md Q2):

```typescript
import { shopifyClient } from '@/lib/shopify/client';
import type { Session } from '@shopify/shopify-api';
import type { ProductUpsertInput } from '@/lib/db/repositories/ProductRepository';

export const PRODUCTS_QUERY = `
  query Products($first: Int!, $after: String) {
    products(first: $first, after: $after) {
      nodes {
        id
        title
        handle
        description
        vendor
        productType
        status
        tags
        updatedAt
        variants(first: 10) {
          nodes {
            id
            sku
            price
            selectedOptions { name value }
          }
        }
        images(first: 10) {
          nodes { id url altText }
        }
        options { id name values }
      }
      pageInfo { endCursor hasNextPage }
    }
  }
`;

export interface ShopifyProductNode { /* matches PRODUCTS_QUERY shape */ }

export async function fetchProductBatch(
  session: Session,
  cursor: string | null,
  batchSize: number,
): Promise<{ products: ShopifyProductNode[]; endCursor: string | null; hasNextPage: boolean }>;

export async function fetchTotalCount(session: Session): Promise<number | null>;

export function mapToUpsertInput(node: ShopifyProductNode): ProductUpsertInput;
```

`ProductUpsertInput` (already exists in `lib/db/repositories/ProductRepository.ts`, lines 41-59) gains one new optional field:
```typescript
updatedAtShopify?: Date | null;
```

GID parsing: `BigInt(node.id.split('/').pop()!)` for `shopifyId`. Variant price: `parseFloat(variant.price)`. `updatedAt` -> `new Date(node.updatedAt)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ProductUpsertInput with updatedAtShopify</name>
  <files>lib/db/repositories/ProductRepository.ts</files>
  <read_first>
    - lib/db/repositories/ProductRepository.ts (current ProductUpsertInput interface at lines 41-59 + findByShopAndId at line 165)
    - .planning/phases/02-sync-pipeline/02-PATTERNS.md §"lib/db/repositories/ProductRepository.ts extension"
    - .planning/phases/02-sync-pipeline/02-CONTEXT.md §D-17 (locked: updatedAtShopify is the conflict-resolution column)
  </read_first>
  <behavior>
    - ProductUpsertInput has an optional `updatedAtShopify?: Date | null` field after this change.
    - Existing upsertProduct implementation writes the field through to `prisma.product.upsert` so the column gets populated on every sync/webhook insert/update.
    - Existing tests in lib/db/repositories/__tests__/ProductRepository.test.ts continue to pass (the new field is optional).
    - A new test case (added in this task) verifies `updatedAtShopify` round-trips: input { updatedAtShopify: someDate } -> prisma.product.upsert called with data.updatedAtShopify equal to that Date.
  </behavior>
  <action>
    1. In `lib/db/repositories/ProductRepository.ts`, locate the `ProductUpsertInput` interface (around lines 41-59). Add one new optional field: `updatedAtShopify?: Date | null;` near the end of the interface (or grouped with other DateTime-typed fields).

    2. Locate the `upsertProduct` method implementation. Find where the input is forwarded into `prisma.product.upsert({ where, create, update })`. Add `updatedAtShopify: input.updatedAtShopify ?? null` to BOTH the `create` and `update` data blocks so the column updates on every upsert.

    3. In `lib/db/repositories/__tests__/ProductRepository.test.ts`, add one new test inside the existing describe block: "upsertProduct writes updatedAtShopify when provided in input" — pass `updatedAtShopify: new Date('2026-05-22T10:00:00Z')` and assert the mocked `prisma.product.upsert` was called with that Date in `data.updatedAtShopify` (both create and update branches if you mock both).

    Do NOT add `findByShopAndHandle` here — Plan 09 owns that addition (or chooses the inline `prisma.product.findFirst` alternative).
  </action>
  <verify>
    <automated>bunx vitest run lib/db/repositories/__tests__/ProductRepository.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "updatedAtShopify" lib/db/repositories/ProductRepository.ts` ≥ 3 (interface field + create branch + update branch).
    - `bunx vitest run lib/db/repositories/__tests__/ProductRepository.test.ts` reports all green, including the new test.
    - `bunx tsc --noEmit` passes.
    - No existing Phase 1 test in that file is modified or removed (additions only).
  </acceptance_criteria>
  <done>ProductUpsertInput exposes updatedAtShopify; upsertProduct writes the column; Phase 1 ProductRepository tests + new test all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ShopifyProductService (fetchProductBatch + fetchTotalCount + mapToUpsertInput)</name>
  <files>services/shopify/ShopifyProductService.ts</files>
  <read_first>
    - services/shopify/ShopifyProductService.ts (current 11-line stub)
    - services/shopify/__tests__/ShopifyProductService.test.ts (Plan 01 RED stubs — this task makes them GREEN)
    - .planning/phases/02-sync-pipeline/02-PATTERNS.md §"services/shopify/ShopifyProductService.ts rewrite" (function signatures + GraphQL query + GID parsing patterns)
    - .planning/phases/02-sync-pipeline/02-RESEARCH.md §"Q2" (variants(first: 10) and images(first: 10) per Q8 query cost analysis)
    - lib/shopify/client.ts (shopifyClient.clients.Graphql is the GraphQL client)
    - .planning/phases/02-sync-pipeline/02-CONTEXT.md §D-02 (batch size 100; service must respect the caller's batchSize param though)
  </read_first>
  <behavior>
    - `PRODUCTS_QUERY` export is a non-empty template literal containing `products(first: $first, after: $after)` and the fields title, handle, description, vendor, productType, status, tags, updatedAt, variants {id, sku, price, selectedOptions}, images {id, url, altText}, options {id, name, values}, plus `pageInfo { endCursor hasNextPage }`.
    - `fetchProductBatch(session, cursor, batchSize)` instantiates `new shopifyClient.clients.Graphql({ session })`, calls `client.request(PRODUCTS_QUERY, { variables: { first: batchSize, after: cursor } })`, and returns `{ products: response.data.products.nodes, endCursor: response.data.products.pageInfo.endCursor, hasNextPage: response.data.products.pageInfo.hasNextPage }`.
    - `fetchTotalCount(session)` calls `query { productsCount { count } }` and returns `response.data?.productsCount?.count ?? null` (null absorbs missing field gracefully — D-04).
    - `mapToUpsertInput(node)` returns a `ProductUpsertInput` with: `shopifyId: BigInt(node.id.split('/').pop()!)`, `title`, `handle`, `description`, `vendor`, `productType`, `status`, `tags`, `updatedAtShopify: new Date(node.updatedAt)`, `variants: node.variants.nodes.map(v => ({ shopifyId: BigInt(v.id.split('/').pop()!), sku: v.sku, price: parseFloat(v.price), selectedOptions: v.selectedOptions }))`, `images: node.images.nodes.map(i => ({ shopifyId: BigInt(i.id.split('/').pop()!), url: i.url, altText: i.altText }))`, `options: node.options.map(o => ({ shopifyId: BigInt(o.id.split('/').pop()!), name: o.name, values: o.values }))`.
    - All RED tests in `services/shopify/__tests__/ShopifyProductService.test.ts` go GREEN.
  </behavior>
  <action>
    Replace the stub in `services/shopify/ShopifyProductService.ts` with the three exported functions + the GraphQL query constant + the `ShopifyProductNode` TypeScript interface. Follow the exact signatures in the `<interfaces>` block above.

    Import shape:
    ```typescript
    import { shopifyClient } from '@/lib/shopify/client';
    import type { Session } from '@shopify/shopify-api';
    import type { ProductUpsertInput } from '@/lib/db/repositories/ProductRepository';
    ```

    GraphQL query: use the exact field set in 02-PATTERNS.md and the `<interfaces>` block above. Pin `variants(first: 10)` and `images(first: 10)` per 02-RESEARCH.md Q8 (keeps query cost ~300 points; well within Shopify's 1000/10s limit at the 100-product batch size).

    Error handling:
    - If `response.data?.products?.nodes` is undefined or null, throw `new Error('ShopifyProductService: malformed GraphQL response')` — the caller (Inngest function) catches this and routes it through the batch-error policy (D-15).
    - `mapToUpsertInput` must NOT throw on optional fields. Use `??` chains for everything optional (description, vendor, productType, tags can all be empty).

    Mapping notes:
    - GID format: `gid://shopify/Product/1234567890`. `node.id.split('/').pop()!` returns `"1234567890"`; wrap in `BigInt()` for the local `shopifyId` field (Phase 1 schema uses BigInt for Shopify-side IDs).
    - Variant prices come back as strings (e.g. `"19.99"`). Use `parseFloat` — the Phase 1 schema stores prices as `Float`.
    - `selectedOptions` is an array of `{name, value}` objects on the GraphQL side; preserve as-is in the upsert input.

    Do NOT add any code path that calls `productRepository.upsertProduct` from this file. Persistence lives in the Inngest function (Plan 06), not the service. This file is read-only against Shopify + pure mapping.
  </action>
  <verify>
    <automated>bunx vitest run services/shopify/__tests__/ShopifyProductService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `services/shopify/ShopifyProductService.ts` exports `fetchProductBatch`, `fetchTotalCount`, `mapToUpsertInput`, and `PRODUCTS_QUERY` (or equivalent named constant).
    - `grep -c "products(first: \\$first, after: \\$after)" services/shopify/ShopifyProductService.ts` ≥ 1.
    - `grep -c "productsCount" services/shopify/ShopifyProductService.ts` ≥ 1.
    - `grep -c "updatedAtShopify" services/shopify/ShopifyProductService.ts` ≥ 1 (mapping sets it from node.updatedAt).
    - `grep -c "toDecimal" services/shopify/ShopifyProductService.ts` ≥ 3 (helper defined once + used for at least `variant.price` and `variant.compareAtPrice`).
    - `grep -c "parseFloat(v.price)" services/shopify/ShopifyProductService.ts` = 0 (defensive helper only — never call parseFloat on a possibly-MoneyV2 value directly).
    - `services/shopify/__tests__/ShopifyProductService.test.ts` has test cases asserting `toDecimal("19.99") === 19.99` AND `toDecimal({amount: "19.99", currencyCode: "USD"}) === 19.99` AND `mapToUpsertInput` correctly maps BOTH variant price shapes.
    - All RED tests in `services/shopify/__tests__/ShopifyProductService.test.ts` now GREEN.
    - `bunx tsc --noEmit` passes.
    - File length is &lt; 200 lines (this is a focused service, not a kitchen sink).
  </acceptance_criteria>
  <done>ShopifyProductService is real; Plan 01's RED tests for SYN-01 + SYN-02 are GREEN; downstream Inngest function (Plan 06) can call these three exports.</done>
</task>

</tasks>

<verification>
1. `bunx vitest run services/shopify/__tests__/ShopifyProductService.test.ts lib/db/repositories/__tests__/ProductRepository.test.ts` — all GREEN.
2. `bunx tsc --noEmit` passes.
3. `bun run test 2>&1 | tail -10` — Phase 1 + Plan 05 tests green; other Wave 0 RED tests still RED on their own modules.
4. No new dependencies added (`git diff package.json` is empty for this plan).
</verification>

<success_criteria>
- ShopifyProductService is no longer a stub.
- ProductUpsertInput recognizes the conflict-resolution column.
- SYN-01 (GraphQL paginate with required fields) and SYN-02 (deterministic mapping) are testably satisfied.
- The seam to Plan 06 (`fetchProductBatch` + `mapToUpsertInput`) is stable: callers do not need to know about Shopify GraphQL internals.
</success_criteria>

<output>
Create `.planning/phases/02-sync-pipeline/02-05-SUMMARY.md` listing the four exports added to ShopifyProductService, the one field added to ProductUpsertInput, and the test count that transitioned from RED to GREEN.
</output>
