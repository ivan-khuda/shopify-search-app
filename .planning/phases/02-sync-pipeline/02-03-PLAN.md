---
phase: 02-sync-pipeline
plan: 03
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - lib/inngest/client.ts
  - app/api/inngest/route.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "lib/inngest/client.ts exports a single `inngest` singleton constructed via `new Inngest({ id: 'smartdiscovery-ai' })`"
    - "app/api/inngest/route.ts exports GET, POST, PUT from `serve({ client: inngest, functions: [...] })`"
    - "Plan 06 can import { inngest } from '@/lib/inngest/client' and have the serve handler pick up its function without further wiring"
  artifacts:
    - path: "lib/inngest/client.ts"
      provides: "Singleton Inngest client (id: smartdiscovery-ai)"
      contains: "export const inngest"
    - path: "app/api/inngest/route.ts"
      provides: "Inngest serve handler exposing functions to the Inngest runtime"
      exports: ["GET", "POST", "PUT"]
  key_links:
    - from: "app/api/inngest/route.ts"
      to: "lib/inngest/client.ts"
      via: "import { inngest } from '@/lib/inngest/client'"
      pattern: "import.*inngest.*from.*lib/inngest/client"
---

<objective>
Inngest client singleton + serve handler — establish the framework integration so Plan 06's sync function has a registered runtime endpoint.

Purpose: Decouple "where Inngest is configured" from "what Inngest runs". Two tiny files; each does exactly one thing.
Output: lib/inngest/client.ts + app/api/inngest/route.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-sync-pipeline/02-CONTEXT.md
@.planning/phases/02-sync-pipeline/02-PATTERNS.md
@.planning/phases/02-sync-pipeline/02-RESEARCH.md
@lib/shopify/client.ts

<interfaces>
Two target files per 02-PATTERNS.md and 02-RESEARCH.md Q1:

```typescript
// lib/inngest/client.ts
import { Inngest } from 'inngest';
export const inngest = new Inngest({ id: 'smartdiscovery-ai' });

// app/api/inngest/route.ts
import { serve } from 'inngest/next';
import { inngest } from '@/lib/inngest/client';
import { syncProductsFunction } from '@/inngest/functions/sync-products';

export const { GET, POST, PUT } = serve({
  client: inngest,
  functions: [syncProductsFunction],
});
```

The `syncProductsFunction` import will dangle until Plan 06 lands. That is expected — Plan 04 (migration apply) runs before Plan 06, but Plan 03 lands here. Either:
  (a) Land Plan 03's `route.ts` WITHOUT the syncProductsFunction import for now (functions: []), then Plan 06 amends it; OR
  (b) Land both files now with the dangling import — `bunx tsc --noEmit` will fail until Plan 06 ships.

**Choice (locked):** option (a) — ship with `functions: []` and leave a `// TODO(02-06): import syncProductsFunction` comment. Plan 06 will add the import as part of its work. This keeps Plan 03 self-contained and `tsc --noEmit` green at the end of Wave 1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/inngest/client.ts singleton</name>
  <files>lib/inngest/client.ts</files>
  <read_first>
    - lib/shopify/client.ts (singleton-export analog from 02-PATTERNS.md §"lib/inngest/client.ts")
    - .planning/phases/02-sync-pipeline/02-RESEARCH.md §"Q1: Inngest 4.4 API Specifics" (verifies the constructor signature)
    - .planning/phases/02-sync-pipeline/02-CONTEXT.md D-11 (locked: id is `smartdiscovery-ai`, path is `lib/inngest/client.ts`)
  </read_first>
  <action>
    Create `lib/inngest/client.ts` containing exactly:
    - `import { Inngest } from 'inngest';`
    - `export const inngest = new Inngest({ id: 'smartdiscovery-ai' });`
    - Optional: leading JSDoc comment explaining "Singleton Inngest client. Both the serve handler (app/api/inngest/route.ts) and any code that fires events (e.g. app/api/shopify/sync/route.ts) import from here." — but no other runtime code.

    No `globalThis` guard (Inngest client is stateless — unlike Prisma's connection pool). No env-var indirection here; Inngest reads `INNGEST_EVENT_KEY` / `INNGEST_SIGNING_KEY` / `INNGEST_DEV` from `process.env` automatically (D-12).
  </action>
  <verify>
    <automated>grep -c "new Inngest({ id: 'smartdiscovery-ai' })" lib/inngest/client.ts && grep -c "export const inngest" lib/inngest/client.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `lib/inngest/client.ts`.
    - Contains exactly one `import { Inngest } from 'inngest';` line.
    - Contains exactly one `export const inngest = new Inngest({ id: 'smartdiscovery-ai' });` line.
    - File is &lt; 10 lines (this is a singleton, not a module — keep it minimal).
    - `bunx tsc --noEmit lib/inngest/client.ts` (or full project tsc) passes.
  </acceptance_criteria>
  <done>Singleton ready for consumption by Plan 06 (sync function) and Plan 07 (sync POST route).</done>
</task>

<task type="auto">
  <name>Task 2: Create app/api/inngest/route.ts serve handler</name>
  <files>app/api/inngest/route.ts</files>
  <read_first>
    - .planning/phases/02-sync-pipeline/02-PATTERNS.md §"app/api/inngest/route.ts" (target pattern verbatim from RESEARCH.md Q1)
    - .planning/phases/02-sync-pipeline/02-CONTEXT.md D-11 (path locked) and D-12 (env-var setup for dev vs prod)
  </read_first>
  <action>
    Create `app/api/inngest/route.ts` containing:

    1. `import { serve } from 'inngest/next';`
    2. `import { inngest } from '@/lib/inngest/client';`
    3. A leading TODO comment: `// TODO(02-06): import syncProductsFunction from '@/inngest/functions/sync-products' and add to functions array once Plan 06 lands.`
    4. `export const { GET, POST, PUT } = serve({ client: inngest, functions: [] });`

    Per the interfaces note above, ship with an empty `functions: []` array so `bunx tsc --noEmit` stays green. Plan 06 will amend this file to import and register `syncProductsFunction`.

    Do NOT add HMAC, CORS, or any custom middleware — Inngest's `serve()` handles its own signing-key validation via env vars (D-12). Do NOT export DELETE, OPTIONS, HEAD — Inngest only needs GET/POST/PUT.
  </action>
  <verify>
    <automated>grep -c "from 'inngest/next'" app/api/inngest/route.ts && grep -c "export const { GET, POST, PUT } = serve" app/api/inngest/route.ts && grep -c "TODO(02-06)" app/api/inngest/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `app/api/inngest/route.ts`.
    - Imports `serve` from `inngest/next` (not `inngest` directly).
    - Imports `inngest` from `@/lib/inngest/client`.
    - Exports GET, POST, PUT via destructuring from `serve(...)`.
    - `functions` array is `[]` with a `TODO(02-06)` comment naming Plan 06 as the follow-up.
    - `bunx tsc --noEmit` passes after this plan lands.
    - The file is &lt; 15 lines.
  </acceptance_criteria>
  <done>Inngest serve handler reachable at /api/inngest; Plan 06 can wire the actual function into its `functions` array.</done>
</task>

</tasks>

<verification>
1. `bunx tsc --noEmit` — passes.
2. `bun run test 2>&1 | tail -5` — Phase 1 tests still green; Wave 0 RED tests still RED in the same way (these two new files don't make the Inngest function test pass).
3. `grep -rn "from '@/lib/inngest/client'" app/ inngest/ lib/` — at least the new route.ts references it (more consumers in Plans 06/07).
</verification>

<success_criteria>
- Two files exist; both compile.
- The TODO comment in the route handler names Plan 06 as the follow-up so reviewers know the `functions: []` is intentional and tracked.
- No environment variables read at this layer (Inngest pulls them itself); no behavioral side effects beyond mounting the framework integration.
</success_criteria>

<output>
Create `.planning/phases/02-sync-pipeline/02-03-SUMMARY.md` noting the two files created and that `functions: []` is intentionally empty until Plan 06.
</output>
