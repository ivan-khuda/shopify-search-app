---
phase: 01-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - middleware.ts
  - app/api/auth/route.ts
  - app/api/auth/callback/route.ts
  - app/(embedded)/onboarding/page.tsx
autonomous: true
requirements: [FND-02]
must_haves:
  truths:
    - "No console.log call exists in middleware.ts, app/api/auth/route.ts, app/api/auth/callback/route.ts, or app/(embedded)/onboarding/page.tsx"
    - "No replacement logger (pino/winston/debug) was introduced — strict delete-only per D-10"
    - "Existing tests for these files continue to pass (deletion is observably no-op for assertions)"
  artifacts:
    - path: "middleware.ts"
      provides: "Middleware without token/header/shop console.log statements"
    - path: "app/api/auth/route.ts"
      provides: "Auth begin route without console.log"
    - path: "app/api/auth/callback/route.ts"
      provides: "Auth callback route without console.log"
    - path: "app/(embedded)/onboarding/page.tsx"
      provides: "Onboarding page without token console.log"
  key_links:
    - from: "static analysis"
      to: "deleted lines"
      via: "grep returns zero matches in target files"
      pattern: "console.log"
---

<objective>
Delete every `console.log` call that emits a session token, Authorization header, shop identifier, redirect URL, or full `shopifyClient` SDK object across the four files identified in Q10 of the research (FND-02, D-10).

Purpose: Stop leaking sensitive material into stdout/stderr. Centralised log aggregators capture stdout from serverless functions, and a single Bearer token in those logs is a credential-equivalent exposure. D-10 is delete-only — no replacement logger, no `if (DEBUG)` silencer.
Output: Four files with surgical line-level deletes. Behaviour observably unchanged: tests pass without modification, OAuth flow still works, middleware still routes the same way.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@CLAUDE.md
@middleware.ts
@app/api/auth/route.ts
@app/api/auth/callback/route.ts
@app/(embedded)/onboarding/page.tsx
@app/api/shopify/sync/__tests__/route.test.ts
@__tests__/middleware.test.ts
@app/(embedded)/__tests__/onboarding.test.tsx
@app/api/auth/__tests__/route.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete console.log calls from all 4 files</name>
  <read_first>
    - middleware.ts (lines 1-47 — delete lines 9, 10, 13)
    - app/api/auth/route.ts (lines 1-20 — delete lines 7, 8)
    - app/api/auth/callback/route.ts (lines 1-19 — delete line 16)
    - app/(embedded)/onboarding/page.tsx (lines 1-67 — delete line 13)
    - .planning/phases/01-foundation/01-RESEARCH.md §Q10 (verified file:line catalogue)
    - .planning/phases/01-foundation/01-PATTERNS.md §§ "app/api/auth/route.ts — DELETE-ONLY", "app/api/auth/callback/route.ts — DELETE-ONLY", "app/(embedded)/onboarding/page.tsx — DELETE-ONLY"
  </read_first>
  <behavior>
    Per D-10, after this task the four files MUST contain ZERO `console.log` statements. The deletions are:
    - middleware.ts: remove `console.log('authHeader', authHeader);` (line 9), `console.log('shop', shop);` (line 10), `console.log('token', token);` (line 13). NOTE: this plan does NOT yet refactor the rest of middleware.ts — the Bearer-fallback block, commented-out session check, and matcher rewrites are owned by Plan 07. This plan ONLY deletes the three log lines. The surrounding code stays untouched.
    - app/api/auth/route.ts: remove `console.log('shop', shop);` (line 7) and `console.log("shopifyClient", shopifyClient);` (line 8).
    - app/api/auth/callback/route.ts: remove `console.log('redirectUrl', redirectUrl.toString());` (line 16). The blank line on line 15 may be removed too for cleanliness, but is optional.
    - app/(embedded)/onboarding/page.tsx: remove `console.log("token", token);` (line 13).
    No replacement logger added. No `if (DEBUG)` wrapper. No silencer.
  </behavior>
  <action>
    Edit the four files in place using the Edit tool, removing exactly the lines enumerated in `<behavior>`. Verify with `grep -n "console.log" <file>` after each edit that the count is zero. Do NOT touch any other lines — middleware's Bearer fallback, commented-out auth block, `runtime` export, and matcher are Plan 07's domain. Do NOT introduce any import for a logging library; D-10 is strictly delete-only. After all four edits, run the existing test suites for these files to confirm behavioural no-op: `bunx vitest run __tests__/middleware.test.ts app/api/auth/__tests__/route.test.ts "app/(embedded)/__tests__/onboarding.test.tsx" app/api/shopify/sync/__tests__/route.test.ts`. Existing passes must remain passing. (`__tests__/middleware.test.ts` was edited in Plan 01 and is partially RED until Plan 07 — that's expected; this plan must not REGRESS any test that was previously passing.)
  </action>
  <verify>
    <automated>! grep -rn "console.log" middleware.ts app/api/auth/ "app/(embedded)/onboarding/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "console.log" middleware.ts` returns 0.
    - `grep -c "console.log" app/api/auth/route.ts` returns 0.
    - `grep -c "console.log" app/api/auth/callback/route.ts` returns 0.
    - `grep -c "console.log" app/(embedded)/onboarding/page.tsx` returns 0.
    - No new imports added to any of the 4 files (e.g., no `import pino from ...`, no `import { debug } from ...`).
    - No new `if (DEBUG)` or `if (process.env.NODE_ENV !== 'production')` blocks introduced as a logger silencer.
    - `bunx vitest run app/api/auth/__tests__/route.test.ts "app/(embedded)/__tests__/onboarding.test.tsx" app/api/shopify/sync/__tests__/route.test.ts` exits 0 (no test regressions in files this plan modifies the deps of). The middleware test file is allowed to be partially RED (Plan 07 work), but cannot regress past its post-Plan-01 baseline.
    - The middleware.ts Bearer-fallback block (current lines 11-20), commented-out session check (lines 22-32), `runtime` export (line 43), and matcher block (lines 45-47) are UNCHANGED by this plan — verified by `git diff middleware.ts` showing only the three deleted log lines.
  </acceptance_criteria>
  <done>All 7 console.log statements from Q10's catalogue are deleted; no replacement logger introduced; non-middleware tests still pass; only the three log lines in middleware.ts changed (not Bearer block or matcher).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application → Log aggregator (stdout/stderr) | Vercel + most hosts capture stdout into centralised logs. A `console.log(token)` writes the token into that pipeline. This plan severs that exfiltration path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Information Disclosure (credentials in logs) | middleware.ts, app/api/auth/*, app/(embedded)/onboarding/page.tsx | mitigate | Delete every `console.log` of Authorization header, Bearer token, shop, redirect URL, and full SDK object. Grep gate enforces zero remaining occurrences in the four files. |
| T-1-03b | Information Disclosure (regression risk) | Future code added to these files | accept | Phase 1 chose delete-only (D-10) over a redacting logger. The grep gate in Plan 09 verification is the ongoing guard. A structured logger with redaction is deferred (CONTEXT.md §Deferred Ideas). |
</threat_model>

<verification>
- Single shell gate: `! grep -rn "console.log" middleware.ts app/api/auth/ "app/(embedded)/onboarding/page.tsx"` — must succeed (returns no matches).
- Vitest run on the affected files' test suites returns the same pass/fail counts as before this plan (deletes are observably no-op for tests).
- `git diff --stat middleware.ts app/api/auth/route.ts app/api/auth/callback/route.ts "app/(embedded)/onboarding/page.tsx"` shows a small, surgical change set (each file: -1 to -3 lines, +0 lines).
</verification>

<success_criteria>
- 7 `console.log` statements deleted (3 in middleware.ts, 2 in app/api/auth/route.ts, 1 in app/api/auth/callback/route.ts, 1 in app/(embedded)/onboarding/page.tsx).
- No new imports, no replacement logger.
- All previously-passing tests in the affected modules continue to pass.
- middleware.ts is otherwise unchanged (Plan 07 owns the rest of the middleware rewrite).
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` recording: 4 files edited, 7 lines deleted, grep gate result, vitest result, and explicit confirmation that no logger library was added.
</output>
